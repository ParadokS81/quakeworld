Compile the TypeScript project and report any errors. Run this command:

```
cd ~/projects/quakeworld/apps/quad && npx tsc --noEmit
```

If there are errors, analyze them and fix the issues. If the build succeeds, confirm it.
