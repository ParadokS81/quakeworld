Start the bot locally for development testing. Run this command:

```
wsl bash -ic "cd /home/paradoks/projects/quake/quad && node --env-file=.env --loader ts-node/esm src/index.ts"
```

Watch the output for errors or confirmation that the bot connected to Discord. If it fails, diagnose and fix the issue.
