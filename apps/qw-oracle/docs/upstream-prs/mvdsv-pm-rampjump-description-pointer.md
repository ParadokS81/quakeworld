# mvdsv `pm_rampjump` help-JSON description -- pointer note

**Dated:** 2026-08-11 (chunk-6 HANDOVER migration repair, Task 13 B8).

mvdsv has no help-JSON surface today (unlike ezquake), so there is nothing to PR yet.
The description text is already drafted and traced (TRACED-CLEAN, V-pass wave B) as
knob 5 in
`docs/superpowers/plans/2026-05-16-ktx-mvdsv-l1-describe-fill/mvdsv-pm-batch-ledger.md`
(source_ref `src/sv_user.c:3799`), which is what HANDOVER.md's small-followup row
points to when it says the text is "parked in PR #1120 side findings." This note is a
mirror pointer only -- apply the already-drafted text if/when mvdsv grows a help-JSON
surface; do not re-derive it from scratch.
