# qw-oracle/docs/

App-wide Layer 3 reference docs + arc history. Subsystem-coupled refs live near their code (e.g. `scripts/extractors/EXTRACTOR-PLAYBOOK.md`).

## Documentation index

| When you need... | Read... |
|---|---|
| Chronological ship log for qw-oracle | `arc-history.md` |
| Per-entity-type formal documentation (Layer 1, 15 types) | `entity-types.md` |
| Multi-extractor walk roadmap + cliffs ahead | `layer1-extraction-roadmap.md` |
| Operator observability cheatsheet (query_log + embedding_api_log queries) | `OBSERVABILITY.md` |

`reviews/` is a grandfathered artifact dir (per-version validation reports) -- not in the index, excluded from docs-check sweeps.
