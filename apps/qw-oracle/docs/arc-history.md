# QW Oracle -- Arc history

Append-only chronicle of shipped arcs. One paragraph per arc, oldest at the bottom. New arcs prepend at the top.

For current state, see `OVERVIEW.md` (living map) and `HANDOVER.md` (active backlog). For why arcs shipped in the order they did, read backwards from the top.

---

## 2026-05-17 -- enforce-L1-runtime-truth -- ARC IN EXECUTION (Phases 1-3 shipped; 4-5 pending)

Enforce L1 to tell the runtime truth in both directions for ezQuake: stop SHOWING non-working commands/cvars (Track A -- libclang call-graph reachability passenger, ghost elimination) and stop HIDING working commands (Track B -- `HUD_Register` hidden-command recovery). One coherent arc, two mechanisms, phased + separately gated, zero mechanism blend. Spec: `docs/superpowers/specs/2026-05-16-libclang-callgraph-reachability-design.md`. Plan: `docs/superpowers/plans/2026-05-17-enforce-l1-runtime-truth/` (D1-D22 + X1-X10; 5 fully-sequential phases; all 5 drafts approved 2026-05-17; pre-execution cross-phase audit RAN + CLEARED -- F8 cross-arc drift caught + resolved). arc-orchestrator-driven (per-phase fresh arc-executor terminals; cross-phase memory + independent boundary verification owned by the orchestrator session; draft-then-execute -- a distinct heavier mode from drafting).

- **Phase 1 (2026-05-17): Track A call-graph reachability passenger -- SHIPPED + orchestrator-boundary-verified.** Tier-1 `extractor_lib/_callgraph.py` (read-only observer in separate dispatch cycles, per-variant root-set + full-subtree BFS + 3-valued reachable/unreachable/not-compiled, the two structurally-separate feeders -- call-graph + standalone commented-register textual scanner per D7.1/OQ-1), the single-boolean `extract.py` seam (D6/X4 fail-safe-off), and the 3-gate self-validation harness. **F9 caught + operator-ratified (the arc's first execution-time refuted premise):** the Phase-1 drafter Recon premise "`cl_view.c` is client-only -> not-compiled in the SERVERONLY build" imported the historical qwsv dedicated-server model; orchestrator primary-source verification (corroborated the executor's harness exactly, then sharpened it) found ezQuake-source has ONE `add_executable(ezquake)` over one 309-file source list, no per-variant CMake target, `SERVERONLY` never CMake-set -> the executor's Option A (parse per-variant source lists) was INAPPLICABLE, Option B is the factually-correct model. Operator ratified Option B -> dated `decisions.md` D5 AMENDMENT (not-compiled is preprocessor-derivable ONLY; D3 intact; D19/level-3 safety unaffected) + review-findings F9 + Phase-1 MD (4 dated corrections, narrative preserved) + Phase-3/4 propagation. Resolution required ZERO `_callgraph.py` change (the module already derived not-compiled purely from preprocessor-scoped TU membership -- the defect was always the drafter's expected Gate-3 value); the executor revised only Gate 3's expected cell (`cl_bobhead` server `not-compiled` -> `reachable`; conclusion `build-excluded` UNCHANGED, the load-bearing answer). **Orchestrator independently re-verified the boundary two ways each (executor "GREEN" treated as hypothesis):** X3 zero-diff -- structural (observer never in `ALL_HANDLERS`, self-private mutation only) + an independent OFF/ON full re-run (8-stem `diff -q` empty); 3-gate -- structural (Gate-3 `expected_shape` is the F9-amended contract, not stricter) + an independent post-revision harness re-run (`GATE 1/2/3 GREEN`, exit 0). D1 no-blend / D7.1 feeder-b pure-text separation confirmed structurally. Memory: `reference_libclang_compiled_means_parsed_not_linked.md` (the generalizable F9 lesson; de-risks the gated FTE/QWCL/MVDSV follow-ons). ADVISORY -> HANDOVER: `verify-callgraph-probes.py` unused `STATE_UNREACHABLE` import (post-revision; trivial, post-arc cleanup). Next: Phase 2 (Track B `_handler_hud.py`, commands-only) -- shares NO code/schema/gate with Phase 1 (D1).

- **Phase 2 (2026-05-18): Track B `ezquake/_handler_hud.py` (commands-only) -- SHIPPED + orchestrator-boundary-verified (DONE; no deviation; R1 GREEN -- clean gate-and-ship, contrast Phase-1 F9).** New Tier-3 ezQuake-private Visitor modeling the `HUD_Register` COMMAND contract by literal/constant AST reading only (zero call-graph, zero comment-scan, zero cvar path -- D1/D8/D11-amended structural): bare `<name>` unconditional (`hud.c:1232`) + double-gated `+hud_<name>`/`-hud_<name>` (`if(show)` over `if(flags & HUD_PLUSMINUS)` -> `Cmd_AddRemCommand`, `hud.c:1265/1269/1275/1278`), each row element-keyed to literal arg0 (D16); single-boolean `extract.py` seam mirroring Phase-1 `CALLGRAPH_OFF` (D9/X4 fail-safe-off; the directory IS the per-fork gate D2/D22); the 3-anchor + R7 + R1 self-validation harness `verify-hud-probes.py` (X2/W4 self-contained -- no L1 column / combined harness / dump / `_callgraph`). 129 recovered commands (83 bare + 23 plus + 23 minus; 83 elements). **R1 -- the prime refuted-premise candidate the prior-phase note named -- did NOT fire:** the libclang AST confirms `nonliteral_count==0`, so D8's literal-only premise is AST-confirmed load-bearing-safe; NO `decisions.md` amendment. **Orchestrator independently re-verified the boundary 3 ways (executor "DONE/GREEN" treated as hypothesis -- `feedback_verify_dispatched_terminal_claims`):** structural (all 3 on-disk files read -- the `visit_cursor` HUD_Register-only filter makes `togglehud` + the `hud.c:1281-1282` comment-trap STRUCTURALLY impossible, not merely asserted; the R1 record+emit-nothing+no-constant-propagate path; zero cvar code path; the seam diff provably mirrors Phase-1 shipped `CALLGRAPH_OFF`), primary-source by-symbol (every `hud.c`/`hud.h`/`hud_radar.c` cite re-verified at pin `3f9e724f` -- zero drift; unlike F9 the executor recon needed NO sharpening), and empirical (own `mktemp` OFF/ON re-run: 8 F6 stems byte-identical, 9th additive, committed-baseline leg non-vacuous + clean, `verify-hud-probes.py` ANCHOR1/2/3+R7+R1 GREEN exit 0, emitted radar/+hud_radar/-hud_radar rows == live `hud_radar.c:1422`). P2->P3 contract a three-way verbatim match (Phase-2 Outputs == Phase-3 Inputs == actual `finalize()`). **One doc hazard caught + tracked (executor surfaced it, did NOT silently fix -- operator-not-technical-gate):** the Phase-2 MD Task-2 X3 baseline command set no OFF env var -> a verbatim copy-run would silently no-op X3 (exact F6 class) -> `review-findings` **F10** + dated Phase-2 MD orchestrator-correction (`HUD_COMMANDS_OFF=1` on the baseline leg; F6 precedent -- no redraft, no D-amendment); the stale F9 phase-ownership-table `OPEN` row tidied to `RESOLVED`. Next: Phase 3 (unified L1 fidelity schema + loader) -- consumes the `hud_commands` JSON contract + the Phase-1 `reachable()` contract; the F8 STANDING RULE binds it (migration ordinal EXECUTOR-DERIVED live `(highest db/migrations/ int)+1`, never frozen; `quality-grid.ts` cites re-derived by symbol -- the sibling ktx-mvdsv arc is STILL ACTIVE on the shared migration chain + `quality-grid.ts`).

- **Phase 3 (2026-05-18): unified L1 fidelity schema + loader -- SHIPPED + orchestrator-boundary-verified (DONE_WITH_CONCERNS -> F11 accept-as-is / F12 dated MD-correction / F13 floor recalibrated; NO `decisions.md` deviation, no D re-opened).** Migration `015_l1_runtime_fidelity_provenance.sql` (ordinal EXECUTOR-DERIVED live per F8 -- `(highest db/migrations/ int)+1`; orchestrator independently re-derived 014->015, exactly one 015, no silent-corruption duplicate) lands TWO physically-separate independently-nullable JSONB columns -- `track_a_reachability` (on `cvar_versions` AND `command_versions`) + `track_b_hud_recovery` (on `command_versions` ONLY) -- nullable, NO CHECK, D12 structural no-blend (no `runtime_fidelity` column, no `kind`); `SCHEMA.md` v18 same task. Both conform to the D14 three-slot spine (conclusion / evidence / dump_confirmation); Track-A evidence feeder-tagged (callgraph -> per_variant over client/server/win/apple + `address_taken_residue`; commented-register -> register_site cite -- D15), Track-B element-linked (the literal `HUD_Register` arg0 -- D16). The OQ-1 Track-A emit seam (`emit_callgraph_signal.py`, the additive 10th file `ezquake-callgraph-reachability-ast.json`, invoked from the existing Phase-1 post-walk behind the SAME Phase-1 boolean -- D6 no-re-pay-the-parse), the Track-A overlay (`load-callgraph-reachability.ts`, overlay-not-create -- X7), the Track-B adapter (`load-hud-commands.ts`, D21 first-class `source_state='source_backed'` `command` entities -- OQ-2 operator-ratified), plus `types.ts`/`natural-keys.ts`/`load-cvars.ts`/`load-commands.ts`/`extract-tag.ts` plumbing; F1 `probeJsonbNotStrings` += the 3 columns + the new `F1.runtime_fidelity_shape` probe + 4 quality-grid.test.ts cases. Slot-3 uniformly level-2 (`high-confidence-generalized`) -- Phase 3 NEVER stamps level-3 (D14/D19 boundary held; the dump cross-check is Phase 4; X2/W4). **Deviation-with-rationale (surfaced, NOT silent -- verified sound):** the two independent single-column writers (Track-B adapter, Track-A overlay) share the MD-mandated `upsertCvarVersion`/`upsertCommandVersion` ON CONFLICT path, so the 3 new provenance columns use `COALESCE(EXCLUDED.col, table.col)` while EVERY existing column keeps `= EXCLUDED.col` (X3 row-parity preserved exactly) -- the necessary correct realization of D12 + the MD's overlay-runs-after ordering; flagged for arc-reviewer, not a D-amendment. **Orchestrator independently re-verified the boundary 3 ways (executor "DONE_WITH_CONCERNS/PASS" treated as hypothesis -- `feedback_verify_dispatched_terminal_claims`):** structural (migration body = 3 bare `ALTER ADD COLUMN JSONB`, information_schema confirms 3 jsonb/nullable/no-CHECK; D12 SQL count=0; the COALESCE upsert read line-by-line -- existing cols `=EXCLUDED`, only the new cols COALESCE, `tx.json()` never `JSON.stringify`); primary-source-by-symbol (the 3-gate JSONB round-trip EXACT: `sb_qtvlist_url` genuine-dead/callgraph/unreachable-everywhere, `gl_outline_scale_world` genuine-dead/commented-register/`r_rmain.c:730`, `cl_bobhead` build-excluded/callgraph/**server=`reachable`** -- the F9 D5-amendment load-bearing assertion, conclusion `build-excluded` UNCHANGED; `radar`/`+hud_radar`/`-hud_radar` `source_backed`, element-linked to the one `radar`, `hud_radar.c:1422`; slot-3 DISTINCT only `high-confidence-generalized`; cross-arc forensics -- 4 ktx-mvdsv commits during the run, NONE touched `db/migrations/` or `quality-grid.ts`); empirical (own F1 grid re-run -> exactly the 2 stale `ezquake.command` floor probes RED, no hidden breakage; own `mktemp` OFF/ON X3 re-run with F10-explicit `CALLGRAPH_OFF=1 HUD_COMMANDS_OFF=1` -> 8 F6 stems byte-identical, 9th+10th additive, 3312 signal entries - 10 skipped == 3302 == 2788 cvar + 514 cmd Track-A populated [exact reconciliation]; post-fix F1 grid FULLY GREEN, 132 probes, 0 regression failures; typecheck clean). P3->P4 contract a verbatim match (Phase-3 Outputs == Phase-4 Inputs). **Three findings caught + routed (executor surfaced, did NOT silently fix -- operator-not-technical-gate):** **F11** (the Task-2 subagent's mig-`015` HEADER comment + SCHEMA.md v18 conflated Track-B's recovered-HUD scope with the Track-A 74/D20 pool -> SCHEMA.md leg DRAINED in-place at execution; the immutable sha256-tracked applied-`015`-header is ACCEPT-AS-IS [body canonical; editing it is the silent-corruption class F8 exists to prevent; SCHEMA.md + Phase-3 MD + decisions all carry the precise scope]; data-corroborated -- 129 `track_b` rows all `type='command'`, 0 cvar [R7], 129 == X7 reverse-diff); **F12** (the Phase-3 MD Task-3/check-3 `load-version` literal is the wrong subcommand -- it ingests one single-type JSON, copy-run HARD-THROWS; check-5's bare `bun test` fails the `qw_oracle_test`-DB safety guard -> orchestrator-applied dated MD-correction to `extract-tag --skip-release-notes` + the canonical test-DB form, F6/F10 narrative-preserved precedent; the executor ran the CORRECT commands at execution); **F13** (Phase-3's OWN correct D21/Track-B deliverable adds +129 first-class recovered HUD commands, legitimately outgrowing the stale calibrated `ezquake.command` floor snapshot -> the 2 regression-family floor probes RED. Orchestrator independently primary-source-verified LEGITIMATE growth NOT idempotency inflation [693 DISTINCT name_fold, 0 dup, `UNIQUE(project,type,name_fold)` forbids re-run inflation, 693-129 == the prior 564 baseline intact, 129 == the Phase-2 handler `_stats.source_total`, source_backed 495->624]; operator-routed -> orchestrator RECALIBRATED `quality-grid.ts` 564->693 / source_backed 495->624 with a dated inline note + the `:1949` snapshot line [F8-scoped surgical edit, disjoint from the active ktx-mvdsv `F1.describe_fill.*` region] and folded the expected-D21-growth note into the F12 dated check-5 MD-correction; the `reference_qw_oracle_floor_vs_clean_reload` family; NO D-amendment -- D20/D21/X7 were always correct, a stale calibrated snapshot catching up to a correct deliverable). **Process note (recorded so arc-reviewer does not re-flag):** the executor's Task-3 subagent brief carried one wrong expected value (`cl_bobhead.address_taken_residue=true`); the Phase-3 MD does NOT gate on the residue value, Phase-1's OQ-3 self-validation mandates `false`, the loader faithfully round-tripped `false`, check-3 PASSES, and the subagent correctly REFUSED to fabricate `true` (F5/X4) -- a non-defect, no track. Next: Phase 4 (acceptance contract -- D17/D18/D19; owns the runtime-dump cross-check that stamps level-3, NONE touched here -- X2/W4 held). Prerequisites item 4 (durable pinned dump) is CLOSED -- the matched triple + README SECURED git-tracked at `apps/qw-oracle/data/detection/`, the F7 embedded-`~<sha>` PRIMARY version-pin proxy leg; Phase 4 re-runs the R6 proxy live at its boundary + the operator attests provenance. The F8 STANDING RULE continues to bind Phase 4/5 (the sibling ktx-mvdsv arc is STILL ACTIVE on the shared migration chain + `quality-grid.ts`).

## 2026-05-17 -- KTX/MVDSV Layer-1 describe-fill -- ARC IN EXECUTION (Phases 0-2 shipped; 3-5 pending)

Every admin-configurable KTX/MVDSV knob (cvars/commands/cmdline/info_keys) gets a provenance-stamped Layer-1 description -- the single source of truth the MCP, the Slipgate snapshot, a future web server-manager, and wiki.slipgate.me render from. Spec: `docs/superpowers/specs/2026-05-15-ktx-mvdsv-l1-describe-fill-design.md`. Plan: `docs/superpowers/plans/2026-05-16-ktx-mvdsv-l1-describe-fill/`. arc-orchestrator-driven (per-phase fresh executor terminals; cross-phase memory + boundary verification owned by the orchestrator session; session-boundary resume docs at `docs/superpowers/parking/2026-05-17-ktx-mvdsv-l1-describe-fill-orchestrator-resume*.md`). Pre-dispatch holistic gate ran to verdict (NOT CLEAN -> 3 findings -> Corrections 1+2+3 landed + verified); once-per-arc, consumed.

- **Phase 0 (2026-05-17): free win + self-built reproducible C3 oracle -- SHIPPED + orchestrator-boundary-verified.** 28 MVDSV command descriptions freed (`f3b356f3`); self-built C3 oracle (fetch dev-head fwd, build mvdsv+ktx, local-server dump, re-extract L1 same build) + ezquake.com shape-quant (`58ce4d91`). F-C3a DISSOLVED (contemporaneity structural, not a risk); **F-C3c ratified** (the ktx/command C3 leg is measurement-invalid -- `mvdsv cmdlist` is structurally blind to KTX `cmds[]` mod-path commands; prevented a 357-command false-death lie); F-C3d added (extract-tag --version head doesn't fetch origin -> Phase 5 D4 explicit fetch+SHA-pin+`--commit`). Re-baseline EXACT: ktx 260/358/7, mvdsv 183/108/11/45 (`19a74b1f`).
- **Phase 1 (2026-05-17): the build-once discipline spine -- SHIPPED + orchestrator-boundary-verified.** F-D4a owned-row guard + migration 014 schema + 2 C5 probes + the D6 `describe-fill-synthesis` skill + the D7 two-tier gate + the D11/D15 audit serializer + the D19 `k_short_gib` end-to-end smoke (`95e8d726` + `a02ba558`). **F-D4a re-derive-safe gate independently proven by the orchestrator** (a real `re-derive --project ktx --type cvar`, 260 entities, left `k_short_gib`'s owned fingerprint byte-identical). F-C5b (probe arc-scoped), F-C5c (tsc gate was vacuous -- perturbation-proven fixed), F-D11b (audit HTML gitignored) ratified. anchor_version convention ratified arc-wide (`git describe` of loaded dev-head; `k_short_gib` = `1.47-2-g67253dc`). The spine is built ONCE for both engines.
- **Phase 2 (2026-05-17): KTX shipped-config mechanical extract (D9) -- SHIPPED + orchestrator-boundary-verified (DONE_WITH_CONCERNS -> both concerns ratified).** New duck-typed standalone sibling extractor (`_handler_shipped_config.py`, registered in `extract.py`) + fill-not-create idempotent loader (`load-ktx-shipped-config.ts` + `index.ts` subcommand) + idempotency/coverage harness (`extract-ktx-mechanical.ts`) + the 2 C5 F1 probes (jsonb extended to ktx + `provenance_entry_exists` added). Commit `953fa0cd`, pushed. **102 KTX cvars** carry a staged `shipped_doc` candidate + retained per-(cvar,source-file) provenance (in-repo / nQuake `ktx.cfg` / `port_template.cfg` drift preserved, NEVER merged -- F-C2a); **103/260 mechanically covered** (incl. Phase-1 `k_short_gib`); **157 residue** enumerated + tracked to Phase 3 (incl. 38 bot `k_fbskill_*` + 11 config-drift non-resolvers `sv_maxrate`/`k_dm2mod`/`k_666`/`k_autoreset`/`k_master`/`sv_www_address`/`sv_www_authkey`/`k_motd5` -- never importance-cut, C1). **Orchestrator independently re-ran the full boundary (executor "PASS" treated as hypothesis, the arc's 8th such catch-or-confirm):** the non-negotiable F-D4a `shipped_doc` re-derive-safe proof (owned-rows fingerprint `5253de8f...` byte-identical before AND after a real `re-derive --project ktx --type cvar`, 260 entities; the guard's `shipped_doc` leg load-bearing for the first time, holds), count 260, idempotent F1==F2==`837f3875...`, 3 C5 probes [PASS], D9 seam 0, F-D9a CRLF leak 0, k_short_gib `synthesized|2|1` byte-identical. **Two concerns surfaced + orchestrator-ratified into the ledger (the F-D9a Phase-routing pattern, not silent):** F-D11c (the live `structured_choices` is the flat `[{value,label}]` Phase-1-locked type at `review-gate.ts:83-89`, NOT the stale phase-2-MD `{enum?,bitmask?}` sub-shape -- Phase 3 + Phase 5 serializers must consume flat) and F-D9b (the clobber-guard is a whole-record skip for terminal owned rows -- the only implementation consistent with the non-negotiable F-D4a gate, proven). Neither needs a `decisions.md` change (the D11 amendment already authorizes additive `structured_choices`; both are faithful realizations). D9 seam held: zero quality verdicts written -- every `shipped_doc` candidate + every still-NULL/source_inline residue cvar flows to the Phase 3 D5-D8 evaluation.

## 2026-05-16 -- L1 entity-name source-case fidelity + structural fold (migration 013) -- SHIPPED (single session)

Closed the recurring cross-project "search misses things" class: `entities.name` now stores the SOURCE capitalization the engine registered (`loadFragfile`, `cl_independentPhysics`, `K_ENTER`) and the case-insensitive fold is enforced STRUCTURALLY by a generated `entities.name_fold` column (migration 013: `lower(name)` except `token_primitive`; `UNIQUE(project,type,name_fold)` replaces the old name-unique; `canonical_id` unchanged so versioned tables / snapshots / MCP keys are stable). The mini-arc parking doc's "VERIFIED -- do not re-derive" block was materially WRONG and the RE-VERIFY checklist caught it: the live fold was NOT `natural-keys.ts upsertEntity` (the doc's premise) but `load-version.ts` `caseFoldMergeEntries` (rekeyed the merged dict to lowercase) PLUS the name->validation chain (`validIdentifier`/`INFO_KEY_NAME_RE`/qc/command regexes rejected uppercase) PLUS upsertEntity PLUS the alias resolver -- a four-site surface, verified across all five engines before any code (cheap probes: 0 case-collision groups anywhere except the sacred `token_primitive $B/$b`; the multi-variant merge effectively never fires). Fix: caseFoldMergeEntries keys by the source-case representative; validation widened to accept uppercase; existence/alias/stale-guard/lookup_entity all match `name_fold`; `lookup_entity` polish also killed a latent `ILIKE` `_`/`%` wildcard bug. Reload of all 29 head feeds was idempotent x3; fidelity counts match the extractor probe exactly (ezquake cvar 63 / flag_bit 50 / keyname 148 camelCase restored; fte/mvdsv/ktx likewise); `$B`/`$b` stays two rows. The one gate deviation, fully root-caused via systematic-debugging: ezquake cvar 2997->2992 (`source_retired` 209->204, `source_backed` 2741 UNCHANGED -- zero real cvars lost) because the first clean reload let `pruneCrossTypeOrphans` delete 5 phantom `doc_only` cvars (`scr_weaponstats_{x,y,order,scale,frame_color}`, `floodprotmsg`, `userdir`) whose names are really `Cmd_AddCommand` commands mis-listed under ezQuake's own `help_variables.json` -- a documented prune target, lowercase-named so case-irrelevant (old code would prune identically); F1 floor re-baselined at `quality-grid.ts:1583` with justification. Debugging surfaced a third name-equality site the doc never enumerated -- the prune's `e2.name=e.name` -- hardened to `name_fold` (case-insensitive cross-type collision; zero current victims, future-proofs the thesis). All 5 projects' regression grids clean; MCP `lookup_entity` verified 9/9 in-process (the stdio smoke harness is pre-broken on env-propagation, unrelated). Prod brought into full lockstep via the runbook `pg_dump` dev -> `psql -1` restore (matrix identical, schema + fidelity + carve-out confirmed; prior prod state archived for Tier-2 rollback); prod MCP image rebuild is the only residual (data refresh needs none per the runbook -- old image still correct because `ILIKE` was always case-insensitive). Commit `8093e42f` (code+migration+SCHEMA.md+API_CONTRACTS.md; pushed). Two tracks spun out to HANDOVER Small followups: upstream ezQuake `help_variables.json` mislabel PR (feeds the help-json-doc-consistency-gate pipeline) and the pre-existing MCP smoke-harness breakage. Memory: `reference_qw_oracle_floor_vs_clean_reload.md` + `feedback_parking_verified_state_is_hypothesis.md`.


## 2026-05-15 (PM) -- ezquake comment-promotion REVERTED + two-audience doc model + provenance guard -- SHIPPED (single session)

Reversal and correction of the morning "source-truth fix" entry below. slime (active ezQuake dev) clarified the model the prior session had inverted: `help_*.json` is USER documentation (WHAT a cvar does); the source `// trailing comment` is CODER rationale (WHY the code is so) -- two different-audience surfaces by design, not mirrors. Primary-sourced: of ezQuake HEAD cvars carrying both, 97% have genuinely different text (3% identical, all in one subsystem = one contributor's habit). Under that model the `2edce42b` cvar comment->`desc` promotion was a PROVENANCE BUG: it laundered coder notes/FIXMEs into the user-doc field and made the loader stamp `description_origin='help_json'` (curated user doc) for code-comment text, masking the exact user-doc gaps the help-JSON audit exists to find. Decisive test (primary-sourced both sides, every extracted tag): upstream `help_variables.json` carries no `desc` for `extralogname`/`skill`/`mvd_info_setup`/`qtv_sayenabled` -- nor for the "legit-mirror" control `cl_voip_demorecord` -- yet our DB had `help_desc` byte-identical to the trailing comment, all `help_json`. Fix: removed the ezQuake-side promotion in `_handler_cvars.py`; the loader's project-gated fallback already surfaces code-only-engine comments as `source_inline` correctly (KTX 68 / MVDSV 35 verified intact -- the revert does NOT regress them). All 15 ezQuake tags re-walked: bug signature 502 rows / 47 entities -> 39 / 3 (the residual 3 are genuine upstream `cl_voip_*` mirrors, correctly `help_json`). Added regression guard `F1.ezquake.anchor.no_comment_promotion` (pins the signature at 3; typecheck clean). The 2 ezQuake floors that surfaced (`cvar_count` 2992->2997, `source_retired` 204->209) were proven PRE-EXISTING `3be4d576` retreat-fix staleness via git-blame + a 0-duplicate-row idempotency check (NOT the desc fix), data verified correct, and re-baselined. The macro `teamplay-restricted` "inverted 9/9" claim in the entry below is a VERIFIED NON-FINDING: the DB matches `src/teamplay.c` HEAD (`_derive_teamplay_restricted` derives source-truth-correctly; never inverted) -- a dispatched-terminal claim that failed primary-source verification; do not propagate. Commits `219f6293` (landscape) / `dc50b3ef` (handler revert) / `1f0227f5` (guard + floor recompute) / `99c21532` (docs) / `40454ccb` (audit-restart handoff). The help-JSON empty-entries audit RESTARTS on clean L1 with the corrected predicate ("documented" = a genuine help-JSON surface is non-empty; a trailing comment is not user documentation) -- fresh-terminal handoff `docs/superpowers/parking/2026-05-15-handoff-ezquake-helpjson-userdoc-gap-audit-restart.md` (operator launched it this session). Memory `reference_ezquake_dual_doc_model.md` rewritten -- it had encoded the wrong "promote the comment" assumption and reinforced rather than caught the error. Landscape + full verification: `docs/superpowers/parking/2026-05-15-l1-extractor-entity-classification-followups.md` -> "Cvar provenance pass (2026-05-15)".

---

## 2026-05-15 -- ezquake extractor source-truth fix (cvar desc + macro teamplay) -- SHIPPED (single session)

> **SUPERSEDED / CORRECTED 2026-05-15 PM (see entry above).** The cvar comment->`desc` promotion described below was later found to be a PROVENANCE BUG (two-audience doc model, confirmed by slime) and was REVERTED; the macro `teamplay-restricted` "9/9 backwards" claim is a VERIFIED NON-FINDING (the DB matched `src/teamplay.c` all along). This paragraph is retained per append-only doctrine as the record of what was believed at the time -- not what is true. Do not act on it.

Root-caused a documentation-drift complaint (three parallel terminals working off poisoned L1 data) to one defect: the ezquake extractor's merge step trusted help-JSON over the captured source/AST signal. Two surgical handler fixes -- `_handler_cvars.py` promotes the source `//` trailing comment to `desc` when help-JSON has none (help-generate is a runtime tool that cannot see comments, so an omission was mis-read as "undocumented"; the empty-entries audit then mis-flagged these as needs_doc), and `_handler_macros.py` derives `teamplay-restricted` from the AST registration form (`Cmd_AddMacro`=false / `Cmd_AddMacroEx` non-NORULES=true) instead of help_macros.json's inverted `incomplete` placeholders (9/9 spot-checked were backwards at head). Pure handler logic -- no schema/loader/migration (loaders already map `entry.desc->help_desc`, `entry['teamplay-restricted']->teamplay_restricted`). Verified across all 15 ezquake versions via the canonical `extract-tag` walk: macro `wrong=0` every version (was inverted 3.6.0+); cvar promotion non-zero every version (42 at 3.1/3.2.x -> 27 at head, a coherent help-JSON-grew-over-time curve); regression guard never tripped; source repo restored to master. Commit `2edce42b`. The "127 contaminated cvars" figure circulated mid-investigation was a cross-project unscoped-query artifact -- project-scoped truth is ezquake 24 / mvdsv 35 / ktx 68. Deferred to a fresh terminal (handover `docs/superpowers/parking/2026-05-15-handoff-ghost-classification-f2-scoping.md`, commit `b417b61f`): ghost classification `source_retired`-vs-`doc_only` for help-JSON-only ghosts like s_stereo (INVESTIGATE not patch -- entangled with the same-day entity-state-retreat fix `3be4d576`); `F2.flickering_presence` scoping out all-versions-`source_file IS NULL` ghosts (pure quality-grid.ts probe edit, no walk); plus consolidation -- quarantine the hallucinated platform "sister finding" commit `291268bd`, relabel the help-JSON audit's `kick_to_ciscon` vocabulary (ezquake-dev is nano/slime; sv_* belongs to mvdsv; ciscon is QWiki/community), and note the convergence tracker's `Cmd_AddLegacyCommand` finding is stale (handler already emits `ast.legacy_alias_of`; gap is loader-persistence + schema). Memory: `reference_ezquake_dual_doc_model.md`. Cross-cutting sidecar: the `upstream-pr-reminder.sh` hook's git-commit/push path was root-caused and rewritten to detect the target repo via `git rev-parse --show-toplevel` vs the monorepo root (was a path-prefix test that false-blocked leading `cd <monorepo-root>` AND false-allowed nested research/repos/* commits) + an 8-case regression harness; commit `37821460`.

## 2026-05-15 -- Entity-state retreat loader fix -- ARC SHIPPED (single session)

Loader correctness arc surfaced during the ezquake HEAD re-walk to `3f9e724f` (PR #1120 docs-cleanup-merge). 154 ezquake entities had `entities.last_seen_version='head'` despite zero rows at HEAD in their per-type `*_versions` table -- `load-version.ts` pruned stale version-rows but never reconciled the entity row. Spec: `docs/superpowers/parking/2026-05-15-entity-state-retreat-loader-bug.md`. F1.last_seen_max_ordinal FAIL 154 -> PASS 0 on ezquake; fte/mvdsv/qwcl/ktx unchanged (shared loader, no pre-existing drift). Three commits: `3be4d576` (retreat block in load-version.ts + LoadVersionResult.entitiesRetreated + .fullyOrphanedEntities + skip asset_category/match_event), `9194e3b9` (ezquake floor + anchor probe re-baseline post-retreat), `b9b9b0c4` (citation-flip to_state derivation HEAD_ORDINAL->doc_only / else->source_retired + prune-cross-type-orphans change_events cascade + second cvar re-baseline after the cleanup pruned 5 scr_weaponstats_* orphans). The retreat exposed two pre-existing latent bugs: the citation-flip scan hardcoded `to_state='source_retired'` for entities still at head (now derives from row.ordinal), and `prune-cross-type-orphans.ts` missed the `change_events` FK cascade so any orphan with diff-versions history blocked the entities DELETE (now deletes change_events before entities). 154 `removed_from_head` transitions logged (153 doc_only->retired + 1 source_backed->retired); 32 silent source_backed->doc_only at-head reconciliations (citation-flip scan already logs those per-version). Prod refreshed twice (post-retreat then post-cleanup) via standard DEPLOYMENT.md routine; final prod state ezquake doc_only=57 / source_backed=3721 / source_retired=275. No schema migration. Floor-probe + citation-flip HANDOVER followups closed during the same session; pruner cascade bug closed opportunistically. Next arc: the 3 pre-existing F2 anomalies (`F2.flickering_presence`, `F2.doc_only_crosstab`, `F2.default_value_ping_pong`) are informational not blockers; fte's 7 unrelated regression failures (doubled asset counts) carried forward untouched.

## 2026-05-08 -- Extractor discipline catch-up -- ARC SHIPPED (Phases 1-7 complete)

Extractor discipline catch-up arc lifts KTX-grade extractor validation discipline
(idempotency / reproducibility / parallel-vs-serial / per-migration probes) to universal
coverage across all 5 extraction projects (ezquake / fte / qwcl / mvdsv / ktx),
retroactively certifies existing projects against the 4 runtime gates, and bakes the
discipline into the onboarding skill so future codebases inherit by default. Arc spec:
`docs/superpowers/parking/2026-05-08-extractor-discipline-catchup.md`. Plan:
`docs/superpowers/plans/2026-05-08-extractor-discipline-catchup/`. Cert doc:
`docs/superpowers/reviews/2026-05-08-extractor-discipline-catchup-arc-review.md`. Single
F1 HANDOVER (pytest sys.path pollution in full-suite mode; per-handler tests PASS in
isolation). KTX-only idempotency-ktx.sh deleted in P1. Next arc: CI setup for qw-oracle
(wires universal probes into workflow YAML; unblocked once P1-P4 ship).

- **Phase 1 (2026-05-08): idempotency gate -- DONE (f64ef308).** Universal idempotency.ts
  lifts KTX-only bash. All 5 projects PASS in steady state. DONE_WITH_CONCERNS (V6 stdout
  contamination + V8 historical-narration grep; both amended in phase MD).
- **Phase 2 (2026-05-08): reproducibility gate -- DONE (2e7808eb).** reproducibility-check.ts;
  filesystem-only; all 5 PASS. Two drain-now fixes (git diff scoped to output/; stderr
  optional chaining) committed inline.
- **Phase 3 (2026-05-08): parallel-vs-serial gate -- DONE (8f561cba).** parallel_serial_helpers.py
  lifted to extractor_lib/tests/; mvdsv new test PASS; ktx tests updated PASS;
  ezquake/fte/qwcl deferred-safe. F1 HANDOVER (sys.path pollution).
- **Phase 4 (2026-05-08): migration probes gate -- DONE (9901f308).** migration-probes.ts
  runner + db/migration-probes.ts registry; 12/12 PASS; no findings.
- **Phase 5: authoring guide + SKILL.md part 1 -- DONE (b2f8a107).** VALIDATION-GATES.md
  (7 sections) + VALIDATION-RUNBOOK.md cross-link + onboard-extractor SKILL.md update
  (register-in-config-dict step + validation-step expansion to 4-5 probes).
- **Phase 6: audit cadence + SKILL.md part 2 -- DONE (aae53d38).** EXTRACTOR-PLAYBOOK.md
  audit-cadence section + HANDOVER.md tracker entry + SKILL.md no-per-project-bash callout.
- **Phase 7: cert doc -- DONE.** Arc-close graduation-readiness cert doc. This entry.

## 2026-05-05 -- KTX Layer 1 Onboarding -- ARC SHIPPED (Phases 0-8 complete)

KTX Layer 1 Onboarding arc adds canonical KTX (https://github.com/QW-Group/ktx) as the 5th codebase in QW Oracle Layer 1. Pass 1 first-class entity surface (cvar / command / info_key / log_template) + Pass 5 gameplay-content surface (game_mode / mode_default / election_type / death_rule / monster / score_system / drop_item / loc_macro / teamplay_message + 7 match_event entity types) + 3 pure-additive migrations + cross-header Pattern 6 lift. Nine phases (0 through 8 plus Phase 5.5 mid-arc retrofit). Spec: `docs/superpowers/specs/2026-05-04-ktx-onboarding-design.md`. Plan: `docs/superpowers/plans/2026-05-04-ktx-onboarding/`. Execution driven by arc-orchestrator (sessions #1-#4) + arc-executor (per-phase fresh terminals). **D16 single-commit-per-phase: 8-for-8 functional streak with two housekeeping asterisks (Phase 1 ship-split + Phase 7 fixture follow-on).** All five doctrine reference sites + arc-touched surface free of KTX-tree-sitter framing post-arc (F19 + F22 verified). HANDOVER backlog item "qw-oracle slim-doc Arc 1 refresh sweep" absorbed and ready for operator deletion. arc-reviewer pass runs in fresh terminal post-arc per skill structural requirement; post-arc handoff at `docs/superpowers/parking/2026-05-07-ktx-onboarding-postarc-handoff.md`.

- **Phase 8 (2026-05-07): end-of-arc docs sweep + 7 EXTRACTOR-PLAYBOOK additions + F19 doctrine survival -- DONE.** Single commit `83288501` (5 files, +414/-51). Three deliverables: (1) qw-oracle slim-doc Arc 1 refresh sweep across `apps/qw-oracle/{README,SCHEMA,OVERVIEW}.md` -- Postgres-correct preamble (SQLite era retired per Arc 1 D2; Bun-only runtime; 12-tool MCP; Discord-only Layer 2 per D9-revised; Layer 3 first-class), KTX schema deltas (channel widening + match_event entity type + 7 gameplay_mechanics.kind admits + monster admit), table count refreshed, F20 HANDOVER backlog absorbed; (2) `apps/qw-oracle/scripts/extractors/EXTRACTOR-PLAYBOOK.md` extended (+298 lines) with the four originals (Pre-Port Discovery Sweep methodology + Pre-Commit Discovery Cross-Check methodology + Handler-grouping rationale per D6 + Pattern 15 STRING_LITERAL-array walker) + Pattern 10 ENUM_DECL widening note (Phase 4) + new Pattern 16 X-macro file-parse (Phase 4) + dual-row design note for log_template + match_event (D10 / F17) + F25 cross-arc invariant (Pattern 13 emission required for any libclang handler with cross-file refs; Phase 5.5 disposition closure) + F26 Pattern 6 string-literal-only scope note (Phase 5) + F27 Pattern 9 banner-coverage variability note (Phase 5; MVDSV ~28% vs KTX 0%) + F28 non-Visitor / non-libclang handler infrastructure section (full 7-method lifecycle stub list + load-version transition-scan exclusion convention; Phase 6) + F29 anchor probe live-data verification discipline (joins F23 + F27 as third instance of the same probe-spec drift class; Phase 7); (3) F19 + F22 doctrine fix survival verified across 5 reference sites (`apps/qw-oracle/OVERVIEW.md` + `EXTRACTOR-PLAYBOOK.md` + `extractors/CLAUDE.md` + `VALIDATION-RUNBOOK.md` + user-memory file). One pre-existing tree-sitter slip in `apps/qw-oracle/scripts/extractors/ezquake/diagnostics/extraction-comparison-report.md` (excluded-path historical artifact predating D2) drained inline with a superseded-by-D2 note (1 line). **Resolves:** F17 (PLAYBOOK addition for dual-row design), F19 (doctrine fixes survived; no recursion), F20 (HANDOVER backlog item absorbed), F22 (VALIDATION-RUNBOOK.md doctrine fix survived), F25 / F26 / F27 / F28 / F29 (PLAYBOOK additions). **Closes the KTX Layer 1 Onboarding arc.** Mostly inline execution per `feedback_no_subagents_for_mechanical_edits.md`; markdown sweep with full content shipped via phase MD. **D-rule compliance:** D14 ✓ (no JSONB changes); D15 ✓ (no loader changes); D16 ✓ single-commit (8-for-8 functional with P1 + P7 housekeeping asterisks); D17 -- this halt + verification IS the boundary; D18 ✓ inline-dominant matches phase shape; D19 ✓ ASCII; D20 ✓ main tree. Working-tree fence held: 23 uncommitted unchanged across Phase 8; operator's MCP-API + qwiki side-tracks untouched. No new findings; no decision amendments. Phase-boundary commit: `83288501`.

- **Phase 7 (2026-05-06): validation surface (F1 quality-grid probes + JSONB regression gate + VALIDATION-RUNBOOK.md KTX section + 5-engine cross-project audit) -- DONE_WITH_CONCERNS (3 inline F29 anchor probe-spec drains; 1 housekeeping follow-on commit for runbook fixture NOT-NULL columns).** Phase 7 main commit `6fabeecd` ships 7 files (+783/-6): 24 KTX F1 probes added to `apps/qw-oracle/scripts/load-knowledge/quality-grid.ts` (10 floor + source_state + 9 gameplay-kind + 5 anchor probes; new `makeGameplayKindProbe` helper; `KTX_FLOOR_PROBES` + `KTX_GAMEPLAY_KIND_PROBES` registered in `REGRESSION_PROBES`); JSONB regression gate `probeJsonbNotStrings` extended to cover 4 KTX-relevant JSONB columns (`match_event_versions.attributes_json` + `emission_call_sites_json`, `gameplay_mechanics.props_json` + `ruleset_gate_json`, `gameplay_entity_defs.props_json`); idempotency probe script at `apps/qw-oracle/scripts/extractors/ktx/idempotency-ktx.sh` (102 lines, executable, gates count drift + content-hash drift on re-run); VALIDATION-RUNBOOK.md KTX section landed (~196 lines) mirroring the prior-engine per-section format; 5-engine cross-project audit report at `docs/superpowers/reviews/2026-05-06-ktx-onboarding-cross-project-audit.md` (204 lines; 4 effective findings: 0 critical / 3 important / 1 nit). **Housekeeping follow-on commit `9019d2d8`** (1 file, +25/-23) corrects per-migration probe fixtures in the runbook section: `log_template_versions` INSERT adds `format_string_normalized` + `extracted_at` NOT NULL + switches to SELECT-from-entities form; `match_event_versions` INSERT adds `event_name` + replaces nullable `xsd_version` with NOT NULL `xsd_path` + adds `extracted_at`; `gameplay_entity_defs` + `gameplay_mechanics` INSERTs add `source_ref` NOT NULL; DO block wraps in `BEGIN/ROLLBACK` at SQL level (original had `ROLLBACK INSIDE` the DO block which postgres rejects); `match_event_versions` index-count comment corrected from "2 indexes" to "3 indexes (PK + 2 named)" to match live pg_indexes output. CHECK widenings themselves correct in 6fabeecd; PASS via corrected fixtures (009 logfile + 010 match_event + 011 monster + 7-of-7 gameplay_mechanics.kind admits + nonexistent_kind rejection in both gameplay_* tables all PASS). Same probe-spec drift class as F23/F27/F29; contained to fixtures the main commit just added; structurally clean follow-on caught at boundary verification Probe 5. **D16 7-for-7 streak holds functionally** -- housekeeping follow-on is the closest precedent Phase 1's perf follow-on `864fdf7c` set, treated as separate from the phase commit count. **F1 grid (live results, post-Phase-7):** all 24 KTX probes PASS at exact equality (cvar=260, command=358, info_key=7, log_template=1195, match_event=7, monster=13, game_mode=27, mode_default=317, election_type=5, death_rule=27, score_system=3, drop_item=31, loc_macro=15, teamplay_message=21; all 5 anchors PASS with executor-time predicate corrections per F29). Prior-engine F1 grid: FTE/QWCL/MVDSV all clean post-KTX (123/123/123 PASS); ezquake has 5 pre-existing FAILs (`F1.ezquake.floor.{cmdline_param,command,cvar,hud_element}_source_state` + `F1.ezquake.anchor.doc_only_count`) tracked at HANDOVER:26 since 2026-04-28 -- the audit's D.1.1 captures one entry-point. None are KTX-onboarding-introduced; spirit of Phase 7 Probe 2 ("Phase 7 broke prior-engine invariant") not violated. **JSONB regression gate:** 17 columns audited cross-project; 0 string scalars (D14 holds). **Idempotency probe** ships correctly (script syntax-clean, executable); not run live in Phase 7 executor terminal because host lacks psql. D15 invariant enforced by Phase 2-6 loaders' boundary checks; Phase 7 ships the audit gate, doesn't re-verify the invariant. **Cross-project audit (4 effective findings):** D.1.1 ezquake doc_only_count stale anchor (Important; HANDOVER follow-up; pre-existing); D.2.1 KTX handler class-name shape inconsistent (Nit; 4 use `<Type>KtxHandler` / 4 use `Ktx<Type>Handler`; cosmetic; defer indefinitely); D.3.1 + D.6.1 KTX gameplay_taxonomies `_stats.election_type.source_total` parallel-aggregator-naive (Important; stat-only under `--workers >1`; row data correct; <30 line Pattern-13-style fix; defer to Phase 8 or future arc); D.8.1 F19 OVERVIEW.md verify (Important; Phase 8 already owns F19 verification). **F22 doctrine fix preserved:** zero KTX-tree-sitter framing in VALIDATION-RUNBOOK.md (verified by Phase 7 boundary probe 7); F19 + F22 doctrine fixes survived the arc through Phase 7. **Three F29 inline drains (Phase 7 MD anchor probe-spec drift):** (1) Anchor 3 monster name `'fish'` -> `'monster_fish'` (live id1-classname form); (2) Anchor 4 `attributes_json` `jsonb_typeof='object'` -> `'array'` (live JSONB array of attribute-descriptor objects per F14); (3) Anchor 5 `format_string LIKE '\t\t\t<%'` -> `LIKE '%<%>%'` (live KTX format strings have varied XML wrapper-level prefixes; 15 of 16 logfile rows match the relaxed predicate). Underlying handler / D10 / D14 / F9 / F14 / F17 invariants hold; the predicates were paraphrased from spec rather than verified against live data. F29 captures the discipline gap (joins F23 + F27 as third instance); Phase 8 EXTRACTOR-PLAYBOOK candidate: "anchor probe authors verify predicates against live dev DB before shipping". **D-rule compliance:** D14 ✓; D15 ✓ (script ships, invariant enforced upstream); D16 ✓ with housekeeping asterisk; D17 -- this halt + verification IS the boundary; D18 ✓ (Sonnet medium for F1 probes + JSONB gate; Opus medium for cross-project audit); D19 ✓; D20 ✓. **Working-tree fence held**: 23 uncommitted unchanged across both Phase 7 commits; operator's MCP-API + qwiki side-tracks untouched. **HANDOVER additions (Recently opened):** KTX gameplay_taxonomies stat-reproducibility fix (D.3.1), KTX handler class-name shape consistency (D.2.1, cosmetic). All 7 phase-boundary probes PASS, independently re-verified by orchestrator session #4 against live tree (F29 at line 512 + ownership table updated at line 543; audit report at 204 lines with 5-engine F1+F2 grid; housekeeping commit content verified pure fixture corrections; F22 doctrine fix preserved; working-tree fence honored). Resolves F21 (validation runbook obligation). Phase-boundary commits: `6fabeecd` + housekeeping `9019d2d8`. F-amendments: F29 (new). Decision amendments: none. Open carry-forwards for Phase 8: F19/F22 verification surviving the arc + F20 HANDOVER backlog absorb + F17/F26/F27/F28/F29 PLAYBOOK additions (7+ EXTRACTOR-PLAYBOOK additions in queue).

- **Phase 6 (2026-05-06): match_event handler (XSD-driven; not libclang) -- DONE_WITH_CONCERNS (3 phase-MD gaps drained inline; F28 surfaces non-Visitor handler infrastructure pattern).** New standalone Python handler `_handler_match_events.py` (438 lines) at `apps/qw-oracle/scripts/extractors/ktx/`; per D3 amendment 2026-05-05 carve-out + spec 5.6.c, this handler does NOT inherit from `Visitor` and does NOT use libclang. Four-stage flow: `_parse_xsd` (xml.etree.ElementTree over `resources/extralog/ktxlog_0.1.xsd`) -> `_grep_emissions` (multi-line regex over `items.c` + `combat.c` + `client.c` + `logs.c`) -> `_merge_emissions_into_events` -> `finalize`. Duck-typed Visitor lifecycle stubs (7 methods: `start_file` / `end_file` / `enter_function` / `exit_function` / `enter_compound` / `exit_compound` / `visit_cursor`) keep `extractor_lib._visitor.walk_tu_dispatch` happy without modification. Output `ktx-match-events-ast.json` carries 7 entries (one per `<xs:choice>` event_name) with attribute schemas (with simpleType constraint resolution: `maxed_integer` 0-200 resolves on death.armorleft + drop_backpack/pick_backpack/pick_mapitem ammo fields) + 13 total emission call sites with `containing_function` heuristic per spec 5.6.b. New TS loader `load-match-events.ts` (74 lines) mirrors `load-log-templates.ts`; `attributes_json` + `emission_call_sites_json` bind JSONB structured values directly via `tx.json(...)` per D14 (no JSON.stringify; F1.jsonb_columns_not_strings regression gate clean). Wiring: `extract.py` registers `KtxMatchEventsHandler`; `types.ts` widens `EntityType` to admit `'match_event'` + adds `MatchEventEntry` / `MatchEventAst` / `MatchEventVersionRow` interfaces; `natural-keys.ts` adds `upsertMatchEventVersion` (PK `(entity_id, version)` per migration 010 ON CONFLICT DO UPDATE); `load-version.ts` adds dispatcher branch + skips the source_file transition scan (see F28); `extract-tag.ts` adds the `ktx.match_event` slot to `ENTITY_JSON_FILES`; `prune-cross-type-orphans.ts` adds the entry to `PER_TYPE_VERSION_TABLE` (cascade from `EntityType` widening). **Counts in dev DB:** `entities` cvar=260 + command=358 + info_key=7 + log_template=1195 + match_event=7 = 1827 total; `match_event_versions` = 7 rows; `log_template_versions` channel='logfile' XML-shaped rows = 9 (UNCHANGED from Phase 2 baseline -- D10/F17 dual-row design holds; Phase 2 printf-handler git-clean since session #3 wrap, 0-line diff verified). **F14 anchors reproduced exactly:** 7 events, 5 named complexTypes (damagetype + deathtype + backpacktype shared by pick/drop_backpack + poweruptype shared by pick/drop_powerup + mapitemtype), 4 named simpleTypes (maxed_integer / iptype / modetype / porttype), 13 emission sites with file distribution items.c=10 + combat.c=2 + client.c=1 + logs.c=0; per-event attrs/sites: damage=8/2 + death=8/1 + drop_backpack=7/1 + drop_powerup=4/1 + pick_backpack=7/1 + pick_mapitem=4/6 + pick_powerup=4/1. **F14 third amendment 2026-05-06** corrects the anchor body's "7 complexTypes" prose mislabel (those are 7 EVENT names; live XSD has 5 named complexTypes shared by 7 events). **D14 JSONB binding:** 0 string-scalar violations (Probe 6); both `attributes_json` and `emission_call_sites_json` are `jsonb_typeof = 'array'`. **D15 idempotency:** load-twice preserves 7+7 counts (Probe 8). **D16 single-commit-per-phase: 6-for-6 streak** (Phases 1, 2, 3, 4, 5, 5.5, 6 each shipped as a single commit since Phase 1's 2-commit deviation lesson; Phase 6 commit `e0133248` is 9 files, +1095/-3 lines). **F25 closed status holds:** match_event handler doesn't accumulate cross-file state (XSD parse + regex grep are pre-fork single-pass), so parallel-safety is moot for this handler. **Three Phase-MD gaps drained inline:** (1) Phase MD listed 5 lifecycle stubs; driver calls 7 (also `enter_compound` / `exit_compound`). Without the missing two the handler crashes mid-walk. Handler ships 7 stubs total; covered by F28. (2) `load-version.ts` (~line 587) transition-scan assumes `source_file` column on every per-type versions table; `match_event_versions` uses `xsd_path` (XSD is source-of-truth). Exclusion list extended from `{asset_category}` to `{asset_category, match_event}`. Future entity types with non-C-source truth hit the same gap; covered by F28. (3) `EntityType` widening forced an explicit entry in `prune-cross-type-orphans.ts:27`'s non-Partial `Record<EntityType, string>`; drained inline. `quality-grid.ts:55` uses `Record<string, string>` so no compile error there; pre-existing coverage gap (info_key/log_template/qc_builtin already absent) -- left to Phase 7 per `feedback_narrow_arc_before_broad.md`. **Three advisory observations:** F14 complexType-vs-event terminology drift (third amendment captures the 7-events-share-5-complexTypes mapping); Phase MD's Inputs section claimed `xsd_version` + `emission_call_sites_json` are NOT NULL but live migration 010 has both nullable (loader populates them anyway, no runtime issue, doc cleanup); Phase MD Task 2 verification claimed `extract.py --help` lists handler names but argparse only shows the static `--handlers` flag help text (load-bearing grep probe still PASSes; human-readable claim imprecise). **No pytest file shipped for match_event handler** -- Phase 6 follows Phase 2's "no dedicated pytest" pattern (Phase 2 also had no test file; Phases 3/4/5 each had one). The 9 phase-boundary probes + downstream Phase 7 F1 quality-grid probes carry the verification load. Acceptable. **New finding F28** (non-Visitor handler infrastructure gaps): the transition-scan source_file assumption + the 5-vs-7 lifecycle stub list. Both drained inline in Phase 6. Phase 7 cross-project audit probe candidate (exclusion-list-vs-per-type-versions-table reconciliation as new entity types land); Phase 8 EXTRACTOR-PLAYBOOK addition for non-Visitor / non-libclang handler patterns including full lifecycle stub list + transition-scan exclusion convention. All 9 phase-boundary probes PASS, independently re-verified by orchestrator session #4 against live tree (AST stats: 7 events / 4 simpleTypes / 5 complexTypes / 13 sites; class shape: standalone non-Visitor; lifecycle stubs: 7 present; flow stages: 3 present; TS type-check clean; printf-handler git-clean since session #3 wrap; working-tree fence honored: 9-file commit confined to extractors/ktx/ + load-knowledge/ paths, no MCP-API or qwiki side-tracks bled in; F28 evidence at `load-version.ts` ~lines 580-610). Phase-boundary commit: `e0133248`. F-amendments: F14 third amendment (complexType vs event terminology). New findings: F28.

- **Phase 0 (2026-05-05): doctrine fixes + obsolete-extractor deletion + OUT_OF_SCOPE catalog.** Five reference sites corrected to libclang/canonical-KTX framing (OVERVIEW.md 3 lines, EXTRACTOR-PLAYBOOK.md 5 lines, extractors/CLAUDE.md 1 line, VALIDATION-RUNBOOK.md 2 lines per F22, user-memory `project_extraction_pipeline_vision.md` 3 lines); obsolete TS regex extractor at `apps/qw-oracle/scripts/extractors/ktx/commands.ts` deleted (F18); `apps/qw-oracle/scripts/extractors/ktx/OUT_OF_SCOPE.md` created with 7 pre-decided SKIP entries per D13 (Bucket-3 indexed-family cvars, truly orphaned drift cvars, lsType_t, gameType_t, fb_spawn_t, stats_format_t, fixed_maps_list). All 8 inline tasks executed without subagent dispatch per `feedback_no_subagents_for_mechanical_edits.md`. F22 (5th doctrine site, discovered during Phase 0 drafting) was already captured in review-findings.md from the planning commit chain (`08510258`) -- no mid-execution finding append needed. F19 was the find-string trigger in Phase 0 Tasks 1/2/3/5 even though F22 expanded its scope; the doctrine-fix corpus is now 5 sites end-to-end. Three deferred items in Phase 0 MD's "Open questions" section remain as documented (Phase 2e label cleanup, F19 vs F22 wording, output/ contents). Tasks 1-7 verification probes PASS; phase-boundary probes 1-8 independently re-verified by the orchestrator session against live tree (zero non-dusty-ktx tree-sitter/quakec residuals; commands.ts gone; output/ preserved; OUT_OF_SCOPE.md present with 7 headings and zero doctrine slips; F22 capture confirmed; commit landed clean; zero runtime imports of the deleted extractor). Phase-boundary commit: 860aaf0d. No mid-execution findings; no decision amendments.

- **Phase 1 (2026-05-05): Pattern 6 cross-header lift + 3 migrations + ktx gameplay_sources row -- DONE_WITH_CONCERNS.** Pattern 6 lifted to depth-1 #include closure in `extractor_lib._source.collect_file_macros`; wired into `walk_tu_dispatch` so every visitor receives `self.file_macros` pre-dispatch (D4); ezQuake's `_handler_commands.py` refactored to consume `self.file_macros` (regex-based local map removed); 4 pytest tests at `extractor_lib/tests/test_collect_file_macros.py` all PASS, including the F15 anchor (`LGCMODE_VARIABLE` -> `"k_lgcmode"` and `TOT_MODE_VARIABLE` -> `"k_tot_mode"` resolve from KTX `commands.c` via depth-1 walk into `g_local.h`). Migration slot collision per D5 amendment confirmed live -- `008_community_schema.sql` already taken by qwiki arc; KTX migrations renumbered to **`009_ktx_log_template_logfile_channel.sql` / `010_ktx_match_event_type.sql` / `011_ktx_gameplay_kinds.sql`**, content unchanged. All three applied to dev DB; all 4 CHECK widenings verified (`log_template_versions.channel += 'logfile'`; `entities.type += 'match_event'`; `gameplay_entity_defs.kind += 'monster'`; `gameplay_mechanics.kind += 7 new values`). `match_event_versions` table created with PK `(entity_id, version)` + 2 indexes (`idx_match_event_versions_complex_type`, `idx_match_event_versions_xsd_version`) + FK to `entities(id)`. `gameplay_sources` row for `'ktx'` seeded (idempotent ON CONFLICT DO UPDATE; Phases 2-6 loaders FK-reference). System include null-pointer fix applied inline during Task 7 (`cursor.get_included_file()` returns None for unresolved includes; 5-line try/except guard added; all 4 tests still pass post-fix). All 9 required boundary probes PASS, independently re-verified by orchestrator. Probe 10 (regression) skipped -- no pre-phase snapshot baseline. **Concerns:** (1) **F16: walk-time overhead 66-163%** above F16's <10% gate (KTX `commands.c` +40ms / 66%; ezQuake `vid_sdl2.c` +130ms / 163%). Cost driver: 901 `MACRO_DEFINITION` cursors across 18 depth-1 headers in `vid_sdl2.c`, each paying for `get_tokens()`. Operator chose option (b) -- in-arc scope-narrowing follow-on commit (iteration-time string-literal filter; ~50% projected overhead reduction; ~30 min effort) before Phase 2 starts. F16 amendment with timing data + post-fix numbers will land alongside the optimization commit. (2) **D16 deviation: 2 commits instead of 1.** Subagent committed Pattern 6 lift in `ecf0151d` before the migrations + phase-MD update landed in `44b289ed`; each commit was independently runnable, but D16's single-commit-per-phase target was missed. Advisory; future executor terminals should coalesce when feasible. (3) **Phase MD Task 3 verification probe spec gap.** Probe `grep -E "'monster'|'game_mode'|..." | wc -l` was specified to return >=8 but returns 4 (multiple values per SQL line in the migration; values present and verified individually via Probe 4's CHECK constraint dump). Inline phase-MD amendment for future re-runs. Phase-boundary commits: `ecf0151d` (Pattern 6 lift + tests + ezquake refactor) + `44b289ed` (migrations 009/010/011 + gameplay_sources + phase-MD update). Perf follow-on commit pending (orchestrator session #2 verifies on resume). No new findings; no decision amendments yet (F16 amendment lands with the perf follow-on).

- **Phase 1 perf follow-on (2026-05-06): diagnostic-only, no code change.** Tested F16's hypothesised optimisation (iteration-time string-literal filter on `cursor.get_tokens()` to skip non-string-bodied macros at depth-1 walk; ~50% projected overhead reduction). Empirically falsified: stage decomposition (5 runs each, median on `vid_sdl2.c`) showed the dominant cost is `cursor.location` access on 9,137 `MACRO_DEFINITION` cursors (+51.3ms), not `get_tokens()` on the 901 in-closure hits (~10-15ms total); peek-then-tokens shape adds equivalent overhead in spelling+extent+byte-find, netting zero benefit. `_source.py` reverted to Phase 1 shipped state; 4 pytest tests at `extractor_lib/tests/test_collect_file_macros.py` PASS post-revert; post-revert timing back at Phase 1 baseline (vid_sdl2.c median 121.7ms / 5 macros, commands.c median 37.9ms / 279 macros). **Disposition (F16 amendment 2026-05-06 in `review-findings.md`):** accept depth-1 walk-time overhead as inherent to the libclang per-cursor attribute model under `PARSE_DETAILED_PROCESSING_RECORD`. Rationale: extractor pipeline is offline (cost class is per-tag drag, not interactive latency); per-TU overhead is order-of-seconds total per tag, well inside the operator's offline budget; F16's <10% gate was over-tight given the actual cost model. **Carry-forward:** future depth-N lift attempts should target `cursor.location` access count (narrower TU traversal scope; libclang APIs that pre-filter by file), NOT `get_tokens()` filter relocation. D4's depth-N revisit caveat already parks the work; this amendment adds the cost-driver finding so a future arc doesn't repeat the filter-relocation experiment. Phase-boundary commit: `864fdf7c`. Phase 1 fully wrapped at this point; Phase 2 unblocked.

- **Phase 3 (2026-05-06): modes handler (game_mode catalog + mode_default overlays) -- DONE_WITH_CONCERNS (F25 architectural; clean workaround in scope).** New KTX libclang handler `_handler_modes.py` (1107 lines) cross-codebase port from `Visitor` only per D3; emits two arrays in `ktx-modes-ast.json` -- 27 game_mode catalog rows + 317 mode_default per-line overlay rows. Walks 3 source files: `commands.c` (17 `<mode>_um_init[]` + `common_um_init[]` + `um_list[]` + cmds[] for race/mutator toggle commands + cvar_toggle_msg + cvar_set("X","0") auto-reset sites), `world.c` (10 activation cvar registrations: k_lgcmode/k_instagib/k_midair/k_bzk/k_yawnmode/k_killquad/k_freshteams/k_nosweep/k_race/k_bloodfest), `race.c` (ToggleRace + apply_race_settings + race_settings[]). Pattern 6 cross-header lift (Phase 1 D4) consumed via `self.file_macros` -- `LGCMODE_VARIABLE` and `TOT_MODE_VARIABLE` resolve from `g_local.h` into `common_um_init` per F15. New TS loader `load-modes.ts` (191 lines) idempotently UPSERTs both arrays into `gameplay_mechanics` keyed on `(gameplay_source_id='ktx', kind, name, ruleset_gate_json)` with JSONB direct-bind via postgres-js per D14. Dispatch wiring: `extract.py` registers `KtxModesHandler`; `index.ts` adds `load-ktx-modes` subcommand; `extract-tag.ts` calls `loadModesFromFile` after the entity-loader loop for `project='ktx'`. Seed `seeds/modes-augment.yaml` ships 16 mainstream-mode entries (`community_name` + `wiki_ref`); operator-authored content, optional. Pytest `tests/test_handler_modes.py` (6 PASSED): `test_catalog_count_27`, `test_mode_default_count_in_band`, `test_cross_header_macros_resolved` (strengthened to assert row names + values), `test_ca_team_structure`, `test_berzerk_activation_cvar_is_k_bzk`, `test_4on4_overlay_has_teamplay`. **Counts in dev DB:** game_mode=27 (17 um_list peers + race + bloodfest + 8 mutators per F5 grid; D11 distribution exactly 17/1/1/8 -- `um_init_string|standalone=17` / `cvar_toggle_with_init_string|standalone=1` (race) / `cvar_toggle_only|standalone=1` (bloodfest) / `cvar_toggle_only|mutator=8`), mode_default=317 (52 baseline `common_um_init` apply_order=1 + 265 per-mode overlays apply_order=2; F6 band [280, 360]). **Cross-header macros (F15):** `k_lgcmode|0` at `commands.c:4178` + `k_tot_mode|0` at `commands.c:4179` (both resolved via Phase 1 lift). **D14 JSONB binding:** 344/344 rows have `gate_type=object` AND `props_type=object` (no string scalars). **D15 idempotency:** 344 -> 344 on re-run; 0 inserted, 27+317 updated. **D16 single-commit discipline held** (Phase 1 deviated to 2 commits; Phases 2 + 3 ship as one each). Mid-execution bug caught + fixed in scope: `_strip_concat_string_literal` was eating the field separator in the macro+literal concat path (`LGCMODE_VARIABLE " 0\n"` resolved to `"k_lgcmode" + "0"` = `"k_lgcmode0"` instead of `"k_lgcmode 0"`); fixed with one-line space injection at concat site; pytest `test_cross_header_macros_resolved` strengthened to assert row names + values for forward regression-catch. **New finding F25** (modes handler not parallel-safe): handler accumulates cross-file refs (`_activation_cvar_refs`, `_race_toggle_ref`, `_mode_default_rows`) on instance state; `multiprocessing.Pool` fork workers each get their own copy and the parent's instance state never populates. Verified: parallel `--workers 4` produces 0 mode_defaults; serial `--workers 1` produces 317. Workaround: serial-mode guard in `extract.py` -- if `modes` handler in selection AND `workers != 1`, force `workers = 1` with one-liner log message. Per-tag extraction goes from ~5s parallel to ~16s serial; offline pipeline budget absorbs it. Two future-arc refactor options documented in F25: (a) emit cross-file refs as typed pseudo-rows from `end_file()` so finalize joins by `_kind`; (b) add `Visitor.parallel_safe: bool` attribute that `extract.py` reads. **Phase 4/5 carry-forward:** taxonomies + tables handlers may surface similar cross-file ref needs (death_rule `related_weapon` joins; teamplay_message banner-comment harvest); flagged for those drafters. F25 disposition: defer principled refactor to a future arc. Phase-boundary commit: `573d8b8e`. All 12 boundary probes PASS, independently re-verified by orchestrator session #2.

- **Phase 5.5 (2026-05-06): modes handler Pattern 13 emission retrofit -- DONE; F25 closed.** Small in-arc retrofit (60 min projection, 5.1 hours actual including subagent dispatch + verification iterations) -- the principled fix landed in-arc rather than parked, on the back of Phase 5's Pattern 13 evidence. `_handler_modes.py` (504 changed lines; 1107 -> ~1080 net, refactor-not-rewrite) replaces 11 cross-file `self._*` accumulators with typed pseudo-row emission from `end_file()`: `_kind` in `{_mode_default, _meta_activation_cvar, _meta_toggle_cmd, _meta_auto_reset, _meta_um_list_row, _meta_um_list_label_raw, _meta_um_init_decl, _meta_race_ref, _meta_unresolved_macro_line, _meta_skipped_line, _meta_by_array_stat}`; `finalize(all_rows=...)` partitions by `_kind`, re-assembles cross-file ref dicts from the meta rows, builds catalog rows from module constants + re-assembled refs, drops meta rows from output. F25 serial guard removed from `extract.py` (lines 302-310 deleted; 12 lines net removed). New regression gate `test_parallel_serial_equivalence` in `tests/test_handler_modes.py` invokes `extract.py` as subprocess at `--workers 1` and `--workers 4`, asserts row-for-row identical mode_default content (with defensive self-skip if F25 guard reappears, future-proofing against partial rollback). **Live verification (orchestrator session #3, 2026-05-06):** parallel-vs-serial extraction produces byte-identical AST output (`diff` empty); 3.3x speedup (serial 16.9s -> parallel 5.1s on 108 .c files). Counts in dev DB unchanged: game_mode=27, mode_default=317, and the full ktx-source inventory (459 rows: 13 monster + 446 mechanics) holds (D15 idempotency). F1 JSONB gate: 344/344 modes rows have `jsonb_typeof('object')` for both `ruleset_gate_json` and `props_json` (D14 holds). F15 cross-header macros still resolve at `commands.c:4178/4179` (`k_lgcmode|0`, `k_tot_mode|0`). pytest 7/7 PASS cold (22.8s; 6 existing + 1 new equivalence gate). **D16 single-commit-per-phase: 5-for-5 streak** (Phases 1, 2, 3, 4, 5, 5.5 each shipped as a single commit since Phase 1's 2-commit deviation lesson). **F25 disposition closed:** Pattern 13 emission is now a three-consumer arc-pattern (Phase 2 commands + Phase 5 tables + Phase 5.5 modes). The `Visitor.parallel_safe: bool = True` opt-out path proposed in original F25 future-arc options is REJECTED in the disposition-closure amendment -- gating opt-out as a per-handler bool would normalise the broken state-on-self design and recreate the divergence this retrofit eliminates. Cross-arc carry-forward: any future libclang handler with cross-file refs MUST use Pattern 13 emission; Phase 8 EXTRACTOR-PLAYBOOK addition will document this as the canonical convention (joins F26 string-literal-scope note + F27 banner-coverage variability note already flagged for Phase 8). The rejection-as-rule lives in F25 amendment prose + Phase 8 PLAYBOOK addition; orchestrator session #3 declined to formalize as a new D-block in decisions.md (decisions.md is KTX-arc-specific commitments; the Pattern 13 mandate is cross-arc infrastructure that belongs in EXTRACTOR-PLAYBOOK.md). Phase-boundary commit: `44f5b894`. No new findings; no decision amendments.

- **Phase 5 (2026-05-06): gameplay tables handler (monster + score_system + drop_item + loc_macro + teamplay_message) -- DONE_WITH_CONCERNS (F26 + F27 amendments; both inline-resolved).** New KTX libclang handler `_handler_gameplay_tables.py` (914 lines) cross-codebase port from `Visitor` only per D3; emits five Pattern 4 (INIT_LIST_EXPR) struct-array walks across 4 .c files + Pattern 9 banner-comment harvest helper + Pattern 13 cross-file emission for teamplay_message handler-function joins. Output `ktx-gameplay-tables-ast.json` carries 13 monster + 3 score_system + 31 drop_item + 15 loc_macro + 21 teamplay_message = 83 new rows. New TS loader `load-gameplay-tables.ts` (286 lines) cross-table dispatcher: monster rows -> `gameplay_entity_defs`; the other four kinds -> `gameplay_mechanics`; F10 loader-side validation gate enforces `len(positions) == 10` per score_system row. Dispatch wiring: `extract.py` registers `KtxGameplayTablesHandler` as `"gameplay_tables"`; `index.ts` adds `load-ktx-gameplay-tables` subcommand; `extract-tag.ts` calls `loadGameplayTablesFromFile` after `loadTaxonomiesFromFile` for `project='ktx'`. Pytest `tests/test_handler_gameplay_tables.py` (17 PASSED). **Counts in dev DB:** monster=13 (in `gameplay_entity_defs`; gate `{"mode":"bloodfest"}` per F9; F9 source-fidelity locked at `hp_for_kill` after double-amendment), score_system=3 (gate `{"mode":"race"}` per F10; positions length=10 invariant holds for all three: formula1/scaled/win_only), drop_item=31 (gate `{}` universal per F11 amendment count; H_ROTTEN/H_MEGA/WEAPON_BIG2 fallback per F26), loc_macro=15 (gate `{}`), teamplay_message=21 (gate `{}`; Pattern 13 `_fn_def` cross-file joins worked first-attempt). monster_fish at array_position=0 with is_first_required=true (F9 sentinel). 5 distinct sp_* drop_tokens (sp_b/sp_cp/sp_dm/sp_r/sp_sp) all route to `dropitem_spawn_spawnpoint`. **D14 JSONB binding:** 0 string-scalar violations across all 5 new kinds (F1 regression gate clean for both `gameplay_entity_defs.monster` and `gameplay_mechanics` 4-kinds). **D15 idempotency:** load-twice identical row counts. **D16 single-commit-per-phase held** (4-for-4 streak since Phase 1 deviation). **F25 evidence (THE headline finding):** Phase 5 handler is parallel-safe BY STRUCTURE, mirroring Phase 4's existence proof. Extraction ran `--workers 12` in 2.1s with NO new F25 serial-mode guard added. All cross-file data flows through `end_file()` return values + `finalize(all_rows=...)` parameter; never accumulated on `self` post-fork. **Pattern 13 emission shape (typed `_fn_def` pseudo-rows from `end_file()` for cross-file teamplay_message handler-function joins) is the second clean consumer after Phase 2's commands handler.** First attempt; no shape resistance; design works. Pattern 13 is now a documented arc-pattern, not a bespoke trick. Combined with Phase 4's structural parallel-safety, F25 disposition evidence supports "modes is the architectural outlier" reading; orchestrator session #3 surfaces Option 1 (Pattern 13 retrofit on modes as Phase 5.5 in this arc) recommendation to operator at post-Phase-5 boundary. **Pattern 9 banner harvest:** 0 of 21 teamplay_message rows got non-null `harvested_description`; F27 documents this is correct handler behavior against KTX teamplay.c source (zero `/* === */` banner blocks; 8 of 21 handlers macro-expanded via `TEAMPLAY_BASIC` with no banner; remaining 13 use `// Cmd_AddCommand(...)` line-comment style). **Two new findings landed during execution (both inline-resolved):** **F26** -- `extractor_lib._source.collect_file_macros` is string-literal-only by design; `WEAPON_BIG2` (integer body, depth-0 same-file) is excluded by the lift's tokenisation filter despite the phase MD's claim that it would resolve through `self.file_macros`. Tactical fix shipped: extended `_DROPITEM_MACRO_FALLBACK` from 2 entries to 3 (added `WEAPON_BIG2 = 1`); F11 second 2026-05-06 amendment captures the correction. Phase 8 PLAYBOOK note candidate documents the depth-0 string-literal scope. **F27** -- Pattern 9 banner-coverage probe assumed `/* === */` blocks that KTX teamplay.c doesn't have; handler implementation correct (port from MVDSV's `_handler_commands.py`); probe 14's PASS condition `with_banner > 0` is impossible to satisfy against this source. Direct precedent: F23. Phase 8 PLAYBOOK note candidate documents per-source-file banner-style variability. All 17 phase-boundary probes PASS, independently re-verified by orchestrator session #3 against live tree + dev DB (probe 10 spawn_function vs drop_token discrepancy was an orchestrator query phrasing issue, not an executor failure -- 5 sp_* drop_tokens with `dropitem_spawn_spawnpoint` confirmed). **Phase MD inline-example drift:** the WEAPON_BIG2 "pre-lift Pattern 6" claim in the phase MD was incorrect; caught at execution time with a 1-line tactical fix; documented for Phase 8 PLAYBOOK. Sonnet MAX subagent for the handler authoring took ~280s and produced first-time-syntactically-correct 914-line file; one logical bug (WEAPON_BIG2 not in fallback) caught by pytest -- exactly the verification-layer-catches-lift-residuals pattern. Phase-boundary commit: `377f8549`. Decision amendments: none.

- **Phase 4 (2026-05-06): gameplay taxonomies handler (election_type + death_rule) -- DONE.** New KTX libclang+text-parse handler `_handler_gameplay_taxonomies.py` (433 lines) cross-codebase port from `Visitor` only per D3; emits two arrays in `ktx-gameplay-taxonomies-ast.json` -- 5 election_type rows + 27 death_rule rows. Two-stage extraction: Stage 1 walks `electType_t` ENUM_DECL in `progs.h` via Pattern 10 (TU-root cursor intercept; F7 anchor reproduced exactly); Stage 2 text-parses `include/deathtype.h` X-macro file directly (Pattern 16 -- new) to recover both the dt enum tag and the per-entry string token (X-macro's second arg is dropped from the AST after preprocessor expansion). New TS loader `load-gameplay-taxonomies.ts` (194 lines) idempotently UPSERTs both arrays into `gameplay_mechanics` keyed on `(gameplay_source_id='ktx', kind, name, ruleset_gate_json)` with JSONB direct-bind via `tx.json(...)` per D14. Dispatch wiring: `extract.py` registers `KtxGameplayTaxonomiesHandler` as `"gameplay_taxonomies"`; `index.ts` adds `load-ktx-taxonomies` subcommand; `extract-tag.ts` calls `loadTaxonomiesFromFile` after `loadModesFromFile` for `project='ktx'`. Pytest `tests/test_handler_gameplay_taxonomies.py` (8 PASSED): election + death lock-table assertions, sentinel-skip checks, dtCHANGELEVEL=structural, mutual exclusivity invariant, `related_weapon` FK-shape gate. **Counts in dev DB:** election_type=5 (captain / coach / admin / suggest_color / late_join; etNone sentinel skipped per F7); death_rule=27 (29 X-macro lines minus 2 sentinels per F8's locked breakdown; dtCHANGELEVEL kept with category='structural'). Category distribution exactly matches phase-MD probe 8: weapon=10 / environment=10 / telefrag=4 / self=2 / structural=1. id1_baseline=18 / ktx_extension=9, mutually exclusive (zero double-flagged rows). All 8 distinct `related_weapon` values resolve against id1 baseline `gameplay_entity_defs` rows (axe / shotgun / super_shotgun / nailgun / super_nailgun / grenade_launcher / rocket_launcher / lightning_gun -- underscored canonical form per F8 amendment + D9 source-fidelity). **D14 JSONB binding:** 32/32 rows have `gate_type=object` AND `props_type=object` (no string scalars; F1 regression gate clean). **D15 idempotency:** load-twice produces identical row count. **D16 single-commit-per-phase held** (3-for-3 since Phase 1 deviation; Phases 2 + 3 + 4 ship as one commit each). **F25 evidence -- Phase 4 was parallel-safe BY STRUCTURE; no serial guard added.** Stage 2 fires pre-fork in `setup()` populating `self._death_rule_rows` on the parent before fork copy-on-write (workers inherit the populated state read-only); Stage 1 fires post-fork in workers, returns rows via `end_file()`, driver merges into `all_rows` and the parent's `finalize()` reads the merged list (not worker instance state) and dedups. The orchestrator's session-#3 prediction held: "Phase 4 walks single-file ENUM_DECL + single-file X-macro text-parse -- likely parallel-safe by structure". No carry-forward to F25; the design is now an existence proof that handlers CAN be parallel-safe by structure when stages are partitioned into pre-fork eager-parse vs post-fork per-file-walk. **Pattern 10 widening + Pattern 16 introduction:** Phase 4 extends Pattern 10 from MACRO_DEFINITION-only to admit ENUM_DECL on the same TU-root intercept mechanic; Pattern 16 (X-macro file text-parse) is wholly new -- both lift to EXTRACTOR-PLAYBOOK in Phase 8 per the standing deferral. **Phase MD Task 4 inline code example was stale** (referenced helpers like `parseArgValue` / `connect()` / `MONOREPO_ROOT` that don't exist in `index.ts`; Phase 3's actual shipped pattern uses `parseArgs` from `node:util` + outer-scope `sql` + `__dirname`-relative defaults). The MD's operative contract was "mirror Phase 3's load-ktx-modes subcommand registration"; executor shipped per Phase 3's actual pattern. ADVISORY -- behavior identical; future-arc reuse may want a phase-MD edit. F8's expected 2026-05-06 amendment was already shipped at draft-time (live `review-findings.md:156` carries the 2026-05-05 "Phase 4 drafter source-walk" amendment with the 29/27/2 breakdown); no re-amendment ran during execution. All 13 phase-boundary probes PASS, independently re-verified by orchestrator session #3. Phase-boundary commit: `03a5b366`. No new findings; no decision amendments.

- **Phase 2 (2026-05-06): Pass 1 first-class entity handlers + dispatch wiring -- DONE.** Four KTX-private libclang handlers shipped under `apps/qw-oracle/scripts/extractors/ktx/` -- `_handler_cvars.py` (Pattern 5 + 6, RegisterCvar / RegisterCvarEx), `_handler_commands.py` (Pattern 4 + 6 + 9 + 13 + 14, three tables: cmd_t cmds[] / frogbot_cmd_t std_commands[] / frogbot_cmd_t editor_commands[]), `_handler_info_keys.py` (SetUserInfo producer-only, Pattern 14 `:userinfo`), `_handler_log_templates.py` (G_bprint / G_sprint / G_cprint / log_printf with new `'logfile'` channel; F17/D10 dual-row design preserved -- XML-shaped log_printfs intentionally captured). Driver `ktx/extract.py` mirrors MVDSV shape with single-variant TU parse (no Win/Linux/Apple splits per spec 1.2). `clang_args_ktx_for` added to `extractor_lib/clang_config.py` with `-DBOT_SUPPORT=1` so frogbot tables compile. Loader wiring: `extract-tag.ts` populates `PROJECT_EXTRACTOR.ktx` + `ENTITY_JSON_FILES.ktx`; `constants.ts` widens `LOG_TEMPLATE_CHANNELS` to admit `'logfile'`. All four handlers cross-codebase port from `extractor_lib._visitor.Visitor` only per D3; no parent-project subclass. End-to-end: extractor runs 108 files in 2.6s; dev DB rows -- cvar=260 (192 k_-prefixed + ~68 non-k_ literal names per Exhaustive Mapping Rule), command=358 (319 cmds[] + 14 std_commands + 25 editor_commands), info_key=7 (all `*`-prefixed star-keys), log_template=1195 (channels broadcast=421 / client=748 / console=10 / logfile=16). Pattern 14 verification: 39 frogbot-suffixed commands; 7/7 info_keys end with `:userinfo`. F17 dual-row preserve: 15 XML-shaped logfile rows (handler captures all `log_printf` calls per D10). D14 JSONB binding regression-gate: `jsonb_typeof` returns `array` for both `all_call_sites_json` and `call_sites_json`. D15 idempotency: re-run produced 260->260 cvar count. All 11 required boundary probes PASS, independently re-verified by orchestrator session #2 against live source + dev DB. **D16 compliance:** single commit `063bae80` -- the Phase 1 split-commit deviation lesson held. Two new findings landed during execution: **F23** (Phase MD probe 5 used `startswith("\t\t\t<")` -- wrong tab depth for KTX's two-tab `\t\t<event>` wrapper shape; corrected probe used live, handler behavior confirmed correct, probe wording stale but design sound), **F24** (`load-version.ts` `validIdentifier` regex `/^[a-z0-9_.+\-]+$/` blocked Pattern 14 colon suffixes -- 39 frogbot rows initially rejected with "skipping entity with invalid name" warnings; fixed inline by adding `validCommand` predicate `/^[a-z0-9_.+\-]+:(frogbot:std|frogbot:editor)$/` parallel to existing `validQcBuiltin`; gated to `options.type === 'command'` so other types unaffected; info_key `:userinfo` was NOT blocked because `INFO_KEY_NAME_RE` already admits `*name:scope` patterns). F1/F2/F3/F4 spec-anchor drift documented as Open Questions in the phase MD per D1 amendment process (live counts win; orchestrator-soft-observation -- F4's "1500-2000 unique format strings" anchor sits above live's 1195, but probe threshold `>= 1000` accommodates; potential future F4 sub-amendment). Phase-boundary commit: `063bae80`.

---

## 2026-05-05 -- QWiki community-reference arc -- Phases 0-3 SHIPPED (Phase 2 reconciled)

QWiki community-reference arc adds Layer 3 community-knowledge (~5,903 players + 822 clans + ~700 tournaments) as typed `curated/<type>-notes/` folders + Postgres `community.*` schema with two-output model per entity (every entity gets a row; only content-rich entries get markdown notes). Eight phases (Phase 0 snapshot finalize -> Phase 7 L2 primer artifact). Spec: `docs/superpowers/specs/2026-05-04-qwiki-community-reference-design.md`. Plan: `docs/superpowers/plans/2026-05-04-qwiki-community-reference/`. Execution driven by arc-orchestrator (this session) + arc-executor (per-phase fresh terminals).

- **Phase 0 (2026-05-05): snapshot finalize.** Committed wiki snapshotter (`apps/qw-oracle/scripts/snapshot-wiki/snapshot.py`) as a permanent Python tool (D14 amendment carved out the snapshotter alongside engine extractors); refetched all 503 slash-title articles with double-underscore slugs (operator-confirmed corpus-wide uniformity over the original 4-collision-victim minimal fix); cleaned up 499 stale single-underscore files; preserved the 4 collision-victim spaceless-title files. Refetched redirects with `arprop=ids|title` -- this surfaced **F14**: MW 1.35.10's `allredirects` does NOT return `fromtitle` (only `fromid`); fixed inline with a two-step `allpages?apfilterredir=redirects` + `query?redirects=1` approach; Phase 0 T3 subagent independently arrived at the same fix. 2,337 redirects loaded after the patch (vs 0 in the broken pre-fix state). Snapshot committed to git (Path A locked, 9,950 files tracked). Manifest re-locked with `phase0_fixes` block. Three additional findings captured: **F15** (V3a probe `ls | grep -c "__"` returns 553 not 503 due to ~50 non-slash files with `__` slugs from other special-char replacements -- V3b is the accurate gate); **F16** (26 slash-title articles have legitimately empty wikitext; tournament sub-pages -- D5 two-threshold model handles natively); **F17** (`.devil.json` is a hidden file; future phase probes use `find -name "*.json"` or Python `iterdir()` instead of `ls | wc -l`). Three commits: af888e45 (snapshotter), 296efc67 (redirect-enumeration fix), 408938c1 (snapshot commit).

- **Phase 1 (2026-05-05): curated/ rename + migration 008.** `git mv apps/qw-oracle/concept-notes/ -> apps/qw-oracle/curated/concept-notes/` with three sibling empty dirs (`player-notes/`, `clan-notes/`, `tournament-notes/`); 14-file path-reference sweep (load-concepts/, parse.ts regex, parse.test.ts, multiple CLAUDE.md / OVERVIEW.md / VISION.md / README.md, root OVERVIEW.md diagram). Migration 008 applied: new `community` schema with five tables (`players`, `clans`, `tournaments`, `player_clan_eras`, `tournament_results`); all empty post-apply. The three load-bearing finding amendments confirmed live in the migrated schema: **F8** (no FK clause on `tournament_results.tournament_slug`); **F9** (`player_clan_eras` surrogate `id BIGSERIAL` PK + nullable `start_year` + new `era_seq INT` + `UNIQUE (player_slug, clan_title, start_year, source)` for idempotency); **F10** (`clans.source_template` CHECK enum widened to include `'infobox_4on4team'`). `SCHEMA.md` extended with the new community schema section (36 tables total). `bunx tsc --noEmit` clean post-rename; `load-concepts` walks the new path successfully (loaded 9, skipped 4, warnings 0). Three findings accrued during execution: **F18** (V3 phase-boundary probe miscalibrated -- regex literals + backward-compat test strings legitimately match; tighter probe needed); **F19** (V8 PASS condition off-by-one -- "skipped 3" vs actual "skipped 4" because CLAUDE.md is also slug-less; N>=9 gate is the meaningful signal); **F20** (D15 append-only applies to the entire migration file via SHA256, not just functional SQL -- comment edits in applied migrations break the migrator; T2's "update 005 comment" step was unsafe and was reverted; live 005 SHA256 matches DB hash). Single phase-boundary commit: af7f5b5b. User-global skill files (`~/.claude/skills/guide-rewrite/SKILL.md`, `extraction-review/SKILL.md`, `docs-check/SKILL.md`) still reference the old `concept-notes/` path (Q2 deferral); operator should update before next `guide-rewrite` invocation.

- **Phase 3 (2026-05-05): clans parser + 822 rows + 350 clan-notes.** Four-branch clan parser (`clan_info` / `infobox_4on4team` / `infobox_clan` / `bullet_prose+none`) handles all clan article shapes. F10-amended migration 008 CHECK enum already in place from Phase 1 (no schema change in Phase 3). T8 has_note tuning ran TWO bounded edits at the operator gate: (1) **Option 2** -- parser-level strip of `{{chtv}}` + `{{clan-stub}}` standalone-line templates from `external_links_section` body during parse (drove has_note 669 -> 430; the 4 stub clans Frag_Messengers/KMFC/Masters_of_Fire/Imperial_Icemarines confirmed via operator eyes-on as genuine stubs with only Challenge-TV link template + stub marker); (2) **F27 27a** -- extended `stripClanStubBoilerplate` to also drop standalone `[[Category:...]]` lines from External links body during parse (drove has_note 430 -> 350; root cause was HTML comments interrupting `extractSectionBody`'s trailing-meta-trim, leaving orphan `[[Category:Clans]]` (18 chars) alive). Final counts: 822 rows in `community.clans`; template distribution clan_info=450 / bullet_prose=326 / infobox_4on4team=44 / infobox_clan=2 / none=0 (zero `none` rows -- Phase 3 parser handles fallback better than Phase 2's player parser); `is_substantive=TRUE` **688** (D6 `>=2 of 5` per spec; original wrap-up cited 397 from a T8 `>=3 of 5` calibration trial -- corrected at orchestrator session #2 boundary; F28); `has_note=TRUE` 350 notes emitted to `curated/clan-notes/`. 130/130 tests pass; V1-V9 PASS (V3 hard-zero gate FAILs at 19 stub-with-note residual; these are legitimate D5 "Not substantive, has note" Flaming_Fist-class outliers that would flip to substantive if F26 achievements-as-6th-signal landed; operator-known and accepted). `getClanTitleToSlugMap()` exported from `clans/index.ts:125` for Phase 5 consumption. **Community-context note for downstream phases:** the four `source_template` values represent two distinct kinds of community entity. `clan_info` (450) and `bullet_prose` (326) are persistent clans with year-spanning identities; `infobox_4on4team` (44) are ephemeral tournament/LAN-roster team articles preserved for history (their nick-recognition window is the tournament dates, not all-of-QW). `lookup_clan` and L2 primer consumers may benefit from filtering or annotating by `source_template='infobox_4on4team'`. Four findings accrued: **F25** (Phase MD T3 fixture misclassification -- Firing_Squad + Slackers have buried Clan-info templates; tests revised, Xband added); **F26** (D6 5-signal heuristic omits achievements; rich-achievements/sparse-infobox clans flagged is_substantive=false; future tuning candidate; deferred); **F27** (HTML-comment trim-interruption; 27a local fix applied to clans/parse.ts; 27b broader shared/wiki-text.ts fix deferred); **F28** (orchestrator-session-#2 cold-verify caught wrap-up doc-transcription error -- 397 was a T8 `>=3 of 5` calibration trial value, not the post-tune `>=2 of 5` ship value; code + DB always at `>=2 of 5 = 688`; corrected README + arc-history + resume handoff). Phase-boundary commit: 2a467645. Note on commit lineage: an earlier orphan commit `7a53d957` swept Phase 3 work into a parallel KTX-Phase-8 prompt augmentation by an unrelated terminal; that sweep was reset out of main's lineage, KTX work cleanly re-committed at `7acbd83e`, Phase 3 cleanly committed at `2a467645`.

- **Phase 2 (2026-05-05): players parser + 5,900 rows + 571 player-notes.** Four-branch wikitext parser (`infobox_player` / `player_info` / `bullet_prose` / `none`) handles all player article shapes; critical fix: 693 articles use unbolded `* Label: value` format -- all three bullet-prose detection patterns and all field parsers made triple-tick-bold optional via `(?:''')?`. `extractSectionBody` extended to strip trailing Category/NOTOC/empty lines + HTML comments (fixes Necroth and similar false-positive has_note inflation). `narrativeClean` computed before 500-char threshold check to avoid HTML comment pollution (Nemesis, Master_(UK_player) cases). `isPlayerArticle` expanded to also accept nationality-subcategory articles (`/^Category:.+ Players$/`) -- recovers Vo0 (**F21**). D7 has_note v1 tuned at T9 operator gate: quotes threshold lifted to >= 5 chars (filters `hv`/`gg`/`wp` noise, keeps `ggs all`/`ez shaft gg` substance). Final counts: 5,900 rows in `community.players`; template distribution infobox_player=475 / player_info=2867 / bullet_prose=2543 / none=15; `is_substantive=TRUE` 2,008; `has_note=TRUE` 571 notes emitted to `curated/player-notes/`. 196 tests pass; V1-V11 all PASS. Phase-boundary commit: d6efa1cd. **Reconciliation 2026-05-05** (commit a0e3ec67): an aborted/partial intermediate loader re-run between ship and orchestrator cold audit had drifted the live DB to 5,896/833/793-none (vs shipped 5,900/571/15-none). Reconciliation via `TRUNCATE community.players CASCADE` + clean `curated/player-notes/*.md` + fresh run from HEAD code restored the DB to match d6efa1cd's shipped state. Working tree byte-identical to d6efa1cd; HEAD code was correct throughout. Four findings accrued: **F21** (actual count 5,900 vs spec's 5,903; Vo0 nationality-subcategory-only editorial omission; inline fix); **F22** (15 none-branch articles -- count amended from initial 13; 6 have bogus real_name from fallback parser; advisory, future cleanup arc); **F23** (silent DB drift between phase ship and orchestrator cold audit; orchestrator cold audit caught it; future improvement candidate is an automated regression probe in load-knowledge/probes/); **F24** (phase MD V5 status-field expectations drift from parser rules; future phase MDs derive V-probe expected values from parser spec).

---

## 2026-05-03 -- Arc 1 SHIP -- Postgres + hybrid retrieval, public MCP live

Arc 1 closed end-to-end. Single-engine Postgres 16 + pgvector + tsvector across all three layers; hybrid retrieval (RRF over lexical + semantic) on `search_entities` and the new `search_concepts`; bidirectional concept graph; `redirect_to_human` tool; Streamable HTTP transport behind Cloudflare Tunnel; **public MCP live at `https://oracle.slipgate.me/mcp`** with per-IP rate limit (60/min) at the CF zone, no app-level auth. Image at `ghcr.io/paradoks81/qw-oracle-mcp:0.4.0` (and `:latest`); Unraid stack (`pgvector/pgvector:pg16` + MCP container + nginx) on `qworacle-net` bridge with persistent state at `/mnt/user/appdata/qw-oracle/`. Eight phases shipped:

- Phase 1: Postgres dev container + migrator (`bun db/migrate.ts`).
- Phase 2: 31 Layer 1 tables ported via schema-as-generator from `schema.ts` (D3); `entities.description` derived from per-version rows (D6); JSONB serialization fix across 8 loader sites (cross-app contract drift sidequest).
- Phase 3: Layer 2 (Discord-only, D9-revised) ported with `'simple'` tsvector config (D7); filter-then-segment session boundaries + reply-graph table (D18).
- Phase 4: Layer 3 concept loader + bidirectional graph + `redirect_targets`; markdown-aware chunker.
- Phase 5: Voyage v4 embedding pipeline; D8 build/query embedding-space verifier (cosine 0.8852 in prod, above the 0.85 floor).
- Phase 6: MCP rewrite on Postgres + Streamable HTTP transport + 12 tools (`lookup_entity`, `search_entities`, `get_concept_note`, `search_solved_issues`, `lookup_map`, `search_maps`, `lookup_gameplay_entity`, `search_gameplay_entities`, `lookup_mechanic`, `search_mechanics`, `search_concepts`, `redirect_to_human`).
- Phase 7: `query_log` + dispatcher wrapper + `OBSERVABILITY.md` cheatsheet.
- Phase 8: eval + calibration sets (disjoint per D10) + Docker prod image + Unraid first-time deploy + CF Tunnel route + Claude Desktop smoke; recall@3 = 83.3% on the 12-query operator-curated eval set.

**Phase 8 executor caught two Phase 6 bugs in flight, both fixed inline before commit.** (1) `search-solved-issues.ts:55` SELECTed `participants` from `sessions`; the column is `participants_json` -- crashed `search_solved_issues` on any session it tried to hydrate. Fixed via column alias `participants_json AS participants`. (2) `transports/http.ts:97` bound `127.0.0.1` only; nginx in a sibling container reaches mcp via the bridge IP, not loopback -- 502 Bad Gateway through the public path. Fixed by binding `0.0.0.0` (compose `ports:` mapping is what controls public exposure, not the bind address inside the container). Both fixes shipped under the same `0.4.0` image tag.

**Phase 8 Dockerfile required three workspace-shape adaptations** the plan didn't anticipate. (1) `--frozen-lockfile` requires every `apps/*/package.json` present, not just qw-oracle's, or bun bails on the trimmed workspace. (2) `--filter=qw-oracle` is needed to avoid quad's `@discordjs/opus` native binding firing in the Alpine Bun image. (3) `serve/mcp/` is NOT a workspace member -- it has its own `bun.lock` and needs a second `bun install --frozen-lockfile --production` step. Final image: 191MB, multi-arch, two-stage (deps + runtime).

**Calibration found STRONG=0.02 / WEAK=0.005** against the 5-query operator calibration set; identical values landed for both dev and prod (same corpus). The thresholds sit just above the single-list-only RRF rank-1 floor (~0.0164), which means most queries fuse to one source rather than truly RRF-fusing both lex and sem -- the bucket only becomes load-bearing for queries where both sources hit the same chunk (RRF coincidence pushes the score to ~0.033). YELLOW at sign-off: a prod `query_log` row showed `top_score=0.0164` labeled `match_quality=none` despite `WEAK=0.005`. **Resolved 2026-05-03 (post-arc orchestrator):** root cause was that `docker-compose.prod.yml` substitutes `${VAR}` from `.env` at create-time; `docker compose restart mcp` keeps the originally-baked Phase 6 placeholders. Fix is `docker compose -f docker-compose.prod.yml up -d mcp` to recreate. Smoke probe post-fix shows the same query bucketing as `weak` correctly. Procedural note added to `apps/qw-oracle/DEPLOYMENT.md` "Environment-variable changes" section. Full post-arc walkthrough: `docs/superpowers/reviews/2026-05-03-qw-oracle-arc1-post-arc-analysis.md`.

**Out-of-corpus failure mode discovered.** `search_solved_issues` uses a hit-count-based `match_quality` (`results.length >= 2 ? 'strong' : 'weak'`) rather than a score-based threshold. Any out-of-corpus query that mentions "ezquake" + a common Discord word fires false-strong via Layer 2 even when the threshold env vars would label the entity / concept tools cleanly. The eval set worked around this by avoiding QW-domain words in out-of-corpus queries (q11 "Minecraft server with custom plugins" → match_quality=weak; q12 "fteqw-sv64 map rotation" → none). Score-based scoring for `search_solved_issues` is queued as a Phase 6 followup, not Arc 1 scope.

**Two operator-curated eval queries left FAIL in the gate** as legitimate Layer 3 authoring leads (not blockers): q5 "screen brightness flicker on Windows after closing ezquake" + q8 "Linux stutters with sys_highpriority and CPU affinity set". The eval-set README is explicit -- queries that should hit a concept note but don't are the prioritised list of new notes to author. Both captured in `docs/superpowers/parking/2026-05-03-layer3-multidomain-bucket-framework.md` under the L3 authoring framework.

**Public smoke (Claude Desktop) PASS.** Three prod `query_log` rows confirmed (`search_concepts` + 2x `lookup_entity`); `consumer_hint='Anthropic/ClaudeAI/1.0.0'` populating correctly through the public Streamable HTTP transport. Voyage embedding pipeline confirmed firing in prod (`embedding_api_log` row at 11:28:11.894 with `voyage-4-lite`, 5 tokens, 509ms -- joins back to the `search_concepts` `query_log` row exactly). The architecture spec's canonical "screen wobble" probe answered with `cl_bob` + `weapon-scripts` concept note as designed.

**Voyage cost this phase:** ~2k tokens (calibration sweeps dev + prod, eval gates dev + prod, public smoke, embedding-space verifier). Cumulative arc lifetime: ~134k of the 200M lifetime free grant (~0.067%). Operator's payment card (Tier 1 throughput, 2000 RPM / 8-16M TPM) added during Phase 5; the lifetime grant remains the dominant capacity.

SQLite era retired; `data/knowledge.db` and `data/qw.db` no longer in the runtime path. Authoritative store is Postgres for everything.

Spec: `docs/superpowers/specs/2026-05-01-qw-oracle-database-architecture-design.md`. Plan: `docs/superpowers/plans/2026-05-02-qw-oracle-arc1/`. Decisions: `docs/superpowers/plans/2026-05-02-qw-oracle-arc1/decisions.md` (D1-D18).

---

## 2026-05-03 -- Arc 1 Phase 6 -- MCP on Postgres + hybrid retrieval + 2 new tools + HTTP transport

Arc 1 Phase 6 shipped end-to-end (commit `08312cc`). The MCP server cuts from the SQLite tripwire stub it became at Phase 2/3 over to the project-wide postgres-js client; `concept-loader.ts`'s startup file-scan retires (replaced by SELECTs against `concept_entities` / `concept_concepts`); `db.ts` becomes a one-line re-export of `shared/db.ts`; `better-sqlite3` + `@types/better-sqlite3` drop from `serve/mcp/package.json`; `express ^5.2.1` lands for the new HTTP transport. All 8 existing tools (`lookup_entity`, `lookup_map`, `search_maps`, `lookup_mechanic`, `search_mechanics`, `lookup_gameplay_entity`, `search_gameplay_entities`, `search_solved_issues`) port to async + postgres-js with the `Database` arg dropped from every signature. `search_entities` upgrades to hybrid retrieval (tsvector lexical + Voyage per-query embedding via `voyage-4-lite`/`'query'` + Reciprocal Rank Fusion); the substring fallback retires. `get_concept_note` reads from `concepts` + `concept_concepts` and surfaces `related_concepts` inline. Two new tools land: `search_concepts` (hybrid RRF over `concept_chunks`) and `redirect_to_human` (reads operator-curated `redirect_targets`, seeded by this phase with 6 placeholder rows). `serve/mcp/src/orientation.ts` carries the Layer-1/2/3 description + iteration recommendation + honest-failure rule, passed to the SDK `Server` constructor's `instructions` field. `index.ts` refactors to a `createServer()` factory + 12-case dispatcher; the D8/F14 verifier from Phase 5 wires into MCP startup with a TTL gate (`EMBEDDING_VERIFY_TTL_HOURS`, default 24h, cached against `oracle_meta.embedding_space_verified_at`); below threshold the server refuses to start, Voyage outage degrades to lexical-only with a WARN. A second transport ships behind `MCP_TRANSPORT=http`: SDK's `StreamableHTTPServerTransport` over Express, bound to `127.0.0.1` (Phase 8 puts nginx + Cloudflare Tunnel in front). 11 tests pass / 1 skip / 0 fail across `shared/rrf.test.ts` + `serve/mcp/src/tools/maps.test.ts` + `search-concepts.test.ts` + `redirect-to-human.test.ts`. Phase 6 does NOT write `query_log` rows -- that observability layer is Phase 7's wrapper.

**Five executor-caught issues, all fixed inline before commit.** (1) `verify-gameplay.ts` script ported to async + postgres-js (plan didn't list it; mechanical fix). (2) `scripts/seed/` directory created (plan claimed it existed from Phase 3 but Phase 3 placed its seeds under `scripts/load-chat/`). (3) `search-concepts.test.ts` self-seeds a fixture concept + chunk in `qw_oracle_test` rather than depending on Phase 4's loader having run there (D13 conformant; alternative would have wasted ~30k Voyage tokens re-embedding the corpus into the test DB). (4) `search-solved-issues.ts` adds an `as 'discord'` cast at SessionHit construction so TS narrowing matches the DB CHECK constraint. (5) Path bug in plan-inlined `search-entities.ts` and `search-concepts.ts`: shipped `'../../../shared/...'` (3 levels) but tool files are 4 levels deep from `apps/qw-oracle/`; corrected to `'../../../../shared/...'`. The plan's `redirect-to-human.test.ts` had the right 4-level path -- inconsistency was in those two inlined tool bodies only.

Smoke (the canonical "screen wobble" query that triggered this whole arc) confirmed hybrid retrieval is firing: `searchEntities('screen wobble bob')` ranks `cl_model_bobbing` / `v_iroll_level` / `v_idlescale` / `v_ipitch_level` ahead of `cl_bob` (which lands at rank 9 of 25 candidates). Both `match_quality='none'` because Phase 6 ships placeholder thresholds (`STRONG=0.05`, `WEAK=0.02`) -- single-list-only RRF tops out at ~0.016, so the bucket only becomes meaningful after Phase 8 calibration. The right answer is in the candidate set; the rank/bucket needs calibrating. Phase 8 territory per plan Open Question 2.

Voyage cost: 24 tokens this phase (3 dev mcp-query + 5 verification probe mcp-query). Cumulative arc lifetime to date: ~131,714 tokens, 0.066% of the 200M lifetime free grant.

Arc 1 status post-Phase-6: Phases 7 (observability) + 8 (eval / calibration / Docker prod / Unraid deploy) remain. The MCP server is functional again for the first time since Phase 2/3 stubbed `db.ts`; Claude Code and any other MCP client can query qw-oracle on Postgres.

## 2026-05-02 -- Arc 1 Phase 5 -- Voyage embedding pipeline + D8/F14 verifier

Arc 1 Phase 5 shipped end-to-end (commit `ffe681b`). Migration `006_embedding_api_log.sql` lands the per-call Voyage log table (`source` enum: `'loader'` / `'mcp-query'` / `'verify'`) -- one row per API call across all three sources, indexed by `called_at` and `source` for Phase 7 observability + Phase 8 spend dashboards. `shared/embedding.ts` is the project's only Voyage call site; exports `embedTexts(texts, model, inputType)`, `verifyEmbeddingSpace()`, `cosineSimilarity()`, `EMBEDDING_SPACE_THRESHOLD = 0.85`, `EMBEDDING_SPACE_PROBE = 'weapon scripts'`. `scripts/embed/verify-embedding-space.ts` ships the D8/F14 closure as a standalone CLI (Phase 6 wires it into MCP startup); on success stamps `oracle_meta(key='embedding_space_verified_at')` + logs a verify-source row pair. `scripts/embed/embed-entities.ts` is the Layer 1 description-embedding pass: hash-skip on `entities.description_embedding_sha256`, batch=64, failures flag stale without overwriting last-good vectors. Hooked into `extract-tag.ts` before its return block -- next ezQuake / FTE / MVDSV / QWCL load-tag run includes embedding without operator action. `scripts/embed/embed-chunks.ts` is the Layer 3 chunk-embedding pass: uses Phase 4's `embedding_stale + embedding IS NULL` predicate as candidate filter; hooked into `load-concepts/index.ts`'s CLI block (not the helper) so test-seeded chunks stay hermetic. End state: 5756 entity rows carry 1024-dim `description_embedding` (3181 entity rows skipped because they have no description text -- ruleset / keyname / etc., by Phase 2's D6 derivation design); 115 concept-chunk rows carry `embedding`; 28 tests pass (8 chunker + 15 parse + 5 integration). All 11 + 1 verification probes PASS including F1 JSONB regression gate (`concepts.frontmatter` `jsonb_typeof = 'string'` returns 0).

**Mid-execution amendment to D8** (stamped 2026-05-03 in `decisions.md`): the executor's first verifier run returned cosine `0.6846` -- well below the 0.85 threshold -- and halted with a hard blocker. Diagnosis: the verifier confounded two Voyage axes. D8's literal claim ("voyage-4-large and voyage-4-lite produce comparable vectors") is about the model-size axis. The Phase 5 plan added `input_type='document'` for the build call and `input_type='query'` for the query call to mirror production retrieval -- but Voyage's `input_type` parameter applies intentional task-specific asymmetry (documented behavior, not a bug; same text under different `input_type` deliberately lands at different vectors so question-and-answer pairs cluster despite different surface text). Sanity probe with `input_type='document'` held constant on both calls returned `0.8850`. Verifier rewritten to hold `input_type='document'` on both calls; production code in `embed-entities.ts` (`'document'`) and Phase 6's `search-entities.ts` / `search-concepts.ts` (`'query'`) keep the asymmetry unchanged. `decisions.md` D8 stamped with the amendment; `phase-5-embeddings.md` carries an executor-found-amendment block; the verifier's source code + error message both name the axis under test with a defensive comment warning future readers not to revert the fix.

Two small executor deviations both correct: (1) `bun install` instead of `npm install --no-workspaces` (the always-on rule is stale -- npm fails on `workspace:*` deps in the local `package.json` regardless of flag; corrected in CLAUDE.md alongside Phase 4 cleanup). (2) Minimal `package.json` edit instead of overwriting (plan's full content was draft-time-stale, missing Phase 3's load-chat:* scripts; plan explicitly anticipated this drift). The plan's dead-code `versionLabel` ternary at the `embedding_metadata` upsert was simplified to direct `buildModel` per orchestrator-handoff cleanup authorization.

Voyage cost: 131,686 tokens this phase (~5756 entity batches @ ~50 tokens each + 115 chunk batches @ ~200 tokens + 4 verifier calls). Operator added a Voyage payment card mid-execution to unlock Tier 1 throughput (2000 RPM / 8-16M TPM per model); free-tier 200M lifetime grant still applies untouched.

Arc 1 status post-Phase-5: vectors sit dormant in Postgres -- the MCP server's tool layer is still stubbed pending Phase 6 cutover. "Database upgraded conceptually but no consumer yet" is the right framing.

## 2026-05-02 -- Arc 1 Phase 4 -- Layer 3 storage + bidirectional graph + chunker

Arc 1 (Postgres + hybrid retrieval) Phase 4 shipped end-to-end (commit `dd9a181`). Migration `005_layer3_concepts.sql` lands 5 new tables: `concepts` (slug PK + JSONB frontmatter passthrough + body_sha256), `concept_chunks` (HNSW on `embedding vector(1024)` + GIN on `tsv`; tsvector config `'english'` per D7 -- Layer 3 is curated English, differs from Layer 2's `'simple'`), `concept_entities` (entity-graph edges; `entity_canonical_id` intentionally NOT a FK so historical concept refs survive Layer 1 retirements), `concept_concepts` (sibling-link graph), `redirect_targets` (empty until Phase 6 seeds it). New `scripts/load-concepts/` subsystem: `parse.ts` partitions `related_entities:` frontmatter into entity refs vs commit/pr/extension externals; `upsert.ts` does atomic per-slug rewrite in one `db.begin()` transaction with body_sha256 skip-on-unchanged; `chunking.ts` markdown-aware chunker (## sections; sentence-fallback then char-window-fallback for oversize sections). 9 concept notes loaded; 115 chunks; 82 entity edges; 0 sibling links (no current note carries `related_concepts:` -- the convention is introduced; backfill is operator authoring work). Embeddings stay NULL -- Phase 5 fills via Voyage. Legacy MCP `serve/mcp/src/concept-loader.ts` keeps reading from disk through Phase 5; Phase 6 retires it. 28 tests green (8 chunker + 15 parse + 5 integration); all 13 verification probes PASS including the orchestrator-added 9b JSONB regression gate (`jsonb_typeof(frontmatter) = 'string'` returns 0).

**Orchestrator audit caught two pre-execution issues** (commit `be6e9b9`, before executor kicked off). CRITICAL: plan-shipped `upsert.ts` had `${JSON.stringify(c.frontmatter)}::jsonb` -- the exact JSONB string-scalar regression Phase 2 fixed. Replaced with `${tx.json(c.frontmatter as never)}` matching the load-knowledge / load-chat convention; comment names the always-on rule. YELLOW: verification step 10 demanded Index Scan but at Phase 4 row counts the planner rationally picks Seq Scan; amended to force `enable_seqscan = OFF` so the test verifies wiring rather than betting on row-count thresholds. Two small executor deviations both correct: used `bun install` instead of `npm install --no-workspaces` (the always-on rule is stale -- npm fails on `workspace:*` deps); minimally edited `package.json` rather than overwriting (plan's full content was draft-time-stale, missing Phase 3's load-chat:* scripts; plan explicitly anticipated this drift).

Arc 1 status post-Phase-4: Phases 5-8 still pending (Voyage embedding pipeline, MCP Postgres rewrite + new tools, observability, eval+deploy). Phase 4 surfaced the npm-rule drift in `apps/qw-oracle/CLAUDE.md` always-on rules; corrected inline alongside this entry.

## 2026-05-02 -- Arc 1 Phase 3 -- Layer 2 Discord port to Postgres + tsvector + reply graph

Arc 1 Phase 3 shipped end-to-end (commit `3825dbe`; open-questions triage at `4f51b11`). Migration `004_layer2_chat.sql` lands 8 Layer 2 tables under Discord-only scope (D9-revised; IRC excluded -- the `messages.platform`, `sessions.platform`, `session_search.platform`, `import_log.platform` CHECK constraints all lock to `'discord'`). `messages` carries raw discord rows + `content_tsv tsvector GENERATED ALWAYS AS (to_tsvector('simple', content)) STORED` (D7 language-agnostic config; mixed Swedish/Russian/German handles would mangle under English stemming). `message_labels` (NULLABLE `session_id` per D18 hygiene -- bot/reaction/system rows are ineligible to participate in sessions) classifies each message into chat/reaction/link/bot/system. `sessions` + `session_search` + `session_references` build on top: filter-then-segment session boundaries (only `category IN ('chat', 'link')` advances the gap clock; 15-minute threshold), session_search joins messages back into per-session FTS bodies, session_references aggregates cross-session reply edges from `referenced_message_id` (Phase 6's `search_solved_issues` consumer). Discord catchup exporter at `apps/quad/scripts/catchup.mjs` lets the import refresh against the live Discord state without re-running the full historical export. Final state: 728,863 messages (chat 675,869 / reaction 26,086 / link 17,837 / bot 8,924 / system 147), 86,423 sessions, 35,157 NULL-session label rows (bot+reaction+system per D18), 15,489 reply edges. JSONB regression gate clean across 5 columns (`messages.attachments_json` / `embeds_json` / `reactions_json` / `sessions.participants_json` / `session_search.participants`). Open-questions triage post-ship documented Q6/Q7 resolutions and Q8-Q12 accepted execution deviations; "author trust weighting in retrieval ranking" lifted from session-close discussion to a HANDOVER future-arc entry (deferred to Phase 6+ retrieval refinement).

**L2 hygiene research dissolved into the port** (`docs/superpowers/specs/2026-05-02-layer2-hygiene-design.md` is the durable reference). Four port-time hygiene fixes absorbed via D18: filter-then-segment, NULLABLE `message_labels.session_id`, `BOT_COMMAND_PATTERNS` removed from classify.ts (Discord exposes `author_is_bot` reliably; the regex slice was IRC-era and false-positive-flagged 359 human messages), `'xd'` duplicate removed from REACTION_WORDS. Issues #2 (micro-session over-segmentation) and #6 (reply-chain-aware merging) remain open for a potential post-Arc-1 pass; no parking prompt currently exists for them.

## 2026-05-02 -- Arc 1 Phase 2 -- Layer 1 port to Postgres + JSONB serialization fix

Arc 1 (Postgres + hybrid retrieval) Phase 2 shipped end-to-end. SQLite + better-sqlite3 retired from `scripts/load-knowledge/`; replaced by Postgres 16 + pgvector + postgres-js. Schema generated by a one-shot `generate-pg-migration.ts` reading the legacy `schema.ts` (then deleted in commits `7c7739a` + `4cc37ab`); 30 Layer 1 tables emitted as `db/migrations/002_layer1_schema.sql`; `003_layer1_entities_search.sql` adds entities embedding + tsvector columns + HNSW + GIN indexes for Phase 6 hybrid retrieval. Loader port walked all 18 (project, version) tuples and matched the SQLite baseline exactly: 4042 ezquake / 3279 fte / 1236 mvdsv / 380 qwcl. Two loader fixes uncovered during the walk: NUL-byte stripping at the JSON.parse boundary (FTE help_desc strings leaked NULs that crashed the cvar upsert under Postgres TEXT/JSONB); test-teardown ordering bug in `quality-grid.test.ts` (afterAll on first describe closed the connection before the second ran).

**Cross-app contract drift sidequest** (commits `6fbe46c` + `b495e5a`). Operator-driven verification surfaced a JSONB serialization bug in 8 loader sites: `JSON.stringify` on JS arrays/objects before binding to JSONB params caused postgres-js to JSON-encode the string a second time, storing JSONB string scalars. Slipgate's `macros` and `cmdline-params` snapshots were broken at the contract boundary. Fix: drop `JSON.stringify` in `load-{macros,cmdline-params,rulesets,hud-elements,info-keys,log-templates,release-notes,gameplay}`; `natural-keys.ts` wraps affected JSONB bindings with `tx.json(...)` for postgres-js type compliance; `types.ts` updates 9 row-type fields from `string | null` to typed array shapes. DB repaired by re-extracting all 18 (project, version) tuples through the corrected loader; 37 entity_defs + 41 mechanics stale duplicate rows deleted from gameplay tables (unique key included the broken JSONB column, so ON CONFLICT couldn't replace in place). New regression probe `F1.jsonb_columns_not_strings` covers all 13 affected (table, column) pairs and reports PASS. Slipgate `bundle.ts` updated: `source_verified` + `dev_only` from `number` to `boolean`. The 4 ezquake snapshot files (`variables`/`commands`/`macros`/`cmdline-params`) regenerated through the fixed pipeline; 3 of 4 byte-identical to the pre-bug 8ed16de baseline; `variables.json` adds the legitimate `s_stereo` cvar from a fresh extract.

Arc 1 status post-Phase-2: Phases 3-8 still pending (Layer 2 enrichment, embedding pipeline, MCP Postgres port, eval set, deploy). Phase 2 left two pre-existing concerns surfaced but out-of-scope: 5 stale `F1.ezquake.floor.*_source_state` probes (extractor classification drift since the 2026-04-28 floor seed; entity totals unchanged) and a broken `scripts/extractors/shared/tests/reserved-subdirs.test.ts` module path (red since before Phase 2). Both filed as small followups in HANDOVER.

## 2026-05-01 -- Help-JSON classification infrastructure (Arc A, Tasks 5-8)

Plan completed end-to-end. Four commits `8601eb4` -> Task 8. **Task 5** wires a sixth review bucket `help-json-classification`: new `findings-help-json-classifications.ts` module reads `seeds/help_json_classifications.yaml` via a `loadHelpJsonSeed` helper in `review/index.ts`, surfaces a finding for any `doc_only` entity not in the seed, and `runReviewCli` gains a repeatable `--fail-on <bucket>` flag (parseArgs `multiple: true`) that exits 2 when a named bucket has any findings. `types.ts` extends `Bucket` + `ReviewCounts`; `draft-writer.ts` extends Summary total. Test uses `node:test + tsx` (better-sqlite3 native addon, Bun cannot load) -- diverged from the plan's `bun:test` snippet to match `quality-grid.test.ts:1-2` convention. Smoke run on ezquake 3.6.8 -> 3.6.9 confirms gate fires zero findings on current seed (193 doc_only / 193 seeded). **Task 6** ships `extractor_lib/_help_json_pr_digest.py` + `scripts/build-help-json-pr-digest.py`; ezQuake digest at `apps/qw-oracle/docs/upstream-prs/ezquake-help-json-cleanup.md` covers 193 entries (renamed / retired / never-implemented sections; aspirational + extractor_gap correctly excluded via `upstream_pr_action == 'none'` filter). **Task 7** adds Help-JSON drift section to PLAYBOOK after Known limits; doc_only budget gate added to RUNBOOK Section 0 Pre-flight (anchored under Pre-flight rather than the plan's "review-time checks" since that section did not exist); `ezquake/CLAUDE.md` Always-on rules gains seeds + classify-help-json pointer. **Task 8** drains HANDOVER: the `-nopriority` followup body and Sub-pattern 2b sidequest deleted (both gaps closed -- `-nopriority` is now `source_backed` in the live DB; SDK stubs landed earlier per PLAYBOOK Bucket 4); Phase 2e residuals rewritten to single `gl_lightmode` anomaly; ongoing-arc entry for Tasks 5-8 + parking prompt file removed. The fresh-session prompt's task numbering had Tasks 7 and 8 swapped vs the plan and dropped the HANDOVER drain entirely -- executed against the plan's actual numbering instead.

## 2026-05-01 -- Help-JSON classification infrastructure (Arc A, Tasks 1-4)

Plan from 2026-04-30 executed end-to-end via subagent-driven-development. Ten commits `bb092fc` -> `26ae789`. New: closed six-value classification taxonomy + schema validator (`extractor_lib/_help_json_classification.py`), single-pass git-pickaxe blame index (`extractor_lib/_help_json_blame.py`, walks `git log --first-parent -p` once and parses with a state machine), cross-project `classify-help-json.py` CLI (`--project`, `--propose`, `--apply --confidence-threshold`). First smoke run on the live ezquake-source clone produced 120/193 false-positive `renamed` proposals at high confidence — diagnosed as code-relocation refactors + bulk-cleanup commits with coincidental new features. Added two filters to `classify_from_blame`: self-relocation skip + SequenceMatcher similarity gate (0.40 LOW / 0.65 HIGH); `rename_similarity` field persisted on every renamed proposal. Re-survey: 25 high-confidence renames + 38 medium-confidence + 87 retired_pre_walk_floor + the rest. Operator-walked anomalies via `git log -S` + commit-message inspection (johnnycz's 2010-01-08 file-browser unification commit `5f7900c6` "one set of settings for all file browsers; -> 50 cvars gone" explained the 47-entry browser cluster; meag's 2019-03-05 MP3-removal explained 22 entries; etc.). Final seed ships at `apps/qw-oracle/scripts/extractors/ezquake/seeds/help_json_classifications.yaml` with **193 classifications covering all 194 doc_only DB rows** (mp3_volume name-collision counts twice in DB, classifies once): 101 retired_pre_walk_floor / 49 renamed / 27 aspirational_documentation / 16 never_implemented. 12 manual overrides documented inline as `evidence_note` fields. One extractor improvement observation captured (-nomouse: bare `COM_CheckParm` call sites are not extracted today — `CMDLINE_DEF` macros only). Tasks 5-8 (TS review-module + PR digest + extraction-review CLI gate + doc updates) deferred to a fresh session via prompt at `docs/superpowers/parking/2026-05-01-help-json-classification-tasks-5-8-prompt.md`.

## 2026-04-29 -- Zero-debt-before-KTX arc

Six-phase qw-oracle hygiene arc. Trailing-comment look-ahead misattribution fixed (Phase 1, ~34% of ezquake cvar trailing comments were wrong; 281->145 non-empty rows at HEAD); 14-tag historical re-walk under post-Phase-6 + post-Phase-1 handlers (Phase 3); Phase 6 ezquake exemptions (1) + (3) closed (source_state two-level model documented as intentional, historical-tag stale shapes resolved). Cross-project byte-reproducibility check: FTE / QWCL / MVDSV all clean; ezquake clean within-day, one cross-day non-determinism on `variables-ast.json` (140-line swap, HANDOVER follow-up). Foundation clean for KTX onboarding. Memory: `project_zero_debt_before_ktx_arc.md`.

## 2026-04-28 -- Cross-extractor Phase 6 (FTE convergence + grid uplift)

Recovers 1085 FTE source-backed cvars from `flags_raw IS NULL` to empty-string sentinel; lifts ezhud merge + commands to use `_normalize_flags_raw`; refreshes 56 quality-grid probes; closes 2 of 3 ezquake structural exemptions (the third -- `r_bloom_*` legacy boolean shape -- defers as joint QWCL-shape positive-contract arc). Plan: `docs/superpowers/plans/2026-04-28-cross-extractor-phase6-fte-convergence-grid-uplift.md`.

## 2026-04-28 -- Per-project Mode B validation (ezQuake / FTE / QWCL)

Three deep validations in parallel via the `validate-extractor` skill against `VALIDATION-RUNBOOK.md`. 0 critical, 6 important, 13 nits. Surfaced S-01 (FTE Phase 2 normalization gap), S-03 (uneven F1 probe coverage), F-EZQ-01 (trailing-comment misattribution -- closed by Zero-debt arc 2026-04-29), F-EZQ-03 / F-QWCL-06 (`registration_file` column misnamed). Synthesis report: `docs/superpowers/reviews/2026-04-28-per-project-validation-synthesis.md`.

## 2026-04-28 -- Cross-extractor pattern audit follow-up arc

Five phases shipped (`566c5be` -> `1a00704`). `resolve_fn_ref` lifted to `extractor_lib/_resolve.py` across 6 private copies; `extractor_lib/_cvar_shared.py` created (`unescape_c_string`, `normalize_flags_raw`, `parse_flag_names`); `extractor_lib/_source.py` created (string-shape helpers, 19 handler files converted, net -402 LOC); FTE cmdline single-prefix policy documented; `INFO_KEY_SCOPES` + `LOG_TEMPLATE_CHANNELS` exported from `schema.ts`. Schema bumped to **v18** (qc_builtin canonical name carries `:<table_name>` suffix). Two follow-ups deferred to HANDOVER.

## 2026-04-28 -- Architecture consolidation

ezQuake handlers relocated from `extractor_lib/handler_*.py` to `ezquake/_handler_*.py` so all four projects share the canonical project-private shape. `extractor_lib/` reduced to Tier-1 infrastructure only. Three-tier handler architecture (shared / family-base / project-private) documented in `EXTRACTOR-PLAYBOOK.md`; fork-vs-port branch added. Fork-override hooks exposed on ezQuake + MVDSV handlers for unezQuake / antilag-mvdsv onboarding via subclassing. Plan: `docs/superpowers/plans/2026-04-28-extractor-architecture-consolidation.md`.

## 2026-04-28 -- Phase 2e MVDSV follow-up arc

5 commits `f7b2a7a` -> `72a1630`. Schema v15 -> **v17**: info_key canonical name carries `<bare>:<scope>` suffix (cross-scope dups survive); protocol_message kind taxonomy widens 6 -> 13; log_template gains `all_call_sites_json`; cvar `flags_raw` canonicalises to empty string for absent / `0` / `CVAR_NONE` (1417 historical rows normalised); cvar `default_value` interprets standard C escapes (16 rows touched). F1.*.count probes converted from floor checks to equality assertions.

## 2026-04-27 -- Phase 2e MVDSV Layer 1

Server-side Layer 1 SHIPPED at 2026-01-04 mvdsv head snapshot (`f816d28`). Schema v13 -> **v15** (4 new entity types: `protocol_message`, `info_key`, `log_template`, `qc_builtin`). 1235 entities loaded; all source_backed (MVDSV ships no help-JSON). Three-variant TU dispatch (server-base + server+Win + server+Linux). Runtime-validated against Ciscon's 1.20-dev nicotinelounge.com dump: zero extractor gaps. New patterns 9-13 added to playbook. 26 commits `320f5de` -> `c158da5`. Plan: `docs/superpowers/plans/2026-04-27-mvdsv-layer1-extraction.md`.

## 2026-04-27 -- Game mechanics Layer 1 arc 1 (id1 baseline)

Schema v13 -> **v14** (gameplay_sources / gameplay_entity_defs / gameplay_mechanics; `qw` namespace, no version arc). 37 entity defs + 41 mechanic rows from `qwcl-original/QW/progs/`; every row source_ref-cited. 4 new MCP tools (`lookup_gameplay_entity`, `lookup_mechanic`, `search_gameplay_entities`, `search_mechanics`). Snapshot at `apps/slipgate-app/src/lib/config/data/qw-gameplay.json`. KTX overrides + sub_select_spawn_point + clan_arena algorithmic mechanics queued as arc 2.

## 2026-04-27 -- Map knowledge layer

Schema v12 -> **v13** (pure-additive `maps` table). 254 maps loaded (38 id1 stock from pak0/pak1 + 216 from maps.qw.nu/base/). Two new MCP tools (`lookup_map`, `search_maps`). Snapshot at `apps/slipgate-app/src/lib/config/data/qw-maps.json`. Plan: `docs/superpowers/plans/2026-04-26-qw-oracle-map-knowledge.md`.

## 2026-04-27 -- FTE Phase 2d-bundle

Asset extraction at FTE build-6698. 28 asset_category + 61 extensions + 13 path_rules + 25 cvar_bindings + 717 loader_sites. Five hand-authored seed YAMLs at `apps/qw-oracle/scripts/extractors/fte/seeds/` + two AST handlers + path-rules verifier. Quality grid 30/30 (16 regression + 14 anomaly). Three Path-1 fixtures green. Plan: `docs/superpowers/plans/2026-04-26-fte-phase-2d-bundle.md`.

## 2026-04-26 -- FTE Phase 2d-core

build-6698 (SHA `35843773`). Schema v10 -> **v11** (additive `source_root` TEXT column on cvar/command/macro version tables). Entities: 2482 cvar (1397 engine + 1085 `plugin:ezhud`) / 556 command / 67 macro / 108 cmdline_param. Pass 1 runtime cvarlist diff closed: 217 runtime-only cvars recovered via Pattern 3 fix (cvar_t nested inside container struct/array initializers); residual 114 all explained (non-ezhud plugins, dynamic registration, runtime-synthesized names). Plan: `docs/superpowers/plans/2026-04-26-fte-layer1-extraction-2d-core.md`.

## 2026-04-26 -- Cross-engine alias schema (ezscript)

Schema v11 -> **v12** (CHECK widening on `entities.type` to admit `cvar_alias`; new `cvar_alias_versions` table). 38 alias entities loaded at FTE@build-6698 from `plugins/ezscript/ezscript.c`: 25 alive / 7 lhs_gone / 4 doc_only / 1 both_gone / 1 rhs_gone. Field set: `target_project / target_kind / target_name / value_transform / default_drift_status / freshness_state / verified_target_version / verified_mimics_version`. Spec: `docs/superpowers/specs/2026-04-26-cross-engine-alias-schema-design.md`.

## 2026-04-25 -- QWCL 2.33 (first cross-codebase port)

Schema v9 -> **v10** (project CHECK widened across 8 tables). 380 entities (187 cvar / 121 command / 72 cmdline_param). Three QWCL-specific handlers under `apps/qw-oracle/scripts/extractors/qwcl/` reuse the shared Visitor + walk_tu_dispatch from `extractor_lib`. Foundational for slipgate-app's planned config converter ("pandoc for configs"). Loader gates established: `PROJECT_VERSION_ALIASES`, `PROJECT_HAS_ASSET_BUNDLE`, per-project `ENTITY_JSON_FILES`.

## 2026-04-25 -- qw-config dissolution (Half 1 + Half 2)

Extractors relocated from `packages/qw-config/scripts/` to `apps/qw-oracle/scripts/extractors/<project>/`. Slipgate consumer reads `apps/slipgate-app/src/lib/config/data/<project>-*.json` directly via the new `build-snapshot` CLI (replaces the legacy slipgate-via-`packages/qw-config/` path). Seed YAMLs follow extractors. `packages/qw-config/` retired.

## 2026-04-25 -- ezQuake deep-time walk to v3.0 floor

14 ezQuake versions clean: v3.0, v3.0.1, 3.1, 3.2, 3.2.1, 3.2.2, 3.2.3, 3.6.0, 3.6.1, 3.6.2, 3.6.5, 3.6.6, 3.6.8, 3.6.9, plus head. Pre-3.0 era explicitly de-scoped (community-security framing -- pre-3.6 has known attack vectors). Walk infrastructure shipped: `extract-tag --skip-prune` + `prune-cross-type-orphans` finalize CLI + per-version `backfill_match` detection. Schema v8 -> **v9** (`source_retired_at_version` added to transitions reason CHECK).

## 2026-04-25 -- Layer 1 doc_only audit closed

Audit of 269 doc_only ezquake entities reduced to 194 across 7 shipped fixes (six extractor patterns + one loader dedup + one architectural multi-variant-parse change): P5a SERVER_ONLY clang arg fix (+1 cvar); P1 Cmd_AddLegacyCommand alias detection (+40 commands); P2 struct-literal table walker (+7 commands); P3 nested cvar_t tables (+10 cvars); P6 #define resolution at Cmd_AddCommand sites (+1); 4-variant parse architecture for win/apple TUs (+7 cvars + bonus rows); loader-side help-JSON type-mismatch dedup (-22 orphans). Pattern 4 reclassified as legitimate cat2 drift (mp3-feature deprecation). One row deferred: `-nopriority` cmdline_param at `sv_sys_win.c:645` needs Windows SDK stub headers — re-considered when MVDSV/FTE hit the same wall. Commits `c6fdcf3` / `a099231` / `8f67843` / `0f8f170` / `5dd466c` / `146cd73`. HANDOVER follow-up: "-nopriority cmdline_param recovery (Windows SDK stubs)" sidequest.

## 2026-04-25 -- Layer 3 concept notes bootstrap

`curated/concept-notes/` directory established with template (`README.md`) + stewardship playbook (`OPERATIONS.md`). Provenance frontmatter schema, two-path curation framing (community-curated imports vs newly-earned authoring), 6 recognized note shapes, tiered voice table. Four note bodies landed during the 3.6.5->3.6.6 shakedown walk: `client-side-server-exec-allowlist`, `skywind-animated-skyboxes`, `completing-legacy-fte-protocol-extensions`, `ruleset-anti-script-restriction-pattern`. Concept-notes count: 6 (now 9 with subsequent additions).

## 2026-04-23 -- Extraction-review skill + CLI

Stateless findings generator over Layer 1 tables. Five finding modules (additions / retirements / semantic-crossings / unclassified / source-invisible). User-global `extraction-review` skill drives interactive walk; CLI emits findings JSON + draft markdown at `docs/reviews/`. Spec: `docs/superpowers/specs/2026-04-23-extraction-review-design.md`. Workstream A tweaks (cluster detection / cross-walk / semantic-match / cross-codebase): `docs/superpowers/specs/2026-04-24-extraction-review-skill-tweaks.md`.

## 2026-04-22 -- Knowledge-service realignment roadmap

Pass 1 + Pass 2 shipped: per-entity-type doc (`docs/entity-types.md`), verification-status field as first-class concept (`ast_verified` / `seed_only_with_ast_support` / `seed_only_no_ast_support` / `orphaned_historical`). Asset reference-resolution graph design (research-foundation, not yet implemented). Roadmap spec: `docs/superpowers/specs/2026-04-22-knowledge-service-realignment-roadmap.md`.

## 2026-04-18 -- Knowledge-service Layer 1 schema + loader

Schema v1 -> v8 across the 2c / 2c.5 / 2c.6 sub-phases. ezQuake fully loaded at head across 10 entity types (3849 entities at the time). Loader pipeline (`scripts/load-knowledge/`) with `load-version`, `load-assets`, `diff`, `enrich` CLIs. Seed-first + AST auto-pass reconciliation. Spec: `docs/superpowers/specs/2026-04-18-qw-knowledge-extraction-schema.md`. Plan: `docs/superpowers/plans/2026-04-18-qw-knowledge-loader-phase-2b.md`.

## 2026-04-14 -- Knowledge service POC

Initial design: three data layers (Layer 1 source-extracted facts, Layer 2 chat corpus, Layer 3 curated concept notes), MCP serving surface, snapshot distribution to slipgate-app. Spec: `docs/superpowers/specs/2026-04-14-qw-knowledge-service-design.md`. Plan: `docs/superpowers/plans/2026-04-14-qw-knowledge-service-poc.md`. POC validated libclang extraction path; full implementation followed.
