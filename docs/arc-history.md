# QuakeWorld monorepo -- Arc history

Append-only chronicle of shipped arcs. One paragraph per arc, oldest at the bottom. New arcs prepend at the top.

For current state, see `OVERVIEW.md` (living map) and `HANDOVER.md` (active backlog). For why arcs shipped in the order they did, read backwards from the top.

---

*(No entries yet. Established at the chunk-6 W17 HANDOVER migration, 2026-08-11, as the repo-root sibling to `apps/qw-oracle/docs/arc-history.md` -- for arcs whose scope is not qw-oracle-specific. The migration's routing table found no SHIPPED disposition whose content belongs here; every shipped-arc retrospective it moved landed at the qw-oracle project's own `arc-history.md` instead. Prepend the first entry here when a repo-scope or non-qw-oracle-project arc ships.)*
