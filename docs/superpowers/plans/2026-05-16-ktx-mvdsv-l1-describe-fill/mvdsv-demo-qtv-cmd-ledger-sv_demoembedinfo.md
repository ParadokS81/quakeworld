```json
{ "project":"mvdsv","knob":"sv_demoembedinfo","type":"command","description":"Embeds the contents of a file into the demo/QTV stream that is currently being recorded or broadcast. The file is read from the server's game directory and stored as a hidden block inside the stream, where it travels with the demo but is ignored by normal playback. Usage: sv_demoembedinfo <filename>. The file may not be a .cfg file, and the filename may not be an absolute path or contain \"..\"; rejected filenames report \"Unsafe filename detected\". Only files up to about 200 KB are embedded.\n\nSet by: server console / rcon.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"SEEDED RE-SYNTH (B4): V-pass + HG2 flagged the clause 'must be in the game directory (no sub-directory paths)'. RE-TRACED: the '(no sub-directory paths)' restriction is FALSE -- it derives only from the dev code comment src/sv_demo_misc.c:808 `// No config files, no sub-directories`, which is NOT the enforcing line. The actual guard is src/sv_demo_misc.c:811 `if (FS_UnsafeFilename(path) || !strcasecmp(COM_FileExtension(name), \"cfg\"))`. CALLEE-FOLLOW: FS_UnsafeFilename at src/fs.c:1021-1029 returns true ONLY for `!fileName || !*fileName` (null/empty), `fileName[1] == ':'` (dos drive-absolute like x:), `*fileName == '\\\\' || *fileName == '/'` (leading backslash/slash = absolute path), or `strstr(fileName, \"..\")` (any traversal). A RELATIVE sub-directory path such as `subdir/file.txt` contains no leading slash and no `..`, so it PASSES the guard -- sub-directories are accepted, not rejected. The corrected clause therefore drops '(no sub-directory paths)' and states only the enforced rejections: .cfg files, absolute paths, and `..` (matching the engine's own \"Unsafe filename detected\" message at src/sv_demo_misc.c:812). The remaining clauses were re-confirmed unchanged. Registration: src/sv_demo.c:1952 Cmd_AddCommand(\"sv_demoembedinfo\", SV_MVDEmbedInfo_f) -- plain Cmd_AddCommand, no access-flag wrapper, baseline reach = server console + rcon (these sv_demo* commands are all registered the same way at sv_demo.c:1948-1952). Handler: src/sv_demo_misc.c:793 SV_MVDEmbedInfo_f. CLAUSE 'requires a <filename> arg / prints usage with no arg': sv_demo_misc.c:802-806. CLAUSE 'file read from the game directory': sv_demo_misc.c:810 `snprintf(path, MAX_OSPATH, \"%s/%s\", fs_gamedir, Cmd_Argv(1))` then sv_demo_misc.c:816 `fopen(path, \"rb\")` -- path is rooted at fs_gamedir; this is what 'read from the server's game directory' means, and a relative sub-path is simply resolved under fs_gamedir. CLAUSE '.cfg rejected AND absolute/traversal paths rejected': sv_demo_misc.c:811-814 (guard above + Con_Printf(\"Unsafe filename detected - cancelled\\n\")), enforced via FS_UnsafeFilename src/fs.c:1021-1029. CLAUSE 'stored as a hidden block, ignored by normal playback': sv_demo_misc.c:842 MVDWrite_HiddenBlockBegin(length) and sv_demo_misc.c:851 `header.type_id = LittleShort(mvdhidden_demoinfo)` written via MVD_SZ_Write (sv_demo_misc.c:852-855); hidden blocks are by design skipped by normal demo playback. CLAUSE 'into the active mvd/qtv stream': MVD_SZ_Write / MVDWrite_HiddenBlock* path targets the active mvd/qtv stream (handler message sv_demo_misc.c:803 'into mvd/qtv stream'; failure message sv_demo_misc.c:861 'failed write to mvd/qtv'). CLAUSE 'only files up to about 200 KB': read capped by MAX_DEMOINFO_SIZE = 1024*200 = 200 KB at sv_demo_misc.c:28; fread at sv_demo_misc.c:822 reads at most MAX_DEMOINFO_SIZE bytes into a MAX_DEMOINFO_SIZE buffer (sv_demo_misc.c:821). The later `if (sz < 2*1024*1024)` guard at sv_demo_misc.c:833 is a non-binding secondary sanity check (sz <= 200 KB by construction); the user-observable cap is ~200 KB, so the 2 MB number is deliberately omitted. F-MV1: grep of research/repos/ktx/src/commands.c for { \"sv_demoembedinfo\", ... } -> none; no KTX override. Confidence high: every material clause maps to an enforcing line incl. the FS_UnsafeFilename callee body; would classify TRACED-CLEAN.","description_proposed":null }
```

## Per-clause enforce-trace

| Clause | Enforcing file:line | Verbatim snippet | Result |
|---|---|---|---|
| Registered, server-side, console/rcon baseline | src/sv_demo.c:1952 | `Cmd_AddCommand ("sv_demoembedinfo", SV_MVDEmbedInfo_f);` | MATCH |
| Requires `<filename>`; prints usage with no arg | src/sv_demo_misc.c:802-806 | `if (Cmd_Argc() == 1) { Con_Printf("Embeds contents of a file into mvd/qtv stream\n"); Con_Printf("Usage: %s <filename>\n", Cmd_Argv(0)); return; }` | MATCH |
| File read from the game directory (path rooted at fs_gamedir) | src/sv_demo_misc.c:810, 816 | `snprintf(path, MAX_OSPATH, "%s/%s", fs_gamedir, Cmd_Argv(1));` ... `if ((src = fopen(path, "rb")) == NULL)` | MATCH |
| **SEEDED CORRECTION** -- "(no sub-directory paths)" is NOT enforced; only a dev comment | src/sv_demo_misc.c:808 | `// No config files, no sub-directories` (comment only -- not the guard) | MISMATCH-with-old-claim (clause dropped) |
| Enforced rejection: `.cfg` files AND absolute / `..` paths | src/sv_demo_misc.c:811-814 | `if (FS_UnsafeFilename(path) || !strcasecmp(COM_FileExtension(name), "cfg")) { Con_Printf("Unsafe filename detected - cancelled\n"); return; }` | MATCH |
| Callee: what FS_UnsafeFilename actually rejects (relative sub-dirs PASS) | src/fs.c:1021-1029 | `return !fileName || !*fileName || fileName[1] == ':' || *fileName == '\\' || *fileName == '/' || strstr(fileName, "..");` | MATCH (no sub-dir test present) |
| Stored as a hidden block (skipped by normal playback) | src/sv_demo_misc.c:842, 851 | `if (MVDWrite_HiddenBlockBegin(length)) {` ... `header.type_id = LittleShort(mvdhidden_demoinfo);` | MATCH |
| Written into the active mvd/qtv stream | src/sv_demo_misc.c:852-855, 861 | `MVD_SZ_Write(&header.length, ...); MVD_SZ_Write(&header.type_id, ...); MVD_SZ_Write(&block_number, ...); MVD_SZ_Write(data, length);` ... `Con_Printf("failed write to mvd/qtv\n");` | MATCH |
| Size cap ~200 KB (user-observable binding limit) | src/sv_demo_misc.c:28, 821-822 | `#define MAX_DEMOINFO_SIZE (1024 * 200)` ... `buf = Q_malloc(MAX_DEMOINFO_SIZE); sz = fread((void*)buf, 1, MAX_DEMOINFO_SIZE, src);` | MATCH |
| 2 MB guard is non-binding (correctly omitted) | src/sv_demo_misc.c:833 | `if (sz < 2 * 1024 * 1024) {` (sz <= 200 KB by construction, so never the limiting check) | MATCH (omission justified) |
| F-MV1: no KTX override | research/repos/ktx/src/commands.c | grep for `{ "sv_demoembedinfo"` -> no result | MATCH (no override) |
