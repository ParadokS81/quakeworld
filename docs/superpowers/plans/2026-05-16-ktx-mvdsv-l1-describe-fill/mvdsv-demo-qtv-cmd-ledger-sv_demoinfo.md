# Ledger -- mvdsv:sv_demoinfo (command)

```json
{ "project":"mvdsv","knob":"sv_demoinfo","type":"command","description":"Prints the contents of a recorded demo's companion info file (the plain-text file stored alongside the demo that holds its notes, scores, or other metadata). If no info file exists for that demo, it prints (empty).\n\nsv_demoinfo <number> = show the info file of the demo at that list position.\nsv_demoinfo * = show the info file of the demo currently being recorded.\n\nSet by: server console / rcon. Also runnable by a connected player as 'cmd demoinfo <number>', which prints the demo's info to that player's own console.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"Handler SV_MVDInfo_f at src/sv_demo_misc.c:915; registered as a server command at src/sv_demo.c:1951 via Cmd_AddCommand AND as a client ucmd 'demoinfo' at src/sv_user.c:3343 ({\"demoinfo\", SV_MVDInfo_f, false}) -- the same handler is reachable both from server console / rcon and from a connected client via 'cmd demoinfo'. CLAUSE 'prints the demo's companion info (.txt) file': target path is the demo's '.txt' sidecar -- for a numeric arg via SV_MVDTxTNum(Q_atoi(Cmd_Argv(1))) (line 941) = SV_MVDName2Txt(SV_MVDNum(num)) (sv_demo_misc.c:556-558), where SV_MVDName2Txt replaces the demo's extension with '.txt' (sv_demo_misc.c:546-550); the file is opened read-text fopen(path,'rt') (line 952) and its bytes are read and printed via Con_Printf('%s', Q_yelltext(buf)) in a loop to EOF (lines 958-962). CLAUSE 'prints (empty) if no info file': if the fopen returns NULL the handler prints '(empty)' and returns (lines 952-955). CLAUSE 'number = list position / * = currently recording': numeric path resolves through SV_MVDNum's date-sorted listing (same numbering as the other demo-num commands); the '*' branch (lines 927-938) targets the currently recording demo via demo.dest->name and errors 'Not recording demo!' if !sv.mvdrecording or !demo.dest (lines 929-933). CLAUSE 'requires an argument': Cmd_Argc()<2 -> usage 'demoinfo <demonum>' (lines 921-925). CLAUSE 'connected player can run cmd demoinfo, output to own console': the sv_user.c:3343 ucmds[] entry dispatches client 'cmd demoinfo' to this handler; output is via Con_Printf, which for a client-issued command is redirected back to that client. This dual-surface is action-changing (a player, not just the admin, can read demo info), so it is stated in the user doc per D20's action-changing-inline rule; KTX itself uses this path (stuffcmd 'cmd demoinfo %s' at ktx/src/commands.c:7991). D20: file:line + helper names kept in reasoning; user doc states observable effect only. F-MV1: KTX src/commands.c has NO Cmd_AddCommand/cmd_t registration of sv_demoinfo; the commands.c:7991 hit is a stuffcmd telling the CLIENT to run its 'cmd demoinfo' ucmd (the sv_user.c:3343 surface), which is consumption via stuffcmd, NOT an override of the MVDSV server command.","description_proposed":null }
```

## Per-clause enforce-trace

| # | Clause | Enforcing file:line | Verbatim snippet | Verdict |
|---|--------|--------------------|------------------|---------|
| 1 | Reads the demo's `.txt` sidecar (companion info file) | src/sv_demo_misc.c:941, 556-558, 546-550 | `name = SV_MVDTxTNum(Q_atoi(Cmd_Argv(1)));` ; `return SV_MVDName2Txt (SV_MVDNum(num));` ; `s[len++]='.'; s[len++]='t'; s[len++]='x'; s[len++]='t';` | MATCH |
| 2 | Prints that file's contents to the console | src/sv_demo_misc.c:952, 958-962 | `if ((f = fopen(path, "rt")) == NULL)` ... `while (!feof(f)) { buf[fread (buf, 1, sizeof(buf) - 1, f)] = 0; Con_Printf("%s", Q_yelltext(buf)); }` | MATCH |
| 3 | Prints `(empty)` when no info file exists | src/sv_demo_misc.c:952-955 | `if ((f = fopen(path, "rt")) == NULL) { Con_Printf("(empty)\n"); return; }` | MATCH |
| 4 | Numeric arg = list position (date-sorted) | src/sv_demo_misc.c:941 -> 556-558 -> 474/484/490-494 | `SV_MVDTxTNum(Q_atoi(Cmd_Argv(1)))` -> `SV_MVDName2Txt(SV_MVDNum(num))` -> `Sys_listdir(... SORT_BY_DATE)` ; `--num;` | MATCH |
| 5 | `*` = currently-recording demo; errors if not recording | src/sv_demo_misc.c:927-933, 937 | `if (!strcmp(Cmd_Argv(1), "*")) { if (!sv.mvdrecording || !demo.dest) { Con_Printf("Not recording demo!\n"); return; }` ; `snprintf(path, ... SV_MVDName2Txt(demo.dest->name));` | MATCH |
| 6 | Requires an argument (Argc<2 -> usage) | src/sv_demo_misc.c:921-923 | `if (Cmd_Argc() < 2) { Con_Printf("usage: demoinfo <demonum>\n...");` | MATCH |
| 7 | Server console / rcon surface | src/sv_demo.c:1951 | `Cmd_AddCommand ("sv_demoinfo", SV_MVDInfo_f);` | MATCH |
| 8 | ALSO a connected-client ucmd `cmd demoinfo`, output to that client | src/sv_user.c:3343 | `{"demoinfo", SV_MVDInfo_f, false},` (entry in `ucmds[]`, the per-client command table) | MATCH |
| 9 | (Corroboration) KTX uses the client surface via stuffcmd | ktx/src/commands.c:7991 | `stuffcmd_flags(self, STUFFCMD_IGNOREINDEMO, "cmd demoinfo %s\n", params_str(1, -1));` | MATCH (consumption, not override) |

V-pass self-classification: TRACED-CLEAN. The dual-surface Set-by clause (8) is included inline because it is action-changing per D20 (a player, not only the admin, can invoke it) -- verified against the actual `ucmds[]` table entry, not inferred from the name. The `overrideable=false` field is a QC-remap flag, NOT an access-class signal, so it is not asserted as a permission claim. F-MV1: no KTX registration; commands.c:7991 is a stuffcmd to the client ucmd, not an override.
