# MVDSV describe-fill -- cross-chunk findings & issues report

**Purpose.** A cumulative, append-only log of *issue-worthy* discoveries surfaced while
documenting MVDSV knobs: suspected upstream bugs, security notes, cross-mod (KTX) couplings
worth a Layer-3 concept note, perf/behavior quirks, and runtime-dead suspects. Routine
enforce-trace notes stay in the per-knob ledgers -- only **actionable** findings land here.
By campaign end this is the consolidated list to mine for upstream PRs and L3 notes.

**How it gets filled.** Synthesis / V-pass agents emit observations via `flags_for_review`
(kinds: `suspected-bug` / `contradiction` / `runtime-dead-suspect` / `hidden-family` /
`cross-mod-override` / `other`; severities: `fyi` / `review` / `blocker`). The workflow runtime
has no filesystem, so agents cannot write this file directly -- their flags reach it through the
**chunk orchestrator (MAIN)**, which curates the issue-worthy flags each chunk and appends rows
here. A directly-dispatched agent that *does* have fs access may append a row itself. Never delete
rows; update **Status** (`open` / `triaged` / `filed-PR` / `wont-fix` / `moved-to-L3`) in place.

**Categories.** `upstream-bug` (code defect/smell in mvdsv or ktx) - `security` -
`cross-mod/L3` (KTX consumes an mvdsv knob -> L3 concept-note candidate) - `perf` -
`behavior-quirk` - `dead-suspect` (registered but possibly unreachable; F-C3b territory).

> All line numbers are anchored to mvdsv `1.11-53-g18d0362` (and ktx at its pinned checkout).
> Re-verify before acting -- a finding is a hypothesis until re-grepped at the time you act on it.

## Findings

| # | Chunk | Knob | Location | Category | Sev | Observation | Status |
|---|-------|------|----------|----------|-----|-------------|--------|
| 1 | c3-dead-network | `sv_www_address` | mvdsv `central.c:694` | upstream-bug | low | Vestigial no-op self-assignment: `if (this->request_id && !strcmp(this->request_id, "upload")) { this = this; }` in the web-response handler -- looks like a leftover debug-breakpoint stub. Harmless but dead code; candidate for an upstream cleanup PR. | open |
| 2 | c3-dead-network | `sv_www_authkey` | mvdsv `central.c:343-344` | security | low | The authKey is sent as a plaintext curl form field (`CURLFORM_COPYCONTENTS`) on *every* outbound request, including the unauthenticated periodic check-in. No TLS/obfuscation at this layer; relies entirely on the configured URL being https. | open |
| 3 | c3-dead-network | `sv_www_address` | ktx `stats.c:554`, `race.c:3286,4980` | cross-mod/L3 | -- | KTX gates match-statistics and race-record uploads on `!strnull(sv_www_address)`. The mvdsv cvar is the on-switch for a KTX feature -> L3 concept note on QW central-server / web integration (which mvdsv core knob lights up which mod behavior). | open |
| 4 | c3-dead-network | `sv_broadcast_enabled` | mvdsv `sv_broadcast.c:620-622` | cross-mod/L3 | -- | KTX `k_spectalk` widens broadcast visibility: when `qwm_name` contains "KTX" and `k_spectalk` is set, mid-game players (not only spectators) see incoming broadcasts. -> L3 note on cross-server broadcast behavior. | open |
| 5 | c3-dead-network | `sv_broadcast_sender_validation_enabled` | mvdsv `sv_broadcast.c:537-541` | perf | low | Every accepted inbound `broadcast` connectionless packet triggers a `server_list_lock`-held linear scan of the known-server list (sender validation). Possible amplification surface under spoofed broadcast floods; worth a glance if broadcast is widely enabled. | open |
| 6 | c3-dead-network | `sv_www_checkin_period` | mvdsv `central.c:738-740` | behavior-quirk | low | `last_checkin_time` is reset to `curtime` on every tick where `server_busy` is true (the `else if`), so the idle check-in clock effectively restarts whenever the server is busy. Possibly intentional ("check in 60s after going idle"); flagged for behavior review, not a description defect. | open |
| 7 | c3-dead-network | `sys_nostdout` (canary) | mvdsv `sv_sys_win.c` (`#ifdef _CONSOLE`) | dead-suspect | low | Observed while canary-testing (not yet a synthesized knob): `sys_nostdout` is declared/enforced only in the dedicated-server sys layers and the Windows print paths are gated by `#ifdef _CONSOLE`; in a non-`_CONSOLE` Windows build it may be inert. Re-verify when `sys_nostdout` is synthesized in a later chunk. | open |
| 8 | physics-movement | `sv_airaccelerate` | mvdsv `sv_phys.c:1129` (bridge) / `pmove.c:534` | upstream-bug | med | Registered (default 10) and copied to `movevars.airaccelerate`, but has **no pmove consumer** -- server-side air acceleration uses `movevars.accelerate` (= `sv_accelerate`) at `pmove.c:534`. The only readers of `movevars.airaccelerate` are the client serverdata broadcast (`sv_user.c:454`) and the MVD demo (`sv_demo.c:1297`). Net: setting `sv_airaccelerate` changes only what the client/demo is told, NOT server-authoritative air movement. Confirm whether client-prediction-only-by-design (matches QW's historical client physics) or a latent quirk. | open |
| 9 | physics-movement | `sv_maxspeed` / `sv_gravity` | mvdsv `sv_user.c:451`, `sv_user.c:458` | upstream-bug | low | Two in-source FIXMEs (`// FIXME: this does't work, Tonik?`): the `svc_playerinfo` writes send the global `movevars.maxspeed` / `movevars.entgravity` instead of the per-client values (`sv_client->maxspeed` / `->entgravity`, commented out). Per-player maxspeed/gravity overrides (e.g. KTX haste) are not transmitted to client prediction. | open |
| 10 | physics-movement | `sv_safestrafe` | mvdsv `server.h:396`, `sv_user.c:3591,3601` | upstream-bug | low | Struct field `client_t.safestrafe.pending_direction` is written at `sv_user.c:3591` and `:3601` but **never read** anywhere in the tree -- vestigial or incomplete safestrafe logic. Does not affect the documented behavior (other fields drive it). | open |
| 11 | physics-movement | `sv_maxspeed` | ktx `client.c:1844`, `ctf.c:750/840`, `runes.c:197`; engine `sv_user.c:889` | cross-mod/L3 | -- | KTX implements haste (CTF grapple / haste rune) by writing the QC entity `maxspeed` field; the engine honors it via `fofs_maxspeed` at `sv_user.c:889` (`sv_client->maxspeed = fofs_maxspeed ? EdictFieldFloat(...) : sv_maxspeed.value`). F-MV1 case (a): per-entity override path, the engine cvar still governs the default. -> L3 note on QW speed/haste. | open |
| 12 | physics-movement | `sv_nailhack` | mvdsv `sv_ents.c:44` | doc-nuance | low | Counterintuitive name+default: `sv_nailhack` default is **1**, and `1` **disables** the compact nail-packet encoding (`if ((int)sv_nailhack.value) return false;` skips `SV_AddNailUpdate`). The name reads as "enable a hack" but the default turns the bandwidth optimization OFF. Captured correctly in the L1 description; flagged so downstream readers aren't surprised. | open |
| 13 | physics-movement | (physics cvars, general) | -- | cross-mod/L3 | -- | Operator guidance to capture as L3: standard competitive QuakeWorld locks server physics (`sv_gravity` 800, `sv_accelerate` 10, `sv_friction` 4, `sv_maxspeed` 320, `sv_stopspeed` 100, ...) to fixed values; changing them yields a **non-standard** server with altered movement feel. The knobs exist for custom modes/experiments, but normal play should leave them at defaults. -> L3 concept-note candidate on QW server physics tuning. | open |
| 14 | admin-ban | `vip_addip` | mvdsv `sv_main.c:2081` (vs guard at `:2205`) | upstream-bug | med | `SV_AddIPVIP_f` does not reject a zero `compare`, unlike its ban-list sibling `SV_AddIP_f` (`\|\| f.compare == 0` at `sv_main.c:2205`). `vip_addip 0` (or any address parsing to compare 0) yields mask 0 / compare 0, and `SV_VIPbyIP` (`sv_main.c:2730`, `(in & 0) == 0`) then matches **every** connecting IP -- granting VIP and reserved-spectator-slot access to all clients. Admin footgun + slot-limit bypass; the fix is the same `compare == 0` guard the ban command already has. | open |
| 15 | admin-ban | `cuff` / `mute` | mvdsv `sv_main.c:2930` (write `:2935`) | upstream-bug | med | `SV_SavePenaltyFilter`'s full-array guard tests `numpenfilters == MAX_IPFILTERS` (1024, `sv_main.c:2009`) but `penfilters[]` is sized `MAX_PENFILTERS` (512, `server.h:840`). Once 512 saved penalties accumulate, `SV_IPCopy(penfilters[numpenfilters]...)` at `:2935` writes out of bounds (indices 512-1023) -- a buffer overflow. Saved penalties are created when cuffed/muted players disconnect, so a long-lived busy server can reach it. Wrong constant in the guard. Re-verified on upstream master 2026-06-03 (guard `sv_main.c:2931`, write `:2936`; sibling arrays guard correctly). | merged #205 |
| 16 | admin-ban | `mute` | mvdsv `sv_user.c:1836` (mute at `:1849`) | cross-mod/L3 | med | The engine chat-mute (`if (curtime < lockedtill)`, `sv_user.c:1849`) is reached only AFTER `if (j) return; // say was handled by mod` (`sv_user.c:1836`, `j = PR_ClientSay(...)`). A mod (KTX) that fully handles `say`/`say_team` returns non-zero and bypasses the engine chat-mute, so muting a player's CHAT may depend on the mod re-implementing it. The name-change (`sv_user.c:2389`) and VOIP (`sv_user.c:2873`) mute blocks are NOT behind this short-circuit and always fire. -> L3 note on mute+mod interaction; verify KTX's say handler to confirm real-world impact before treating the mute chat-block as authoritative on a KTX server. | open |
| 17 | admin-ban | `addip` family | mvdsv `sv_main.c:2022` (cvar) / `:2391-2393` (enforce) | behavior-quirk | low | The IP-ban family's block/allow polarity is governed by a separate cvar `filterban` (default `1`). At 1, listed addresses are refused; at 0, `SV_FilterPacket` inverts the list into an ALLOW-list -- only listed addresses may connect (`:2391` `return filterban.value` on a ban match, `:2393` `return !filterban.value` otherwise) -- a private-server / LAN-only mode. The addip/listip/writeip user-docs describe the default (ban) behavior; `filterban` is the sibling switch to document alongside them (and an L3 "private server" note) when its cvar is filled. | open |
| 18 | admin-ban | `penaltyremove` | mvdsv `sv_main.c:2876` | behavior-quirk | low | `SV_RemoveIPFilter` compacts `penfilters[]` by shifting entries down (`penfilters[i] = penfilters[i+1]`) and decrementing `numpenfilters`, so every `penaltyremove` re-indexes all higher-numbered saved entries. An admin removing several saved penalties must re-run `penaltylist` between removes, or the indices shift under them. The `penaltyremove` description points at penaltylist for the index; flagged so the re-index gotcha is on record. | open |
| 19 | server-control-logging | `logtelnet` | mvdsv `sv_ccmds.c:182` (toggle); enum `log.h:25` | dead-suspect | low | `logtelnet` opens `qtelnet_<port>_NNNN.log` and `telnet_log_level` sets `logs[TELNET_LOG].log_level`, but **no `SV_Write_Log(TELNET_LOG, ...)` call exists anywhere in the tree** -- nothing ever writes to the telnet log, so the file is always empty. The telnet-console subsystem that would feed it appears removed (no telnet socket/accept; `sv_sys_win.c` has zero telnet refs). Vestigial command+cvar surface; candidate for upstream retirement or a build-variant note. (L1 row hedged accordingly.) | open |
| 20 | server-control-logging | `chmod` / `log*` | mvdsv `sv_main.c:1701` (master tier) / `:1754-1764` (blocklist) | behavior-quirk | low | The regular `rcon_password` tier runs a command blocklist (`rm`/`rmdir`/`ls`/`chmod`/`sv_admininfo`/`if`/`localcommand`/`sv_crypt_rcon`/`sv_timestamplen`/`log*` via `!strncasecmp(tstr,"log",3)`/`sys_command_line`) -> `bad_cmd=true` -> `do_cmd=false` (`:1774`). Only the local console and the `master_rcon_password` tier (validated at `:1701`, unfiltered) can run these. The chunk-4 Set-by lines for `chmod` + the five `log*` commands were tightened to reflect this (regular rcon cannot run them). | triaged |
| 21 | server-control-logging | (rcon blocklist loop) | mvdsv `sv_main.c:1769` (break) vs comment `:1736-1738` | upstream-bug | low | The blocklist loop's `break;` (`:1769`) sits inside the `for` but outside the `if`, so after skipping leading empty tokens it inspects exactly ONE non-empty token -- yet the comment (`:1736-1738`, "must check *all* tokens, because a command/var may not be the first token") states the opposite intent. Likely benign now (semicolons disabled per `:1743-1745`, so only the first token executes as a command), but the code does not do what its comment claims -- maintainer glance. | open |
| 22 | server-control-logging | `updatebroadcasts` (shared module) | mvdsv `sv_broadcast.c:373`/`:382`/`:387` | upstream-bug | med | Wrong-mutex unlock on an error path in `SV_Broadcast`: it acquires `broadcast_lock` (`:373`), but the `broadcast_in_progress` early-return (`:382`) unlocks `servers_update_lock` instead -- leaving `broadcast_lock` held (subsequent broadcasts then fail to acquire it) and spuriously releasing an unrelated mutex. The success path (`:387`) unlocks `broadcast_lock` correctly. Off-scope for the `updatebroadcasts` knob itself (uses `servers_update_lock` correctly) but surfaced in the shared broadcast module. Re-verified on upstream master 2026-06-03 (`:373`/`:382`/`:387` unchanged). | merged #206 |
| 23 | script-meta-cheats-web | `script` | mvdsv `sv_main.c:2843-2853` | security | med | Path-traversal carve-out in `SV_Script_f`: a single leading `../` / `..\` is advanced past (`path += 3`, `:2844`) BEFORE the `strstr(path, "..")` reject check (`:2846`), then the original arg is RE-FETCHED (`path = Cmd_Argv(1)`, `:2853`) and passed to `Sys_Script` (`:2866`). So `script ../foo` runs `<fs_gamedir>/../foo.qws` -- one directory above the game dir, defeating the intended `..` sandbox. Multi-`..` / non-leading `..` are still rejected. Admin-gated (console/rcon) so not remotely exploitable, but the guard does not do what it intends; the `//bliP: need subdirs here` comment shows the carve-out was deliberate, the bug is re-fetching the un-advanced arg. | open |
| 24 | script-meta-cheats-web | `sv_web_get` / `sv_web_post` | mvdsv `central.c:528`,`:358-360` | upstream-bug | low | `Web_SendRequest(qbool post)` (`central.c:528`) never references its `post` parameter; the HTTP method is decided solely at `:358-360` (`if (first_form_ptr) curl_easy_setopt(..., CURLOPT_POST, 1)`), and the authKey form field is always added so `first_form_ptr` is always set. `Web_GetRequest_f` passes `false`, `Web_PostRequest_f` passes `true`, but both produce an identical curl POST -- `sv_web_get` is misnamed (does not issue an HTTP GET). | open |
| 25 | script-meta-cheats-web | `sv_web_get` | mvdsv `central.c:765` | dead-suspect | low | `sv_web_get` has ZERO callers anywhere in the mvdsv or KTX trees -- the only occurrence is its own `Cmd_AddCommand` registration (`central.c:765`). KTX exercises `sv_web_post` (race.c:4989) and `sv_web_postfile` (stats.c:590, race.c:3288), never `sv_web_get`. Candidate for upstream retirement, or reserved for external/manual use. | open |
| 26 | script-meta-cheats-web | `sv_web_*` (request_id) | mvdsv `central.c:50`,`:694-695`; CMakeLists.txt:88-96 | dead-suspect | low | The `request_id` arg accepted by all three sv_web_* commands is inert in this version: the struct-field comment (`central.c:50`, "if set, content will be passed to game-mod") has NO live consumer (grep of pr2_cmds/pr_cmds for request_id empty), the only runtime read is the dead `if (this->request_id && !strcmp(this->request_id,"upload")){ this=this; }` no-op (`:694-695`, == finding #1), and `Web_PostResponse` parses only Broadcast/Upload/UploadPath. L1 rows hedge request-id to "an opaque tag stored with the request". Separately the whole central.c web layer is CURL-build-conditional (CMakeLists.txt:88-96) -- a curl-less build registers no sv_web_* commands (F-C3b; documented-as-live). [cross-ref #1] | open |
| 27 | script-meta-cheats-web | `sv_lastscores` | mvdsv `sv_demo_misc.c:980` (vs `:985-986`) | upstream-bug | low | The lastscores usage string advertises "<numlastdemos> = '0' for all demos" (`sv_demo_misc.c:980`), but the gate maps any `Q_atoi(arg) <= 0` to `MAXDEMOS` (10) at `:985-986` -- there is no list-all path, and the count is further capped at `dir.numfiles`. Passing 0 lists 10, not all. The engine's own help text contradicts the code; the L1 row describes live behavior. | open |
| 28 | script-meta-cheats-web | `sv_lastscores` | ktx `commands.c:899-900` (handler `:6981`) | cross-mod/L3 | -- | KTX registers its OWN `lastscores` and `lastscoresktx` QC console commands (CF_BOTH, handler `lastscores` at commands.c:6981) showing KTX's in-mod match history -- distinct from the engine's `sv_lastscores`/`lastscores` (SV_LastScores_f) which lists saved demo score summaries. On a KTX server the in-game `lastscores` a player types may resolve to the MOD's command, not the engine's. -> L3 note on lastscores (engine demo-summary vs KTX in-mod history). | open |
| 29 | script-meta-cheats-web | `cache_print` / `cache_report` | mvdsv `zone.c:333-689` (`#ifdef SERVERONLY`) | dead-suspect | low | The server-side data-cache subsystem has NO producer -- `Cache_Alloc` / `Cache_TryAlloc` / `Cache_Check` are never called by any server code outside zone.c (tree-wide grep empty), so `cache_print` always lists an empty cache. Also `cache_report` labels its output "data cache free" but the number it prints is the HUNK arena's free space (`hunk_size - high - low used`), not a cache pool. Candidate for upstream retirement / relabel. | open |
| 30 | script-meta-cheats-web | `localcommand` | mvdsv `sv_ccmds.c:771` (scaffolding `:751`,`:793`) | security | low | `SV_LocalCommand_f` runs its argument string through `system()` (`sv_ccmds.c:771`) and is bracketed by `//bliP: REMOVE ME REMOVE ME ...` comments (`:751`,`:793`) -- author-flagged debug scaffolding. Exposure is limited (registered only with the `-enablelocalcommand` launch flag; on the normal-rcon blocklist so console + master-rcon only), but it is an arbitrary-OS-command path the source itself marks for removal. | open |
| 31 | script-meta-cheats-web | `profile` / `mod` | mvdsv `pr2_exec.c:77`,`pr_edict.c:1273` | behavior-quirk | low | `profile` (PR2_Profile_f, `pr2_exec.c:77`) only profiles legacy PR1/qwprogs code -- it calls `PR_Profile_f()` (`:81`) inside the `!sv_vm` branch, so under a QVM/native mod (KTX; sv_vm set) it does nothing. There are TWO `Cmd_AddCommand("profile", ...)` registrations: the live PR2 one (`pr2_exec.c:70`) and a PR1 one (`pr_edict.c:1273`) dead under unconditional USE_PR2. `VM_VmProfile_f` (`vm.c:1518`) is defined but never registered or called -- no QVM profiler command is wired. L1 row notes the QVM no-op. | open |
| 32 | cmdline-params | `-port` / `-ip` | mvdsv `net.c:1407` (-port), `net.c:1095`/`:1166` (-ip) | upstream-bug | low | Trailing-token off-by-one: the value-read guard is `p < COM_Argc()` / `i < COM_Argc()`, not the sibling form `... < COM_Argc() - 1` (used by `+gamedir` at fs.c:557 and by `-u`/`-t`/`-g` which use `j + 1 < com_argc`). If `-port`/`-ip` is the LAST argv token, `COM_Argv(p+1)` is read one index past the args -- but this is BENIGN, not an OOB read: `COM_Argv` (common.c:836) bounds-checks (`if (arg < 0 \|\| arg >= com_argc) return ""`), so a value-less `-port`/`-ip` yields `atoi("")=0` (port 0) / `inet_addr("")` rather than reading adjacent memory. (The chunk-6 cold V-pass on `-port` over-claimed an "out-of-bounds / reads adjacent memory" read; the COM_Argv guard refutes it -- caught at finding-write.) Cosmetic guard inconsistency; one-line fix for uniformity. Separately, a malformed `-ip` value makes inet_addr return INADDR_NONE and the bind fails -> socket-open aborts (fail-closed). | open |
| 33 | cmdline-params | `-game` / `+gamedir` | mvdsv `fs.c:561` (vs reject at `:457-460`) | upstream-bug | med | Rejected-gamedir serverinfo divergence: `FS_InitEx` runs `FS_SetGamedir(COM_Argv(i+1), true)` then UNCONDITIONALLY `Info_SetValueForStarKey(svs.info, "*gamedir", COM_Argv(i+1), ...)` (fs.c:559/561). But `FS_SetGamedir` rejects any value containing `..`/`/`/`\`/`:` and returns early without changing the filesystem (fs.c:457-460). So `-game foo/bar` leaves the loaded gamedir at `qw` yet still publishes `foo/bar` into the `*gamedir` serverinfo key -> connecting clients are told (via svc_serverdata) a gamedir the server is not actually running. The adjacent `// FIXME: move in FS_SetGamedir() instead!!!` (fs.c:560) flags this exact ordering. Fix: set the serverinfo key only on FS_SetGamedir success. | open |
| 34 | cmdline-params | `-noerrormsgbox` / `-d` | mvdsv `sv_sys_win.c:368`/`:406`/`:803` (`_CONSOLE`); `CMakeLists.txt` (no `_CONSOLE`) vs `tools/old_mvs_files/mvdsv_vc2017.vcxproj:163` | dead-suspect | low | Windows build-variant divergence: the fatal-error MessageBox suppression (`sv_sys_win.c:407`) and the GPF-dialog suppression + `-d` daemon handling live in the non-`_CONSOLE` GUI/WinMain path (`#else // _CONSOLE`, sv_sys_win.c:803-902). The canonical CMake/MinGW build defines `_CONSOLE` nowhere (grep of CMakeLists.txt empty; CI gcc-mingw-w64) -> WinMain build -> `-noerrormsgbox`/`-d` are LIVE (L1 documents the live behavior). The legacy MSVC project (mvdsv_vc2017.vcxproj defines `_CONSOLE` in all 5 configs) builds the `int main` console variant where both flags are inert. `-noerrormsgbox` L1 row kept at medium confidence for this reason; F-C3b document-as-live. | open |
| 35 | cmdline-params | `-tcpport` / `-clientport` (off-scope) | mvdsv `net.c:1429` (`#if 0`), `net.c:1320` (`#ifndef SERVERONLY`) | dead-suspect | low | Two command-line params present in source but unreachable in the mvdsv dedicated-server binary (NOT extracted as L1 entities; surfaced during chunk-6 recon, like chunk-1 #7): `-tcpport` sits inside `#if 0 // qqshka: TCP port shared with QTV now` (net.c:1429-1435) -- dead, the TCP/QTV port is opened elsewhere; `-clientport` is read only in `NET_InitClient` under `#ifndef SERVERONLY` (net.c:1320-1326), and mvdsv compiles with SERVERONLY (CMakeLists.txt:169) -- never compiled. Candidates for upstream retirement; recorded so a later cmdline sweep does not treat them as live. | open |
| 36 | cmdline-params | `-basedir` | mvdsv `fs.c:27-29` | upstream-bug | low | Stale doc-comment: the FS_InitEx header says "The sys_* files pass this to host_init in `quakeparms_t->basedir`", but mvdsv never seeds `fs_basedir` from any quakeparms struct -- the only writers are `-basedir` (fs.c:527) and the literal `.` fallback (fs.c:534). Comment-rot inherited from upstream Quake; describes a code path that does not exist here. Cosmetic; upstream comment-cleanup candidate. | open |
| 37 | version-build-identity | `qws_*` / `qwm_*` (identity bank) | ktx `motd.c:77-81`, `commands.c:1664-1701`, `g_main.c:501-507` | cross-mod/L3 | -- | The 14 server/mod identity cvars are an engine-exposed bank whose user-facing purpose is realized by KTX, not the mvdsv engine: mvdsv reads only `qwm_name` (KTX-detection, see #38). KTX READS the read-only `qws_*` for its displays (`qws_name`+`qws_version` in the MOTD server line, motd.c:77-81; `qws_fullname`/`qws_version`/`qws_buildnum` as the Name/Version/Build rows of its version command, commands.c:1664-1680) and WRITES all 7 `qwm_*` placeholders at mod init with its own identity (g_main.c:501-507: qwm_name=MOD_NAME, qwm_fullname=MOD_FULLNAME, qwm_version=MOD_VERSION, qwm_buildnum=GIT_COMMIT, qwm_platform=QW_PLATFORM_SHORT, qwm_builddate=MOD_BUILD_DATE, qwm_homepage=MOD_URL), then renders `qwm_buildnum`-`qwm_platform` as its own Build row (commands.c:1698-1701). So `qws_*` = engine identity that the mod reads; `qwm_*` = mod identity that the mod fills. -> L3 concept note on QW server/mod identity reporting (which engine cvar feeds which KTX display row). | open |
| 38 | version-build-identity | `qwm_name` | mvdsv `sv_init.c:424`, `sv_broadcast.c:622` (decl `:3422`; `sv_pext_mvdsv_serversideweapon` default `:199`) | behavior-quirk | low | The engine gates two behaviors on `strstr(Cvar_String("qwm_name"), "KTX")` -- the server-side-weapon extension (sv_init.c:424, when `sv_pext_mvdsv_serversideweapon` is set, which defaults to "1"/ON at sv_main.c:199) and spectalk broadcast-to-players (sv_broadcast.c:622, with `k_spectalk`). `qwm_name` is a plain WRITABLE cvar (no CVAR_ROM, sv_main.c:3422), normally set by the mod to MOD_NAME; an admin or any mod can set it to any string CONTAINING "KTX" (case-sensitive substring, not an exact match) to flip these engine behaviors on without actually running KTX. Self-inflicted on one's own server, but a real coupling: engine feature-gating keyed on a free-form, mod/admin-writable identity string. [cross-ref #4, the spectalk half] | open |
| 39 | version-build-identity | `qws_version` | mvdsv `sv_main.c:3416` (no reader), `:3684` (`*version`), `:63`/`:3915` (legacy `version`) | behavior-quirk | low | The server version string exists in THREE parallel carriers, all from the `SERVER_VERSION` macro: (1) the `qws_version` cvar (sv_main.c:3416) has ZERO read-sites and is not CVAR_SERVERINFO; (2) the `*version` serverinfo star-key (sv_main.c:3684, `Info_SetValueForStarKey(svs.info,"*version",SERVER_NAME " " SERVER_VERSION,...)`) is the actual client/browser-facing advertisement; (3) the legacy `version` CVAR_ROM cvar (sv_main.c:63, set via `Cvar_SetROM` to `SERVER_NAME " " SERVER_VERSION` at :3915). `qws_version` is inert as a data source -- a consumer needing "the version the server advertises to clients" must read the `*version` serverinfo key, not this cvar. (Not a bug; this is the root of the "advertises" near-miss HG2 corrected on qws_name/fullname/version.) The whole `qws_*` family except `qws_buildnum` is read-only-by-engine, identity-only. | open |
| 40 | version-build-identity | `qws_buildnum` | mvdsv `version.h:72`, `sv_main.c:3592-3593` | dead-suspect | low | `qws_buildnum` is declared "unknown" and overwritten with `GIT_COMMIT` only when `GIT_COMMIT[0]` is non-empty (sv_main.c:3592-3593). In this tracked tree `GIT_COMMIT` is `""` (version.h:72) and no build path injects it (CMake's `find_package(Git)` is for submodules only, no `-DGIT_COMMIT`; CI workflows don't patch version.h; no top-level Makefile), so a from-source build leaves `qws_buildnum` = "unknown". Only a release/CI pipeline that defines `GIT_COMMIT` yields a real commit hash. The L1 row's "Default: unknown" is therefore the effective runtime value for dev-head/source builds -- a reader expecting a populated commit by default would be wrong. Build-variant note (F-C3b document-as-live; mirrors #34). | open |
| 41 | gameplay-limits | `deathmatch`/`fraglimit`/`teamplay`/`timelimit` | mvdsv (storage only) / ktx `world.c:1555-1558`, `combat.c:325-336`, `match.c:1721`, `pr_edict.c:1011` | cross-mod/L3 | -- | The four classic match-rule cvars are F-MV1 compatibility knobs: mvdsv stores them in serverinfo + exposes them to the mod but does NOT enforce the rules. `fraglimit` has ZERO engine `.value` reads; `timelimit` is engine-read only for demo metadata; `deathmatch` the engine reads only as a 0-vs-nonzero spawn filter (`pr_edict.c:1011`, removes single-player-only items) + force-0-on-coop; `teamplay` the engine uses only to route team VOICE + label demos (team TEXT chat is mod-handled). KTX is the real governor: loads all four (`world.c:1555-1558`), ends matches on frags per-player + team-sum (`combat.c:325-336`), runs the time/overtime clock (`match.c:1721`), branches the deathmatch mode-number (1-5), and overwrites deathmatch/teamplay. -> L3 concept note on QW match-rule cvars (which engine-stored cvar the mod enforces). | open |
| 42 | gameplay-limits | `registered` | mvdsv `sv_main.c:186` (decl), `vfs_os.c:212-218` (dead gate) | dead-suspect | low | `registered` (CVAR_ROM, hardcoded "1") has NO `.value`/`.string` read anywhere in the mvdsv tree -- the only shareware-era consumer, the file-access gate at `vfs_os.c:212-218` (`if (!static_registered) ...`), is commented out. On the dedicated server it is an inert compatibility stub; its sole live consumer is off-tree server-game QC (mods read it via `cvar()` to gate full-game features). L1 row hedged. -> also cross-mod/L3 (the mod, not the engine, acts on registered). | open |
| 43 | gameplay-limits | `sv_mod_extensions` | mvdsv `sv_main.c:58` (CVAR_ROM, no reader) / ktx `g_main.c:509`, `include/g_consts.h:339-340` | cross-mod/L3 | -- | `sv_mod_extensions` (CVAR_ROM "2") has NO engine read-site in mvdsv -- it is an outbound capability advertisement. KTX reads it (`g_main.c:509` `sv_extensions = cvar("sv_mod_extensions")`) to learn which optional server extensions the build supports; the bitfield is `SV_EXTENSIONS_KTXEXTENSION1`=1 (reserved, no consumer) and `SV_EXTENSIONS_MVDHIDDEN`=2 (the default -- hidden MVD-demo data). The mvdsv<->KTX extension handshake. -> L3 concept note. | open |
| 44 | gameplay-limits | `sv_pr2references` | mvdsv `pr2_exec.c:602-606` | behavior-quirk | low | `sv_pr2references` is registered as a plain admin-settable cvar (default 0) but is effectively NOT admin-controllable: the game module overwrites it on every load (`pr2_exec.c:602` `sv_vm->pr2_references = ... && sv_pr2references.value`), so a server-config value has no lasting effect. It only matters for 64-bit NATIVE (.so/.dll) mods -- the mandatory-references `SV_Error` (`pr2_exec.c:603-606`) is gated on `type == VMI_NATIVE` inside `#ifdef idx64`; QVM/bytecode mods (KTX ships as .qvm) run fine with the value at 0. An admin-facing cvar that is really a mod-owned, native-only handshake. | open |
| 45 | gameplay-limits | `sv_serveme_fix` | mvdsv `sv_main.c:192` (CVAR_ROM), read `sv_ents.c:1124` | behavior-quirk | low | `sv_serveme_fix` is CVAR_ROM with default "1" and has NO write site anywhere in the tree -- permanently on; an admin can never disable it. When on it stops the world stream (broadcasts, entity/player updates) to a spectator named exactly "[ServeMe]" and lets that client skip pre-spawn/outdated-client checks (`sv_ents.c:1124`). A read-only "fix" toggle hardwired to 1 -- the cvar surface is misleading (cannot be turned off). | open |
| 46 | gameplay-limits | `sv_csqc_progname` | mvdsv `sv_user.c:1462` (vs `sv_init.c:215`) | upstream-bug | low | The CSQC download auto-allow hardcodes the literal filename: `sv_user.c:1462` `else if (!strncmp(name, "csprogs.dat", 11)) allow_dl = true;` -- it does NOT read `sv_csqc_progname.string`. An admin who points `sv_csqc_progname` at a non-default file gets that file advertised in serverinfo (`sv_init.c:215` loads it) but clients cannot auto-download it (the auto-exempt only whitelists "csprogs.dat"). Likely-benign (the default name is near-universal) but the download exempt and the cvar disagree. (Whole CSQC path is `#ifdef FTE_PEXT_CSQC`.) | open |
| 47 | gameplay-limits | `vm_rtChecks` (forceDataMask) | mvdsv `vm_local.h:225`, `vm_x86.c:679-680`, `:1211` | upstream-bug | low | `vm->forceDataMask` (declared `vm_local.h:225`, read at `vm_x86.c:679-680` and `:1211`) is NEVER assigned anywhere in the tree -- a dead, always-false field (vmTable static-zeroed). It gates whether the JIT emits a data-access AND-mask even when `vm_rtChecks` bit 8 is off; permanently false, so bit-8 OFF always removes bounds masking. Looks like a forced-sandbox mode never wired up. Separately `vm_rtChecks` governs ONLY the x86 JIT backend (`vm_x86.c`); the bytecode interpreter performs the same checks unconditionally -- captured in the L1 row's scope note. | open |
| 48 | gameplay-limits | `sv_paused` | mvdsv `server.h:725` / ktx `commands.c:8740` | behavior-quirk | low | The `sv_paused` declaration carries a Quake-heritage bitfield comment `// 1 - normal, 2 - auto (single player), 3 - both` (`server.h:725`), but only bit 1 has a live consumer: the engine mirrors `sv.paused` into the read-only cvar (`sv_user.c:2042`) and KTX reads bit 1 (`commands.c:8740` `(int)cvar("sv_paused") & 1`; also `g_cmd.c:230`/`:238`, `client.c:945`). Bits 2 ("auto single-player") and 3 ("both") have no consumer in the dedicated-server build -- partially-stale source comment (source-comment-is-a-hypothesis). KTX also uses sv_paused as a cross-mod signal to freeze flood/say timers -> minor L3. | open |
| 49 | networking-rate-download-logging | `sv_maxrate` | mvdsv `sv_main.c:3193` (engine cap) / ktx `world.c:1560`, `:1749-1751` | cross-mod/L3 | -- | The engine enforces sv_maxrate as a per-client send-rate cap (sv_main.c:3193, bytes/sec, 0 = uncapped). KTX additionally OWNS the value: every world-rules refresh it clamps `bound(0, cvar("sv_maxrate"), 500000)` (ktx world.c:1560) and writes the clamped value back via `cvar_fset("sv_maxrate", k_maxr)` (ktx world.c:1749-1751). So on a KTX server an admin's sv_maxrate above 500000 is silently lowered and the cvar is mod-echoed -- the value an admin ends up with is KTX's, not necessarily what was set. -> L3 note on QW server bandwidth (engine cap vs KTX clamp). | open |
| 50 | networking-rate-download-logging | `sv_local_addr` | mvdsv `net.c:1420` (engine-set CVAR_ROM) / ktx `stats.c:559`, `logs.c:100`, `race.c:186` | cross-mod/L3 | -- | sv_local_addr is a CVAR_ROM cvar the engine auto-fills with the detected local IP:port (net.c:1420), left empty if the socket fails to open. KTX READS it (`cvar_string("sv_local_addr")`) to build the server's IP:port identity for stats uploads (ktx stats.c:559), frag logging (logs.c:100) and race-record uploads (race.c:186). So a wrong/empty detected address (behind NAT, or socket-open failure) propagates into KTX's upload identity -- a cross-engine dependency for the identity/bandwidth L3 note. (sv_serverip is the operator override for the engine's own real-IP probe, but KTX reads sv_local_addr here, not sv_serverip.) | open |
| 51 | networking-rate-download-logging | `sys_command_line` | mvdsv `sv_main.c:3469` (set), `:1764` (blocklist) | security | low | sys_command_line is a read-only cvar holding the server's FULL launch command line -- executable path plus every startup argument (sv_main.c:3469). If an operator passes a secret on the command line (e.g. `+set rcon_password X`, or a master password), it becomes readable by anyone who can query this cvar. The normal-rcon blocklist (sv_main.c:1764, the finding-#20 table) blocks NAMING sys_command_line as a non-master rcon token, so plain admin-rcon cannot read it -- but local console and master-rcon can, and the value may still surface via other cvar/serverinfo query paths. Information-exposure surface; prefer not to pass secrets on the command line. [cross-ref #20] | open |
| 52 | networking-rate-download-logging | `sv_maxuploadsize` | mvdsv `sv_user.c:1290-1301` (SV_CancelUpload) | upstream-bug | low | When a server-requested screenshot upload is cancelled (e.g. it exceeds sv_maxuploadsize), SV_CancelUpload (sv_user.c:1290-1301) only fclose()s the handle -- it does NOT remove/unlink the partial `.pcx` already written under fs_gamedir/snap/. A client that repeatedly triggers the size-cancel (or otherwise aborts mid-upload) can accumulate partial snap files on disk. Minor disk-hygiene leak; the cancel path should unlink the partial file. | open |
| 53 | networking-rate-download-logging | `telnet_log_level` | mvdsv `sv_main.c:3899` (OnChange), `:87` (decl) | dead-suspect | low | telnet_log_level is registered with a working OnChange that sets `logs[TELNET_LOG].log_level` (sv_main.c:3899) and is re-applied on telnet-logfile open (sv_ccmds.c:135), BUT an exhaustive census finds NO `SV_Write_Log(TELNET_LOG, ...)` caller anywhere -- the TELNET_LOG channel is never written, so the threshold gates a log that does not exist and the cvar has no observable runtime effect. Same dead telnet-logging subsystem as the `logtelnet` command (#19); L1 row hedged + document-as-live (F-C3b, not a suspect-pool member). [cross-ref #19] | open |
| 54 | networking-rate-download-logging | (net_chan, showpackets-adjacent) | mvdsv `net_chan.c:245` | upstream-bug | low | Surfaced while tracing showpackets: Netchan_Transmit's message-overflow branch sets `chan->fatal_error = true;` immediately followed by the in-source comment `//FIXME: THIS DOES NOTHING` (net_chan.c:245) -- a self-documented dead write (the flag is never read to abort the connection). Off-scope for the showpackets knob itself; recorded as an engine smell. | open |
| 55 | spectator-voip-mod-system | `sv_specprint` | mvdsv `pr_cmds.c:350`, `pr2_cmds.c:428` (vs sprint `pr_cmds.c:294`) | upstream-bug | low | sv_specprint's per-client narrowing reads the WRONG client for 2 of 3 message kinds. The sprint path (pr_cmds.c:294, pr2_cmds.c:374) checks the SPECTATOR's own bit (`cl->spec_print` where cl = the loop spectator) -- correct. But centerprint (pr_cmds.c:350, pr2_cmds.c:428) and stuffcmd (pr_cmds.c:859, pr2_cmds.c:769) check `cl->spec_print` where cl = the FOLLOWED PLAYER (svs.clients[entnum-1]), not the spectator (the loop var is `spec`). So a spectator's own `sp` userinfo has no effect on mirrored centerprints/stuffcmds -- the followed player's `sp` decides it. Looks like a copy-paste bug (the sprint loop names the spectator `cl`; the others name it `spec` but kept `cl` in the bit check). L1 row describes the live behavior. | open |
| 56 | spectator-voip-mod-system | `sv_allowlastscores` | mvdsv `sv_main.c:700`, `:716` | cross-mod/L3 | -- | sv_allowlastscores gates ONLY the connectionless `lastscores`/`laststats` network query (SVC_LastScores sv_main.c:700, SVC_LastStats :716 -- both `if(!sv_allowlastscores.value) return`). The in-game `lastscores` a player types is a DIFFERENT path (the engine SV_LastScores_f demo-summary command + KTX's own lastscores QC command). So this cvar does not gate in-game lastscores, only the out-of-band query a browser/tool issues. -> L3 on the lastscores surfaces. [cross-ref #27, #28] | open |
| 57 | spectator-voip-mod-system | `vip_values` | mvdsv `sv_main.c:2750` (parse), `sv_user.c:295` (publish *VIP) | cross-mod/L3 | -- | vip_values maps VIP passwords to a numeric level (sv_main.c:2750); on connect the resulting level is published to the `*VIP` userinfo star-key (sv_user.c:295, `Info_SetStar(..., "*VIP", va("%d", vip))`) and thereby handed to the mod's QC ClientConnect. So the engine cvar assigns the VIP number but the MOD decides what each level grants (KTX reads *VIP). -> L3 on QW VIP / reserved-slot handling (engine assigns, mod acts). | open |
| 58 | spectator-voip-mod-system | `pext_ezquake_verfortrans` | mvdsv `sv_user.c:367-391` | cross-mod/L3 | -- | A cross-ENGINE protocol gate: for an ezQuake client that negotiated the FTE_PEXT_TRANS (entity transparency/alpha) extension, the server reads the client's build revision from its *client userinfo and, if it is below pext_ezquake_verfortrans (default 7814), strips the transparency extension for that client and warns "Alpha support disabled due to buggy client" (sv_user.c:367-391). Only affects ezQuake clients that requested the extension; the value is an ezQuake build/revision number, not a version string. -> L3 on cross-engine protocol-extension gating. | open |
| 59 | spectator-voip-mod-system | `sv_mod_msg_file` | mvdsv `sv_mod_frags.c:87` (parse), `:160-170` (index) | upstream-bug | low | The per-pattern `id` field is parsed straight from the admin-supplied pattern file with no bounds check (`qwmsg[i]->id = Q_atoi(str_tok)`, sv_mod_frags.c:87) and then used to index fixed arrays `qw_weapon[qwmsg[i]->id]` (:160/162) and `qw_system[qwmsg[i]->id]` (:168/170) with no clamp -- a malformed or hostile pattern file with an out-of-range id triggers an out-of-bounds array read. Admin-gated (the admin chooses the file via sv_mod_msg_file), so not remotely exploitable, but the loader should bound id. (Also: the emitted record is backslash-delimited, not tab; the L1 "tab-style" was corrected at synthesis.) | open |
| 60 | spectator-voip-mod-system | `sv_voip_record` (voip) | mvdsv `sv_user.c:3004-3009` | dead-suspect | low | The "a spectator hears whatever the player they are tracking can hear" voip feature is compiled OUT: the block is inside `#if 0 // qqshka: I am turned it off.` (sv_user.c:3004-3009). So on this build a spectator cannot receive the voice of the player they follow, regardless of voip settings. Vestigial/disabled feature; flagged so voip docs do not imply spectator-follow voice works. | open |
| 61 | command-stragglers | `svadmin` | mvdsv `sv_main.c:1672` (set), `:214`/`:431`/`:1645-1654` (only other refs) | dead-suspect | low | svadmin (SV_Admin_f) claims/releases a global "Rcon Watch" pointer WatcherId (set sv_main.c:1672; cleared on disconnect :431-432 and on `svadmin off` :1649), but a tree-wide grep shows WatcherId is read ONLY by svadmin's own held/release logic (:1645-1654) -- NO rcon-forwarding path ever consumes it. So the Rcon Watch feature is non-functional: svadmin selects a watcher that nothing forwards rcon commands/output to. Beyond the console confirmation + held/released state, the command has no observable effect. L1 row hedged accordingly. Vestigial QW262-era feature (`// QW262` comment at :214); upstream-retirement or wiring-completion candidate. | open |
| 62 | rigor-catchup-commands | `rm` / `rmdir` | mvdsv `sv_ccmds.c:651-656` (rm) / `:611-617` (rmdir) vs `ls` `:545-553` | security | med | `rm` and `rmdir` path-escape validation is strictly weaker than the sibling `ls`: both lack the bare-`..` dirname guard (`!strncmp(dirname,"..",3)`) and the trailing-`/..` check that `ls` has. `rm .. demo.txt` resolves to `Sys_remove("../demo.txt")` and `rm a/.. x` to `a/../x` -- escaping one level above the working dir (`rm .. *` enumerates+deletes the parent dir); `rmdir ..` similarly. Restricted to local console + master_rcon_password (normal rcon blocks rm/rmdir, sv_main.c:1754-1764), so not remotely-unauth, but a real path-traversal gap vs the guard's evident intent. Sub-note: `ls`'s trailing-`/..` check `!strncmp(dirname+i-3,"/..",4)` uses length 4 for a 3-char literal (benign, exact-suffix); rm's filename `/..`-suffix branch (:653) is dead (`strchr(filename,'/')` fires first). | open |
| 63 | rigor-catchup-commands | `setmaster` | mvdsv `sv_master.c:44-46` (loop) / `server.h:35` (`MAX_MASTERS` 8) | upstream-bug | med | `SV_SetMaster_f`'s populate loop `for (i=1; i<Cmd_Argc(); i++)` writes `master_adr[i-1]` with NO bound against `MAX_MASTERS` (8). `Cmd_Argc()` can reach `MAX_ARGS`=80 (cmd.h:172), so passing 9+ addresses writes past the 8-element `netadr_t master_adr[MAX_MASTERS]` global -- an out-of-bounds write. Privileged (server console / rcon) so low exploitability, but a genuine OOB (same class as the penfilters OOB #15). Fix: bound the loop at MAX_MASTERS. | open |
| 64 | rigor-catchup-commands | `acc_create` | mvdsv `sv_login.c:90` (validAcc) vs `:250`/`:265` (usage) + `:314` (store) | upstream-bug | low | Validator/usage/storage disagree on the login+password length cap. `validAcc` accepts length `<= MAX_LOGINNAME` (=DIGEST_SIZE*2+1=41, sv_login.c:35) at `:90`, but the usage strings advertise `MAX_LOGINNAME-1`=40 (`:250`/`:265`) and `strlcpy(...,MAX_LOGINNAME)` at `:314` truncates the stored value to 40 chars. So a 41-char login passes validation then is silently truncated on store. Cosmetic off-by-one; the L1 row was corrected to "at least 3 characters (only the first 40 are stored)". | open |
| 65 | rigor-catchup-commands | `acc_create` / `acc_list` (account-file load) | mvdsv `sv_login.c:168-169` | security | low | `SV_LoadAccounts` parses the on-disk `accounts` file with `fscanf(f, "%s", acc->login)` into a fixed `login[MAX_LOGINNAME]` buffer with no field-width limit -- an unbounded-`%s` overflow on a malformed/oversized account file. The source carries its own FIXME at `:168` (`// Is realy safe to use 'fscanf(f, "%s", s)'? FIXME!`). The accounts file is admin-controlled (written by acc_create), so not remotely-reachable, but the loader should bound the field width. Surfaced while tracing the acc_list populator. | open |
| 66 | rigor-catchup-commands | `flush` | mvdsv `zone.c:535` (Cache_Flush), reg `:592` | dead-suspect | low | `flush` (Cache_Flush, zone.c:535) is reachable, but the data cache it empties has NO live producer on the SERVERONLY build: `Cache_Alloc`/`Cache_Flush` have zero live callers tree-wide (the only other refs, fs.c:488/:595, are commented out). So `flush` always operates on an empty cache and has no observable effect -- the same dead cache subsystem as cache_print/cache_report (#29). L1 row describes the (no-op) live behavior. [cross-ref #29] | open |
| 67 | rigor-catchup-commands | `edict` / `edicts` | mvdsv `pr_edict.c:1270-1271` (PR1) vs `pr2_exec.c:67-68` (PR2) | dead-suspect | low | `edict`/`edicts` are each registered twice -- the PR1 handlers `ED_PrintEdict_f`/`ED_PrintEdicts` (pr_edict.c:1270-1271, inside PR1_Init) and the PR2 handlers `ED2_PrintEdict_f`/`ED2_PrintEdicts` (pr2_exec.c:67-68). The build defines USE_PR2 unconditionally (CMakeLists.txt:170), so `PR_Init` resolves to PR2 init and the PR1 registrations are dead. Same dead-PR1-duplicate pattern as profile (#31); the live handler is the PR2 one (cited in the L1 rows). [cross-ref #31] | open |
| 68 | rigor-catchup-commands | `kick` | mvdsv `sv_ccmds.c` (SV_Kick_f, by userid) / ktx `commands.c:794` (AdminKick) | cross-mod/L3 | -- | KTX registers its OWN `kick` command (`AdminKick`, `CF_BOTH_ADMIN`, ktx commands.c:794) carrying the in-source comment `/* FIXME: interference with ezq server kick command | CF_PARAMS */`. So on a KTX server the in-game `kick` an admin issues may resolve to the MOD's AdminKick (name-based, KTX param handling) rather than the engine `SV_Kick_f` (kicks by user-id from console/rcon). -> L3 note on the engine-vs-mod kick surfaces (mirrors the lastscores #28 dual-command pattern). | open |
| 69 | rigor-catchup-commands | `gamedir` | mvdsv `sv_ccmds.c:1684-1690` (`#ifndef SERVERONLY`) / CMakeLists.txt:169 | behavior-quirk | low | `SV_Gamedir_f`'s "you must disconnect before changing gamedir" guard (which would refuse a live gamedir switch while a client is connected) is wrapped in `#ifndef SERVERONLY`, and the dedicated server defines `SERVERONLY` (CMakeLists.txt:169) -- so the guard is COMPILED OUT: on the mvdsv dedicated server `gamedir` switches the loaded directory with no connected-client check. The L1 row deliberately omits any disconnect-first claim. Client-build-only guard; admin-facing surprise. | open |
| 70 | rigor-catchup-cvars | `skill` | mvdsv `sv_init.c:344` (Cvar_Set) vs `:345-346` (upper clamp) | upstream-bug | low | Asymmetric clamp at map spawn: `Cvar_Set(&skill, va("%d", current_skill))` (sv_init.c:344) writes the cvar string BEFORE the upper clamp `if (current_skill > 3) current_skill = 3;` (:345-346). So `skill 5` leaves the `skill` cvar reading "5" while entity spawning behaves as skill 3 (the lower bound at :342-343 IS applied before the write). The stored value diverges from the effective spawn difficulty; the L1 row was corrected to say spawning clamps to 0-3. | open |
| 71 | rigor-catchup-cvars | `password` / `spectator_password` | mvdsv `sv_main.c:1052`/`:1085` vs `sv_user.c:2674`/`:2765` | behavior-quirk | low | The literal "none" that disables a password gate is matched case-INSENSITIVELY at the connect-time gates (`strcasecmp(pwd,"none")`, sv_main.c:1052 spectator / :1085 player) but case-SENSITIVELY elsewhere (`strcmp(...,"none")` at sv_user.c:2674 / :2765, the needpass/advertiser paths). So a password set to "None"/"NONE" disables the connect check yet is treated as a real password by the other paths -- inconsistent. Minor; normalize to one comparison. | open |
| 72 | rigor-catchup-cvars | `maxfps` | mvdsv `sv_main.c:50` (CVAR_SERVERINFO), `sv_phys.c:1031`/`sv_user.c:4520` / ktx `world.c:772`,`:1580-1585` | cross-mod/L3 | -- | `maxfps` is a misnomer: the in-source comment (sv_main.c:50) says "It actually should be called maxpps (max packets per second)". It is a CVAR_SERVERINFO value classic clients read to pace their OWN send rate; the server does NOT cap its outgoing traffic by it (absent from sv_send.c). Its only engine `.value` reads are bot-physics (sv_phys.c:1031) + antilag prediction (sv_user.c:4520), each clamping a LOCAL copy to 77 when outside 20-1000 (no write-back; the published value stays unclamped). KTX additionally registers its OWN `maxfps` (world.c:772), re-clamps to `bound(50,x,1981)` (a DIFFERENT range) and writes it back via cvar_fset (world.c:1585). L1 row rewritten off the server-cap claim. -> L3 on QW packet-rate. | open |
| 73 | rigor-catchup-cvars | `extralogname` | mvdsv `sv_demo.c:1828` (easyrecord write) / ktx `logs.c:119`,`match.c:2353` | cross-mod/L3 | -- | Cross-mod handoff cvar: mvdsv's easyrecord (SV_MVDEasyRecord_f, sv_demo.c:1828) WRITES a `<sv_demoDir>/<demoname>.xml` path into extralogname but never reads it, and SV_MVDStop never resets it -- so it retains the last demo's path indefinitely. The CONSUMER and actual log-writer is KTX: ktx `logs.c:119` `log_open("%s", cvar_string("extralogname"))`, cleared at match end `match.c:2353`. The original raw L1 text ("no sv_ prefix? WTF!" dev-aside) and the first synth ("the server writes the log") both mis-attributed it; rewritten to the engine-writes / mod-consumes split. -> L3 on QW match-logging. | open |
| 74 | rigor-catchup-cvars | `qtv_sayenabled` / `hostname` / `sv_login` / `coop` | ktx `match.c:1373`,`:1192`; `vote.c:1554`,`:1134` | cross-mod/L3 | -- | KTX overwrites several engine cvars at match/mode boundaries, so an admin's manually-set value does not persist on a KTX server: qtv_sayenabled forced to 0 at every match_start (match.c:1373); hostname team-tagged during a match (match.c:1192+ saves to _k_host, then appends " (te1 vs. te2)"); sv_login driven by the private-server vote (vote.c:1554-1557: 1 when spectators allowed, else 2); coop toggled by vote (vote.c:1134). The engine stores these; KTX is the runtime governor. -> L3 on which engine cvars KTX commandeers. | open |
