# Info_key fill -- drafts for review (83 keys, 2 parked)

Generated by workflow `wxc6ur2rt` (info-key-fill), 2026-06-03. Each description is source-verified by a
fan-out subagent. Review with the operator bucket-by-bucket, then apply the approved descriptions to
`entities.description` (origin='synthesized'). Apply handoff: `2026-06-03-info-key-fill-apply-handoff.md`.

Some drafts run richer than one line where a bitmask/value enum is load-bearing (e.g. `needpass`, `wps`,
`ev`, `sp`, `svf`) -- trim at review if you want them terser.

---

## !! Parks + scope findings (read first) -- 2 keys

The KTX extractor mis-scoped these as `userinfo`, but the source shows they are **serverinfo** (read via
`iKey(world, ...)`, set via `serverinfo <key>`). Do NOT apply them as userinfo keys -- correct the scope in
L1 (or re-extract) first. This is an extractor data-quality finding worth a HANDOVER note.

- **ktx `fpd`** -- SCOPE FIX to serverinfo. fpd is a serverinfo key, not a client userinfo key. KTX reads it exclusively via iKey(world, "fpd") and sets it via `serverinfo fpd <value>`. The DB scope tag 'userinfo' appears to be a misclassification. Cannot write a client-facing userinfo description because there is no client userinfo read-site.
  - src: `src/commands.c:2017,3254,3688,3705,3721,3738; src/match.c:311,1302,2114`
- **ktx `matchtag`** -- SCOPE FIX to serverinfo. Source is unambiguous: matchtag is a serverinfo key (set via `serverinfo matchtag "..."`, read via `infokey(world,"matchtag")` throughout match.c, commands.c, client.c, stats_json.c, stats_xml.c). The DB scope label 'userinfo' contradicts the source. Describing it as a userinfo key would be incorrect; the entity classification needs correction before a description can be written.
  - Draft ready once scope corrected: "Server-side label identifying the event or match context (e.g. "EQL semifinal"), broadcast in match reports and used to gate pause permissions. Set by admins via the `usermode` command or directly as serverinfo."
  - src: `src/commands.c:4156 (comment), src/commands.c:4595 (UserMode_SetMatchTag), src/match.c:286, src/client.c:5796 (PlayerCanPause)`

---

## (a) Borrow from ezQuake/FTE cvar prose -- 16 keys

- **mvdsv `bottomcolor`** (userinfo) -- Client-set userinfo value (ezQuake cvar bottomcolor): the player's pants color index. Read by the server/mod.
  - src: `ezquake cvar bottomcolor`
- **mvdsv `topcolor`** (userinfo) -- Client-set userinfo value (ezQuake cvar topcolor): the player's shirt color index. Read by the server/mod.
  - src: `ezquake cvar topcolor`
- **mvdsv `skin`** (userinfo) -- Client-set userinfo value (ezQuake cvar skin): the player model skin name. Read by the server/mod.
  - src: `ezquake cvar skin`
- **mvdsv `rate`** (userinfo) -- Client-set userinfo value (ezQuake cvar rate): maximum bytes per second the server should send to the client. Read by the server/mod.
  - src: `ezquake cvar rate`
- **mvdsv `msg`** (userinfo) -- Client-set userinfo value (FTE cvar msg): console message filter level -- 0=pickup messages, 1=death messages, 2=critical messages, 3=chat. Read by the server/mod.
  - src: `fte cvar msg`
- **mvdsv `mtu`** (userinfo) -- Client-set userinfo value (ezQuake cvar mtu): suggested maximum network packet size; the server honors it if supported. Read by the server/mod.
  - src: `ezquake cvar mtu`
- **mvdsv `drate`** (userinfo) -- Client-set userinfo value (FTE cvar drate): target download bandwidth in bytes per second. Read by the server/mod.
  - src: `fte cvar drate`
- **mvdsv `team`** (userinfo) -- Client-set userinfo value (ezQuake cvar team): the player's team name. Read and written by the server/mod.
  - src: `ezquake cvar team`
- **mvdsv `name`** (userinfo) -- Client-set userinfo value (ezQuake cvar name): the player's display name. Read and written by the server/mod.
  - src: `ezquake cvar name`
- **mvdsv `spectator`** (userinfo) -- Client-set userinfo value: non-zero when the client connected as a spectator. Read by the server to determine the client's role.
  - src: `mvdsv src/sv_main.c:1032 CheckPasswords`
- **ktx `bottomcolor`** (userinfo) -- Client-set userinfo value (ezQuake cvar bottomcolor): the player's pants color index. Read and written by the mod.
  - src: `ezquake cvar bottomcolor`
- **ktx `topcolor`** (userinfo) -- Client-set userinfo value (ezQuake cvar topcolor): the player's shirt color index. Read and written by the mod.
  - src: `ezquake cvar topcolor`
- **ktx `skin`** (userinfo) -- Client-set userinfo value (ezQuake cvar skin): the player model skin name. Read by the mod.
  - src: `ezquake cvar skin`
- **ktx `rate`** (userinfo) -- Client-set userinfo value (ezQuake cvar rate): maximum bytes per second the server should send to the client. Read by the mod.
  - src: `ezquake cvar rate`
- **ktx `team`** (userinfo) -- Client-set userinfo value (ezQuake cvar team): the player's team name. Read and written by the mod.
  - src: `ezquake cvar team`
- **ktx `gender`** (userinfo) -- Client-set userinfo value (ezQuake cvar gender): the player's gender, used for pronoun selection in death messages. Read by the mod.
  - src: `ezquake cvar gender`

## (a') Protocol pointer (authored from read-site) -- 27 keys

- **mvdsv `*client`** (userinfo) -- Client engine name and build revision set by the client on connect (e.g. "ezQuake 12345"); read by the server to gate protocol extensions such as alpha transparency support.
  - src: `mvdsv src/sv_user.c:370 (Cmd_New_f)`
- **mvdsv `*qtv`** (userinfo) -- Presence marker injected by a QTV proxy when it connects on behalf of viewers; the server reads it at connect time to disable incompatible protocol extensions that do not work through a relay.
  - src: `mvdsv src/sv_main.c:1399 (SVC_DirectConnect)`
- **mvdsv `*skill`** (userinfo) -- Numeric skill level set by KTX when it spawns a bot; the server reads it in place of a real ping value for bot clients.
  - src: `mvdsv src/sv_main.c:476 (SV_CalcPing); set by ktx src/bot_commands.c:350`
- **mvdsv `status`** (serverinfo) -- Match phase string set by KTX throughout a game's lifecycle (values include "Standby", "Countdown", "N min left", "Forcestart"); read by the server and QTV to determine whether a game has started.
  - src: `mvdsv src/sv_demo_qtv.c:732 (QTVcmd_Say_f); set by ktx src/match.c, src/admin.c, src/world.c`
- **mvdsv `Qizmo`** (userinfo) -- Presence marker injected by the Qizmo proxy when it connects; the server reads it at connect time alongside *qtv to disable protocol extensions that cannot operate through a proxy.
  - src: `mvdsv src/sv_main.c:1398 (SVC_DirectConnect)`
- **mvdsv `emodel`** (userinfo) -- CRC checksum of the client's eyes (axe viewmodel) model file, sent by the client at spawn; the server compares it against the server's own checksum and warns if a non-standard model is detected.
  - src: `mvdsv src/sv_user.c:1010 (Cmd_Begin_f); set via setinfo by client parse (ezquake src/cl_parse.c:610)`
- **mvdsv `pmodel`** (userinfo) -- CRC checksum of the client's player model file, sent by the client at spawn; the server compares it against the server's own checksum and warns if a non-standard model is detected.
  - src: `mvdsv src/sv_user.c:1009 (Cmd_Begin_f); set via setinfo by client parse (ezquake src/cl_parse.c:609)`
- **mvdsv `chat`** (userinfo) -- Flag set by the client when the player is in the console (typing); the server reads it to suppress movement inputs while chat is active, allowing only the jump button.
  - src: `mvdsv src/sv_user.c:4712 (SV_ExecuteClientMessage, under CHAT_ICON_EXPERIMENTAL)`
- **mvdsv `dupe`** (userinfo) -- Number of duplicate server-to-client packets the client wants (0-3); set by the client to improve reliability on lossy connections, read by the server to configure the channel duplication count.
  - src: `mvdsv src/sv_main.c:3841 (SV_ExtractFromUserinfo)`
- **mvdsv `sp`** (userinfo) -- Bitmask set by the client that controls which game messages a spectator receives while tracking a player: bit 1 enables centerprint, bit 2 enables sprint, bit 4 enables stuffcmd forwarding.
  - src: `mvdsv src/sv_main.c:3852 (SV_ExtractFromUserinfo)`
- **mvdsv `dev`** (userinfo) -- Debug flag (value "1") a client sets to receive verbose server-side weapon-script diagnostics; when set the server prints best-weapon selection and firing-state traces back to that client.
  - src: `mvdsv src/sv_user.c:3996 (SV_ExecuteServerSideWeaponForgetOrder)`
- **mvdsv `svf`** (userinfo) -- Client preference bitmask for spectator-on-full behaviour: bit 1 (SVF_SPEC_ONFULL) requests automatic spectator fallback when the server is full; bit 2 (SVF_NO_SPEC_ONFULL) opts out of that fallback.
  - src: `mvdsv src/sv_main.c:1354-1358 (SVC_DirectConnect); flag definitions in src/server.h:717-719`
- **mvdsv `address`** (userinfo) -- Network address of the QTV relay that is opening a stream connection to this server; read at stream-init time and stored with the stream record for identification.
  - src: `mvdsv src/sv_demo_qtv.c:46 (SV_InitStream)`
- **mvdsv `streamid`** (userinfo) -- Numeric stream ID supplied by a QTV relay when connecting; used to associate the incoming stream connection with a specific QTV stream slot on this server.
  - src: `mvdsv src/sv_demo_qtv.c:47 (SV_InitStream)`
- **mvdsv `qul`** (userinfo) -- Client-set opt-in flag (value "1") that subscribes the client to QTV user-list notifications; when set, the server pushes //qul messages whenever a QTV viewer joins, leaves, or changes.
  - src: `mvdsv src/sv_demo_qtv.c:934 (QTVsv_EventToClients), src/sv_main.c:1494, src/sv_user.c:2480`
- **ktx `*cheats`** (userinfo) -- Server-published flag (value "ON" when cheats are active) set by mvdsv when sv_cheats is enabled; KTX reads it from the world/serverinfo string to gate cheat commands such as "giveme".
  - src: `ktx src/commands.c:8951 (giveme); set by mvdsv src/sv_init.c:350, src/sv_ccmds.c:1825 into svs.info`
- **ktx `*client`** (userinfo) -- Client identity string written into userinfo by the client on connect (ezQuake writes "ezQuake NNNN" where NNNN is the build revision); KTX reads it to detect ezQuake and extract its version number.
  - src: `ktx src/g_main.c:621 (check_ezquake); written by ezquake src/cl_main.c:1990`
- **ktx `*state`** (userinfo) -- Server-computed per-client connection state (free / zombie / preconnected / connected / spawned); KTX reads it to filter the klist output and to locate reconnecting players by userid.
  - src: `ktx src/commands.c:5143 (klist), src/g_utils.c:1506; computed by mvdsv src/pr2_cmds.c:1632`
- **ktx `*userid`** (userinfo) -- Server-assigned numeric ID that uniquely identifies a client for the duration of their session; KTX reads it to look up players by slot number and to display in the klist.
  - src: `ktx src/commands.c:5163 (klist), src/g_utils.c:1507, src/race.c:160; computed by mvdsv src/pr2_cmds.c:1624`
- **ktx `*version`** (userinfo) -- Server version string published in serverinfo by mvdsv on startup (e.g. "mvdsv 0.35"); KTX reads it from the world entity to detect whether the hosting server is FTE and enable FTE-specific features.
  - src: `ktx src/g_main.c:148 (vmMain); set by mvdsv src/sv_main.c:3684 into svs.info`
- **ktx `ip`** (userinfo) -- Client's network address, computed by the server from the active connection; KTX reads it for RCON/admin audit logging in chat messages.
  - src: `ktx src/g_cmd.c:402 (ClientSay); computed by mvdsv src/pr2_cmds.c:1610`
- **ktx `login`** (userinfo) -- Username string set by the server after a client successfully authenticates via the mvdsv login system; KTX reads it to include in match stats JSON and to check whether a player is logged in for vote/admin purposes.
  - src: `ktx src/stats_json.c:346 (json_player_detail), src/vote.c:1607; set by mvdsv src/sv_login.c:611`
- **ktx `nocolors`** (userinfo) -- Client-set flag indicating the client does not support colored text; when present with any value, KTX sends plain uncolored text in say/chat messages to that client.
  - src: `ktx src/g_cmd.c:284 (isSupport_ColoredText)`
- **ktx `ping`** (userinfo) -- Client's current round-trip time in milliseconds, computed by the server; KTX reads it when building the match stats JSON output at end-of-game.
  - src: `ktx src/stats_json.c:345 (json_player_detail); computed by mvdsv src/pr2_cmds.c:1616`
- **ktx `Qizmo`** (userinfo) -- Marker injected by the Qizmo proxy into userinfo when a client connects through it; KTX and mvdsv detect it to use Qizmo-specific disconnect/reconnect command sequences instead of the standard ones.
  - src: `ktx src/admin.c:963 (do_force_spec), src/match.c:651; mvdsv src/sv_main.c:1398`
- **ktx `tp_need_weapon`** (userinfo) -- Client-supplied bitmask (set via the ezQuake tp_need_weapon cvar or setinfo) listing which weapons should count as "needed" for teamplay need-weapon messages; KTX reads it to decide which pickups trigger a tp_msgneedweapon report.
  - src: `ktx src/teamplay.c:602 (TeamplayNeedFlags); cvar registered in ezquake src/teamplay.c:131`
- **ktx `tptook`** (userinfo) -- Client-supplied bitmask (set via setinfo) controlling which item-pickup events should fire a teamplay "took" message; if absent or zero KTX reports all item types, otherwise only the flagged item types trigger a report.
  - src: `ktx src/teamplay.c:106 (TeamplayEventItemTaken)`

## (b) mvdsv synthesis (server-produced keys) -- 20 keys

- **mvdsv `*admin`** (serverinfo) -- Publishes the server administrator's contact details (name and/or email) so players and tools can see who runs the server. The server fills it from the admin-info setting and drops it when that setting is left blank.
  - src: `src/sv_main.c:3890 OnChange_admininfo_var (set); mirrors sv_admininfo cvar; removed via Info_RemoveKey when value empty`
- **mvdsv `*auth`** (userinfo) -- Stamps a player's verified login name on their connection after they successfully authenticate with the central account service, so everyone on the server can see a confirmed identity rather than just a chosen nickname. It is cleared when the player logs out.
  - src: `src/central.c:217-221 Auth_ProcessLoginAttempt (set on login); src/sv_login.c:644-646 SV_Logout (cleared); whitelisted for client propagation at src/sv_user.c:2292`
- **mvdsv `*cheats`** (serverinfo) -- Set to ON when the server was started with cheats enabled, telling connected clients that cheat commands are allowed in this session. The server re-publishes it on each new map.
  - src: `src/sv_ccmds.c:1825 SV_InitOperatorCommands (set when SV_CommandLineEnableCheats); reasserted at src/sv_init.c:350`
- **mvdsv `*csprogs`** (serverinfo) -- Publishes the checksum of the server's client-side game code (CSQC progs) so a connecting client can confirm it has the exact matching file. It is blank when the server runs no client-side progs.
  - src: `src/sv_init.c:221 SV_LoadCSQC (set to checksum of sv_csqc_progname file; cleared at :229)`
- **mvdsv `*csprogsname`** (serverinfo) -- Names the client-side game-code file (CSQC progs, default csprogs.dat) the server is running, telling each connecting client which file to load. It is blank when the server runs no client-side progs.
  - src: `src/sv_init.c:224 SV_LoadCSQC (set from sv_csqc_progname cvar; cleared at :231)`
- **mvdsv `*csprogssize`** (serverinfo) -- Publishes the byte size of the server's client-side game module (CSQC) so connecting clients can confirm they have the matching file. It is blank on servers that don't use a custom client module.
  - src: `src/sv_init.c:223 (SV_LoadCSQC); blank case src/sv_init.c:230`
- **mvdsv `*flag`** (userinfo) -- A login badge the server stamps onto a player after a successful account login, supplied by the central login service and shown to other players. It is cleared when the player logs out.
  - src: `src/central.c:234 (Auth_ProcessLoginAttempt); logout-clear src/sv_login.c:645 (SV_Logout); broadcast list src/sv_user.c:2293 (shortinfotbl)`
- **mvdsv `*gamedir`** (serverinfo) -- Advertises which game directory the server is running from so clients fetch maps, models, and other content from the right folder. Admins can override it with the sv_gamedir console command.
  - src: `src/fs.c:561 (FS_InitEx, from -game/+gamedir); sv_gamedir command src/sv_ccmds.c:1583 (SV_Gamedir)`
- **mvdsv `*progs`** (serverinfo) -- Publishes a checksum of the server's loaded game logic so clients can tell which mod the server is running and detect mismatches. When the mod is a native compiled module instead, it shows the module file extension rather than a checksum.
  - src: `src/pr_edict.c:1187 (PR1_LoadProgs, QuakeC CRC); QVM CRC src/vm.c:700 (VM_LoadQVM); native extension src/vm.c:1225 (VM_LoadNative)`
- **mvdsv `*qvm`** (serverinfo) -- Signals that the server is running a native compiled game module rather than standard QuakeC, with its value set to the module's file extension. It is absent on servers using ordinary game logic.
  - src: `src/vm.c:1224 (VM_LoadNative)`
- **mvdsv `*spectator`** (userinfo) -- Server-set flag marking a connected client as a spectator rather than a participating player. The server stamps it when someone joins as a spectator, is moved to spectator because the server is full, or is auto-spectated for failing the ping requirement.
  - src: `src/sv_main.c:1066 (CheckPasswords sets on spectator-password join); also sv_main.c:1364 (forced-spectator on full server), sv_user.c:466/2796 (auto-spectate / SpawnSpectator), sv_user.c:2711 (removed)`
- **mvdsv `*version`** (serverinfo) -- Identifies the server software and version a client is connected to, published by the server at startup (for example "MVDSV 1.20-dev"). Clients and server browsers read it to know which server build is running.
  - src: `src/sv_main.c:3684 (SV_InitLocal, value = SERVER_NAME " " SERVER_VERSION from version.h)`
- **mvdsv `*VIP`** (userinfo) -- Server-set value recording a connected client's VIP privilege level, derived from a matching VIP password or an allowlisted IP. A non-zero level grants reserved-slot access and lets the player connect even when the server is otherwise full.
  - src: `src/sv_main.c:1437 (SVC_DirectConnect stamps level at connect); refreshed sv_user.c:295 (SV_PreSpawn); authoritative copy read from cl->vip in pr2_cmds.c:1628`
- **mvdsv `*z_ext`** (serverinfo) -- Advertises which optional ZQuake protocol extensions the server supports, as a numeric capability value clients read when connecting. The overlap between what the server offers here and what a client supports decides which extended features (such as server-stamped time) are used.
  - src: `src/sv_main.c:3685 (SV_InitLocal, value = SERVER_EXTENSIONS from qwprot/src/protocol.h:114); negotiated overlap consumed e.g. sv_send.c:816`
- **mvdsv `*z_ext`** (userinfo) -- Carries the set of optional ZQuake protocol extensions a connecting client supports, as a numeric capability value. The server reads it once at connect time to record which extended features that client can use, then discards the key.
  - src: `src/sv_main.c:1425 (SVC_DirectConnect reads into newcl->extensions), sv_main.c:1426 (removed immediately after)`
- **mvdsv `epoch`** (serverinfo) -- Records the wall-clock start time of an MVD recording as a Unix timestamp. The server stamps it into the demo's server info when recording begins, so tools can show when the demo was captured.
  - src: `src/sv_demo.c:1309 (SV_MVD_SendInitialGamestate)`
- **mvdsv `map`** (serverinfo) -- Names the map the server is currently running. The server publishes it whenever a level loads, so clients and server browsers can display the active map.
  - src: `src/sv_init.c:659 (SV_SpawnServer)`
- **mvdsv `needpass`** (serverinfo) -- Tells connecting clients which passwords the server requires: 1 for the play password, 2 for the spectator password, 4 for the VIP password, added together when more than one applies. Left empty when no password is needed.
  - src: `src/sv_main.c:3243-3245 (SV_CheckVars)`
- **mvdsv `password`** (userinfo) -- The password a player sends to join a protected server. The server checks it against the server's configured password, then strips it from the player's info so it is never shared with others.
  - src: `src/sv_main.c:1044,1075,1098 (CheckPasswords)`
- **mvdsv `w_rank`** (userinfo) -- Holds the player's effective weapon-switch order as worked out by the server's server-side weapon logic, encoded as a string of weapon ranks. The server stamps it onto the player so game mods can act on the chosen best weapon.
  - src: `src/sv_user.c:4214 (SV_UserSetWeaponRank)`

## (c) KTX rewrite (gameplay codes) -- 18 keys

- **ktx `di`** (userinfo) -- Enables damage indicator callbacks during pre-match standby: set to 1 to receive hit notifications only when damaging other players, or 2 to also receive notifications for self-damage. When active, KTX stuffcmds a `//ktx di` callback to the attacker's client after each hit.
  - src: `src/combat.c:827 (T_Damage)`
- **ktx `ev`** (userinfo) -- Bitmask that selects which server events trigger a client-side alias callback. Bit 1 = on_connect, bit 4 = on_matchstart, bit 8 = on_matchend, bit 16 = on_matchbreak, bit 128 = on_admin, bit 256 = on_unadmin. KTX stuffcmds the matching alias name to the client when each event fires.
  - src: `src/g_utils.c:2376,2459,2476,2493,2510,2520 (on_connect, on_match_start, on_match_end, on_match_break); src/g_userinfo.c:55 (info_ev_update)`
- **ktx `fs`** (userinfo) -- Sets the default force-spec target for an admin: use a player name to target one player, or `*` to target all not-ready players. When an admin issues the `force_spec` command without an argument, KTX falls back to this value.
  - src: `src/admin.c:987 (force_spec)`
- **ktx `ti`** (userinfo) -- Controls whether KTX sends team overlay data to this client. Set to -1 to opt out entirely; set to 0 (default) to let KTX auto-detect based on client type (ezQuake clients receive it automatically); set to a positive value to force team info on for non-ezQuake clients.
  - src: `src/client.c:4728 (CheckTeamStatus)`
- **ktx `ln`** (userinfo) -- Vertical position offset for the centerprint nick display shown by +shownick. A positive value inserts that many blank lines before the name; a negative value inserts blank lines after it.
  - src: `src/commands.c:4112 (ShowNick)`
- **ktx `lra`** (userinfo) -- Vertical position offset for the arena round-end stats centerprint (armor and health summary). A positive value inserts blank lines before the stats block; a negative value inserts blank lines after it.
  - src: `src/arena.c:497 (PrintStats)`
- **ktx `ls`** (userinfo) -- Vertical position offset for the +scores centerprint overlay. A positive value inserts blank lines before the score block; a negative value inserts blank lines after it.
  - src: `src/client.c:3360 (Print_Scores)`
- **ktx `lw`** (userinfo) -- Vertical position offset for the +wp_stats weapon accuracy centerprint overlay. A positive value inserts blank lines before the stats block; a negative value inserts blank lines after it.
  - src: `src/client.c:3166 (Print_Wp_Stats)`
- **ktx `lw_x`** (userinfo) -- Horizontal indent for the +wp_stats weapon accuracy centerprint overlay. A positive value prepends that many spaces before the stats content; a negative value appends spaces after it.
  - src: `src/client.c:3166 (Print_Wp_Stats)`
- **ktx `mi`** (userinfo) -- Spectator moreinfo subscription level (0-4). Controls which item pickup notifications the spectator receives while watching: 0 = off, higher levels add powerups, armors, megahealth, and weapon pickups progressively. Cycle with the `moreinfo` command.
  - src: `src/commands.c:7123 (mi_print), src/commands.c:7162 (moreinfo)`
- **ktx `nrb`** (userinfo) -- Non-random bullets toggle. When set to 1, shotgun and super-shotgun pellet spread becomes deterministic (fixed pattern) instead of random. Only takes effect outside of a match in progress.
  - src: `src/weapons.c:551 (FireBullets)`
- **ktx `ktpl`** (userinfo) -- KTPro compatibility mode. When set to 1, the weapon index values used by `lw`, `ls`, and `ln` are shifted by +12 to match KTPro's weapon numbering, allowing configs written for KTPro to work without remapping.
  - src: `src/client.c:3165 (Print_Wp_Stats), src/client.c:3361 (Print_Scores), src/commands.c:4113`
- **ktx `runes`** (userinfo) -- CTF rune HUD opt-in. When set to 1, KTX stuffs the name of the rune the player currently carries (res, str, hst, or rgn) into the client cvar `rune`, enabling client-side HUD scripts to display it.
  - src: `src/runes.c:480, src/runes.c:491 (CheckStuffRune); src/g_userinfo.c:81 (comment)`
- **ktx `railcolor`** (userinfo) -- Sets the color of your rail trail in instagib mode. Accepts an integer color index; the value is passed directly to the trail renderer each time you fire.
  - src: `src/weapons.c:533 (FireInstaBullet), src/weapons.c:743`
- **ktx `pbspeed`** (userinfo) -- Controls demo playback speed as a percentage (0-200). A value of 100 is normal speed; 50 is half speed; 200 is double speed. 0 also plays at normal speed.
  - src: `src/commands.c:8195 (mv_playback)`
- **ktx `wps`** (userinfo) -- Bitmask selecting which weapons appear in your end-of-match accuracy report. Each bit corresponds to one weapon (axe=1, SG=2, SSG=4, NG=8, SNG=16, GL=32, RL=64, LG=128). Defaults to GL+RL+LG when unset.
  - src: `src/client.c:3167 (Print_Wp_Stats)`
- **ktx `wpsx`** (userinfo) -- Opt-in flag that enables live weapon accuracy updates while spectating. When non-zero, KTX sends per-shot hit/attack counts to the spectator client as play progresses.
  - src: `src/client.c:4378 (WS_OnSpecPovChange), src/client.c:4427 (WS_CheckUpdate)`
- **ktx `w_rank`** (userinfo) -- Sets a custom weapon priority order for auto-switching on pickup. Value is a string of digit characters 1-8 (axe through LG) from highest to lowest preference; KTX switches to the best available weapon in this order instead of the default.
  - src: `src/weapons.c:1970 (W_BestWeapon), src/items.c:768 (DoWeaponChange)`

