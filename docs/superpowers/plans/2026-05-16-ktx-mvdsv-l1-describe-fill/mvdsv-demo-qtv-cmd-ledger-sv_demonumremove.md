# Ledger -- mvdsv:sv_demonumremove (command)

```json
{ "project":"mvdsv","knob":"sv_demonumremove","type":"command","description":"Deletes a recorded server-side demo by its position number in the demo list (the number shown by the demo listing / lastscores commands), rather than by filename. The demo's saved info/scores text file is removed along with it.\n\nsv_demonumremove <number> = delete the demo at that list position. Counting starts at 1 for the oldest demo.\n\nSet by: server console / rcon.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"Handler SV_MVDRemoveNum_f at src/sv_demo_misc.c:652; registered src/sv_demo.c:1948 via Cmd_AddCommand (server command table; not present in the sv_user.c ucmds[] client table, so server console / rcon only). CLAUSE 'deletes by position number not name': handler requires exactly one arg (Cmd_Argc()!=2 -> usage 'rmdemonum <#>', sv_demo_misc.c:658-662); parses it as int via Q_atoi (line 665) and resolves the file through name=SV_MVDNum(num) (line 671), in contrast to the sibling SV_MVDRemove_f (sv_demo_misc.c:562) which takes a filename string (strlcpy(name, Cmd_Argv(1)...) line 619). CLAUSE 'number = position in the demo list, counting from 1 = oldest': SV_MVDNum (sv_demo_misc.c:435-497) lists the demo dir sorted by date (Sys_listdir(... SORT_BY_DATE), line 474) and for a plain positive num does --num (line 484) then walks num entries (lines 490-494), so num=1 selects the first (oldest) listed file; the same SORT_BY_DATE listing is what lastscores/laststats number for the user (Con_Printf('%i. ', ++i) at sv_demo_misc.c:1014). The high-bit forms (0x00800000 / 0xFF000000, lines 444/477) are the internal 'cmd dl . ..' dotted-relative addressing and are not surfaced in the user doc. CLAUSE 'companion info/scores text file removed along with it': after Sys_remove(path) on the .mvd (line 679), the handler calls Sys_remove(SV_MVDName2Txt(path)) (line 697); SV_MVDName2Txt (sv_demo_misc.c:500-554) returns the same base name with the extension replaced by '.txt', i.e. the demo's sidecar info file. On an invalid number SV_MVDNum returns NULL and the handler prints 'invalid demo num' (lines 702-703). D20: file:line + helper names kept in reasoning; user doc states observable effect only. F-MV1: KTX src/commands.c has NO registration of sv_demonumremove (only an unrelated stuffcmd of 'cmd demoinfo' at commands.c:7991, which targets a client ucmd, not this command); no KTX override.","description_proposed":null }
```

## Per-clause enforce-trace

| # | Clause | Enforcing file:line | Verbatim snippet | Verdict |
|---|--------|--------------------|------------------|---------|
| 1 | Resolves demo by NUMBER (int arg), not filename | src/sv_demo_misc.c:665, 671 | `if ((num = Q_atoi(val)) == 0 && val[0] != '0')` ; `name = SV_MVDNum(num);` | MATCH |
| 2 | Contrast: sibling sv_demoremove takes a filename string | src/sv_demo_misc.c:619 | `strlcpy(name, Cmd_Argv(1), MAX_DEMO_NAME);` | MATCH |
| 3 | Exactly one arg required; usage `rmdemonum <#>` | src/sv_demo_misc.c:658-660 | `if (Cmd_Argc() != 2) { Con_Printf("rmdemonum <#>\n"); return; }` | MATCH |
| 4 | Number = position in date-sorted demo list | src/sv_demo_misc.c:474, 484, 490-494 | `dir = Sys_listdir(... sv_demoRegexp.string, SORT_BY_DATE);` ; `--num;` ; `while (list->name[0] && num) { list++; num--; }` | MATCH |
| 5 | Counting starts at 1 = oldest (same numbering lastscores prints) | src/sv_demo_misc.c:1009-1014 | `for (i = dir.numfiles - demos; i < dir.numfiles; )` ; `Con_Printf("%i. ", ++i);` | MATCH |
| 6 | Deletes the .mvd file | src/sv_demo_misc.c:678-679 | `snprintf(path, ... sv_demoDir.string, name);` ; `if (!Sys_remove(path))` | MATCH |
| 7 | Also removes the sidecar info/scores .txt | src/sv_demo_misc.c:697 + 500-553 | `Sys_remove(SV_MVDName2Txt(path));` ; (SV_MVDName2Txt replaces ext with `.txt`) `s[len++]='.'; s[len++]='t'; s[len++]='x'; s[len++]='t';` | MATCH |
| 8 | Set by: server console / rcon (not a client ucmd) | src/sv_demo.c:1948 ; src/sv_user.c (absent) | `Cmd_AddCommand ("sv_demonumremove", SV_MVDRemoveNum_f);` ; (no `SV_MVDRemoveNum_f` entry in `ucmds[]`) | MATCH |
| 9 | Invalid number -> "invalid demo num", no deletion | src/sv_demo_misc.c:702-703 | `else Con_Printf("invalid demo num\n");` | MATCH |

V-pass self-classification: TRACED-CLEAN. F-MV1: no KTX registration (commands.c:7991 is a `cmd demoinfo` stuffcmd to a client, not an override of this command).
