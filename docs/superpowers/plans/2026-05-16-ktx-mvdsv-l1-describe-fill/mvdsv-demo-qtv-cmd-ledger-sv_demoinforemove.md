# Ledger -- mvdsv:sv_demoinforemove (command)

```json
{ "project":"mvdsv","knob":"sv_demoinforemove","type":"command","description":"Deletes a demo's companion info file (the plain-text file stored alongside a recorded demo that holds its notes, scores, or other metadata). Only the info file is removed; the demo recording itself is left in place.\n\nsv_demoinforemove <number> = delete the info file of the demo at that list position.\nsv_demoinforemove * = delete the info file of the demo currently being recorded.\n\nSet by: server console / rcon.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"Handler SV_MVDInfoRemove_f at src/sv_demo_misc.c:871; registered src/sv_demo.c:1950 via Cmd_AddCommand (server command table; absent from sv_user.c ucmds[] -> server console / rcon only). CLAUSE 'deletes the companion info file': the path is the demo's '.txt' sidecar -- for a numeric arg via SV_MVDTxTNum(Q_atoi(Cmd_Argv(1))) (line 895) = SV_MVDName2Txt(SV_MVDNum(num)) (sv_demo_misc.c:556-558), where SV_MVDName2Txt replaces the demo's extension with '.txt' (sv_demo_misc.c:546-550); the file is then deleted with Sys_remove(path) (line 906). CLAUSE 'only the info file, demo left in place': the handler operates ONLY on the .txt path -- there is no Sys_remove on the .mvd anywhere in SV_MVDInfoRemove_f (contrast SV_MVDRemoveNum_f which removes both the .mvd at line 679 and the .txt at line 697). CLAUSE 'number = list position / * = currently recording': numeric path resolves through SV_MVDNum's date-sorted listing (same numbering as the other demo-num commands); the '*' branch (lines 881-892) targets the currently recording demo via demo.dest->name and errors 'Not recording demo!' if !sv.mvdrecording or !demo.dest (lines 883-887). CLAUSE 'requires an argument': Cmd_Argc()<2 -> usage 'demoInfoRemove <demonum>' (lines 875-879). Outcome messages: 'file %s removed' on success, 'failed to remove the file %s' on failure (lines 906-909); invalid number -> 'invalid demo num' (lines 897-901). D20: file:line + helper names kept in reasoning; user doc states observable effect only. F-MV1: KTX src/commands.c has NO registration of sv_demoinforemove (the only related token is a 'cmd demoinfo' stuffcmd at commands.c:7991, a client ucmd, unrelated); no KTX override.","description_proposed":null }
```

## Per-clause enforce-trace

| # | Clause | Enforcing file:line | Verbatim snippet | Verdict |
|---|--------|--------------------|------------------|---------|
| 1 | Target is the demo's `.txt` sidecar (companion info file) | src/sv_demo_misc.c:895, 556-558, 546-550 | `name = SV_MVDTxTNum(Q_atoi(Cmd_Argv(1)));` ; `return SV_MVDName2Txt (SV_MVDNum(num));` ; `s[len++]='.'; s[len++]='t'; s[len++]='x'; s[len++]='t';` | MATCH |
| 2 | Deletes that info file | src/sv_demo_misc.c:906 | `if (Sys_remove(path))` (path = the .txt) | MATCH |
| 3 | Only the info file is removed; the .mvd recording is NOT touched | src/sv_demo_misc.c:871-913 (whole body) | (no `Sys_remove` on the `.mvd` path anywhere in the function; only the `.txt` path is removed at line 906) | MATCH |
| 4 | Numeric arg = list position (date-sorted) | src/sv_demo_misc.c:895 -> 556-558 -> 474/484/490-494 | `SV_MVDTxTNum(Q_atoi(Cmd_Argv(1)))` -> `SV_MVDName2Txt(SV_MVDNum(num))` -> `Sys_listdir(... SORT_BY_DATE)` ; `--num;` | MATCH |
| 5 | `*` = currently-recording demo; errors if not recording | src/sv_demo_misc.c:881-887, 891 | `if (!strcmp(Cmd_Argv(1), "*")) { if (!sv.mvdrecording || !demo.dest) { Con_Printf("Not recording demo!\n"); return; }` ; `snprintf(path, ... SV_MVDName2Txt(demo.dest->name));` | MATCH |
| 6 | Requires an argument (Argc<2 -> usage) | src/sv_demo_misc.c:875-877 | `if (Cmd_Argc() < 2) { Con_Printf("usage:demoInfoRemove <demonum>\n...");` | MATCH |
| 7 | Set by: server console / rcon (not a client ucmd) | src/sv_demo.c:1950 ; src/sv_user.c (absent) | `Cmd_AddCommand ("sv_demoinforemove",SV_MVDInfoRemove_f);` ; (no `SV_MVDInfoRemove_f` in `ucmds[]`) | MATCH |
| 8 | Invalid number -> "invalid demo num" | src/sv_demo_misc.c:897-900 | `if (!name) { Con_Printf("invalid demo num\n"); return; }` | MATCH |

V-pass self-classification: TRACED-CLEAN. Clause 3 ("demo recording left in place") is the load-bearing distinction vs sv_demonumremove and is verified by the ABSENCE of any `.mvd` removal in the handler body (negative trace over lines 871-913). F-MV1: no KTX registration.
