# Ledger -- mvdsv `sv_demolist`

```json
{ "project":"mvdsv","knob":"sv_demolist","type":"command","description":"Lists the server-side MVD demo files stored in the demo directory, oldest first. With no arguments it lists every demo; each extra argument is a plain text filter and a demo is shown only if its filename contains all of the given words. Each line shows an index number, the filename, and its size; a demo that is currently being recorded is marked with a leading '*'. During a live game, if more than 100 demos match, only the 100 most recent are shown. After the list it prints the total size of the demo directory, and (when a maximum directory size is configured) the remaining free space.\n\nsv_demolist [word] [word] ... = list demos whose filename contains every given word.\n\nSet by: server console / rcon.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"SEEDED CORRECTION (B4): an independent V-pass flagged the listing-order clause as inverted and HG2 confirmed it; this re-synthesis fixes 'newest first' -> 'oldest first' and surfaces the previously-omitted live-game >100 truncation caveat. Order re-traced independently to its enforcing lines and confirmed: src/sv_demo_misc.c:351 passes SORT_BY_DATE (==1, src/sys.h:65) to Sys_listdir, which at src/sv_sys_unix.c:189-194 dispatches SORT_BY_DATE to qsort(list, ..., Sys_compare_by_date); Sys_compare_by_date at src/sv_main.c:4190-4193 returns (int)(a->time - b->time) -- ASCENDING by file_t.time. file_t.time is a time_t (src/sys.h:44) set from buf.st_mtime in Sys_FileSizeTime at src/sv_sys_unix.c:65 (called at src/sv_sys_unix.c:178), so larger time = more recent; ascending sort therefore puts OLDEST first. The print loop iterates the sorted array front-to-back via the files[] index array (src/sv_demo_misc.c:398-406), so printed order is oldest first -- the prior 'newest first' was inverted. Truncation caveat re-traced: src/sv_demo_misc.c:398 `for (j = (GameStarted() && n > 100) ? n - 100 : 0; files[j]; j++)` -- when GameStarted() (src/sv_main.c:218) is true and the match count n exceeds 100, the loop starts at n-100, skipping the oldest matches and printing only the last 100 entries, i.e. the 100 most recent matches; outside a live game or with <=100 matches all matches print. Registered Cmd_AddCommand(\"sv_demolist\", SV_DemoList_f) at src/sv_demo.c:1944. Handler SV_DemoList_f at src/sv_demo_misc.c:425-428 is a thin wrapper calling SV_DemoList(false); the false selects plain-substring filtering (vs regex). Other clauses spot-confirmed clean (V-pass): 'lists demos from fs_gamedir/sv_demoDir filtered by sv_demoRegexp' src/sv_demo_misc.c:351; 'no args lists every demo' inner loop src/sv_demo_misc.c:360 j=1..Cmd_Argc()-1 so Cmd_Argc()==j at src/sv_demo_misc.c:391 is immediately true; 'each extra arg is plain text, shown only if filename contains all words' else branch src/sv_demo_misc.c:386-388 `if (strstr(list->name, Cmd_Argv(j)) == NULL) break;` reaching files[n++] (src/sv_demo_misc.c:393) only when the j==Cmd_Argc() guard (src/sv_demo_misc.c:391) holds; use_regex=false so the regex branch src/sv_demo_misc.c:362-385 is not taken; 'index, filename, size' src/sv_demo_misc.c:405 Con_Printf(\"%4d: %s (%dk)\", i, list[i-1].name, list[i-1].size/1024); 'recording demo marked with leading *' src/sv_demo_misc.c:402-403 `if ((d = DestByName(list[i-1].name)))` prints the '*%4d' form; 'total directory size' src/sv_demo_misc.c:415 (after adding live dest totals src/sv_demo_misc.c:408-413); 'free space only when max dir size set' src/sv_demo_misc.c:416-421 gated on `if ((int)sv_demoMaxDirSize.value)`. Set-by: registered via Cmd_AddCommand (server console command, reachable over rcon); not server-config-only and not vote. The same handler is ALSO bound in the client ucmd table under separate command names (dlist/demolist/etc.) -- those are distinct entity names, out of scope for this sv_* knob. F-MV1: no KTX registration of sv_demolist in research/repos/ktx/src/commands.c (only an unrelated `dlist` at commands.c:965). No Default line: command has no meaningful no-arg default value to state. Confidence high: every clause traced to its enforcing line; the corrected order clause and the added truncation caveat are each verified against their enforcing source lines above.","description_proposed":null }
```

## Per-clause enforce-trace

| Clause | Enforcing file:line | Verbatim snippet | Result |
|---|---|---|---|
| Registered as server console command | src/sv_demo.c:1944 | `Cmd_AddCommand ("sv_demolist", SV_DemoList_f);` | MATCH |
| Handler selects plain (non-regex) filtering | src/sv_demo_misc.c:425-428 | `void SV_DemoList_f (void) { SV_DemoList (false); }` | MATCH |
| Lists demos from fs_gamedir/sv_demoDir filtered by sv_demoRegexp | src/sv_demo_misc.c:351 | `dir = Sys_listdir(va("%s/%s", fs_gamedir, sv_demoDir.string), sv_demoRegexp.string, SORT_BY_DATE);` | MATCH |
| Oldest first (CORRECTED from "newest first") | src/sv_demo_misc.c:351 -> src/sv_sys_unix.c:192-193 -> src/sv_main.c:4192 | `..., SORT_BY_DATE);` -> `qsort(..., Sys_compare_by_date);` -> `return (int)(((file_t *)a)->time - ((file_t *)b)->time);` (ascending mtime; print loop iterates front-to-back at :398-406) | MATCH |
| Live game with >100 matches shows only 100 most recent (ADDED caveat) | src/sv_demo_misc.c:398 | `for (j = (GameStarted() && n > 100) ? n - 100 : 0; files[j]; j++)` | MATCH |
| No args -> lists every demo | src/sv_demo_misc.c:360,391 | `for (j = 1; j < Cmd_Argc(); j++)` ... `if (Cmd_Argc() == j)` | MATCH |
| Each arg = plain substring; shown only if all words present | src/sv_demo_misc.c:386-391 | `else if (strstr(list->name, Cmd_Argv(j)) == NULL) break;` ... `if (Cmd_Argc() == j) files[n++]=i;` | MATCH |
| use_regex=false -> regex branch not taken | src/sv_demo_misc.c:362 | `if (use_regex)` (false from wrapper) | MATCH |
| Each line shows index, filename, size | src/sv_demo_misc.c:405 | `Con_Printf("%4d: %s (%dk)\n", i, list[i - 1].name, list[i - 1].size / 1024);` | MATCH |
| Recording demo marked with leading '*' | src/sv_demo_misc.c:402-403 | `if ((d = DestByName(list[i - 1].name))) Con_Printf("*%4d: %s (%dk)\n", ...)` | MATCH |
| Prints total directory size | src/sv_demo_misc.c:415 | `Con_Printf("\ndirectory size: %.1fMB\n", (float)dir.size / (1024 * 1024));` | MATCH |
| Free space printed only when max dir size set | src/sv_demo_misc.c:416-421 | `if ((int)sv_demoMaxDirSize.value) { free_space = ...; Con_Printf("space available: %.1fMB\n", free_space); }` | MATCH |
| F-MV1 no KTX override | research/repos/ktx/src/commands.c | only `{ "dlist", dlist, ... }` at :965; no `sv_demolist` | MATCH (no override) |
