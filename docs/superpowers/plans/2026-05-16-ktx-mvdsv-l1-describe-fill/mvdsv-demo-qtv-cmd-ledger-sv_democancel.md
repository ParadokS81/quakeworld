# MVDSV describe-fill ledger -- `sv_democancel`

Verdict: **synthesized** (affirms and extends the existing comment "Stops recording, and removes the demo" -- substance confirmed at the callee, reshaped into D20 user-doc form).

```json
{ "project":"mvdsv","knob":"sv_democancel","type":"command","description":"Stops the server-side MVD demo currently being recorded and deletes the partial demo file (and its accompanying .txt file) instead of keeping it. Use this to abandon a recording you do not want, where the plain stop command would save it. Unless silent recording is enabled, a \"Server recording canceled, demo removed\" message is broadcast to players. If no demo is being recorded it replies \"Not recording a demo.\"\n\nSet by: server console / rcon.\n\nAlso registered as `cancel`.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"SEEDED RE-SYNTH (B4): V-pass + HG2 flagged the no-recording clause 'the command does nothing'. RE-TRACED: when not recording, the handler does NOT silently no-op -- it PRINTS. SV_MVDStop src/sv_demo.c:935-939 `if (!sv.mvdrecording){ Con_Printf (\"Not recording a demo.\\n\"); goto out; }`. Corrected to the exact phrasing the sibling stop/record descriptions already use ('If no demo is being recorded it replies \"Not recording a demo.\"') for cross-command consistency. All other clauses re-confirmed unchanged. Handler SV_MVD_Cancel_f at src/sv_demo.c:1028; namespaced name registered Cmd_AddCommand(\"sv_democancel\", SV_MVD_Cancel_f) at src/sv_demo.c:1941 (source comment src/sv_demo.c:1940 \"that how thouse commands should be called\" marks sv_demo* as the intended canonical form). Same handler as the bare alias `cancel` -> identical behavior, no name-branching. F-MV1: grep of research/repos/ktx/src/commands.c shows NO KTX registration of sv_democancel (only unrelated race_cancel / CD_RCANCEL at commands.c:1008/634); no override. Existing L1 comment \"Stops recording, and removes the demo\" is substantively CORRECT but is a terse dev one-liner not in D20 shape and not self-contained -> per the 2026-05-30 operator decision a serviceable one-liner not already in D20 shape routes to synthesize; this AFFIRMS + EXTENDS it. CALLEE-FOLLOW (B1 / dropquad lesson): SV_MVD_Cancel_f is a one-liner SV_MVDStop(2, true) (src/sv_demo.c:1030) -- the enforcing logic is in the callee SV_MVDStop, NOT the caller, so every clause is traced into SV_MVDStop and DestClose. ENFORCE-TRACE: (a) stops the recording -- reason==2 path at SV_MVDStop src/sv_demo.c:943 calls DestCloseAllFlush(true, mvdonly) (src/sv_demo.c:949) then sets sv.mvdrecording=false when no dests remain (src/sv_demo.c:951-952) -- MATCH. (b) deletes the demo file + .txt (NOT merely stops) -- DestCloseAllFlush(destroyfiles=true) -> DestClose(d, destroyfiles=true) (src/sv_demo.c:303); the true branch DestClose src/sv_demo.c:151-156 runs Sys_remove(path) on the .mvd then rewrites the extension to \"txt\" and Sys_remove again -- so the partial demo AND its .txt are removed -- MATCH. This is the distinguishing behavior vs plain stop, which calls DestCloseAllFlush(false,...) at src/sv_demo.c:986 (keeps files). (c) \"partial/incomplete\" framing -- cancel takes the reason==2 early-return branch (src/sv_demo.c:943-969) which does NOT write the EndOfDemo svc_disconnect trailer (that is only the reason==0 stop path at src/sv_demo.c:974-977), and it deletes the file, so what is discarded is an unfinished recording -- MATCH. (d) broadcast message gated on sv_silentrecord -- if(!sv_silentrecord.value) SV_BroadcastPrintf(PRINT_CHAT, \"Server recording canceled, demo removed\\n\") at src/sv_demo.c:960-961 -- MATCH. (e) replies \"Not recording a demo.\" when not recording (SEEDED CORRECTION) -- if(!sv.mvdrecording){ Con_Printf(\"Not recording a demo.\\n\"); goto out; } at src/sv_demo.c:935-939 -- MATCH. (f) Set-by server console / rcon -- registered via plain Cmd_AddCommand (src/sv_demo.c:1941); rcon path validates then runs Cmd_ExecuteString (src/sv_main.c:1799,1828) and the rcon blocklist (src/sv_main.c:1754-1765) does not include sv_democancel, so it is reachable from console and rcon -- MATCH. INTENTIONALLY OMITTED from user doc (internal/jargon, D20): Cvar_SetROM(&serverdemo, \"\") clears the current-demo serverinfo (src/sv_demo.c:964); the reason==3/4 (QTV-disconnect / serious-error) paths share SV_MVDStop but are not reachable from this command (it always passes reason 2). No Default line: command, no no-arg default value.","description_proposed":null }
```

## Per-clause enforce-trace

| clause | file:line | snippet | MATCH/MISMATCH |
|---|---|---|---|
| handler identity | src/sv_demo.c:1028 | `void SV_MVD_Cancel_f (void)` | MATCH |
| caller passes reason=2, destroy=true | src/sv_demo.c:1030 | `SV_MVDStop(2, true);` | MATCH |
| namespaced registration (canonical) | src/sv_demo.c:1940-1941 | `// that how thouse commands should be called.` ... `Cmd_AddCommand ("sv_democancel", SV_MVD_Cancel_f);` | MATCH |
| stops the recording | src/sv_demo.c:943,949,951-952 | `if (reason == 2 ...)` ... `DestCloseAllFlush(true, mvdonly);` ... `if (!demo.dest) sv.mvdrecording = false;` | MATCH |
| deletes the .mvd demo file | src/sv_demo.c:303,151,153-154 | `DestClose(d, destroyfiles);` ... `if (destroyfiles){ snprintf(path, ..., "%s/%s/%s", fs_gamedir, d->path, d->name); Sys_remove(path);` | MATCH |
| deletes the companion .txt | src/sv_demo.c:155-156 | `strlcpy(path + strlen(path) - 3, "txt", ...); Sys_remove(path);` | MATCH |
| discards an unfinished demo (no EndOfDemo trailer) | src/sv_demo.c:943-969 vs 974-977 | reason==2 branch returns before `MVD_MSG_WriteString ("EndOfDemo");` | MATCH |
| broadcast gated on sv_silentrecord | src/sv_demo.c:960-961 | `if ( !sv_silentrecord.value ) SV_BroadcastPrintf (PRINT_CHAT, "Server recording canceled, demo removed\n");` | MATCH |
| **SEEDED CORRECTION** -- replies "Not recording a demo." when not recording (was "does nothing") | src/sv_demo.c:935-939 | `if (!sv.mvdrecording){ Con_Printf ("Not recording a demo.\n"); goto out; }` | MATCH |
| contrast: plain stop keeps files | src/sv_demo.c:986 | `numclosed = DestCloseAllFlush(false, mvdonly);` | MATCH (destroyfiles=false) |
| Set-by: console / rcon | src/sv_demo.c:1941; src/sv_main.c:1799,1828,1754-1765 | `Cmd_AddCommand(...)`; `if (do_cmd) ... Cmd_ExecuteString(str);` + blocklist excludes sv_democancel | MATCH |
| F-MV1 KTX override | research/repos/ktx/src/commands.c (grep) | no `{ "sv_democancel", ... }` registration; only `race_cancel`/`CD_RCANCEL` | MATCH (no override) |
