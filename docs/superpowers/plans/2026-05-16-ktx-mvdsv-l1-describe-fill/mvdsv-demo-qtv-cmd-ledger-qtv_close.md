```json
{ "project":"mvdsv","knob":"qtv_close","type":"command","description":"Disconnects one or all QTV (QuakeTV) proxies currently attached to the server's demo stream. Usage: qtv_close <#id | all>. Give the numeric id of a connection (as shown by qtv_list) to drop that one proxy, or the word all to drop every connected proxy. The selected proxies are flagged and disconnected shortly after the command is run. Prints a not-found message if no connection has the given id, or reports the list is empty when no QTV proxies are connected.\n\nSet by: server console / rcon.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"Cold synth; no prior description. Registration: src/sv_demo_qtv.c:1520 Cmd_AddCommand(\"qtv_close\", Qtv_Close_f) -- plain Cmd_AddCommand, no access-flag wrapper, so baseline reach is server console + rcon. Handler: src/sv_demo_qtv.c:1445 Qtv_Close_f. CLAUSE 'requires one arg <#id | all>; prints usage otherwise': sv_demo_qtv.c:1451-1455 `if (Cmd_Argc() < 2 || !*Cmd_Argv(1)) { Con_Printf (\"Usage: %s <#id | all>\\n\", Cmd_Argv(0)); return; }`. CLAUSE 'reports empty when no QTV proxies connected': sv_demo_qtv.c:1457-1464 pre-scan `for (d = demo.dest; d; d = d->nextdest) if (d->desttype == DEST_STREAM) break;` then `if (!d) { Con_Printf (\"QTV list alredy empty\\n\"); return; }` (sic 'alredy' in source). CLAUSE 'numeric id targets that one proxy; the word all targets every proxy' (polarity/scope): sv_demo_qtv.c:1466-1467 `id = atoi(Cmd_Argv(1)); all = !strcasecmp(Cmd_Argv(1), \"all\");` and the selection at sv_demo_qtv.c:1475 `if (all || d->id == id)` within the DEST_STREAM filter (sv_demo_qtv.c:1472-1473 `if (d->desttype != DEST_STREAM) continue;`). The `all ||` short-circuit means passing 'all' does NOT also collide with id 0 (atoi(\"all\")==0) -- verified: when all is true the d->id==id term is never evaluated. CLAUSE 'flagged and disconnected shortly after' (drop mechanism): sv_demo_qtv.c:1476-1478 `Con_Printf (\"QTV id:%d aka %s will be dropped asap\\n\", d->id, NET_AdrToString(d->na)); d->error = true; cnt++;` -- it sets d->error = true rather than closing inline; the actual teardown happens later in the demo-write/cleanup path keyed on d->error, hence 'will be dropped asap' and the user-observable 'disconnected shortly after'. CLAUSE 'not-found message when id has no match': sv_demo_qtv.c:1482-1483 `if (!cnt) Con_Printf (\"QTV id:%d not found\\n\", id);`. DEST_STREAM == QTV destination type confirmed via the `// not qtv` comment at sv_demo_qtv.c:1473 and src/server.h:473 desttype_t enum. F-MV1: grep of research/repos/ktx/src/commands.c for a KTX { \"qtv_close\", ... } registration -> none; no KTX override. Confidence high: every material clause incl. arg parsing, polarity, and the error-flag drop mechanism maps to an enforcing line; would classify TRACED-CLEAN.","description_proposed":null }
```

## Per-clause enforce-trace

| Clause | Enforcing file:line | Verbatim snippet | Result |
|---|---|---|---|
| Registered, server-side, console/rcon baseline | src/sv_demo_qtv.c:1520 | `Cmd_AddCommand ("qtv_close", Qtv_Close_f);` | MATCH |
| Requires `<#id | all>`; prints usage otherwise | src/sv_demo_qtv.c:1451-1455 | `if (Cmd_Argc() < 2 || !*Cmd_Argv(1)) { Con_Printf ("Usage: %s <#id | all>\n", Cmd_Argv(0)); return; }` | MATCH |
| Reports empty when no QTV proxies connected | src/sv_demo_qtv.c:1457-1464 | `for (d = demo.dest; d; d = d->nextdest) if (d->desttype == DEST_STREAM) break;` ... `if (!d) { Con_Printf ("QTV list alredy empty\n"); return; }` | MATCH |
| numeric id targets one; `all` targets every (polarity) | src/sv_demo_qtv.c:1466-1467, 1475 | `id = atoi(Cmd_Argv(1)); all = !strcasecmp(Cmd_Argv(1), "all");` ... `if (all || d->id == id) {` | MATCH |
| Only QTV (DEST_STREAM) destinations considered | src/sv_demo_qtv.c:1472-1473 | `if (d->desttype != DEST_STREAM) continue; // not qtv` | MATCH |
| Selected proxies flagged + dropped shortly after | src/sv_demo_qtv.c:1476-1478 | `Con_Printf ("QTV id:%d aka %s will be dropped asap\n", ...); d->error = true; cnt++;` | MATCH |
| Not-found message when id has no match | src/sv_demo_qtv.c:1482-1483 | `if (!cnt) Con_Printf ("QTV id:%d not found\n", id);` | MATCH |
| F-MV1: no KTX override | research/repos/ktx/src/commands.c | grep for `{ "qtv_close"` -> no result | MATCH (no override) |
