```json
{ "project":"mvdsv","knob":"qtv_status","type":"command","description":"Shows an overview of the server's QTV (QuakeTV) broadcasting subsystem: whether the QTV listen socket is open (\"listen\") or not (\"invalid\"), the port it listens on, the number of QTV proxies currently streaming, and the number of streams still being set up (pending). Takes no arguments.\n\nSet by: server console / rcon.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"Cold synth; no prior description. Registration: src/sv_demo_qtv.c:1521 Cmd_AddCommand(\"qtv_status\", Qtv_Status_f) -- plain Cmd_AddCommand, no access-flag wrapper, so baseline reach is server console + rcon. Handler: src/sv_demo_qtv.c:1486 Qtv_Status_f. CLAUSE 'listen socket open (listen) vs not (invalid)' (polarity): sv_demo_qtv.c:1493 `Con_Printf (\"Listen socket  : %s\\n\", NET_GetSocket(NS_SERVER, true) == INVALID_SOCKET ? \"invalid\" : \"listen\");` -- the ternary prints 'invalid' when the socket IS INVALID_SOCKET (not open) and 'listen' otherwise (open); description polarity matches. CLAUSE 'the port it listens on': sv_demo_qtv.c:1494 `Con_Printf (\"Port           : %d\\n\", listenport);` where listenport is the QTV TCP stream port -- traced to sv_demo_qtv.c:139 `listenport = streamport;` and sv_demo_qtv.c:142 `NET_InitServer_TCP(listenport);` (streamport is the qtv_streamport cvar value used to open/close the QTV TCP socket). CLAUSE 'number of QTV proxies currently streaming': sv_demo_qtv.c:1496-1500 `for (cnt = 0, d = demo.dest; d; d = d->nextdest) if (d->desttype == DEST_STREAM) cnt++; Con_Printf (\"Streams        : %d\\n\", cnt);` -- counts DEST_STREAM (QTV) destinations on the active demo.dest list; DEST_STREAM == QTV confirmed at src/server.h:473 desttype_t enum. CLAUSE 'number of streams still being set up (pending)': sv_demo_qtv.c:1502-1505 `for (cnt = 0, p = demo.pendingdest; p; p = p->nextdest) cnt++; Con_Printf (\"Pending streams: %d\\n\", cnt);` -- counts the separate demo.pendingdest list (pending/not-yet-established streams), field at src/server.h:544 `struct mvdpendingdest_s *pendingdest;`. CLAUSE 'takes no arguments': handler body never calls Cmd_Argc/Cmd_Argv; reads only globals. F-MV1: grep of research/repos/ktx/src/commands.c for a KTX { \"qtv_status\", ... } registration -> none; no KTX override. Confidence high: every printed field maps to its enforcing line incl. the socket-check polarity; would classify TRACED-CLEAN.","description_proposed":null }
```

## Per-clause enforce-trace

| Clause | Enforcing file:line | Verbatim snippet | Result |
|---|---|---|---|
| Registered, server-side, console/rcon baseline | src/sv_demo_qtv.c:1521 | `Cmd_AddCommand ("qtv_status", Qtv_Status_f);` | MATCH |
| Listen socket open ("listen") vs not ("invalid") -- polarity | src/sv_demo_qtv.c:1493 | `Con_Printf ("Listen socket  : %s\n", NET_GetSocket(NS_SERVER, true) == INVALID_SOCKET ? "invalid" : "listen");` | MATCH |
| The QTV listen port | src/sv_demo_qtv.c:1494, 139, 142 | `Con_Printf ("Port           : %d\n", listenport);` ... `listenport = streamport;` ... `NET_InitServer_TCP(listenport);` | MATCH |
| Count of QTV proxies currently streaming | src/sv_demo_qtv.c:1496-1500 | `for (cnt = 0, d = demo.dest; d; d = d->nextdest) if (d->desttype == DEST_STREAM) cnt++; Con_Printf ("Streams        : %d\n", cnt);` | MATCH |
| DEST_STREAM == QTV destination type | src/server.h:473 | `typedef enum {DEST_NONE, DEST_FILE, DEST_BUFFEREDFILE, DEST_STREAM} desttype_t;` | MATCH |
| Count of pending (not-yet-established) streams | src/sv_demo_qtv.c:1502-1505, src/server.h:544 | `for (cnt = 0, p = demo.pendingdest; p; p = p->nextdest) cnt++; Con_Printf ("Pending streams: %d\n", cnt);` ... `struct mvdpendingdest_s *pendingdest;` | MATCH |
| Takes no arguments | src/sv_demo_qtv.c:1486-1506 | handler body has no Cmd_Argc/Cmd_Argv call; reads only globals | MATCH |
| F-MV1: no KTX override | research/repos/ktx/src/commands.c | grep for `{ "qtv_status"` -> no result | MATCH (no override) |
