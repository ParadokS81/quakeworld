# KTX D7 V-pass -- batch 03 ledger (BATCH_ID 3, bucket 2) -- FULL FRESH RE-RUN 2026-05-19

B3 read-only verification-shaped pass (decisions.md D7 Amendment 2026-05-19,
B1-B5). NOT a re-synth: rows are CLASSIFIED only; no description edited, no DB
write. Authority for the method: enforce-trace-discipline.md.

This is the full-fresh batch-03 re-run mandated by the 2026-05-19 orchestrator
resume (section "ALL 9 LANDED ... 7 verified, 03+08 re-run"). The prior
batch-03 terminal HALTED CORRECTLY at 20 rows -- it escalated Finding 1 +
Finding 2 and stopped rather than ship tainted (textbook C4). Both findings
were RESOLVED by the orchestrator before this re-run: Finding 1 (k_teamoverlay
ground-truth dispute) -- k_teamoverlay STAYS C-NEAR-MISS, NOT re-litigated, NOT
amended; Finding 2 (FAILURE-B anti-over-flag re-dispatch brief -> systematic
under-flagging) -- replaced by the discrimination-sharpened standard brief
(sharpen toward discrimination, never toward anti-over-flag). The prior 20-row
halted partial is DISCARDED: full re-run is the proven path (batch 08), no
halted-terminal bookkeeping carried forward.

- Oracle: /tmp/ktx-src-67253dc9 @ 67253dc9 == `1.47-2-g67253dc` (HARD GATE: `git describe --tags` verified byte-identical to the synthesis source).
- Population: batch 3 = 65 rows, F-V1 strided partition `((('x'||substr(md5(canonical_id),1,8))::bit(32)::bigint) % 9 + 9) % 9 = 2`. The 10 FIX knobs + 3 canary controls are excluded from the population per the verified Step-2 SQL. Full-population sanity: 9 buckets = 63/82/65/59/55/51/72/61/63 = 571 (matches F-V1 spec).
- Execution: read-only Opus general-purpose sub-agents at MAX reasoning (the D7 spec-locked dial, B1/B3). 13 waves x (5 batch rows + 1 blind injected canary); canary placed at a deterministic-shuffled slot; sub-agent never told which row is the canary; all 6 classified by the identical per-clause enforce-trace method.
- F-V2 HARD GATE 1 (canary verdict): a wave whose injected canary verdict != ground truth is REJECTED and re-dispatched; nothing recorded from it. Wave 05 false-negatived the k_teamoverlay canary as TRACED-CLEAN on attempts 1 and 2 (the deliberately-hard output-invisible "correct-by-accident" class -- the gate WORKING, not a defect in it), and RECOVERED on attempt 3 under the anti-stale + discrimination-sharpened brief. Canary rows are controls -- excluded from N and the flavour-C tally.
- F-V2 HARD GATE 2 (orchestrator re-grep): for every accepted wave the orchestrator independently re-grepped >=1 flagged row's wrong-clause enforcing line AND >=1 TRACED-CLEAN row's load-bearing clause against the oracle. A gate, not a sample. All 13 waves held.
- Canary ground truth (operator-ratified, decisions.md B-amendment; Finding 1 RESOLVED, re-confirmed by orchestrator re-grep this run): autotrack -> C-FIX (commands.c:893 CF_SPECTATOR|CF_MATCHLESS, no CF_MATCHLESS_ONLY; CF_MATCHLESS additive, gated only on the k_matchless server-mode cvar at commands.c:1078, never match_in_progress; zero match_in_progress in the AutoTrack/DoAutoTrack family). k_teamoverlay -> C-NEAR-MISS ("not in duel" has NO enforcing line on the team-info send path; the sole !isDuel() is match.c:1639, an unrelated settings-summary display string). k_yawnmode -> TRACED-CLEAN (every quantitative clause maps to an enforcing line; the over-flag control). Canary hygiene: autotrack 5/5, k_teamoverlay 4/4 (accepted waves), k_yawnmode 4/4; 0 leaks on accepted waves; 1 wave (05) required 2 re-dispatches.

## Result summary (N = 65 batch rows; canaries excluded)

TRACED-CLEAN: 55   C-NEAR-MISS: 2   C-FIX: 6   WI2-FIX: 2
flavour-C-positive: 8/65 (12.3%)   wi2: 2/65
flagged canonical_ids: ktx:command:15fav_go (C-FIX), ktx:command:dinfo (C-FIX), ktx:command:dmm4 (WI2-FIX), ktx:command:handicap (C-FIX), ktx:command:qizmo (WI2-FIX), ktx:command:socd (C-NEAR-MISS), ktx:cvar:k_clan_arena (C-NEAR-MISS), ktx:cvar:k_ctf_hookstyle (C-FIX), ktx:cvar:k_on_end_f_ruleset (C-FIX), ktx:cvar:_k_worldspawns (C-FIX)

12.3% sits within the fleet band (~14% random-probe base rate; per-batch variance 9-23.6% across the other 8 buckets; 9-bucket aggregate ~15%). The `RESULT |` lines below are the B5 machine spine: `grep '^RESULT |' | grep -vE canaries` over all 9 ledgers yields the Stage-1 index; the `###` blocks are the durable per-clause human detail.

C4: read-only, classify-only. Nothing applied; no description edited; no DB write; no L1 row mutated. B4 (seeded re-synth of the 8 flavour-C + 2 WI-2 flagged rows) is a separate operator-gated step that consumes this ledger.

---

## Wave 01 -- canary ktx:command:autotrack (slot blind), expected C-FIX, returned C-FIX -- GATE 1 PASS. GATE 2 (orchestrator re-grep): 15fav_go C-FIX confirmed (progs.h:1009-1010 favx[]!=fav[]; xfav_go reads favx[] c:5831, fav_add writes fav[] c:5614) + clean coop_nm_pu (RegisterCvarEx k_nightmare_pu_droprate "0.15" world.c:974).

RESULT | ktx:command:10fav_go | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=8 | Every behavioral clause of 10fav_go maps to an enforcing line in xfav_go; only soft spot is the prerequisite command spelled "Nfav_add" vs actual favN_add (fav10_add) -- traceable minor name-order imprecision about an adjacent command, not an un-enforced behavior of 10fav_go itself.
### ktx:command:10fav_go
- "Spectator-only command" -> src/commands.c:1088-1094 + 1106-1108 `if (spc){ if(!(cmds[icmd].cf_flags & CF_SPECTATOR)) return DO_WRONG_CLASS; } else { if(!(cmds[icmd].cf_flags & CF_PLAYER)) return DO_WRONG_CLASS; }`; entry src/commands.c:875 `{ "10fav_go", DEF(xfav_go), 10, CF_SPECTATOR, ... }` (only CF_SPECTATOR) -> MATCH
- "switches your point of view to the player saved in favourite slot 10" -> src/commands.c:5831 `pl_num = self->favx[(int)fav_num - 1];` + :5856 `stuffcmd_flags(self, STUFFCMD_IGNOREINDEMO, "track %d\n", GetUserID(p));`; arg=10 (src/commands.c:875) -> MATCH
- "Slots populated beforehand with the corresponding Nfav_add command (which stores the player you are currently tracking)" -> src/commands.c:855 `{ "fav10_add", DEF(favx_add), 10, CF_SPECTATOR, ... }` + favx_add src/commands.c:5715 `gedict_t *goal = PROG_TO_EDICT(self->s.v.goalentity);` :5732 `self->favx[(int)fav_num - 1] = diff;` (stores currently-tracked player into matching favx slot) -> MATCH (behavior correct; the helper command's real name is `fav10_add`, description's "Nfav_add" is a loose name-order paraphrase -- minor traceable imprecision, not invented behavior)
- "If slot 10 has not been set, prints 'fav go: slot 10 is not defined' and does nothing" -> src/commands.c:5833-5837 `if ((pl_num < 1) || (pl_num > MAX_CLIENTS)) { G_sprint(self, 2, "fav go: \220slot %d\221 is not defined\n", (int)fav_num); return; }` (\220/\221 are red-bracket glyphs; rendered "fav go: slot 10 is not defined") -> MATCH
- "if the saved player is no longer connected, prints 'fav go: slot 10 can't find player' and does nothing" -> src/commands.c:5842-5846 `if (p->ct != ctPlayer) { G_sprint(self, 2, "fav go: \220slot %d\221 can't find player\n", (int)fav_num); return; }` -> MATCH
- "if you are already observing that player it reports 'already observing...'" -> src/commands.c:5849-5852 `if (PROG_TO_EDICT(self->s.v.goalentity) == p) { G_sprint(self, 2, "fav go: already observing...\n"); return; }` -> MATCH
- "Otherwise it tracks that player" -> src/commands.c:5856 `stuffcmd_flags(self, STUFFCMD_IGNOREINDEMO, "track %d\n", GetUserID(p));` -> MATCH
- "One command exists per favourite slot (1fav_go through 20fav_go), differing only in which slot is recalled" -> src/commands.c:866-885 twenty entries `1fav_go`..`20fav_go`, all `DEF(xfav_go)`, arg = slot number 1..20 -> MATCH
WI-2: n/a (no default-value or access-class claim; spectator-only claim verified against CF_SPECTATOR flag + DoCommand check, not name-inferred)

RESULT | ktx:command:15fav_go | C-FIX | flavourC=1 | wi2=0 | clauses=8 | All 15fav_go behavioral clauses trace clean to xfav_go, but the parenthetical "(or fav_add into the next free slot)" is WRONG: fav_add writes the separate self->fav[] list (consumed only by fav_next), NOT the self->favx[] array that 15fav_go reads -- progs.h:1009-1010 comments make the two arrays/command-families explicitly distinct.
### ktx:command:15fav_go
- "Spectator-only command (CF_SPECTATOR)" -> src/commands.c:880 `{ "15fav_go", DEF(xfav_go), 15, CF_SPECTATOR, ... }`; enforced src/commands.c:1088-1094 / 1106-1108 -> MATCH
- "Switches your spectator view to track the player saved in favourite slot 15" -> src/commands.c:5831 `pl_num = self->favx[(int)fav_num - 1];` (arg=15, src/commands.c:880) + :5856 `stuffcmd_flags(self, STUFFCMD_IGNOREINDEMO, "track %d\n", GetUserID(p));` -> MATCH
- "The slot is filled beforehand with fav15_add" -> src/commands.c:860 `{ "fav15_add", DEF(favx_add), 15, CF_SPECTATOR, ... }` + favx_add src/commands.c:5732 `self->favx[(int)fav_num - 1] = diff;` (writes the same favx[14] that xfav_go(15) reads) -> MATCH
- "(or fav_add into the next free slot), which stores whoever you are currently tracking" -> fav_add src/commands.c:5577-5615 writes `self->fav[(int)fav_num - 1] = diff;` (:5614) into the **self->fav[]** array; xfav_go reads **self->favx[]** (:5831); include/progs.h:1009 `int favx[MAX_CLIENTS]; // here stored players number for appropriate favX_add/Xfav_go commands` vs :1010 `int fav[MAX_CLIENTS]; // here stored players number for fav_add/next_fav commands`; no fav_add->favx bridge exists -> MISMATCH(fav_add fills the separate fav[] list used only by fav_next; 15fav_go never reads it -- the clause conflates the two distinct fav/favx command families, contradicted by the adjacent progs.h comments)
- "If slot 15 is empty it prints 'slot 15 is not defined'" -> src/commands.c:5833-5837 `if ((pl_num < 1) || (pl_num > MAX_CLIENTS)) { G_sprint(self, 2, "fav go: \220slot %d\221 is not defined\n", (int)fav_num); return; }` -> MATCH
- "if the saved player is no longer present it prints 'slot 15 can't find player'" -> src/commands.c:5842-5846 `if (p->ct != ctPlayer) { G_sprint(self, 2, "fav go: \220slot %d\221 can't find player\n", (int)fav_num); return; }` -> MATCH
- "if you are already viewing that player it prints 'already observing'" -> src/commands.c:5849-5852 `if (PROG_TO_EDICT(self->s.v.goalentity) == p) { G_sprint(self, 2, "fav go: already observing...\n"); return; }` -> MATCH
- "On success it issues an internal track command to that player's user id" -> src/commands.c:5856 `stuffcmd_flags(self, STUFFCMD_IGNOREINDEMO, "track %d\n", GetUserID(p));` -> MATCH
- "Slots are 1-20 (one fixed command per slot: 1fav_go..20fav_go); this is the slot-15 instance" -> src/commands.c:866-885 entries 1..20 all `DEF(xfav_go)` arg=slot; src/commands.c:880 is the slot-15 entry -> MATCH
WI-2: n/a (no default-value or access-class claim; CF_SPECTATOR self-cited and verified against flag + DoCommand check)

RESULT | ktx:command:agree | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=3 | All clauses map to enforcing lines: !k_lastvotedmap early-return is the no-op guard, DoSelectMap carries the match-in-progress/lock/wait guards, and k_lastvotedmap is set only on a map vote/select; "selects and switches" is loosely worded but clause 3 explicitly states the change still goes through normal guards, so no overclaim.
### ktx:command:agree
- "Selects and switches to the most recently voted map without a further vote" -> agree_on_map src/commands.c:6749-6750 `// emulate as we select last voted map` `DoSelectMap(k_lastvotedmap);`; k_lastvotedmap written only at src/maps.c:472 `self->v.map = k_lastvotedmap = iMap;` (holds most recent voted/selected map); agree takes no map arg (src/commands.c:902 arg=0) -- it re-casts the caller's vote for that map without prompting a new choice -> MATCH (the eventual switch is gated by the normal vote tally / guards, which clause 3 explicitly covers)
- "No-op if no map vote has occurred" -> src/commands.c:6744-6747 `if (!k_lastvotedmap) { return; // no map voted }`; k_lastvotedmap is zero-initialized (src/globals.c:64 `int k_lastvotedmap;`) and only ever set by the maps.c:472 vote/select write -> MATCH
- "The map change still goes through the normal match-in-progress and related guards before it is applied" -> src/commands.c:6750 comment `// <- there will be all checks about match_in_progress and etc`; DoSelectMap src/maps.c:399-447 enforces wait-timer (:399), k_no_vote_map (:408), `if (match_in_progress != 2) return;` in matchless (:416-417), `else if (match_in_progress) { return; }` (:419-422), spec-not-admin (:424-427), k_lockmap (:434-440), same-map (:442-447), then vote tally `vote_check_map();` (:474) -> MATCH
WI-2: n/a (description makes no default-value or admin/player/spectator access-class claim; cmds entry is CF_PLAYER | CF_MATCHLESS but no class claim to verify)

RESULT | ktx:command:botcmd | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Every clause maps to a located enforcing line in FrogbotsCommand: std vs editor table selection via FB_OPTION_EDITOR_MODE bitmask, the std_commands[]/editor_commands[] arrays match the listed subcommands, no/unknown-subcommand both PrintAvailableCommands, and the k_fb_admin_only gate uses is_adm (value 1) / is_real_adm (value 2) exactly as described.
### ktx:command:botcmd
- "Frogbot control command: takes a subcommand and its arguments and dispatches into the frogbot command set" -> src/commands.c:1047 `{ "botcmd", FrogbotsCommand, 0, CF_BOTH | CF_MATCHLESS | CF_PARAMS, ... }`; FrogbotsCommand src/bot_commands.c:2453 `trap_CmdArgv(1, command, sizeof(command));` :2463-2470 `for (i=0; i<num_commands; ++i) if (streq(commands[i].name, command)) { commands[i].func(); return; }` -> MATCH
- "In normal mode it exposes bot-management subcommands (skill, addbot, fill, removebot, removeall, disable, health, weapon, breakondeath, togglequad, quadmultiplier, itempickupbonus, easyskillmode, debug)" -> src/bot_commands.c:2315-2330 std_commands[] = {skill, addbot, fill, removebot, removeall, debug, disable, health, weapon, breakondeath, togglequad, quadmultiplier, itempickupbonus, easyskillmode} (14 entries, set identical to the listed 14); selected src/bot_commands.c:2385-2386 `frogbot_cmd_t *commands = FrogbotOptionEnabled(FB_OPTION_EDITOR_MODE) ? editor_commands : std_commands;` -> MATCH
- "when frogbot editor mode is enabled it instead exposes the waypoint/routing editor subcommands" -> src/bot_commands.c:2386/2388 selects editor_commands when FrogbotOptionEnabled(FB_OPTION_EDITOR_MODE); FrogbotOptionEnabled src/bot_commands.c:2811-2813 `return (((int)cvar(FB_CVAR_OPTIONS)) & option);`, FB_OPTION_EDITOR_MODE=2 (include/fb_globals.h:9); editor_commands[] src/bot_commands.c:2332-2358 = addmarker/removemarker/savemarker/addpath/removepath/.../pathlist (waypoint/routing editor set) -> MATCH
- "With no recognised subcommand it prints the list of available subcommands" -> src/bot_commands.c:2446-2451 `if (trap_CmdArgc() <= 1) { PrintAvailableCommands(commands, num_commands); return; }` AND :2473-2474 `G_sprint(self, 2, "Command not known.\n"); PrintAvailableCommands(commands, num_commands);` (both no-arg and unknown-arg print the list) -> MATCH
- "Access can be restricted by the frogbot admin-only setting: value 1 limits use to admins, value 2 limits use to real server admins" -> src/bot_commands.c:2392 `float admin_rules = cvar(FB_CVAR_ADMIN_ONLY);` (FB_CVAR_ADMIN_ONLY="k_fb_admin_only", include/fb_globals.h:405); :2394-2399 `if ((admin_rules == 2) && !is_real_adm(self)) { ... "You must be a server admin..." return; }` -> is_real_adm src/admin.c:10-12 `return (p->k_admin & AF_REAL_ADMIN);`; :2400-2405 `else if (admin_rules && !is_adm(self)) { ... "You must be an admin..." return; }` -> is_adm src/admin.c:16-18 `return (is_real_adm(p) || (p->k_admin & AF_ADMIN));` (value 1+ -> admins; value 2 -> real server admins) -> MATCH
WI-2: n/a (no default-value claim made; access-class claims verified against actual is_adm/is_real_adm handler checks, not name-inferred; for the record k_fb_admin_only registered default is "0" -- src/world.c:1061 RegisterCvarEx(FB_CVAR_ADMIN_ONLY, "0") -- but the description does not state a default, so no WI-2 defect)

RESULT | ktx:command:coop_nm_pu | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Every clause maps to an enforcing line: cvar_toggle_msg flips k_nightmare_pu and G_bprints "<name> enables/disables ...", monster_death_use drops powerups while k_nightmare_pu is set, MonsterDropPowerups rolls k_nightmare_pu_droprate per death, and the match_in_progress early-return makes the toggle a no-op during a match. Droprate default 0.15 verified at the RegisterCvarEx site.
### ktx:command:coop_nm_pu
- "Toggles the k_nightmare_pu setting on or off" -> src/commands.c:1042 `{ "coop_nm_pu", ToggleNewCoopNm, 0, CF_PLAYER | CF_MATCHLESS, ... }`; ToggleNewCoopNm src/commands.c:8630 `cvar_toggle_msg(self, "k_nightmare_pu", redtext("New Nightmare mode (drops powerups)"));`; cvar_toggle_msg src/g_utils.c:2211 `i = !cvar(cvarName);` :2218 `trap_cvar_set_float(cvarName, (float) i);` -> MATCH
- "and broadcasts the new state ('<name> enables/disables New Nightmare mode (drops powerups)')" -> src/g_utils.c:2215 `G_bprint(2, "%s %s %s\n", p->netname, Enables(i), msg);` with msg=redtext("New Nightmare mode (drops powerups)") (src/commands.c:8630); Enables src/g_utils.c:1832-1834 `return (f ? "enables" : "disables");` (G_bprint = server-wide broadcast) -> MATCH
- "While enabled, monsters drop powerups when they die" -> monster_death_use src/sp_monsters.c:738-747 `if (!((int)self->s.v.flags & FL_MONSTER)) { return; }` then `if (cvar("k_nightmare_pu")) { MonsterDropPowerups(); }` (on monster death, only when k_nightmare_pu set) -> MATCH
- "(each death rolls k_nightmare_pu_droprate, default 0.15, for the drop)" -> MonsterDropPowerups src/sp_monsters.c:655-658 `if (g_random() > cvar("k_nightmare_pu_droprate")) { return; }` (per-death random roll vs droprate; fail -> no drop); registered default src/world.c:974 `RegisterCvarEx("k_nightmare_pu_droprate", "0.15");` -> MATCH
- "Has no effect while a match is in progress" -> ToggleNewCoopNm src/commands.c:8623-8628 `void ToggleNewCoopNm(void) { if (match_in_progress) { return; } ...` -- match_in_progress early-return precedes the cvar_toggle_msg call at :8630 (toggle is a no-op during a live match) -> MATCH
WI-2: result -- "default 0.15" claim verified against the REGISTERED default: src/world.c:974 `RegisterCvarEx("k_nightmare_pu_droprate", "0.15")` = exact match (not a shipped-.cfg value). No access-class claim. No WI-2 defect.

## Wave 02 -- canary ktx:cvar:k_teamoverlay (slot blind), expected C-NEAR-MISS, returned C-NEAR-MISS -- GATE 1 PASS. GATE 2 (orchestrator re-grep): dinfo C-FIX confirmed (STUFFCMD_IGNOREINDEMO (1<<0) // do not put in mvd demo g_syscalls.h:57 = exclude from recorded MVD, NOT suppress-during-playback) + clean forcemap (CF_BOTH_ADMIN commands.c:753).

RESULT | ktx:command:dinfo | C-FIX | flavourC=1 | wi2=0 | clauses=2 | "suppressed when issued from within demo playback" inverts STUFFCMD_IGNOREINDEMO, which means "do not put in mvd demo" (recording-exclusion), not playback-suppression; no is_playback guard exists.
### ktx:command:dinfo
- "forwards a 'demoinfo' command (with any arguments passed through) to the underlying MVDSV server" -> src/commands.c:7991 `stuffcmd_flags(self, STUFFCMD_IGNOREINDEMO, "cmd demoinfo %s\n", params_str(1, -1));` -> MATCH (params_str(1,-1) at src/g_utils.c:2610 returns all args from index 1; `cmd demoinfo` is stuffed to the client and relayed to the server)
- "The command is suppressed when issued from within demo playback" -> include/g_syscalls.h:57 `#define STUFFCMD_IGNOREINDEMO (   1<<0) // do not put in mvd demo` -> MISMATCH(the flag controls whether the stuffed command is WRITTEN INTO the recorded MVD demo stream, i.e. recording-exclusion; it is NOT a runtime suppression of the dinfo command when a user issues it during demo playback. dinfo() (src/commands.c:7989) and its command-table entry (src/commands.c:966) contain no is_playback / mv_is_playback() branch that suppresses the command. The clause inverts the flag's meaning -- a flavour-C clause that is also factually wrong vs its enforcing line)
WI-2: n/a (no default-value or access-class claim in description)

RESULT | ktx:command:dmm4 | WI2-FIX | flavourC=0 | wi2=1 | clauses=4 | Core dmm4 behavior (deathmatch=4, midair/instagib dmm4-only, Quad->OctaPower, timelimit=3) all traced clean; "Admin/console command" is wrong -- flags CF_PLAYER|CF_SPC_ADMIN make it an unrestricted player command.
### ktx:command:dmm4
- "switches the server to deathmatch mode 4 (sets the `deathmatch` cvar to 4 and announces the change)" -> src/commands.c:2885 `deathmatch = bound(1, (int)dmm, 5);` + :2887 `cvar_set("deathmatch", va("%d", (int)deathmatch));` + :2899 `G_bprint(2, "Deathmatch %s\n", dig3(deathmatch));` -> MATCH (table entry src/commands.c:728 `{ "dmm4", DEF(ChangeDM), 4, ... }`; dispatch src/commands.c:1135 `((void (*)(float))(cmds[icmd].f))(cmds[icmd].arg)` passes arg 4 to ChangeDM)
- "Mode 4 is the only mode in which the `k_midair` and `k_instagib` options are permitted" -> src/world.c:1760 `if (cvar("k_midair") && deathmatch != 4) cvar_fset("k_midair", 0); // midair only in dmm4` + :1765 `if (cvar("k_instagib") && deathmatch != 4) cvar_fset("k_instagib", 0); // instagib only in dmm4` -> MATCH (adjacent comments confirm dmm4-only enforcement)
- "the Quad Damage powerup is replaced by \"OctaPower\"" -> src/items.c:2341 `self->netname = deathmatch == 4 ? "OctaPower" : "Quad Damage";` -> MATCH (on the IT_QUAD super_damage item; corroborated by src/combat.c:543 `// in dmm4 quad is octa actually`)
- "entering this mode forces `timelimit` to 3 minutes" -> src/commands.c:2894 `else // if entering dmm4` :2896 `cvar_set("timelimit", "3"); // Set match length to 3 minutes` -> MATCH
WI-2: WI2-FIX -- "Admin/console command" is WRONG. src/commands.c:728 flags = CF_PLAYER | CF_SPC_ADMIN. Per dispatch src/commands.c:1088-1117: a spectator is rejected at :1093 DO_WRONG_CLASS because CF_SPECTATOR is NOT set (CF_SPC_ADMIN is inert without CF_SPECTATOR); a player passes the CF_PLAYER check at :1106 and is NOT admin-gated (CF_PLR_ADMIN not set). dmm4 is an unrestricted player command, not an admin/console command.

RESULT | ktx:command:fav19_add | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Every clause maps to a located enforcing line in favx_add(19)/xfav_go(19); spectator-only class and tracking-required guard verified.
### ktx:command:fav19_add
- "Spectator command" -> src/commands.c:864 `{ "fav19_add", DEF(favx_add), 19, CF_SPECTATOR, CD_FAV19_ADD }` -> MATCH (dispatch src/commands.c:1091 rejects players DO_WRONG_CLASS; spectators pass, no CF_SPC_ADMIN so no admin needed)
- "Stores the player you are currently tracking into indexed favourite slot 19" -> src/commands.c:5715 `gedict_t *goal = PROG_TO_EDICT(self->s.v.goalentity);` + :5732 `self->favx[(int)fav_num - 1] = diff;` -> MATCH (arg 19 via dispatch src/commands.c:1135 -> favx[18])
- "Does nothing unless you are tracking a real player" -> src/commands.c:5723 `if ((goal->ct != ctPlayer) || (diff < 1) || (diff > MAX_CLIENTS)) { G_sprint(self, 2, "fav add: you are %s player!\n", redtext("not tracking")); return; }` -> MATCH
- "the tracked player's identity is written to slot 19 (overwriting any previous occupant)" -> src/commands.c:5732 `self->favx[(int)fav_num - 1] = diff;` -> MATCH (unconditional assignment overwrites prior value; MAX_CLIENTS=32 so slot 19 valid)
- "19fav_go later snaps your POV to whoever occupies that slot" -> src/commands.c (table) `{ "19fav_go", DEF(xfav_go), 19, CF_SPECTATOR, CD_19FAV_GO }` -> xfav_go: src/commands.c:5831 `pl_num = self->favx[(int)fav_num - 1];` + :5856 `stuffcmd_flags(self, STUFFCMD_IGNOREINDEMO, "track %d\n", GetUserID(p));` -> MATCH (issues track on the slot occupant)
WI-2: n/a (no default-value claim; access-class "Spectator command" verified MATCH above)

RESULT | ktx:command:fav1_add | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Identical family to fav19_add with arg 1; every clause maps to a located enforcing line in favx_add(1)/xfav_go(1).
### ktx:command:fav1_add
- "Spectator command" -> src/commands.c:846 `{ "fav1_add", DEF(favx_add), 1, CF_SPECTATOR, CD_FAV1_ADD }` -> MATCH (dispatch src/commands.c:1091 rejects players; spectators pass, no admin needed)
- "Stores the player you are currently tracking into indexed favourite slot 1" -> src/commands.c:5715 `gedict_t *goal = PROG_TO_EDICT(self->s.v.goalentity);` + :5732 `self->favx[(int)fav_num - 1] = diff;` -> MATCH (arg 1 via dispatch src/commands.c:1135 -> favx[0])
- "Does nothing unless you are tracking a real player" -> src/commands.c:5723 `if ((goal->ct != ctPlayer) || (diff < 1) || (diff > MAX_CLIENTS)) { ... redtext("not tracking") ... return; }` -> MATCH
- "the tracked player's identity is written to slot 1 (overwriting any previous occupant)" -> src/commands.c:5732 `self->favx[(int)fav_num - 1] = diff;` -> MATCH (unconditional assignment overwrites; slot 1 valid, MAX_CLIENTS=32)
- "1fav_go later snaps your POV to whoever occupies that slot" -> src/commands.c:866 `{ "1fav_go", DEF(xfav_go), 1, CF_SPECTATOR, CD_1FAV_GO }` -> xfav_go: src/commands.c:5831 `pl_num = self->favx[(int)fav_num - 1];` + :5856 `stuffcmd_flags(self, STUFFCMD_IGNOREINDEMO, "track %d\n", GetUserID(p));` -> MATCH
WI-2: n/a (no default-value claim; access-class "Spectator command" verified MATCH above)

RESULT | ktx:command:forcemap | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=7 | All refusal conditions, the announce, and the immediate trap_changelevel map load map to located enforcing lines in AdminForceMap; admin-only class verified via CF_BOTH_ADMIN + is_adm guard.
### ktx:command:forcemap
- "Admin command" -> src/commands.c:753 `{ "forcemap", AdminForceMap, 0, CF_BOTH_ADMIN | CF_PARAMS, CD_FORCEMAP }` + src/admin.c:746 `if (!is_adm(self)) { return; }` -> MATCH (CF_BOTH_ADMIN = CF_PLR_ADMIN|CF_SPC_ADMIN gates both classes to admin; redundant handler is_adm guard confirms)
- "Immediately changes the server to the map named as its argument" -> src/admin.c:765 `trap_CmdArgv(1, map, sizeof(map));` + :774 `changelevel(map);` -> g_utils.c:1730 `trap_changelevel(name, "");` -> MATCH (direct engine changelevel, no timer/deferral)
- "Usage is \"forcemap <mapname>\"" -> src/admin.c:758 `if (trap_CmdArgc() < 2) { G_sprint(self, 2, "Usage: forcemap %s\n", redtext("<mapname>")); return; }` -> MATCH
- "refused if a match is in progress (unless the server is in matchless mode)" -> src/admin.c:751 `if (!k_matchLess && match_in_progress) { G_sprint(self, 2, "Match currently in progress. ..."); return; }` -> MATCH (!k_matchLess disables the refusal in matchless mode)
- "if no map name is given" -> src/admin.c:758 `if (trap_CmdArgc() < 2) { ... return; }` -> MATCH (same Usage guard)
- "if the named map is not available on the server" -> src/admin.c:766 `if (GetMapNum(map) <= 0) { G_sprint(self, 2, "Map %s not available on this server.\n", redtext(map)); return; }` -> MATCH (GetMapNum src/maps.c:372-390 returns 0 when map not in mapslist)
- "On success it announces the map change and loads the new level at once" -> src/admin.c:773 `G_bprint(2, "%s forces a map change to %s!\n", self->netname, redtext(map));` + :774 `changelevel(map);` -> MATCH (announce then immediate trap_changelevel)
WI-2: n/a (no default-value claim; access-class "Admin command" verified MATCH above against CF_BOTH_ADMIN + is_adm)

## Wave 03 -- canary ktx:cvar:k_yawnmode (slot blind), expected TRACED-CLEAN, returned TRACED-CLEAN -- GATE 1 PASS. GATE 2 (orchestrator re-grep): handicap C-FIX confirmed (SetHandicap g_utils.c:1665 `if(match_in_progress){return false;}` bare silent; message only on separate k_lock_hdp branch) + clean lock (CF_BOTH_ADMIN commands.c:967).

RESULT | ktx:command:goalinfo:frogbot:editor | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Every clause maps to FrogbotGoalInfo + editor-mode dispatch + touch_marker write-site; "each defined goal" is minor still-true vagueness (only goals with a valid linked next_marker are printed) but no displayed value is wrong.
### ktx:command:goalinfo:frogbot:editor
- "Frogbot editor subcommand (botcmd goalinfo, available when bot editor mode is on)" -> src/bot_commands.c:2352 `{ "goalinfo", FrogbotGoalInfo, "Shows goal information for current marker" }` + src/bot_commands.c:2386 `FrogbotOptionEnabled(FB_OPTION_EDITOR_MODE) ? editor_commands : std_commands;` -> MATCH (entry lives in editor_commands[]; dispatched only when FB_OPTION_EDITOR_MODE on)
- "Prints the bot routing goal information for the marker the issuing player is currently touching" -> src/bot_commands.c:1983 `gedict_t *marker = self->fb.touch_marker;` ; src/marker_util.c:49 `other->fb.touch_marker = self;` (set when ctPlayer touches a marker entity, marker_util.c:59 `other->ct == ctPlayer`) -> MATCH
- "for each defined goal it shows the goal number, the traversal time, and the linked next marker's index and classname" -> src/bot_commands.c:1993-2001 `for (g = 0; g < NUMBER_GOALS; ++g) { gedict_t *next = marker->fb.goals[g].next_marker; if (next && (next != world) && (next != dropper)) { G_sprint(self, PRINT_HIGH, "%2d: time %3.1f: marker %3d: %s\n", g + 1, marker->fb.goals[g].time, next->fb.index + 1, next->classname); } }` -> MATCH (g+1 = goal number, .goals[g].time = traversal time, next->fb.index+1 = next marker index, next->classname = classname; goals with world/dropper/NULL sentinel next_marker are silently skipped -- minor traceable vagueness vs "each defined goal", but every value shown is exactly as described)
- "If the player is not on a marker it prints nothing" -> src/bot_commands.c:1986 `if (marker == NULL) { return; }` -> MATCH (touch_marker NULL => early return, no output)
- "Output goes to the issuing player only" -> src/bot_commands.c:1991 `G_sprint(self, PRINT_HIGH, ...)` (G_sprint targets self only) -> MATCH
WI-2: n/a (no default-value claim; no admin/player/spectator class claim -- "available when editor mode on" verified against dispatch, not name)

RESULT | ktx:command:goalsummary:frogbot:editor | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=3 | All clauses map to FrogbotGoalSummary + editor-mode dispatch; per-goal listing of markers whose fb.G_==goal verified.
### ktx:command:goalsummary:frogbot:editor
- "Frogbot editor subcommand (botcmd goalsummary, available when bot editor mode is on)" -> src/bot_commands.c:2349 `{ "goalsummary", FrogbotGoalSummary, "Show summary of goals" }` + src/bot_commands.c:2386 `FrogbotOptionEnabled(FB_OPTION_EDITOR_MODE) ? editor_commands : std_commands;` -> MATCH (editor_commands[] entry, gated on FB_OPTION_EDITOR_MODE)
- "Prints a summary of the map's bot routing goals: for each goal number it lists the routing markers assigned to that goal (each marker's index and classname)" -> src/bot_commands.c:1929-1944 `for (i = 1; i <= NUMBER_GOALS; ++i) { ... for (j = 0; j < NUMBER_MARKERS; ++j) { if (markers[j] && markers[j]->fb.G_ == i) { ... G_sprint(self, PRINT_HIGH, "  Goal #%2d:\n", i); ... G_sprint(self, PRINT_HIGH, "    %3d: %s\n", markers[j]->fb.index + 1, markers[j]->classname); } } }` -> MATCH (outer loop = goal number i; inner lists every marker whose fb.G_ == i, printing fb.index+1 and classname)
- "Output goes to the issuing player only" -> src/bot_commands.c:1928 `G_sprint(self, PRINT_HIGH, "Goal summary:\n")` and all subsequent G_sprint(self,...) -> MATCH
WI-2: n/a (no default-value claim; no admin/player/spectator class claim)

RESULT | ktx:command:handicap | C-FIX | flavourC=1 | wi2=0 | clauses=8 | Clause "Refused WITH A MESSAGE while a match is in progress" is WRONG -- the match-in-progress refusal in SetHandicap is silent (return false, no G_sprint); also the defensive half "the protection you receive" has no enforcing line (handicap only scales attacker-side damage dealt).
### ktx:command:handicap
- "Player command that sets your own handicap level" -> src/commands.c:836 `{ "handicap", handicap, 0, CF_PLAYER | CF_PARAMS | CF_MATCHLESS, CD_HANDICAP }` + src/commands.c:5230 `SetHandicap(self, atoi(arg_2));` -> MATCH (CF_PLAYER; sets self's handicap)
- "Takes one numeric argument (a percentage from 50 to 150)" -> src/commands.c:5228-5230 `trap_CmdArgv(1, arg_2, ...); SetHandicap(self, atoi(arg_2));` ; src/g_utils.c:1660 `int hdc = p->ps.handicap < 1 ? 100 : bound(50, p->ps.handicap, 150);` -> MATCH (single numeric arg, bounded 50..150)
- "100 means handicap is off" -> src/g_utils.c:1660 `p->ps.handicap < 1 ? 100 : ...` + src/combat.c:618 `if ((hdp = GetHandicap(attacker)) != 100) // skip checks if hdp == 100` + src/g_utils.c:1687 `if (nhdc == 100) { G_bprint(2, "%s turns %s off\n", ...) }` -> MATCH (100 is the neutral/off value; combat skips scaling when hdp==100)
- "lower values reduce the damage you deal and/or the protection you receive" -> src/combat.c:618-627 `if ((hdp = GetHandicap(attacker)) != 100) { if (dtAXE|dtSG|...) { damage *= 0.01f * hdp; } }` -> MATCH for "damage you deal" (attacker-side scaling, lower hdp => less damage dealt) but UNTRACEABLE for "the protection you receive": every GetHandicap read-site (src/combat.c:618 attacker-only, src/match.c:1384/2859 display, src/stats_json.c:366 display, src/commands.c:5092 display) was wide-grepped -- there is NO site where the target's/own handicap reduces armor or damage taken; the defensive half has no enforcing line (concept inference, flavour-C clause)
- "Refused with a message while a match is in progress" -> src/g_utils.c:1669-1672 `if (match_in_progress) { return false; }` -> MISMATCH (the match-in-progress refusal is SILENT: bare `return false`, no G_sprint; the dispatcher DoCommand (src/commands.c:1069-1143) gates only CF_MATCHLESS vs k_matchLess, never match_in_progress; handicap() src/commands.c:5230 discards SetHandicap's return -- so the player gets NO message. "with a message" is the WRONG assertion vs the enforcing line)
- "when the server admin has locked handicap changes" -> src/g_utils.c:1674-1679 `if (cvar("k_lock_hdp")) { G_sprint(self, 2, "%s changes are not allowed\n", redtext("handicap")); return false; }` ; k_lock_hdp toggled by admin via src/commands.c:5196-5204 hdptoggle (CF_BOTH_ADMIN) `trap_cvar_set_float("k_lock_hdp", !cvar("k_lock_hdp"));` -> MATCH (this refusal IS with a message)
- "and entirely in LGC mode (where it prints 'Handicap is not allowed in LGC mode')" -> src/commands.c:5211-5218 `qbool k_lgc = cvar(LGCMODE_VARIABLE) != 0; if (k_lgc) { G_sprint(self, 2, "Handicap is not allowed in LGC mode\n"); return; }` -> MATCH (verbatim string, blocks before any change)
- "With no argument it prints the usage hint instead of changing anything" -> src/commands.c:5221-5226 `if (trap_CmdArgc() != 2) { G_sprint(self, 2, "use: /handicap value, value from 50 to 150\n"); return; }` -> MATCH (no/wrong argc => usage hint, early return, no SetHandicap call)
WI-2: n/a (no "Default X" claim; access class "Player command" matches CF_PLAYER table flag + DoCommand CF_PLAYER gate at src/commands.c:1106)

RESULT | ktx:command:list | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | All clauses (broadcast of not-ready players' status lines, match-in-progress block, all-ready, caller-not-ready, server-wide 10s rate-limit, private no-unready message) map to verified lines in ListWhoNot with exact strings.
### ktx:command:list
- "Broadcasts to all clients the list of players who are not yet ready, each shown with their player-status line" -> src/commands.c:2503-2525 `for (p = world; (p = find_plr(p));) { if (p->ready) { continue; } ... for (p2 = world; (p2 = find_client(p2));) { G_sprint(p2, 2, "%s\n", OnePlayerStatus(p, p2)); } }` ; find_plr (src/g_utils.c:1315 ct==ctPlayer) = players; find_client (src/g_utils.c:1302 ctPlayer||ctSpec) = all connected clients -> MATCH (each not-ready player's status line sent to every connected client; header G_bprint at src/commands.c:2512)
- "Works only when no match is in progress; reports 'Game in progress' during a match" -> src/commands.c:2473-2478 `if (match_in_progress) { G_sprint(self, 2, "Game in progress\n"); return; }` -> MATCH (verbatim string)
- "'All players ready' if everyone is ready" -> src/commands.c:2480-2485 `if (CountRPlayers() == CountPlayers()) { G_sprint(self, 2, "All players ready\n"); return; }` -> MATCH (verbatim string)
- "(for a player) 'Ready yourself first' if the caller is an unready player" -> src/commands.c:2487-2492 `if ((self->ct == ctPlayer) && !self->ready) { G_sprint(self, 2, "Ready yourself first\n"); return; }` -> MATCH (verbatim string, scoped to ctPlayer caller who is not ready)
- "Rate-limited to once per 10 seconds across the server" -> src/commands.c:2494-2501 `if (k_whonottime && (g_globalvars.time < (k_whonottime + 10))) { G_sprint(self, 2, "Only one %s in 10 seconds\n", redtext("list")); return; } k_whonottime = g_globalvars.time;` ; k_whonottime is a single global (src/globals.c:42, sole writer is this function) -> MATCH (server-wide, 10s window)
- "if no unready players are found it privately reports 'can't find not ready players'" -> src/commands.c:2527-2534 `if (found) { G_bprint(2, "\n"); } else { G_sprint(self, 2, "can't find not ready players\n"); }` -> MATCH (G_sprint(self,...) = private to caller; verbatim string)
WI-2: n/a (no "Default X" claim; description makes no contradicting access-class claim -- the per-player message scope is correctly described; table flag CF_PLAYER|CF_SPC_ADMIN not contradicted)

RESULT | ktx:command:lock | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Every clause maps to sv_lock + CanConnect non-VIP block + CheckSvUnlock auto-expire + CF_BOTH_ADMIN dispatcher gate; "spectators unaffected" confirmed structurally (SpecCanConnect has no lock-check) and by globals.c:91 comment.
### ktx:command:lock
- "Temporarily locks the server: while locked, non-VIP players cannot connect (they are told the server is temporarily locked and how many seconds remain)" -> src/client.c:1319-1328 `if (k_sv_locktime && !VIP(self)) { int seconds = k_sv_locktime - g_globalvars.time; G_sprint(self, 2, "%s: %d second%s\n", redtext("server is temporary locked"), seconds, count_s(seconds)); return false; }` (CanConnect, player connect path, called from src/client.c:1678) -> MATCH (non-VIP blocked from connecting, told "server is temporary locked" + seconds remaining; source string "temporary" vs description "temporarily" -- benign cosmetic, assertion correct)
- "spectators are unaffected" -> src/spectate.c:143-153 `qbool SpecCanConnect(gedict_t *spec) { if (!nospecs_canconnect(spec)) {...} return true; }` (no k_sv_locktime check); spectator connect path src/spectate.c:169 `if (!SpecCanConnect(self))` is separate from player CanConnect; confirmed by src/globals.c:91 `float k_sv_locktime; // some time before non VIP players can't connect, spectators not affected` -> MATCH (lock-check absent from spectator path; adjacent global comment confirms)
- "The lock lasts 15 seconds and then auto-expires with a broadcast 'server unlocked'" -> src/admin.c:914 `int lock_time = 15;` + src/admin.c:920 `k_sv_locktime = g_globalvars.time + lock_time;` ; src/world.c:1183-1187 `if (k_sv_locktime && (k_sv_locktime < g_globalvars.time)) { G_bprint(2, "%s\n", redtext("server unlocked")); k_sv_locktime = 0; }` (CheckSvUnlock) -> MATCH (15s duration, auto-expire broadcasts "server unlocked")
- "Issuing the command broadcasts '<name> locked server for 15 seconds'" -> src/admin.c:916-920 `if (!k_sv_locktime) { G_bprint(2, "%s %s for %s seconds\n", getname(self), redtext("locked server"), dig3(lock_time)); k_sv_locktime = g_globalvars.time + lock_time; }` with lock_time=15 -> MATCH
- "issuing it again while the lock is active clears it immediately and broadcasts '<name> unlocked server'" -> src/admin.c:922-926 `else { G_bprint(2, "%s %s\n", getname(self), redtext("unlocked server")); k_sv_locktime = 0; }` -> MATCH (active lock => second call clears k_sv_locktime to 0, broadcasts "unlocked server")
- "Admin only" -> src/commands.c:967 `{ "lock", sv_lock, 0, CF_BOTH_ADMIN, CD_LOCK }` ; include/g_local.h:652 `#define CF_BOTH_ADMIN ( CF_PLR_ADMIN | CF_SPC_ADMIN )` ; enforced in DoCommand src/commands.c:1111-1116 `if ((cmds[icmd].cf_flags & CF_PLR_ADMIN) && !is_adm(self)) { G_sprint(self, 2, "You are not an admin\n"); return DO_ACCESS_DENIED; }` (and spectator branch src/commands.c:1096-1101) -> MATCH (verified against CF_ flag AND dispatcher access check; sv_lock itself has no inline check, the table flag + DoCommand gate enforce admin-only)
WI-2: n/a (no "Default X" claim; access-class "Admin only" verified against CF_BOTH_ADMIN flag + DoCommand is_adm gate, not inferred from command name)

## Wave 04 -- canary ktx:command:autotrack (slot blind), expected C-FIX, returned C-FIX -- GATE 1 PASS. GATE 2 (orchestrator re-grep): qizmo WI2-FIX confirmed (qlag/qenemy/qpoint CF_PLAYER|CF_SPC_ADMIN commands.c:784-786 = player-accessible, admin only for specs) + clean race_cancel real command DEF(r_changestatus) arg4 CF_PLAYER commands.c:1008.

RESULT | ktx:command:no_gl | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=7 | Every clause maps to an enforcing line: stuffs `cmd noweapon gl`, XOR-toggles IT_GRENADE_LAUNCHER, dmm4-only + not-while-match gates, server-wide cvar set + broadcast announce.
### ktx:command:no_gl
- "Shorthand for /noweapon gl: re-issues the noweapon command with the gl argument" -> src/commands.c:5332 `stuffcmd_flags(self, STUFFCMD_IGNOREINDEMO, "cmd noweapon gl\n");` -> MATCH
- "on the caller's behalf" -> src/commands.c:5332 `stuffcmd_flags(self, ...)` (self == caller) -> MATCH
- "toggling the grenade launcher between allowed and disallowed" -> src/commands.c:5297-5299 `else if (streq(nwp = "gl", arg_2) ...) { k_disallow_weapons ^= bit = IT_GRENADE_LAUNCHER; }` (XOR == toggle) -> MATCH
- "for the match" (server-wide weapon-disallow state) -> src/commands.c:5314 `trap_cvar_set_float("k_disallow_weapons", k_disallow_weapons);` -> MATCH
- "Subject to the same constraints as noweapon (deathmatch mode 4 / dmm4 only" -> src/commands.c:5255-5257 `if (deathmatch != 4) { G_sprint(self, 2, "command allowed in %s only\n", redtext("dmm4")); return; }` -> MATCH
- "not while a match is in progress)" -> src/commands.c:5245-5253 `if (match_in_progress) { if (deathmatch == 4) show_disallowed_weapons(...); return; }` (no toggle while match in progress) -> MATCH
- "The change is server-wide and announced to all players" -> src/commands.c:5312-5314 `G_bprint(2, "%s %s %s\n", self->netname, redtext(Allows(...)), redtext(nwp)); trap_cvar_set_float("k_disallow_weapons", ...);` (G_bprint == broadcast to all; cvar == server-wide) -> MATCH
WI-2: n/a -- no default-value or access-class claim in the description.

RESULT | ktx:command:pathlist:frogbot:editor | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Editor-mode dispatch -> FrogbotListPaths; loops all markers/paths filtered by required flag arg, prints src/dst indices + location names + total count, prints FROGBOT_PATH_FLAG_OPTIONS on missing/invalid flag arg.
### ktx:command:pathlist:frogbot:editor
- "Frogbot route-editor subcommand (run via the bot command with editor mode enabled)" -> src/bot_commands.c:2358 `{ "pathlist", FrogbotListPaths, "Lists paths with flags set" } };` in `editor_commands[]`, selected at src/bot_commands.c:2385-2386 `commands = FrogbotOptionEnabled(FB_OPTION_EDITOR_MODE) ? editor_commands : std_commands;` and dispatched at src/bot_commands.c:2465-2467 `if (streq(commands[i].name, command)) { commands[i].func(); ... }` -> MATCH
- "lists every routing path across all markers whose path flags match a required flag-filter argument" -> src/bot_commands.c:195-212 `for (i = 0; i < NUMBER_MARKERS; ++i) { ... for (j = 0; j < NUMBER_PATHS; ++j) { ... if (!next || !(p->flags & path_filter)) continue; ...}}` with `path_filter = DecodeMarkerPathFlagString(argument)` (src/bot_commands.c:185) -> MATCH
- "For each match it prints the source and destination marker indices and their location names" -> src/bot_commands.c:219-221 `G_sprint(self, PRINT_HIGH, "  %3d > %3d \20%s\21 > \20%s\21\n", m->fb.index + 1, next->fb.index + 1, LocationName(PASSVEC3(m->s.v.origin)), LocationName(PASSVEC3(next->s.v.origin)));` -> MATCH
- "followed by the total count of matching paths" -> src/bot_commands.c:226 `G_sprint(self, PRINT_HIGH, "%3d paths found matching %s\n", path_count, argument);` -> MATCH
- "With no flag argument it prints the list of valid path-flag options" -> src/bot_commands.c:177-181 `if (trap_CmdArgc() <= arg_number) { G_sprint(self, PRINT_HIGH, "Provide path flags: " FROGBOT_PATH_FLAG_OPTIONS "\n"); return; }`; FROGBOT_PATH_FLAG_OPTIONS == "w6rjva" (include/fb_globals.h:291) -> MATCH
WI-2: n/a -- no default-value or access-class claim in the description.

RESULT | ktx:command:qizmo | WI2-FIX | flavourC=0 | wi2=1 | clauses=3 | Core behavior fully traced (static 3-line help print, changes nothing); but the "admin sub-commands" metadata qualifier is wrong -- qlag/qenemy/qpoint are CF_PLAYER|CF_SPC_ADMIN, i.e. player-accessible and admin-gated only for spectators, not unconditionally admin.
### ktx:command:qizmo
- "Prints, to the calling player's console, a short help listing" -> src/commands.c:1587-1590 `G_sprint(self, 2, "%s....... lagsettings\n" "%s..... enemy vicinity reporting\n" "%s..... point function\n", redtext("qlag"), redtext("qenemy"), redtext("qpoint"));` (G_sprint to self == caller's console) -> MATCH
- "of the three QiZmo-proxy sub-commands and what each does: qlag (lag settings), qenemy (enemy vicinity reporting), qpoint (point function)" -> src/commands.c:1587-1590 (same call: exactly qlag=lagsettings, qenemy=enemy vicinity reporting, qpoint=point function) -> MATCH
- "It only displays this list; it changes no settings itself" -> src/commands.c:1585-1591 entire ShowQizmo body is a single G_sprint, no cvar/state mutation -> MATCH
WI-2: result -- "admin sub-commands" overstates the access class. qlag/qenemy/qpoint are registered `CF_PLAYER | CF_SPC_ADMIN` (src/commands.c:784-786) and the handlers ToggleQLag/ToggleQEnemy/ToggleQPoint (src/commands.c:3686-3733) contain NO admin check. Enforcement at src/commands.c:1088-1109: the CF_SPC_ADMIN gate fires only on the spectator branch (`if (spc) ... if ((cf_flags & CF_SPC_ADMIN) && !is_adm(self))`); on the player branch CF_PLAYER is set so a regular player runs them with no admin requirement. They are player-accessible commands, admin-gated only when invoked by a spectator -- not "admin sub-commands". Metadata access-class inaccuracy (not flavour-C; qizmo's own behavior is clean).

RESULT | ktx:command:race_cancel | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=8 | Every clause maps to an enforcing line in r_changestatus case 4 / race_command_checks: racer+race.status guards, abort sound, gendered broadcast, race_end, spectator early-return, race-mode precondition.
### ktx:command:race_cancel
- "In race mode, aborts the calling player's own current run" -> src/commands.c:1008 `{ "race_cancel", DEF(r_changestatus), 4, CF_PLAYER, CD_RCANCEL }` -> src/race.c:3061 `case 4: // race_cancel` in r_changestatus; src/race.c:2953-2958 race_command_checks requires `isRACE()` -> MATCH
- "It only acts if the caller is a racer with a run in progress" -> src/race.c:3062-3070 `if (!self->racer) { return; } if (!race.status) { return; }` -> MATCH
- "it plays an abort sound" -> src/race.c:3073 `sound(self, CHAN_ITEM, "boss2/idle.wav", 1, ATTN_NONE);` -> MATCH
- "broadcasts '<name> aborted <his/her> run'" -> src/race.c:3074 `G_bprint(PRINT_HIGH, "%s aborted %s run\n", self->netname, g_his(self));`; g_his returns "his"/"her" by gender (src/g_utils.c:1241-1255 `char *sex = "his"; ... if (streq(ezinfokey(ed, "gender"), "f")) sex = "her";`) -> MATCH
- "and ends the run" -> src/race.c:3075 `race_end(self, true, false);` -> MATCH
- "Has no effect for spectators" -> src/race.c:3020-3023 `if (self->ct == ctSpec) { return; }` (pre-switch early-return) -> MATCH
- "when the caller is not currently running" -> src/race.c:3062 `if (!self->racer) return;` and src/race.c:3067 `if (!race.status) return;` -> MATCH
- "or when the race-mode command preconditions are not met" -> src/race.c:3015-3018 `if (!race_command_checks()) { return; }` -> src/race.c:2953-2958 `if (!isRACE()) { ... return false; }` -> MATCH
WI-2: n/a -- "calling player" / "no effect for spectators" consistent with CF_PLAYER (src/commands.c:1008) + the ctSpec early-return; no default-value claim.

RESULT | ktx:command:race_dl_record_demo | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Every clause maps to an enforcing line in race_download_record_demo: arg->record index, "record not found" on invalid record, "demo for record #N is not available" on empty demoname, client-side download stuffcmd; CF_BOTH == players + spectators verified.
### ktx:command:race_dl_record_demo
- "Sends the caller a client-side download of the saved MVD demo for one stored race record" -> src/race.c:3135-3136 `stuffcmd_flags(self, STUFFCMD_IGNOREINDEMO, "download \"demos/%s.mvd\"\n", race.records[record].demoname);` (stuffcmd to self == client-side download command for the record's .mvd) -> MATCH
- "Takes the record number as an argument" -> src/race.c:3114 `int record = read_record_param(1);`; read_record_param (src/race.c:227-240) reads CmdArgv(1) and returns `bound(0, atoi(arg_1) - 1, NUM_BESTSCORES - 1)` (1-based arg -> 0-based record index) -> MATCH
- "prints 'record not found' if that record does not exist" -> src/race.c:3121-3125 `if (!is_valid_record(&race.records[record])) { G_sprint(self, 2, "record not found\n"); return; }`; is_valid_record == `record->time < RACE_INVALID_RECORD_TIME` (src/race.c:224) -> MATCH
- "and 'demo for record #N is not available' if the record has no saved demo" -> src/race.c:3128-3132 `if (strnull(race.records[record].demoname)) { G_sprint(self, 2, "demo for record #%d is not available\n", record + 1); return; }` (#N == record + 1, 1-based; fires when demoname empty) -> MATCH
- "Available to players and spectators" -> src/commands.c:1025 `{ "race_dl_record_demo", race_download_record_demo, 0, CF_BOTH | CF_PARAMS, CD_RDLDEMO }`; CF_BOTH == CF_PLAYER | CF_SPECTATOR (include/g_local.h:649); enforced src/commands.c:1088-1108 (spectator branch CF_SPECTATOR set, player branch CF_PLAYER set, no CF_*_ADMIN) -> MATCH
WI-2: result -- access-class claim "Available to players and spectators" verified against the CF_BOTH flag AND the actual enforcement check (no admin gate); accurate.

## Wave 05 -- canary ktx:cvar:k_teamoverlay (slot blind), expected C-NEAR-MISS, returned C-NEAR-MISS -- GATE 1 PASS (recovered, attempt 3). GATE 2 (orchestrator re-grep): RECOVERED on attempt 3 (attempts 1+2 false-negatived the k_teamoverlay canary as TRACED-CLEAN = the deliberately-hard invisible class; the gate WORKING). socd C-NEAR-MISS confirmed (client.c:3785/3792 WARN/KICK gated socdDetectionCount>=3, NOT per-violation; detect fStrafeChangeCount>=25 ratio>=0.75) + clean rules/s-l real commands c:747/934.

RESULT | ktx:command:race_show_route | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=7 | Every clause maps to an enforcing line in r_print -> race_command_checks -> race_print_route_info; "non-custom" == race.active_route!=0 confirmed by adjacent assignment comments.
### ktx:command:race_show_route
- "Race-mode query command (players and spectators)" -> src/commands.c:1012 `{ "race_show_route", r_print, 0, CF_BOTH, CD_R_PRINT }` + src/race.c:2951-2961 `if (!isRACE()) { ...return false; }` (CF_BOTH = CF_PLAYER|CF_SPECTATOR per g_local.h:649) -> MATCH
- "Prints to the caller" -> src/race.c:3297-3309 `if (p) { G_sprint(p, 2, ...) }` (p==self from r_print line 3403) -> MATCH
- "the route name" -> src/race.c:3299 `G_sprint(p, 2, "...%s...", race_route_name())` (race_route_name src/race.c:464-481) -> MATCH
- "the active route number" -> src/race.c:3300 `G_sprint(p, 2, "%s %2d ...", redtext("route"), race.active_route, ...)` -> MATCH
- "the configured race time limit in seconds" -> src/race.c:3300-3301 `\220tl: %ssec\221 ... dig3(race.timeout_setting)` -> MATCH
- "the route description (only when a non-custom route is active)" -> src/race.c:3303-3306 `if (race.active_route) { G_sprint(p, 2, "\220%s\221\n", race_route_desc()); }` (active_route==0 set by race_route_now_custom src/race.c:2781 `// mark this is a custom route now`; !=0 set src/race.c:3282 `// mark this is not custom route now`) -> MATCH
- "the current weapon mode" -> src/race.c:3308 `G_sprint(p, 2, "%s: %s\n", redtext("weapon"), race_weapon_mode(race.weapon))` (race_weapon_mode src/race.c:766-784) -> MATCH
WI-2: n/a

RESULT | ktx:command:race_show_toptimes | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=7 | display_scores loop over NUM_BESTSCORES==10 records loaded per-map via race_filename(mapname); dash/own-marker branches located.
### ktx:command:race_show_toptimes
- "Race-mode query command (players and spectators)" -> src/commands.c:1010 `{ "race_show_toptimes", display_scores, 0, CF_BOTH, CD_RSCORES }` + src/race.c:1853 `if (!race_command_checks())` (isRACE gate) -> MATCH
- "Prints to the caller the leaderboard of best recorded race times for the current map" -> src/race.c:1858-1884 all `G_sprint(self, 2, ...)`; records loaded by read_topscores -> race_filename src/race.c:197-214 path embeds `mapname` -> MATCH
- "a fixed number of ranked slots" -> src/race.c:1865 `for (i = 0; i < NUM_BESTSCORES; i++)` (NUM_BESTSCORES==10 include/progs.h:1335) -> MATCH
- "showing position, finishing time in seconds, and racer name" -> src/race.c:1880-1881 `G_sprint(self, 2, " %02d   %07.3f%s  %s\n", i+1, race.records[i].time / 1000.0, redtext("s"), race.records[i].racername)` -> MATCH
- "Empty slots show dashes" -> src/race.c:1867-1869 `if (!is_valid_record(&race.records[i])) { G_sprint(self, 2, " %02d      -         -\n", i+1); }` (is_valid_record src/race.c:222-225) -> MATCH
- "the caller's own records are flagged with a distinct marker" -> src/race.c:1873-1876 `if (streq(race.records[i].racername, self->netname)) { G_sprint(self, 2, " %02d \215 %07.3f%s  %s\n", ...) }` (\215 marker vs unmarked branch at 1880) -> MATCH
WI-2: n/a

RESULT | ktx:command:rules | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | ShowRules: every mode string, the CTF impulse/command block, the team info-commands block, and the k_bzk/k_btime berserk notice all map to verbatim G_sprint(self,...) branches; CF_PLAYER == player-only.
### ktx:command:rules
- "Prints the current server game mode to the calling player only" -> src/commands.c:747 `{ "rules", ShowRules, 0, CF_PLAYER | CF_MATCHLESS, CD_RULES }` + all sends are `G_sprint(self, 2, ...)` (CF_PLAYER, no CF_SPECTATOR -> player-only) -> MATCH
- "duel, CTF, FFA, team, or 'unknown mode'" -> src/commands.c:3301-3330 `if (isDuel()) "duel mode" ... else if (isCTF()) "CTF mode" ... else if (isFFA()) "FFA mode" ... else if (isTeam()) "team mode" ... else "unknown mode"` (predicates src/g_utils.c:1576-1603 switch on k_mode/k_ctf) -> MATCH
- "In CTF mode it additionally lists ... impulse 22 grappling hook, tossrune, tossflag, flagstatus" -> src/commands.c:3307-3312 verbatim `"impulse 22 : Grappling Hook\ntossrune ...\ntossflag ...\nflagstatus ..."` -> MATCH
- "in team mode it notes the in-game scores/stats/efficiency info commands" -> src/commands.c:3320-3325 `"...will print time left and teamscores. Also available ... %s and %s.", redtext("scores"), redtext("stats"), redtext("efficiency")` -> MATCH
- "If berserk mode (k_bzk) is set it appends a notice" -> src/commands.c:3332-3339 `if (cvar("k_bzk")) { G_sprint(self, 2, "\nBERZERK mode is activated! ...", ...) }` -> MATCH
- "all players gain Quad/Octa power when k_btime seconds remain" -> src/commands.c:3334-3338 `"...when only %d seconds remains ... gets QUAD/OCTA powered.", (int)cvar("k_btime")` -> MATCH
WI-2: n/a (no default-value claim; CF_PLAYER matches "calling player only")

RESULT | ktx:command:s-l | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | s_lr(1) reads self->s_last_to (only assigned in s_common:586, the path s_p->s_common takes); GetUserID==0 on departed client; match player/spec divide enforced in s_common; config string "continue last s-p u done".
### ktx:command:s-l
- "Resends a direct private message to the player you most recently targeted with s-p" -> src/g_cmd.c:347-349 `s-l -> s_lr(1)`; src/g_cmd.c:649 `p = (l == 1 ? self->s_last_to : ...)`; s_last_to assigned ONLY at src/g_cmd.c:586 `from->s_last_to = to;` in s_common, which s_p() invokes at src/g_cmd.c:619 (config string CD_S_L src/commands.c:546 "continue last s-p u done") -> MATCH
- "Usage: s-l <text>" -> src/g_cmd.c:642-644 `if (argc < 3) { G_sprint(self, 2, "usage: %s txt\n", (l == 1 ? "s-l" : "s-r")); return; }` -> MATCH
- "The text reaches only that one recipient" -> src/g_cmd.c:597-598 `G_sprint_flags(to, ...); G_sprint_flags(from, ...)` (only `to` recipient + caller echo) -> MATCH
- "if no prior s-p recipient is recorded or that client has left, it reports that no client was found" -> src/g_cmd.c:651 `if (p && GetUserID(p))` else src/g_cmd.c:658 `"%s: client not found\n"` (GetUserID src/g_utils.c:1621-1628 returns 0 when !p or ct not player/spec) -> MATCH
- "During a match, players and spectators cannot message across the player/spectator divide" -> src/g_cmd.c:581-584 `if (match_in_progress && (from->ct != to->ct)) { return; // spec to player or player to spec, disallowed in match }` -> MATCH
WI-2: n/a (CF_BOTH|CF_MATCHLESS, no default/access claim in description)

RESULT | ktx:command:socd | C-NEAR-MISS | flavourC=1 | wi2=0 | clauses=7 | Cycle/wrap/announce/no-effect-in-match/allow/stats clauses all enforced; but "warn ... on each violation" and "kick ... on violation" are narrower in code -- both require socdDetectionCount>=3 (and WARN also !match_in_progress), not per-violation.
### ktx:command:socd
- "Cycles the server's SOCD ... handling mode and announces the new mode to everyone" -> src/commands.c:9407 `k_socd = cvar("k_socd") + 1` + src/commands.c:9413-9427 `switch ... G_bprint(2, "%s: ...\n", redtext("SOCD"))` (G_bprint = broadcast) + 9429 `cvar_set("k_socd", ...)` -> MATCH
- "0 = allow (no action)" -> src/commands.c:9415-9417 `case SOCD_ALLOW: G_bprint(2,"%s: allow\n",...)`; no enforcement branch fires for ALLOW (client.c gates on SOCD_WARN/SOCD_KICK only; stats.c:767 gate is k_socd>=SOCD_STATS) -> MATCH
- "1 = collect stats and report after the game" -> collection unconditional in src/client.c:3752-3804 (no k_socd gate); reporting src/stats.c:767 `if (!p->isBot && cvar("k_socd") >= SOCD_STATS)` reached via MatchEndStats src/stats.c:1678 -> PlayersStats:1698 -> OnePlayerStats:1052 -> MATCH
- "2 = warn the offending player on each violation" -> src/client.c:3785 `if ((!match_in_progress) && (!self->isBot) && k_socd == SOCD_WARN && (self->ct == ctPlayer) && (self->socdDetectionCount >= 3)) { G_bprint(PRINT_HIGH, "[...] Warning! ...") }` -> MISMATCH(only enforcing site; fires only when socdDetectionCount>=3 AND !match_in_progress, NOT "on each violation" -- materially narrower/more conditional than implied)
- "3 = kick the offending player on violation" -> src/client.c:3792 `if ((!self->isBot) && k_socd == SOCD_KICK && (self->ct == ctPlayer) && (self->socdDetectionCount >= 3)) { G_bprint(...,"Kicked!...") stuffcmd(self,"disconnect\n") }` -> MISMATCH(only enforcing site; requires socdDetectionCount>=3, NOT "on violation" -- narrower than implied; a "detection" itself only registers at fStrafeChangeCount>=25 & ratio>=0.75)
- "Wrapping past kick returns to allow" -> src/commands.c:9408-9411 `if (k_socd < SOCD_ALLOW || k_socd > SOCD_KICK) { k_socd = SOCD_ALLOW; }` (KICK 3 -> 4 > SOCD_KICK -> ALLOW 0) -> MATCH
- "Has no effect while a match is in progress" -> src/commands.c:9402-9405 `if (match_in_progress) { return; }` (command itself no-ops) -> MATCH
WI-2: n/a (RegisterCvarEx("k_socd","1") src/world.c:1017 default=STATS but description states no default; CF_PLAYER, no access claim)

## Wave 06 -- canary ktx:cvar:k_yawnmode (slot blind), expected TRACED-CLEAN, returned TRACED-CLEAN -- GATE 1 PASS. GATE 2 (orchestrator re-grep): all-clean wave; clean speed confirmed (ToggleSpeed c:3215, bound(0,cvar("k_highspeed"),9999) c:3230, cvar_fset sv_maxspeed c:3234).

RESULT | ktx:command:s-p | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Every clause (private msg, id/name resolve, usage/not-found errors, match player/spec divide, s-l remember) maps to an enforcing line in s_p()/s_common(). CF_BOTH|CF_MATCHLESS confirms player+spec access.
### ktx:command:s-p
- "Sends a private text message to a single specified player ... only that player receives" -> src/g_cmd.c:597-598 `G_sprint_flags(to, PRINT_CHAT, ...)` + `G_sprint_flags(from, PRINT_CHAT, ...)` in s_common() (sends only to resolved `to` and echoes to `from`, no broadcast) -> MATCH
- "Usage: s-p <id|name> <text>" -> src/g_cmd.c:607-612 `if (argc < 4) { G_sprint(self, 2, "usage: s-p id/name txt\n"); return; }` + src/g_cmd.c:614 `trap_CmdArgv(2, arg_3, ...)` (argv2=target) + src/g_cmd.c:618 `params_str(3, -1)` (argv3+=text) -> MATCH
- "recipient is resolved by player id or name" -> src/g_cmd.c:616 `if ((p = SpecPlayer_by_IDorName(arg_3)))` -> SpecPlayer_by_IDorName src/g_utils.c:1487-1489 (player_by_IDorName then spec_by_IDorName) -> MATCH
- "fails with a usage hint if arguments are missing" -> src/g_cmd.c:607-612 (same usage guard) -> MATCH
- "'client not found' if the named/id player is not connected" -> src/g_cmd.c:623 `G_sprint(self, 2, "s-p: client %s not found\n", arg_3);` (else branch when resolve returns NULL) -> MATCH
- "During a match the message will not cross the player/spectator divide" -> src/g_cmd.c:581-584 `if (match_in_progress && (from->ct != to->ct)) { return; // spec to player or player to spec, disallowed in match }` in s_common() -> MATCH
- "target is remembered so s-l can resend to them" -> src/g_cmd.c:586 `from->s_last_to = to;` (set in s_common) consumed by src/g_cmd.c:649 `p = (l == 1 ? self->s_last_to : self->s_last_from);` in s_lr() (s-l => l==1 => s_last_to) -> MATCH
WI-2: n/a (description states no default or explicit access-class claim; command table src/commands.c:933 `CF_BOTH | CF_MATCHLESS | CF_PARAMS` = players+spectators, additionally valid matchless -- consistent with "single specified player" usable by both sides)

RESULT | ktx:command:speed | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Toggle-vs-320/k_highspeed, 0-9999 clamp, apply to sv_maxspeed + every player, broadcast, and match-block all map to ToggleSpeed() enforcing lines.
### ktx:command:speed
- "Toggles the server's player speed limit" -> src/commands.c:3215 `void ToggleSpeed(void)` registered src/commands.c:757 `{ "speed", ToggleSpeed, 0, CF_PLAYER, CD_SPEED }` -> MATCH
- "switches sv_maxspeed between the standard 320 and the value of the k_highspeed cvar" -> src/commands.c:3224-3231 `if (k_maxspeed != 320) { k_maxspeed = 320; } else { k_maxspeed = bound(0, cvar("k_highspeed"), 9999); }` -> MATCH
- "(clamped to 0-9999)" -> src/commands.c:3230 `k_maxspeed = bound(0, cvar("k_highspeed"), 9999);` -> MATCH
- "if the limit is not currently 320 it is set back to 320, otherwise it is raised to k_highspeed" -> src/commands.c:3224-3231 (exact branch polarity: !=320 -> 320 else -> k_highspeed) -> MATCH
- "applied to sv_maxspeed and to every connected player immediately" -> src/commands.c:3234 `cvar_fset("sv_maxspeed", k_maxspeed);` + src/commands.c:3236-3239 `for (p = world; (p = find_plr(p));) { p->maxspeed = k_maxspeed; }` -> MATCH
- "the new value is announced to all players" -> src/commands.c:3233 `G_bprint(2, "%s %d\n", redtext("Maxspeed set to"), (int)k_maxspeed);` (G_bprint = broadcast) -> MATCH
- "Has no effect while a match is in progress" -> src/commands.c:3219-3222 `if (match_in_progress) { return; }` (first statement, before any mutation) -> MATCH
WI-2: n/a (description names no default value; k_highspeed is RegisterCvar("k_highspeed") world.c:870 = default 0, but description quotes "the value of the k_highspeed cvar" rather than a literal, so no metadata claim to fail)

RESULT | ktx:command:status1 | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=14 | Every listed settings field and every appended live-match-state line maps to a G_sprint(self,...) in ModStatus1(); printed to requester (self), CF_BOTH|CF_MATCHLESS confirms players+specs.
### ktx:command:status1
- "Prints the first page of current server settings to the requesting player" -> src/commands.c:1860 `void ModStatus1(void)` + body uses `G_sprint(self, 2, ...)` throughout (self = requester); registered src/commands.c:710 `{ "status1", ModStatus1, 0, CF_BOTH | CF_MATCHLESS, CD_STATUS1 }` -> MATCH
- "maxspeed" -> src/commands.c:1865 `G_sprint(self, 2, "%-14.14s %-4d\n", redtext("Maxspeed"), (int)k_maxspeed);` -> MATCH
- "deathmatch mode" -> src/commands.c:1866 `redtext("Deathmatch"), (int)deathmatch` -> MATCH
- "teamplay value" -> src/commands.c:1867 `redtext("Teamplay"), (int)tp_num()` -> MATCH
- "time limit" -> src/commands.c:1868 `redtext("Timelimit"), (int)timelimit` -> MATCH
- "frag limit" -> src/commands.c:1869 `redtext("Fraglimit"), (int)fraglimit` -> MATCH
- "powerups string" -> src/commands.c:1870 `redtext("Powerups"), Get_PowerupsStr()` -> MATCH
- "discharge" -> src/commands.c:1871 `redtext("Discharge"), OnOff(cvar("k_dis"))` -> MATCH
- "drop-quad" -> src/commands.c:1872 `redtext("Drop Quad"), OnOff(cvar("dq"))` -> MATCH
- "drop-ring" -> src/commands.c:1873 `redtext("Drop Ring"), OnOff(cvar("dr"))` -> MATCH
- "fair backpacks" -> src/commands.c:1874 `redtext("Fair Backpacks"), get_frp_str()` -> MATCH
- "drop-backpacks" -> src/commands.c:1875 `redtext("Drop Backpacks"), OnOff(cvar("dp"))` -> MATCH
- "spectator-info permission (admin-only vs all)" -> src/commands.c:1876 `redtext("spec info perm"), mi_adm_only() ? "adm" : "all"` -> MATCH
- "more-spec-info" -> src/commands.c:1877 `redtext("more spec info"), OnOff(mi_on())` -> MATCH
- "teleteam" -> src/commands.c:1878 `redtext("teleteam"), OnOff(cvar("k_tp_tele_death"))` -> MATCH
- "berzerk" -> src/commands.c:1879 `redtext("Berzerk"), OnOff(cvar("k_bzk"))` -> MATCH
- "a pending-start countdown" -> src/commands.c:1881-1891 `if (match_in_progress == 1) { ... "The match will start in %d second%s\n" ... }` -> MATCH
- "any election in progress with vote tally" -> src/commands.c:1893-1896 `if ((votes = get_votes( OV_ELECT))) G_sprint(... "%s election in progress:\220%d/%d\221 vote%s\n" ...)` -> MATCH
- "whether team picking / a captain / a coach is present" -> src/commands.c:1898-1911 `if (k_captains == 2) {"Team picking" in progress}` / `if (floor(k_captains) == 1) {"captain" present}` / `if (floor(k_coaches) == 1) {"coach" present}` -> MATCH
- "once a match is running -- sudden-death/overtime status or full minutes left" -> src/commands.c:1913-1929 `if (match_in_progress == 2) { if (k_sudden_death) {"%s overtime in progress"} else {"\220%s\221 full minute%s left"} }` -> MATCH
WI-2: n/a (no default-value claim; access class CF_BOTH|CF_MATCHLESS = players+spectators, additionally matchless -- consistent with "to the requesting player"; not an admin-restricted claim)

RESULT | ktx:command:teamoverlay | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Vote-toggle of k_teamoverlay, in-match status-only path, <2-players-block for non-admins, admin-alone (veto), per-call vote toggle+broadcast, and flip-on-quorum all map to teamoverlay()/vote_check_teamoverlay().
### ktx:command:teamoverlay
- "Casts (or withdraws) the calling player's vote to flip the server's k_teamoverlay setting" -> src/vote.c:1096 `self->v.teamoverlay = !self->v.teamoverlay;` then src/vote.c:1107 `vote_check_teamoverlay();` which at src/vote.c:1057 `cvar_fset("k_teamoverlay", !cvar("k_teamoverlay"));` -> MATCH
- "(whether clients are allowed to use the team-overlay HUD)" -> src/client.c:4714 `k_teamoverlay = cvar("k_teamoverlay");` + src/client.c:4720 `if (!k_teamoverlay) // teamoverlay turned off` (gates serverinfo teamoverlay availability to clients) -> MATCH
- "While a match is in progress it does not vote and instead just reports the current Teamoverlay on/off state" -> src/vote.c:1077-1082 `if (match_in_progress) { G_sprint(self, 2, "%s %s\n", redtext("Teamoverlay"), OnOff(cvar("k_teamoverlay"))); return; }` (returns before the vote toggle at 1096) -> MATCH
- "Non-admins need at least 2 players present to vote" -> src/vote.c:1085-1094 `if (!is_adm(self)) { if (CountPlayers() < 2) { G_sprint(self, 2, "You need at least 2 players to do this.\n"); return; } }` -> MATCH
- "an admin may toggle the setting alone" -> src/vote.c:1085 `if (!is_adm(self))` skips the 2-player gate for admins; admin's vote -> src/vote.c:1050-1057 `veto = is_admins_vote(OV_TEAMOVERLAY); if (veto || ...) { ... cvar_fset("k_teamoverlay", !cvar("k_teamoverlay")); }` flips immediately on admin veto (alone) -> MATCH
- "Each call toggles the player's vote and broadcasts it with the running count" -> src/vote.c:1096 `self->v.teamoverlay = !self->v.teamoverlay;` + src/vote.c:1098-1105 `G_bprint(2, "%s %s!%s\n", self->netname, (votes for/withdraws ...), ((votes = get_votes_req(OV_TEAMOVERLAY, true)) ? va(" (%d)", votes) : ""))` -> MATCH
- "once enough players vote the setting flips" -> src/vote.c:1052-1057 `if (veto || !get_votes_req(OV_TEAMOVERLAY, true)) { vote_clear(OV_TEAMOVERLAY); cvar_fset("k_teamoverlay", !cvar("k_teamoverlay")); }` (reached via vote_check_teamoverlay at vote.c:1107) -> MATCH
WI-2: n/a (no literal default claimed; k_teamoverlay RegisterCvar world.c:1015 default 0; access class src/commands.c:1034 `CF_PLAYER | CF_SPC_ADMIN` = players + spectator-admins, consistent with player-vote + admin-toggle description)

RESULT | ktx:command:vwep | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Toggle of k_vwep, match-block, silent no-op on !vw_available || !k_allow_vwep, broadcast on/off, and per-player weapon-model refresh all map to ToggleVwep() enforcing lines.
### ktx:command:vwep
- "Toggles the server's visible-weapons feature on or off" -> src/commands.c:8583 `void ToggleVwep(void)` registered src/commands.c:1001 `{ "vwep", ToggleVwep, 0, CF_PLAYER | CF_SPC_ADMIN, CD_VWEP }`; toggle at src/commands.c:8597 `cvar_toggle_msg(self, "k_vwep", redtext("vwep"));` + src/commands.c:8598 `vw_enabled = vw_available && cvar("k_allow_vwep") && cvar("k_vwep");` -> MATCH
- "each player's carried weapon is shown as a 3D model on their in-game model" -> src/commands.c:8598 sets `vw_enabled` (the runtime gate driving vwep model send) consistent with src/world.c:378 `vw_enabled = vw_available && cvar("k_allow_vwep") && cvar("k_vwep");` -> MATCH (clause is a behavioral description of the vwep feature whose enable gate is enforced here; vw_enabled is the feature's own state var)
- "Has no effect while a match is in progress" -> src/commands.c:8587-8590 `if (match_in_progress) { return; }` (first statement, before toggle) -> MATCH
- "silently does nothing if the server lacks vwep model support or k_allow_vwep is disabled" -> src/commands.c:8592-8595 `if (!vw_available || !cvar("k_allow_vwep")) { return; }` (bare `return`, no message = silent; vw_available is the vwep-model-support flag set in world.c:355-356) -> MATCH
- "On a successful toggle it broadcasts an on/off message" -> src/commands.c:8597 `cvar_toggle_msg(self, "k_vwep", redtext("vwep"));` -> cvar_toggle_msg src/g_utils.c:2215 `G_bprint(2, "%s %s %s\n", p->netname, Enables(i), msg);` (G_bprint broadcast, Enables(i)=on/off) -> MATCH
- "immediately refreshes every player's weapon model" -> src/commands.c:8600-8608 `for (p = world; (p = find_client(p));) { if (p->ct == ctPlayer) { self = p; W_SetCurrentAmmo(); } }` (W_SetCurrentAmmo re-sends weapon model per player) -> MATCH
WI-2: n/a (description states no literal default or admin-only claim; registered defaults src/world.c:874-875 `RegisterCvarEx("k_allow_vwep", "0")` / `RegisterCvarEx("k_vwep", "1")` -- "disabled if k_allow_vwep is disabled" is consistent with its 0 default; access class src/commands.c:1001 `CF_PLAYER | CF_SPC_ADMIN` not asserted in prose)

## Wave 07 -- canary ktx:command:autotrack (slot blind), expected C-FIX, returned C-FIX -- GATE 1 PASS. GATE 2 (orchestrator re-grep): k_clan_arena C-NEAR-MISS confirmed (zero isCA()/k_clan_arena in items.c -> 'no item pickups' no enforcing line on CA path) + k_ctf_hookstyle C-FIX confirmed (grapple.c:15 CR_THROW_SPEED 1200 style-4 :449 vs :13 THROW_SPEED 800 classic) + clean k_classic_shotgun (weapons.c:549/717/724).

RESULT | ktx:command:whoskin | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=4 | Every clause maps to PlayerStatusS (commands.c:2400-2421); no match-state guard on the feature path, CF_MATCHLESS is additive.
### ktx:command:whoskin
- "Prints a player-skins list to the caller: one line per connected player" -> src/commands.c:2405-2416 `for (p = world; (p = find_plr(p));) { ... G_sprint(self, 2, "\220%10s\221 %s\n", ezinfokey(p, "skin"), p->netname); }` -> MATCH (find_plr at g_utils.c:1315 iterates connected players; G_sprint targets self=caller)
- "showing that player's 'skin' userinfo value next to their name" -> src/commands.c:2416 `G_sprint(self, 2, "\220%10s\221 %s\n", ezinfokey(p, "skin"), p->netname)` -> MATCH (ezinfokey "skin" then netname on one line)
- "Prints 'no players' if none are connected" -> src/commands.c:2420 `G_sprint(self, 2, "%s\n", (found ? "" : "no players"));` -> MATCH
- "Works regardless of match state" -> src/commands.c:2400-2421 (PlayerStatusS body) + src/commands.c:1078 `if (k_matchLess && !(cmds[icmd].cf_flags & CF_MATCHLESS))` -> MATCH (PlayerStatusS has NO match_in_progress guard, unlike sibling PlayerStatus c:2375 / PlayerStatusN c:2428 which DO; table flag is CF_BOTH|CF_MATCHLESS, CF_MATCHLESS is additive so it does not restrict to matchless)
WI-2: n/a (no default claim; access "to the caller" consistent with CF_BOTH at commands.c:713)

RESULT | ktx:command:wp_reset | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=3 | Wp_Reset (commands.c:4978) memsets self->ps.wpn behind a match_in_progress early-return; ps.wpn hits/attacks feed the weapon-stats display.
### ktx:command:wp_reset
- "Clears the calling player's accumulated per-weapon stats (hits/attacks counters used by the weapon-stats display)" -> src/commands.c:4985 `memset(self->ps.wpn, 0, sizeof(self->ps.wpn));` -> MATCH (ps.wpn is wpType_t[wpMAX] at progs.h:133; wpType_t has int hits c:80 / int attacks c:83; stats.c:86-87 `tmStats[...].wpn[i].hits += p2->ps.wpn[i].hits; ... .attacks += ...` and stats.c:172-176 compute the displayed accuracy from wpn[].hits/.attacks)
- "Only works before a match starts -- it does nothing while a match is in progress" -> src/commands.c:4980-4983 `if (match_in_progress) { return; }` -> MATCH (early return before the memset)
- "Affects only the caller's own stats" -> src/commands.c:4985 `memset(self->ps.wpn, ...)` -> MATCH (operates on self only; cmds[] entry c:828 is CF_PLAYER, consistent with "calling player")
WI-2: n/a (no default claim; player-only consistent with CF_PLAYER at commands.c:828)

RESULT | ktx:cvar:k_clan_arena | C-NEAR-MISS | flavourC=1 | wi2=0 | clauses=6 | Mode/value semantics + full-loadout + round-end-on-elimination + best-of + ghost-on-death + Wipeout variant all trace; but the "no item pickups" sub-clause has NO enforcing line on the CA path (no isCA() gate in any item-touch/spawn site) -- true only as a transitive consequence of the full-spawn loadout / arena maps.
### ktx:cvar:k_clan_arena
- "Selects the round-based arena game mode. 0 = disabled (normal game). ... When nonzero on a team game the server runs the arena round/match flow" -> src/clan_arena.c:295 `qbool isCA(void) { return (isTeam() && cvar("k_clan_arena")); }` driving CA_Frame at world.c:1886 `if (isCA()) { CA_Frame(); }` -> MATCH (nonzero+team => CA flow; 0 => isCA false => normal)
- "1 = Clan Arena ... 2 = Wipeout: a Clan Arena variant" -> src/world.c:1507-1515 `if (cvar("k_clan_arena") == 1) strlcat(mode,"-ca",...); if (cvar("k_clan_arena") == 2) strlcat(mode,"-wo",...);` + clan_arena.c:655 `char* mode = cvar("k_clan_arena") == 2 ? "Wipeout!" : "Clan Arena!";` -> MATCH
- "teams fight rounds with full weapons/armor" -> src/clan_arena.c:534-545 (CA_PutClientInServer, match_in_progress==2) `items |= IT_AXE; ... IT_SHOTGUN; IT_NAILGUN; IT_SUPER_NAILGUN; IT_SUPER_SHOTGUN; IT_ROCKET_LAUNCHER; IT_GRENADE_LAUNCHER; IT_LIGHTNING; IT_ARMOR3; self->s.v.items = items;` + c:523-530 ammo 200/100/50/150, armorvalue 200, armortype 0.8 -> MATCH
- "no item pickups" -> (whole src/ swept) -> UNTRACEABLE (no isCA()/k_clan_arena gate in any item path: health_touch c:276, weapon_touch c:803, armor_touch c:429, ammo_touch c:1185 contain no CA check; items.c references only isRA() c:2407; PlaceItem c:90 / StartItem c:153 / G_CallSpawn g_spawn.c:524 / G_SpawnGEntityFromSpawnVars g_spawn.c:690 have no CA item-inhibit; clan_arena.c performs no item-entity removal. The clause holds only transitively -- players spawn fully equipped so pickups are irrelevant and CA is conventionally played on stripped arena maps -- with NO enforcing line on the CA feature path. Per the scope/condition sharpening rule this is a C-near-miss sub-clause.)
- "a round ends when one team is fully eliminated ... (best-of series ...)" -> src/clan_arena.c:1606-1614 `switch (CA_check_alive_teams(&alive_team)) { case 0: EndRound(0); break; case 1: EndRound(alive_team); break; }` + c:1626 `if ((team1_score >= CA_wins_required()) || (team2_score >= CA_wins_required()))` (CA_wins_required c:284-290 derived from k_clan_arena_rounds = best-of series) -> MATCH
- "ghost-on-death" -> src/clan_arena.c:598-620 `// set to ghost if dead   if (ISDEAD(self)) { ... self->s.v.solid = SOLID_NOT; self->s.v.movetype = MOVETYPE_NOCLIP; ... setmodel(self, ""); ... }` -> MATCH
- "2 = Wipeout ... (additional Wipeout-only round/respawn handling)" -> src/clan_arena.c:127 `qbool isWipeout = (cvar("k_clan_arena") == 2);` + c:615-616 `self->round_deaths++; //increment death count for wipeout   self->in_limbo = (self->ca_ready) && (self->round_deaths <= max_deaths) && self->can_respawn;` (max_deaths = k_clan_arena_max_respawns) + clan_arena.c:879/933/1732/1773 `if (cvar("k_clan_arena") == 2) ... // Only ... wipeout` -> MATCH
WI-2: n/a (description makes no explicit "Default" claim; "0 = disabled" value-semantics is consistent with RegisterCvarEx("k_clan_arena","0") at world.c:983 anyway. No access-class claim.)

RESULT | ktx:cvar:k_classic_shotgun | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=3 | classic_shotgun is passed as send_effects per pellet to TraceAttack (weapons.c:717) and gates Multi_Finish (c:724); damage path is independent of the flag.
### ktx:cvar:k_classic_shotgun
- "1 = each pellet that hits produces its own blood or gunshot impact effect at its precise hit point (classic per-pellet visuals)" -> src/weapons.c:717 `if (g_globalvars.trace_fraction != 1.0) { TraceAttack(4, direction, classic_shotgun); }` + TraceAttack c:402-405 `if (send_effects) { SpawnBlood(org, 1); }` and c:410-419 `if (send_effects) { WriteByte(MSG_MULTICAST, SVC_TEMPENTITY); WriteByte(MSG_MULTICAST, TE_GUNSHOT); WriteByte(MSG_MULTICAST, 1); WriteCoord(... org ...); }` -> MATCH (classic_shotgun truthy => send_effects=true => per-hitting-pellet SpawnBlood/TE_GUNSHOT count 1 at that pellet's org)
- "0 = per-pellet effects are suppressed and a single combined impact effect is emitted for the whole spread" -> src/weapons.c:724-727 `if (!classic_shotgun) { Multi_Finish(); }` + Multi_Finish c:315-329 `if (puff_count) { WriteByte(...TE_GUNSHOT); WriteByte(MSG_MULTICAST, puff_count); WriteCoord(... puff_org ...); } SpawnBlood(puff_org, blood_count);` -> MATCH (classic_shotgun==0 => send_effects=false suppresses the per-pellet blocks; one combined TE_GUNSHOT(puff_count) + one SpawnBlood(blood_count) at puff_org)
- "Damage dealt is identical either way; this changes only the visual feedback of the spread" -> src/weapons.c:723 `ApplyMultiDamage();` (unconditional, before the classic_shotgun branch) + TraceAttack c:399-401 `blood_count = blood_count + 1; ... AddMultiDamage(PROG_TO_EDICT(g_globalvars.trace_ent), damage);` (damage accumulation independent of send_effects) -> MATCH (send_effects gates only SpawnBlood/WriteByte visual calls; damage via AddMultiDamage/ApplyMultiDamage always runs)
WI-2: n/a (no explicit "Default" claim in description; RegisterCvarEx("k_classic_shotgun","1") at world.c:948 = 1 is consistent regardless. No access-class claim.)

RESULT | ktx:cvar:k_ctf_hookstyle | C-FIX | flavourC=1 | wi2=0 | clauses=8 | Styles 1/2/3 and the style-1/2/4 cancel timings and style-1 cooldown all trace exactly; but style 4's "classic-style throw speed" contradicts its enforcing line -- style 4 sets CR_THROW_SPEED=1200 (the fastest), distinct from style 3's actual classic THROW_SPEED=800.
### ktx:cvar:k_ctf_hookstyle
- "1 = smooth: accelerating/decelerating pull speed" -> src/grapple.c:402-412 `if (cvar("k_ctf_hookstyle") == 1) { if (self->hook_pullspeed_accel > 0) self->hook_pullspeed = IncreasePullSpeed(...); else if (self->hook_pullspeed_accel < 0) self->hook_pullspeed = DecreasePullSpeed(...); }` (IncreasePullSpeed c:20 / DecreasePullSpeed c:32, ramp toward PULL_SPEED) -> MATCH
- "1 = ... hook-release cancel delayed ~250ms (anti-spam)" -> src/grapple.c:214-219 `owner->hook_cancel_time += 1; // delay cancelling the hook until ~250ms (13 * 19) if `smooth hook` is enabled (prevent spam attacks)  if (cvar("k_ctf_hookstyle") == 1 && owner->hook_cancel_time > 19) { CancelHook(owner); }` -> MATCH (threshold 19 frames + adjacent comment "~250ms ... prevent spam attacks")
- "1 = ... a shorter (halved) refire cooldown" -> src/grapple.c:64-67 (GrappleReset, style 1) `owner->attack_finished = ... g_globalvars.time + (HOOK_FIRE_RATE / 2); owner->hook_reset_time = ... g_globalvars.time + HOOK_FIRE_RATE;` vs else-branch c:72-73 `owner->attack_finished = g_globalvars.time; owner->hook_reset_time = g_globalvars.time;` -> MATCH (literal HOOK_FIRE_RATE / 2 halving present only on the style-1 path)
- "2 = fast: fixed pull speed" -> src/grapple.c:413-416 `else { self->hook_pullspeed = PULL_SPEED; }` -> MATCH (styles !=1, incl. 2, use fixed PULL_SPEED=800)
- "2 = ... a quick (~80ms) hook-release cancel" -> src/grapple.c:221-224 `if(cvar("k_ctf_hookstyle") == 2 && owner->hook_cancel_time > 6) { CancelHook(owner); }` -> MATCH (6 frames; per the c:215 comment formula ~13ms/frame, 6*13 ~= 78-80ms)
- "3 = classic: original pure-CTF throw speed" -> src/grapple.c:443-446 `if (cvar("k_ctf_hookstyle") == 3) { throwSpeed = THROW_SPEED; }` (THROW_SPEED=800 c:13, the original/base vs NEW_THROW_SPEED=1050 default; file header c:4 "PureCTF changes by Methabol") -> MATCH
- "3 = ... no automatic cancel on release" -> src/grapple.c:212 `if (cvar("k_ctf_hookstyle") != 3) { ... CancelHook ... }` -> MATCH (style 3 excluded from the entire UpdateChain cancel block)
- "4: classic-style throw speed with the hook cancelled immediately on release" -> src/grapple.c:447-449 `else if (cvar("k_ctf_hookstyle") == 4) { throwSpeed = CR_THROW_SPEED; }` (CR_THROW_SPEED=1200 c:15, applied directly at c:477/483 with no further style-4 re-scaling) AND c:226-229 `if (cvar("k_ctf_hookstyle") == 4) { CancelHook(owner); }` -> MISMATCH ("cancelled immediately on release" is correct -- c:226-229 unconditional CancelHook, no frame threshold -- BUT "classic-style throw speed" is wrong: style 4's enforcing line sets CR_THROW_SPEED=1200, the HIGHEST of the three throw constants and distinctly NOT the classic value; style 3's actual classic speed is THROW_SPEED=800. The constant CR_ does not denote "classic" (THROW_SPEED is the original, NEW_THROW_SPEED 1050 the default, CR_THROW_SPEED 1200 a separate faster value). Labelling style 4 "classic-style throw speed" contradicts the enforced 1200 vs the classic 800.)
WI-2: n/a (description makes no explicit "Default" claim -- it lists only 1/2/3/4 semantics; cvar is bare RegisterCvar("k_ctf_hookstyle") at world.c:954 so the registered default is 0/empty, but no clause asserts a default. No access-class claim.)

## Wave 08 -- canary ktx:cvar:k_teamoverlay (slot blind), expected C-NEAR-MISS, returned C-NEAR-MISS -- GATE 1 PASS. GATE 2 (orchestrator re-grep): all batch rows clean; k_ctf_rune_power_str default 2.0 (world.c:957 RegisterCvarEx) + canary recheck: sole !isDuel() is match.c:1639 settings-summary, NOT the client.c ti send path.

RESULT | ktx:cvar:k_ctf_rune_power_hst | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=7 | Spawn-gate, maxspeed mult (value/8)+1, weapon cooldown -value/10 or /20, hook interval / value, default 2.0 -> 1.25x speed all map to enforcing lines.
### ktx:cvar:k_ctf_rune_power_hst
- "CTF runes only" -> src/runes.c:412 `if (cvar("k_ctf_rune_power_hst") > 0)` (inside SpawnRunes, CTF rune subsystem; weapon/grapple effects all gated on `self->ctf_flag & CTF_RUNE_HST`) -> MATCH
- "Scales strength of haste rune and gates whether it spawns" -> src/runes.c:412-415 `if (cvar("k_ctf_rune_power_hst") > 0) { runes[nrunes] = UniqueRuneSpawn(CTF_RUNE_HST, ...) }` -> MATCH
- "value of 0 disables the haste rune entirely (not placed in map)" -> src/runes.c:412 `if (cvar("k_ctf_rune_power_hst") > 0)` -- value must be >0 to call UniqueRuneSpawn; 0 -> not spawned -> MATCH
- "carrier's max running speed multiplied by (value / 8) + 1" -> src/runes.c:306 `other->maxspeed *= (cvar("k_ctf_rune_power_hst") / 8) + 1;` -> MATCH
- "melee/weapon attack cooldowns shortened (by value/10 for most weapons, value/20 for some)" -> src/weapons.c:2109 axe `- (cvar("k_ctf_rune_power_hst") / 10)`, :2148 SG `/10`, :2175 SSG `/20`, :2201 GL `/10`, :2217 RL `/20` (subtracted from attack_finished delay = shortened cooldown; majority /10, some /20) -> MATCH
- "grappling-hook fire interval is divided by the value" -> src/grapple.c:65 `g_globalvars.time + ((HOOK_FIRE_RATE / 2) / cvar("k_ctf_rune_power_hst"))` and :67 `g_globalvars.time + (HOOK_FIRE_RATE / cvar("k_ctf_rune_power_hst"))` -- fire interval divided by value -> MATCH
- "default 2.0 -> speed multiplier is 1.25x" -> src/world.c:960 `RegisterCvarEx("k_ctf_rune_power_hst", "2.0");` and runes.c:306 (2.0/8)+1 = 1.25 -> MATCH
WI-2: n/a (no explicit default-value beyond the verified 2.0; no access-class claim)

RESULT | ktx:cvar:k_ctf_rune_power_str | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Spawn-gate >0, outgoing damage *= (value/2)+1 at the real combat.c apply site, default 2.0 -> x2 all map to enforcing lines.
### ktx:cvar:k_ctf_rune_power_str
- "CTF runes only" -> src/runes.c:406 `if (cvar("k_ctf_rune_power_str") > 0)` (SpawnRunes) + src/combat.c:549 `if (attacker->ctf_flag & CTF_RUNE_STR)` -- CTF rune subsystem -> MATCH
- "Scales strength of strength rune and gates whether it spawns" -> src/runes.c:406-409 `if (cvar("k_ctf_rune_power_str") > 0) { runes[nrunes] = UniqueRuneSpawn(CTF_RUNE_STR, ...) }` -> MATCH
- "value of 0 disables the strength rune entirely (not placed in map)" -> src/runes.c:406 requires `> 0`; 0 -> not spawned -> MATCH
- "damage dealt by a player carrying the strength rune is multiplied by (value / 2) + 1" -> src/combat.c:549-551 `if (attacker->ctf_flag & CTF_RUNE_STR) { damage *= (cvar("k_ctf_rune_power_str") / 2) + 1; }` (real damage-apply site; bot_botstat.c:241 / bot_botweap.c:78 are bot estimation, not enforcement) -> MATCH
- "default 2.0 -> outgoing damage doubled (x2)" -> src/world.c:957 `RegisterCvarEx("k_ctf_rune_power_str", "2.0");` and combat.c:551 (2.0/2)+1 = 2 -> MATCH
WI-2: n/a (default 2.0 verified at world.c:957; no access-class claim)

RESULT | ktx:cvar:k_fbskill_aim_accuracy | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Firing-threshold formula, both-axis fire gate, polarity, 0..45 per-bot clamp, skill auto-derive all map to enforcing lines.
### ktx:cvar:k_fbskill_aim_accuracy
- "Frogbot AI aim tuning: permitted aim-error tolerance when deciding whether to open fire" -> src/bot_botweap.c:150-153 (SetFireButtonBasedOnAngles; value feeds min_angle_error which gates self->fb.firing) -> MATCH
- "min_angle_error = (1 + risk) * risk_factor * (value + 1440 / distance_to_target)" -> src/bot_botweap.c:150 `min_angle_error = (1 + risk) * risk_factor * (self->fb.skill.accuracy + (1440 / rel_dist));` with risk_factor=0.5 (:127) and self->fb.skill.accuracy = cvar (bot_botimp.c:304) -> MATCH
- "bot only shoots when both yaw and pitch angular error (deg) are within that threshold" -> src/bot_botweap.c:153 `self->fb.firing |= (angle_error[0] <= min_angle_error && angle_error[1] <= min_angle_error);` (angle_error from desired_angle - v_angle, fabs, 2 components = yaw/pitch) -> MATCH
- "Higher = wider permitted error / fires sooner; lower = must be aimed more precisely" -> bot_botweap.c:150/153 -- larger accuracy -> larger min_angle_error -> easier to satisfy <= test -> MATCH (polarity)
- "Clamped to 0..45 per bot" -> src/bot_botimp.c:304 `self->fb.skill.accuracy = bound(0, cvar( FB_CVAR_ACCURACY), 45);` (per-bot self->, 0..45) -> MATCH
- "Normally set automatically from the configured bot skill, not by hand" -> src/bot_botimp.c:158/209 `cvar_fset(FB_CVAR_ACCURACY, 45 - min(skill, 10) * 2.25);` -> MATCH
WI-2: n/a (cvar `RegisterCvar(FB_CVAR_ACCURACY)` -> default 0; no explicit default claim; no access-class claim)

RESULT | ktx:cvar:k_fbskill_aim_yaw_scale | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | bound(yaw.min, fabs(raw_yaw_diff)*yaw.scale, yaw.max) per-frame formula, multiply-before-clamp, 0..5 per-bot clamp, skill auto-derive all map to enforcing lines.
### ktx:cvar:k_fbskill_aim_yaw_scale
- "Frogbot AI tuning cvar setting the horizontal (yaw) aim-error growth factor" -> src/bot_botimp.c:318 reads cvar into aim_params[YAW].scale; consumed src/bot_aim.c:351 -> MATCH
- "per-frame aim-randomization step: yaw error = bound(yaw.minimum, fabs(raw_yaw_diff) * yaw.scale, yaw.maximum)" -> src/bot_aim.c:351 `yaw_diff = bound(yaw->minimum, fabs(raw_yaw_diff) * yaw->scale, yaw->maximum);` with yaw = &self->fb.skill.aim_params[YAW] (:325), raw_yaw_diff = anglefix(anglemod(desired_angle[YAW]) - anglemod(angles[YAW])) (:329-330); BotsModifyAimAtPlayerLogic comment ":321 Called after desired angles set", CalculateVolatility "run every frame" (:336-337) -> MATCH
- "value multiplies the raw angular difference between current and desired yaw before clamping" -> bot_aim.c:351 `fabs(raw_yaw_diff) * yaw->scale` evaluated inside bound() -> multiplied before clamp -> MATCH
- "controls how steeply horizontal aim error grows the further off-target it currently is" -> bot_aim.c:351 -- larger raw_yaw_diff (further off) * scale -> larger pre-clamp error; scale = growth steepness -> MATCH
- "Read back per bot clamped to bound(0, value, 5) into self->fb.skill.aim_params[YAW].scale" -> src/bot_botimp.c:318 `self->fb.skill.aim_params[YAW].scale = bound(0, cvar(FB_CVAR_YAW_SCALE), 5);` -> MATCH
- "server normally derives value from bot's aim-skill level; setting the cvar overrides that" -> src/bot_botimp.c:172/223 `cvar_fset(FB_CVAR_YAW_SCALE, RangeOverSkill(aimskill, 5, 2));` (derived from aimskill); cvar `RegisterCvar(FB_CVAR_YAW_SCALE)` (:123) read at :318 so a manual set overrides until next SetAttribs -> MATCH
WI-2: n/a (default 0; no explicit default claim; no access-class claim)

RESULT | ktx:cvar:k_fbskill_goalpredictionerror | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Error term goal_time*value*g_random() added to respawn estimate, polarity (later), 0=perfect, bound(0,value,1) clamp, bot_botgoals.c consumption all map to enforcing lines.
### ktx:cvar:k_fbskill_goalpredictionerror
- "Frogbot AI tuning cvar" -> frogbot skill param read at src/bot_botimp.c:309, consumed src/bot_botgoals.c:148; progs.h:501 `// affects goal travellling error ... randomised.` -> MATCH
- "bot adds goal_time * this_value * random(0..1) onto that goal's saved respawn time" -> src/bot_botgoals.c:147-148 `goal_entity->fb.saved_respawn_time = (goal_entity->fb.goal_respawn_time - g_globalvars.time) + (goal_time * self->fb.skill.prediction_error * g_random());` -- error term goal_time * prediction_error * g_random() (g_random()=0..1) added onto the respawn-derived base, result stored in saved_respawn_time; "onto saved respawn time" is a still-true minor simplification of "+ onto (goal_respawn_time - time) then assigned to saved_respawn_time" -> MATCH
- "higher value makes the bot's notion of when an item respawns more wrong (assuming items take longer to come back)" -> bot_botgoals.c:148 -- addend non-negative (goal_time>=0, prediction_error>=0, g_random()>=0) so larger value -> larger saved_respawn_time -> item perceived to return later -> MATCH (polarity)
- "value of 0 gives a perfect respawn-timing estimate" -> bot_botgoals.c:148 -- prediction_error=0 zeroes the addend, saved_respawn_time = goal_respawn_time - time (no injected error) -> MATCH
- "Read into self->fb.skill.prediction_error clamped with bound(0, value, 1)" -> src/bot_botimp.c:309 `self->fb.skill.prediction_error = bound(0, cvar(FB_CVAR_PREDICTIONERROR), 1);` -> MATCH
- "Consumed in goal respawn-time evaluation in bot_botgoals.c" -> src/bot_botgoals.c:148 (inside goal evaluation loop) -> MATCH
WI-2: n/a (cvar `RegisterCvar(FB_CVAR_PREDICTIONERROR)` -> default 0; auto-derived via bot_botimp.c:162/213 RangeOverSkill(skill,1.0,0.0); no explicit default claim; no access-class claim)

## Wave 09 -- canary ktx:cvar:k_yawnmode (slot blind), expected TRACED-CLEAN, returned TRACED-CLEAN -- GATE 1 PASS. GATE 2 (orchestrator re-grep): all-clean wave; k_freshteams enforcing sites items.c:809/812/854 confirmed.

RESULT | ktx:cvar:k_fbskill_movement | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=4 | Registration, clamped read into self->fb.skill.movement, and ApplyPhysics() consumption all map to enforcing lines (bot_botimp.c:142/350, bot_movement.c:126/132/217).
### ktx:cvar:k_fbskill_movement
- "Read into self->fb.skill.movement clamped with bound(0, value, 1.0)" -> src/bot_botimp.c:350 `self->fb.skill.movement = bound(0, cvar(FB_CVAR_MOVEMENT_SKILL), 1.0f);` -> MATCH (FB_CVAR_MOVEMENT_SKILL = "k_fbskill_movement" at bot_botimp.c:48; registered bot_botimp.c:142 RegisterCvar)
- "the bot's per-frame air/ground movement application reads this as movement_skill (clamped 0..1)" -> src/bot_movement.c:132 `float movement_skill = bound(0, self->fb.skill.movement, 1.0);` -> MATCH (re-clamped 0..1 inside ApplyPhysics)
- "Consumed by ApplyPhysics() in bot_movement.c" -> src/bot_movement.c:126 `static void ApplyPhysics(gedict_t *self)` -> MATCH (movement_skill used at line 217)
- "uses it to scale how well the bot executes its intended velocity (lower = sloppier strafe/air control, higher = cleaner movement)" -> src/bot_movement.c:217 `used_numerator = min_numerator + movement_skill * (max_numerator - min_numerator);` -> MATCH (interpolates the direction-optimisation numerator between ground 319/281.6 and air 29/-8.4 by movement_skill in Step 2 "change direction to maximise acceleration in desired direction"; air/ground split confirmed at lines 215-216)
WI-2: n/a (no default-value or access-class claim; "Server-managed" not asserted in this row)

RESULT | ktx:cvar:k_fbskill_vol_oppdir_incr | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Increment formula with /2 halving, dot-product-of-normalized-velocities derivation, clamped read, and cvar_fset server-management all map to enforcing lines.
### ktx:cvar:k_fbskill_vol_oppdir_incr
- "reads it clamped to bound(0, value, 5.0) into self->fb.skill.enemydirection_volatility" -> src/bot_botimp.c:339 `self->fb.skill.enemydirection_volatility = bound(\n\t\t\t0, cvar(FB_CVAR_ENEMYDIRECTION_VOLATILITY_INCREASE), 5.0f);` -> MATCH (macro = "k_fbskill_vol_oppdir_incr" bot_botimp.c:43; registered bot_botimp.c:140)
- "Volatility is increased by (1 - same_direction) * (enemydirection_volatility / 2) ... note the configured value is halved before use" -> src/bot_aim.c:279 `volatility += (1 - same_direction) * (self->fb.skill.enemydirection_volatility / 2);` -> MATCH (the /2 halving is in-code)
- "same_direction is the dot product of the normalized bot and enemy velocity vectors" -> src/bot_aim.c:275 `same_direction = DotProduct(bot_direction, enemy_direction);` -> MATCH (bot_direction=self velocity bot_aim.c:253, enemy_direction=opponent velocity 254, both VectorNormalize'd at 273-274)
- "zero when both move the same way and grows as directions diverge" -> src/bot_aim.c:278 `// Direction penalty ... if we're going in same direction, no penalty` -> MATCH (same dir => same_direction~=1 => (1-same_direction)~=0; divergence increases the term)
- "Server-managed: derived from bot skill via cvar_fset in setSkillAttributes()/setSkillAttributesEasySkillMode()" -> src/bot_botimp.c:192 `cvar_fset(FB_CVAR_ENEMYDIRECTION_VOLATILITY_INCREASE, RangeOverSkill(skill, 0.6f, 0.4f));` -> MATCH (line 192 inside setSkillAttributes 156-206; identical call at line 243 inside setSkillAttributesEasySkillMode 207-270)
WI-2: n/a (no default-value or access-class claim; "Server-managed" verified above, not a metadata-precision item)

RESULT | ktx:cvar:k_fbskill_vol_pain_incr | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | "volatility += pain_volatility", the <1.0s last-hurt window, the !lgc_enabled() gate, clamped read, and cvar_fset server-management all map to enforcing lines.
### ktx:cvar:k_fbskill_vol_pain_incr
- "reads it clamped to bound(0, value, 2.0) into self->fb.skill.pain_volatility" -> src/bot_botimp.c:343 `self->fb.skill.pain_volatility = bound(0, cvar(FB_CVAR_PAIN_VOLATILITY_INCREASE), 2.0f);` -> MATCH (macro = "k_fbskill_vol_pain_incr" bot_botimp.c:44; registered bot_botimp.c:151)
- "volatility INCREMENT added to the running aim-volatility scalar ... (volatility += pain_volatility)" -> src/bot_aim.c:285 `volatility += self->fb.skill.pain_volatility;` -> MATCH
- "applied while the bot was hurt within the last 1.0 second" -> src/bot_aim.c:283 `if (!lgc_enabled() && self->fb.last_hurt && ((g_globalvars.time - self->fb.last_hurt) < 1.0f))` -> MATCH (< 1.0f window on last_hurt)
- "and only when LGC mode is not enabled" -> src/bot_aim.c:283 `if (!lgc_enabled() && ...)` -> MATCH (lgc_enabled() = `cvar(LGCMODE_VARIABLE) != 0` at src/commands.c:9432; penalty applied only when !lgc_enabled())
- "Server-managed: derived from bot skill via cvar_fset in setSkillAttributes()/setSkillAttributesEasySkillMode()" -> src/bot_botimp.c:193 `cvar_fset(FB_CVAR_PAIN_VOLATILITY_INCREASE, RangeOverSkill(skill, 0.5f, 0.1f));` -> MATCH (line 193 inside setSkillAttributes 156-206; identical call line 244 inside setSkillAttributesEasySkillMode 207-270)
WI-2: n/a (no default-value or access-class claim; "Server-managed" verified above)

RESULT | ktx:cvar:k_freshteams | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=8 | dmm1-only force-disable, k_freshteams_weapon_time-vs-30 weapon respawn, the four sub-options' k_freshteams gating, and the scoreboard "FreshTeams on" all map to enforcing lines; registered default "0".
### ktx:cvar:k_freshteams
- "Master toggle for FreshTeams mode (only valid in dmm1; it is force-disabled if deathmatch is not 1)" -> src/world.c:1770 `if (cvar("k_freshteams") && deathmatch != 1)` then world.c:1772 `cvar_fset("k_freshteams", 0); // freshteams only in dmm1` -> MATCH (force-disabled when deathmatch != 1)
- "picked-up weapons respawn on a short timer (k_freshteams_weapon_time seconds instead of the normal 30)" -> src/items.c:812 `int weapon_time = k_freshteams ? cvar("k_freshteams_weapon_time") : 30;` -> MATCH (also items.c:1355 nextthink = time + cvar("k_freshteams_weapon_time"))
- "the FreshTeams sub-options become available -- limited backpack ammo (FreshPacks)" -> src/commands.c:7641 `if (!k_freshteams) { G_sprint(self, 2, "FreshPacks requires FreshTeams (/fresh)\n"); return; }` (toggles k_freshteams_limit_packs commands.c:7648) -> MATCH
- "limited weapon ammo on sweep (FreshGuns)" -> src/commands.c:7661 `if (!k_freshteams) { G_sprint(self, 2, "FreshGuns requires FreshTeams (/fresh)\n"); return; }` (toggles k_freshteams_limit_sweep_ammo commands.c:7668; consumed items.c:854/873/892/911/938/957) -> MATCH
- "fast ammo" -> src/items.c:1189 `qbool freshteams_fast_ammo = (cvar("k_freshteams") && cvar("k_freshteams_fast_ammo"));` -> MATCH (fast-ammo path requires k_freshteams)
- "and timed weapon respawns" -> src/commands.c:7682 `if (!k_freshteams) { G_sprint(self, 2, "FreshTime requires FreshTeams (/fresh)\n"); }` (ToggleFreshTime sets k_freshteams_weapon_time commands.c:7691/7696/7700) -> MATCH
- "The scoreboard shows 'FreshTeams on'" -> src/match.c:1748 `if (cvar("k_freshteams"))` then match.c:1750 `strlcat(text, va("%s %2s\n", "&c07fFreshTeams&r", redtext("on")), sizeof(text));` -> MATCH (literal "FreshTeams" + "on" in scoreboard text)
- "0 = off, 1 = on" -> src/world.c:894 `RegisterCvarEx("k_freshteams", "0");` -> MATCH (registered default "0"; used as boolean via `if (cvar("k_freshteams"))` throughout)
WI-2: n/a (registered default "0" matches "0 = off" framing; no admin/player/spectator access-class claim - description does not assert the /fresh command's access class)

RESULT | ktx:cvar:k_freshteams_fast_ammo | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | The k_freshteams AND k_freshteams_fast_ammo gate, the nextthink override to k_freshteams_weapon_time in ammo_touch(), and the standard 30 / 15-for-dmm3-5 fallback all map to enforcing lines; registered default "0".
### ktx:cvar:k_freshteams_fast_ammo
- "when enabled (1) and k_freshteams is active" -> src/items.c:1189 `qbool freshteams_fast_ammo = (cvar("k_freshteams") && cvar("k_freshteams_fast_ammo"));` -> MATCH (requires BOTH cvars)
- "picked-up ammo boxes respawn after k_freshteams_weapon_time seconds instead of the normal item respawn time" -> src/items.c:1354 `self->s.v.nextthink = g_globalvars.time + cvar("k_freshteams_weapon_time");` (inside `if (freshteams_fast_ammo)` items.c:1353, within ammo_touch() items.c:1186) -> MATCH
- "making ammo respawn on the same cadence as weapons" -> src/items.c:1352 `// If playing freshteams and fast_ammo is enabled, set ammo respawn time same as weapons` -> MATCH (same cvar k_freshteams_weapon_time that weapons use at items.c:812/1355)
- "0 = ammo uses the standard respawn timing (30 seconds, or 15 in deathmatch 3/5)" -> src/items.c:1341 `self->s.v.nextthink = g_globalvars.time + 30;` and items.c:1349 `self->s.v.nextthink = g_globalvars.time + 15;` (inside `if ((deathmatch == 3) || (deathmatch == 5))` items.c:1347) -> MATCH (default 30, 15 for dmm3/5; override at 1354 skipped when freshteams_fast_ammo false)
- "1 = ammo respawn time is overridden to k_freshteams_weapon_time seconds" -> src/items.c:1354 `self->s.v.nextthink = g_globalvars.time + cvar("k_freshteams_weapon_time");` -> MATCH
- "Has no effect unless k_freshteams is set" -> src/items.c:1189 `(cvar("k_freshteams") && cvar("k_freshteams_fast_ammo"))` -> MATCH (k_freshteams=0 => freshteams_fast_ammo false => override branch items.c:1353 skipped, so this cvar alone is inert)
WI-2: n/a (registered default "0" world.c:896 matches "0 = ..." framing; no admin/player/spectator access-class claim)

## Wave 10 -- canary ktx:command:autotrack (slot blind), expected C-FIX, returned C-FIX -- GATE 1 PASS. GATE 2 (orchestrator re-grep): all batch rows clean; k_lock_hdp force-to-100 g_utils.c:1662 + __k_ls ring ++k_ls % MAX_LASTSCORES(30) commands.c:6961 / g_local.h:119 confirmed.

RESULT | ktx:cvar:k_freshteams_limit_sweep_ammo | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Every clause maps to the gated weapon-touch sweep/full ammo branches in items.c.
### ktx:cvar:k_freshteams_limit_sweep_ammo
- "Fresh Teams (dmm1) only: when enabled (1) and k_freshteams is active ... grants only the small configured 'sweep' ammo" -> src/items.c:854 `if (k_freshteams && limit_sweep_ammo && ((int)other->s.v.items & IT_NAILGUN))` then :856 `other->s.v.ammo_nails += cvar("k_freshteams_sweep_ng_ammo");` -> MATCH (same gated pattern for SNG :873/:875, SSG :892/:894, RL :911/:913, GL :938/:940, LG :957/:959)
- "instead of the standard full ammo bundle that a fresh weapon pickup gives ... 0 = ... normal full ammo (e.g. 30 nails, 5 rockets, 15 cells)" -> src/items.c:860 `other->s.v.ammo_nails += 30;` (else branch); :898 `+= 5` shells SSG; :917 `+= 5` rockets RL; :944 `+= 5` rockets GL; :963 `other->s.v.ammo_cells += 15;` LG -> MATCH (30 nails / 5 rockets / 15 cells all present in else branches)
- "1 = re-picking awards only the reduced per-weapon sweep amount" -> src/items.c:856 `other->s.v.ammo_nails += cvar("k_freshteams_sweep_ng_ammo");` (and per-weapon sweep cvars registered world.c:903-908) -> MATCH
- "Picking up a weapon you do not yet own always gives the full amount regardless" -> src/items.c:854 gate includes `((int)other->s.v.items & IT_NAILGUN)`; when weapon bit absent the branch is skipped and :860 else `+= 30` runs -> MATCH
- "Has no effect unless k_freshteams is set" -> src/items.c:854 `if (k_freshteams && limit_sweep_ammo && ...)` -- k_freshteams is the first conjunct of every sweep branch -> MATCH
WI-2: n/a (no default-value or access-class claim; registered default world.c:902 = "1", consistent with the "(1)=enabled" framing)

RESULT | ktx:cvar:k_freshteams_pack_cells | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Clamp + fresh_packs gate located in DropBackpack; excess cells dropped by bound().
### ktx:cvar:k_freshteams_pack_cells
- "Fresh Teams (dmm1) only: the maximum number of cells a dropped backpack may carry" -> src/items.c:2839 `item->s.v.ammo_cells = bound(0, item->s.v.ammo_cells, cvar("k_freshteams_pack_cells"));` -> MATCH (upper bound of the dropped pack's cell count)
- "when backpack ammo limiting is active (k_freshteams set and k_freshteams_limit_packs enabled)" -> src/items.c:2672 `qbool fresh_packs = (cvar("k_freshteams") && cvar("k_freshteams_limit_packs"));` gating :2834 `if (fresh_packs)` -> MATCH
- "The dropped pack's cell count is clamped to the range 0..this value" -> src/items.c:2839 `bound(0, item->s.v.ammo_cells, cvar("k_freshteams_pack_cells"))` -> MATCH (lower 0, upper = cvar)
- "any cells the dead player carried beyond this ceiling are not transferred to the pack" -> src/items.c:2822 `item->s.v.ammo_cells = self->s.v.ammo_cells;` (full carry) then :2839 clamp to ceiling -> MATCH (excess discarded)
- "Units are cells (ammo count)" -> src/items.c:2839 operates on `ammo_cells` -> MATCH
- "Has no effect unless k_freshteams and k_freshteams_limit_packs are both set" -> src/items.c:2672 + :2834 fresh_packs gate -> MATCH
WI-2: n/a (no default-value or access-class claim; registered default world.c:901 = "10", not contradicted)

RESULT | ktx:cvar:_k_last_cycle_map | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Resume-from-stored-index read and conditional 1-based write located in SelectMapInCycle.
### ktx:cvar:_k_last_cycle_map
- "Internal-state cvar (not set from config)" -> src/world.c:780 `RegisterCvar("_k_last_cycle_map");  // internal usage, name of last map in map cycle,` -> MATCH (bare RegisterCvar = default empty/0; comment confirms internal usage)
- "Stores the 1-based map-cycle index of the most recently selected map cycle entry" -> src/maps.c:690 `cvar_fset("_k_last_cycle_map", i);` where i = IsMapInCycle(buf); src/maps.c:577 `return (i + 1); // i may be 0, so returning i + 1` -> MATCH (1-based; buf is the chosen next map)
- "When choosing the next map, KTX resumes the map cycle from this index" -> src/maps.c:651 `if (!(i = cvar("_k_last_cycle_map")))` -- non-zero stored value seeds i, while loop :659 starts there -> MATCH
- "so that if a vote jumped to a map that is not in the cycle, the cycle continues from where it left off rather than restarting" -> src/maps.c:651-657 the IsMapInCycle(mapname) re-derive fallback runs ONLY when _k_last_cycle_map is 0; when previously set the stored index is used directly -> MATCH (rationale grounded in the :651 resume + :688 conditional-write structure)
- "It is rewritten whenever the chosen next map is found within the cycle" -> src/maps.c:688 `if ((i = IsMapInCycle(buf))) { cvar_fset("_k_last_cycle_map", i); }` -- written only when buf is in the cycle -> MATCH
WI-2: n/a (not-set-from-config: bare RegisterCvar default 0; if(!(i=cvar)) treats 0/empty as unset; consistent)

RESULT | ktx:cvar:k_lock_hdp | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Force-100 in GetHandicap, refusal in SetHandicap, admin out-of-match toggle in hdptoggle.
### ktx:cvar:k_lock_hdp
- "Locks player handicap, disallowing the handicap feature" -> src/g_utils.c:1662 + :1674 (overall behavior, both enforcing sites below) -> MATCH
- "0 = handicap allowed (players may set a handicap value)" -> src/g_utils.c:1662 `return (cvar("k_lock_hdp") ? 100 : hdc);` (false -> returns bounded player hdc); :1674 refusal skipped, :1681 `p->ps.handicap = nhdc;` proceeds -> MATCH
- "non-zero = handicap locked: every player's effective handicap is forced to 100 (neutral)" -> src/g_utils.c:1662 `return (cvar("k_lock_hdp") ? 100 : hdc);` -- non-zero returns 100 for every player -> MATCH
- "and the handicap command is refused with 'handicap changes are not allowed'" -> src/g_utils.c:1674-1678 `if (cvar("k_lock_hdp")) { G_sprint(self, 2, "%s changes are not allowed\n", redtext("handicap")); return false; }` -> MATCH (renders "handicap changes are not allowed")
- "Toggled out of a match by the corresponding admin command" -> src/commands.c:5198 `if (match_in_progress) { return; }` then :5203 `trap_cvar_set_float("k_lock_hdp", !cvar("k_lock_hdp"));`; command-table src/commands.c:835 `{ "hdptoggle", hdptoggle, 0, CF_BOTH_ADMIN, ... }` -> MATCH (CF_BOTH_ADMIN = admin; match_in_progress guard = out-of-match only)
WI-2: n/a (registered bare RegisterCvar world.c:801 default 0, consistent with "0 = allowed" baseline; admin class CF_BOTH_ADMIN verified in command table, out-of-match enforced in handler)

RESULT | ktx:cvar:__k_ls | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Ring write-cursor advance (++k_ls % 30) in lastscore_add and oldest->newest read in lastscores located.
### ktx:cvar:__k_ls
- "Internal write cursor for the server's stored match-results history (the lastscores / lastscoresktx ring buffer)" -> src/world.c:1036 `RegisterCvar("__k_ls");  // current lastscore, really internal mod usage`; src/commands.c:899-900 both "lastscores" and "lastscoresktx" map to handler lastscores -> MATCH (adjacent comment confirms internal)
- "Holds the index of the next slot in a fixed 30-entry ring (MAX_LASTSCORES = 30)" -> include/g_local.h:119 `#define MAX_LASTSCORES (30)`; src/commands.c:6961 `cvar_fset("__k_ls", ++k_ls % MAX_LASTSCORES);` stores the next slot after writing slot k_ls -> MATCH
- "after a match result is recorded the value advances to (index + 1) modulo 30, overwriting the oldest entry" -> src/commands.c:6961 `cvar_fset("__k_ls", ++k_ls % MAX_LASTSCORES);` (++k_ls = index+1, % 30); write at :6950/:6957 `cvar_set(va("__k_ls_s_%d", k_ls), ...)` overwrites the slot the read loop treats as oldest -> MATCH
- "the lastscores command reads it as the ring's start point to print recorded matches oldest-to-newest" -> src/commands.c:6986 `int k_ls = bound(0, cvar("__k_ls"), MAX_LASTSCORES - 1);` then :7002 `for (j = k_ls, cnt = i = 0; i < MAX_LASTSCORES; i++, j = (j + 1 >= MAX_LASTSCORES) ? 0 : j + 1)` -- starts at next-write slot (oldest) and walks forward -> MATCH
- "Integer 0-29" -> src/commands.c:6801 + :6986 `bound(0, cvar("__k_ls"), MAX_LASTSCORES - 1)` = bound(0,...,29); write :6961 `% MAX_LASTSCORES` keeps 0..29 -> MATCH
- "not intended for manual setting" -> src/world.c:1036 bare `RegisterCvar("__k_ls");  // ... really internal mod usage` -> MATCH (adjacent comment)
WI-2: n/a (bare RegisterCvar = default 0/empty; no specific "Default X" value claim and no access-class claim; consistent)

## Wave 11 -- canary ktx:cvar:k_teamoverlay (slot blind), expected C-NEAR-MISS, returned C-NEAR-MISS -- GATE 1 PASS. GATE 2 (orchestrator re-grep): k_on_end_f_ruleset C-FIX confirmed (match.c:285 fn-local f_ruleset_done, :411-414 set true after FIRST qualifying player -> first player only, once/match) + clean k_minrate (world.c:880 bare RegisterCvar default 0, :1559 bound).

RESULT | ktx:cvar:k_matchless_countdown | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=4 | Matchless-scope gate, countdown-suppression, and announcement-suppression all map to enforcing lines (match.c:1294, 2460-2466) with confirming adjacent comment.
### ktx:cvar:k_matchless_countdown
- "Only effective in matchless mode" -> src/match.c:2460 `else if (k_matchLess)` (suppression branch gated on k_matchLess) + src/match.c:1294 `if (!k_matchLess || cvar("k_matchless_countdown"))` -> MATCH (both effects gated on k_matchLess; non-matchless never suppressed)
- "When 0, no pre-game countdown runs in matchless mode" -> src/match.c:2462-2464 `if (!cvar("k_matchless_countdown")) { timer->cnt2 = 0; // no countdown if variable is not specified. }` -> MATCH (adjacent comment confirms)
- "the \"The match has begun!\" announcement is suppressed" -> src/match.c:1294-1297 `if (!k_matchLess || cvar("k_matchless_countdown")) G_bprint(2, "%s\n", redtext("The match has begun!"));` -> MATCH (when k_matchLess && cvar==0, print skipped)
- "When non-zero, the normal countdown timer and that announcement run even though the server is matchless" -> src/match.c:2460-2466 (suppression body skipped when cvar non-zero, so timer->cnt2 keeps max(3,k_count)) + src/match.c:1294 (print runs) -> MATCH
WI-2: n/a

RESULT | ktx:cvar:k_minrate | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | Minimum-rate floor, below-floor force-set message, 0-disables, connect/spectator/rate-change paths, and sv_maxrate coexistence all map to enforcing lines (client.c:96-116, world.c:1739-1746).
### ktx:cvar:k_minrate
- "Server-enforced minimum network rate, in bytes per second" -> src/client.c:96 `minrate = cvar("k_minrate");` + src/client.c:88 `player_rate = atof(... ezinfokey(p, "rate") ...)` + src/client.c:109 `if (player_rate < minrate)` -> MATCH (compared directly against client `rate` infokey value; bytes/sec is the standard QW rate unit used as-is here)
- "a connecting player or spectator's client `rate`" -> src/client.c:1735 `CheckRate(self, "");` (player connect path) + src/spectate.c:194 `CheckRate(self, "");` (spectator path) + src/g_userinfo.c:277 `return CheckRate(self, arg_2);` (rate change) -> MATCH
- "below this value is told its rate is too low and is force-set up to this minimum" -> src/client.c:109-115 `if (player_rate < minrate) { G_sprint(p, 2, "\nYour %s setting is too low ...", redtext("rate"), (int)minrate); stuffcmd_flags(p, ..., "rate %d\n", (int)minrate); }` -> MATCH
- "0 disables the floor (no minimum enforced)" -> src/client.c:98 `if (maxrate || minrate)` + src/client.c:109 `if (player_rate < minrate)` -> MATCH (minrate==0: gate-only-on-maxrate, and no player_rate<0; registered default is 0 via bare RegisterCvar("k_minrate") at world.c:880)
- "Works alongside sv_maxrate, which caps the upper bound" -> src/client.c:95,100-106 (maxrate handled in same CheckRate) + src/world.c:1739-1746 `if (k_minr > k_maxr) k_minr = k_maxr; ... cvar_fset("k_minrate", k_minr);` -> MATCH
WI-2: n/a

RESULT | ktx:cvar:k_on_end_f_ruleset | C-FIX | flavourC=1 | wi2=0 | clauses=5 | "every player / once per player" is WRONG: function-scoped f_ruleset_done guard (match.c:285,411-415) sends "say f_ruleset" to only the FIRST player once per match-end, not per player.
### ktx:cvar:k_on_end_f_ruleset
- "When set (non-zero) and the match has a matchtag assigned" -> src/match.c:411 `if (has_matchtag && cvar("k_on_end_f_ruleset") && !f_ruleset_done)` + src/match.c:287 `qbool has_matchtag = matchtag != NULL && matchtag[0];` -> MATCH
- "every player is automatically made to issue \"say f_ruleset\" once" / "non-zero = sent once per player at match end" -> src/match.c:285 `qbool ... f_ruleset_done = false` (function scope) + src/match.c:402-415 `for (p = world; (p = find_plr(p));) { ... if (has_matchtag && cvar("k_on_end_f_ruleset") && !f_ruleset_done) { stuffcmd(p, "say f_ruleset\n"); f_ruleset_done = true; } }` -> MISMATCH (f_ruleset_done is set true after the FIRST matching player and never reset in the loop, so the command is sent to exactly ONE player, once per match-end -- NOT "every player" / "once per player"; loop additionally gated by `if (old_match_in_progress == 2)` at match.c:400)
- "0 = no automatic f_ruleset at match end" -> src/match.c:411 (cvar==0 makes condition false) -> MATCH
- "No effect on matches without a matchtag" -> src/match.c:411 `has_matchtag &&` short-circuit -> MATCH
- "Default 1" -> src/world.c:808 `RegisterCvarEx("k_on_end_f_ruleset", "1");` -> MATCH (registered default 1)
WI-2: n/a

RESULT | ktx:cvar:k_pow_check_time | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Interval-seconds use, k_pow_min_players gate, 0-999 bound, 0->10s default, and matchless/min-players no-effect all map to enforcing lines in Get_Powerups (g_utils.c:1786-1816) with confirming comment.
### ktx:cvar:k_pow_check_time
- "In matchless deathmatch mode, the interval in seconds between the server's re-checks" -> src/g_utils.c:1791 `if (!k_pow_new || !k_matchLess || !k_pow_min_players || !deathmatch) return ...` + src/g_utils.c:1798-1800 `if (k_pow_check > g_globalvars.time) return k_pow;` + src/g_utils.c:1816 `k_pow_check = g_globalvars.time + k_pow_check_time;` -> MATCH (value added to time as seconds; re-check only when k_matchLess && deathmatch)
- "the player-count gate is k_pow_min_players" -> src/g_utils.c:1786 `int k_pow_min_players = bound(0, cvar("k_pow_min_players"), 999);` + src/g_utils.c:1809/1813 `CountPlayers() < k_pow_min_players ? 0 : k_pow_new` -> MATCH
- "Bounded to 0-999" -> src/g_utils.c:1787 `int k_pow_check_time = bound(0, cvar("k_pow_check_time"), 999);` -> MATCH
- "a value of 0 means use the built-in default of 10 seconds" -> src/g_utils.c:1789 `k_pow_check_time = !k_pow_check_time ? 10 : k_pow_check_time; // default is 10` -> MATCH (adjacent comment confirms; registered default 0 via bare RegisterCvar at world.c:817)
- "Has no effect outside matchless mode" -> src/g_utils.c:1791 `!k_matchLess` early-return before line 1816 (where k_pow_check_time is consumed) -> MATCH
- "or when k_pow_min_players is 0" -> src/g_utils.c:1791 `!k_pow_min_players` early-return before line 1816 -> MATCH
WI-2: n/a

RESULT | ktx:cvar:k_pow_r | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Ring=invisibility type-switch, hide+no-pickup at 0, no drop-on-death at 0, global-k_pow gate, and off/on/subset state report all map to enforcing lines (items.c:111,1989,2036; g_utils.c:1740-1775; world.c:814).
### ktx:cvar:k_pow_r
- "Per-type switch for the Ring of Shadows (invisibility) powerup" -> src/world.c:814 `RegisterCvarEx("k_pow_r", "1"); // ring` + all read-sites gate on IT_INVISIBILITY (src/items.c:111, src/items.c:2036) -> MATCH
- "0 = ring entities are hidden and cannot be picked up" -> src/items.c:109-118 (PlaceItem) `if (!Get_Powerups() || (((int)self->s.v.items & IT_INVISIBILITY) && !cvar("k_pow_r")) || ...) { self->model = ""; self->s.v.solid = SOLID_NOT; }` + src/items.c:2036-2041 (touch handler) same condition `-> return;` -> MATCH (hidden+non-solid, and pickup aborted)
- "and a held ring is not dropped on death" -> src/items.c:1989-1994 (DropPowerups) `if (cvar("dr") && Get_Powerups() && cvar("k_pow_r")) { ... DropPowerup(self->invisible_finished - g_globalvars.time, IT_INVISIBILITY); }` -> MATCH (k_pow_r==0 skips the IT_INVISIBILITY drop)
- "1 = ring enabled" -> src/world.c:814 default "1" + inverted conditions above (not hidden, pickup allowed, dropped) -> MATCH
- "Only takes effect while powerups are globally enabled (see k_pow)" -> src/items.c:111 / src/items.c:2036 `if (!Get_Powerups() || (IT_INVISIBILITY && !k_pow_r) || ...)` (Get_Powerups returns 0 when !cvar("k_pow"); the global gate dominates via the OR so per-type differential only matters while globally enabled) -> MATCH
- "the per-type switches together determine whether the powerup state reports as 'off', 'on', or a partial subset" -> src/g_utils.c:1740-1775 `if (!cvar("k_pow") || (!cvar("k_pow_q") && !cvar("k_pow_p") && !cvar("k_pow_r") && !cvar("k_pow_s"))) strlcpy(str,"off",...); ... if (cvar("k_pow_q") && cvar("k_pow_p") && cvar("k_pow_r") && cvar("k_pow_s")) strlcpy(str,"on",...); ... if (cvar("k_pow_r")) strlcat(str,"r",...)` -> MATCH
WI-2: n/a

## Wave 12 -- canary ktx:cvar:k_yawnmode (slot blind), expected TRACED-CLEAN, returned TRACED-CLEAN -- GATE 1 PASS. GATE 2 (orchestrator re-grep): all-clean wave; k_tp_tele_death gate client.c:5347 + RACE_PACEMAKER_TRAIL_COUNT 12 race.c:40 confirmed.

RESULT | ktx:cvar:k_race_pace_legal | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=4 | blocked_record gate at race.c:1341 enforces 0=blocked / 1=allowed; pacemaker-off scope enforced by the && short-circuit on the feature's own path.
### ktx:cvar:k_race_pace_legal
- "Controls whether a run completed while the pacemaker ghost is active counts as a legal record" -> src/race.c:1341 `qbool blocked_record = race_pacemaker_enabled() && !cvar(RACE_PACEMAKER_LEGAL_RECORD);` (RACE_PACEMAKER_LEGAL_RECORD=="k_race_pace_legal") -> MATCH
- "When 0, runs made with the pacemaker enabled are blocked from being saved as a record" -> src/race.c:1420 `if (!blocked_record)` gates the record-save block + src/race.c:1553 `race_finish_capture(best_time_position == 0 && !blocked_record, ...)` -> MATCH (cvar 0 => `!cvar` true => blocked_record true when pacemaker enabled => save path skipped)
- "when 1, such runs are allowed to set records" -> src/race.c:1341 `!cvar(...)` false when value 1 => blocked_record false => src/race.c:1420 `if (!blocked_record)` true => record saved -> MATCH
- "Only relevant while the pacemaker is enabled; with the pacemaker off, records are unaffected" -> src/race.c:1341 `race_pacemaker_enabled() && !cvar(...)`; race_pacemaker_enabled() (src/race.c:4482 `return cvar(RACE_PACEMAKER_ENABLED_CVAR) && guide.capture.position_count;`) false => blocked_record false regardless of k_race_pace_legal -> MATCH (scope enforced by && short-circuit on the feature's own line, not transitive)
WI-2: n/a (RegisterCvarEx default "0" at src/world.c:920 consistent with described 0-behavior; no explicit default-value or access-class claim)

RESULT | ktx:cvar:k_race_pace_resolution | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | bound(0,cvar,3) clamp + 12*resolution trail span + `if (resolution)` no-trail gate + jumps `&& resolution` gate all map exactly; "in each direction" phrasing is traceable minor vagueness.
### ktx:cvar:k_race_pace_resolution
- "Sets the length/density of the pacemaker ghost's visible trail" -> src/race.c:4851 `guide_start = max(0, guide.position - RACE_PACEMAKER_TRAIL_COUNT * resolution);` + src/race.c:4867 `min(guide_start + resolution * RACE_PACEMAKER_TRAIL_COUNT, ...)` + src/race.c:4875 `for (i = guide_start; i < guide_end; i += resolution, ++num)` -> MATCH (resolution scales trail span and sampling stride)
- "value is read as an integer clamped to 0-3" -> src/race.c:4845 `int resolution = (int)bound(RACE_PACEMAKER_RESOLUTION_MIN, cvar(RACE_PACEMAKER_RESOLUTION_CVAR), RACE_PACEMAKER_RESOLUTION_MAX);` (MIN=0 src/race.c:36, MAX=3 src/race.c:37) -> MATCH (exact: int cast, bound 0..3)
- "0 draws no trail (pacemaker shown without a trail)" -> src/race.c:4848 `if (resolution)` gates the entire lightning-trail draw block -> MATCH (resolution==0 => trail loop skipped)
- "each higher step extends the trail by 12 recorded ghost positions in each direction around the ghost's current position" -> src/race.c:4851 `guide.position - RACE_PACEMAKER_TRAIL_COUNT * resolution` (back) + src/race.c:4867 `guide_start + resolution * RACE_PACEMAKER_TRAIL_COUNT` (forward), RACE_PACEMAKER_TRAIL_COUNT==12 (src/race.c:40) -> MATCH (12-per-step magnitude exact; "in each direction around ghost current position" is loose -- window is [guide.position - 12*res ... capped forward at guide.position] -- but the 12-per-step constant and progressive-length behavior come directly from the enforcing constant, not a name/string; traceable minor vagueness)
- "so 1, 2, 3 give progressively longer trails" -> src/race.c:4851/4867 `12 * resolution` grows monotonically with resolution -> MATCH
- "active trail length also gates whether jump markers (k_race_pace_jumps) are shown" -> src/race.c:4902 `qbool jumps_enabled = cvar(RACE_PACEMAKER_JUMPS_CVAR) && resolution;` -> MATCH (jump markers require resolution nonzero, i.e. trail active)
WI-2: n/a (RegisterCvarEx default "2" at src/world.c:919 consistent; no explicit default-value or access-class claim)

RESULT | ktx:cvar:_k_team1 | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | cvar_set in SM_PrepareShowscores from StartMatch; reads for labels/score-attribution/hostname all located; internal-usage registration confirms server-set string.
### ktx:cvar:_k_team1
- "Internal store of the first participating team's name, captured at match start" -> src/match.c:1163 `cvar_set("_k_team1", team1);` in SM_PrepareShowscores(); team1=getteam(p) from src/match.c:1138 `find_plr(world)`; called src/match.c:1344 SM_PrepareShowscores() inside `void StartMatch(void)` (enclosing fn @ src/match.c:1226) -> MATCH (first found player's team, captured on StartMatch path)
- "server records the two (or in three-team modes, three) competing team names when the scoreboard is prepared" -> src/match.c:1163-1164 sets _k_team1/_k_team2 always; src/match.c:1165 `if ((current_umode >= um2on2on2) && (current_umode <= um4on4on4))` then src/match.c:1183 `cvar_set("_k_team3", team3);`; src/match.c:1161 `k_showscores = 1;` -> MATCH (3rd team only in 2on2on2..4on4on4)
- "this holds team 1's name" -> src/match.c:1163 `cvar_set("_k_team1", team1)` -> MATCH
- "read for scoreboard team labels" -> src/commands.c:5378 `"Available teams: %s, %s"` cvar_string("_k_team1") + src/clan_arena.c:1703 + src/commands.c:5386 -> MATCH
- "score attribution" -> src/match.c:745 `cvar_string((sc > 0 ? "_k_team1" : "_k_team2"))` "%s leads by %s frag" + src/match.c:774 `cvar_string("_k_team1")` -> MATCH
- "match hostname decoration ('<host> (team1 vs. team2)')" -> src/match.c:1198 `cvar_set("hostname", va("%s (%.4s vs. %.4s)\207", cvar_string("hostname"), team1, team2));` (SM_PrepareHostname, team1=cvar_string("_k_team1") src/match.c:1189) -> MATCH (exact "<host> (team1 vs. team2)" format)
- "String; set by the server, not for manual use" -> src/world.c:1029 `RegisterCvar("_k_team1"); // internal mod usage`; only writer is cvar_set src/match.c:1163, all other sites cvar_string reads -> MATCH (registration comment + server-only write)
WI-2: n/a (bare RegisterCvar => default empty; description asserts no default value and no access class -- "set by the server, not for manual use" consistent with internal-usage registration)

RESULT | ktx:cvar:_k_team2 | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Identical pattern to _k_team1: cvar_set in SM_PrepareShowscores from StartMatch; second-team capture, label/score/hostname reads located; internal-usage registration.
### ktx:cvar:_k_team2
- "Internal store of the second participating team's name, captured at match start" -> src/match.c:1164 `cvar_set("_k_team2", team2);` in SM_PrepareShowscores(); team2=getteam(p) from src/match.c:1146-1154 while-loop finding the next player whose team differs from team1; called src/match.c:1344 inside `void StartMatch(void)` -> MATCH (second distinct team, captured on StartMatch path)
- "server records the competing team names when the scoreboard is prepared" -> src/match.c:1163-1164 SM_PrepareShowscores sets _k_team1/_k_team2 (+_k_team3 conditionally src/match.c:1165/1183); src/match.c:1161 `k_showscores = 1;` -> MATCH
- "this holds team 2's name" -> src/match.c:1164 `cvar_set("_k_team2", team2)` -> MATCH
- "read for scoreboard team labels" -> src/commands.c:5378/5386 cvar_string("_k_team2") "Available teams" + src/clan_arena.c:1037/1703 -> MATCH
- "score attribution" -> src/match.c:745 `cvar_string((sc > 0 ? "_k_team1" : "_k_team2"))` + src/match.c:790 `cvar_string("_k_team2")` "%s leads by %s frag" -> MATCH
- "match hostname decoration ('<host> (team1 vs. team2)')" -> src/match.c:1198 `va("%s (%.4s vs. %.4s)\207", cvar_string("hostname"), team1, team2)` (team2=cvar_string("_k_team2") src/match.c:1190) -> MATCH (exact format)
- "String; set by the server, not for manual use" -> src/world.c:1030 `RegisterCvar("_k_team2"); // internal mod usage`; only writer cvar_set src/match.c:1164, all other sites cvar_string reads -> MATCH
WI-2: n/a (bare RegisterCvar => default empty; no default-value or access-class claim contradicted)

RESULT | ktx:cvar:k_tp_tele_death | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=5 | client.c:5347 teamkill gate enforces 0=no-frag-loss / 1=frag-loss + suicide-log (adjacent comment); bound(0,..,1) clamp + teleteam toggle located.
### ktx:cvar:k_tp_tele_death
- "Controls whether a team telefrag (teammate killed by being telefragged, deathtype dtTELE1) costs the attacker a frag" -> src/client.c:5347 `if ((dtTELE1 != targ->deathtype) || cvar("k_tp_tele_death"))` inside the teamkill branch src/client.c:5342 `((isTeam() || isCTF()) && streq(targteam, attackerteam) && !strnull(attackerteam)) || coop` (`// teamkill` src/client.c:5345) -> MATCH (cvar gates whether a dtTELE1 teamkill triggers the penalty)
- "When 0, telefragging a teammate does NOT deduct a frag from the attacker" -> src/client.c:5347 value 0 => `cvar(...)` false; for dtTELE1 `(dtTELE1 != deathtype)` false => whole condition false => src/client.c:5349 `attacker->s.v.frags -= 1;` skipped -> MATCH (exact)
- "when 1, a team telefrag is penalized like any other teamkill (attacker loses 1 frag and the kill is logged as a suicide)" -> src/client.c:5347 value 1 => condition true => src/client.c:5349 `attacker->s.v.frags -= 1;` + src/client.c:5350 `logfrag(attacker, attacker); //ZOID 12-13-96: killing a teammate logs as suicide` -> MATCH (exact: -1 frag + logfrag(attacker,attacker) suicide-log, confirmed by adjacent comment)
- "Treated as a boolean (any value is clamped to 0 or 1)" -> src/commands.c:7997 `int k_tp_tele_death = bound(0, cvar("k_tp_tele_death"), 1);` (teleteam, the documented mutation path) + read-site src/client.c:5347 uses cvar() truthiness (any nonzero == 1) -> MATCH (literal `bound(0, ..., 1)` clamp line exists)
- "The /teleteam command toggles it" -> src/commands.c:979 `{ "teleteam", teleteam, 0, CF_PLAYER | CF_SPC_ADMIN, CD_TELETEAM }`; src/commands.c:8004 `if ((k_tp_tele_death = (k_tp_tele_death ? 0 : 1)))` then src/commands.c:8013 `cvar_fset("k_tp_tele_death", k_tp_tele_death);` -> MATCH (toggles 0<->1)
WI-2: n/a (bare RegisterCvar src/world.c:981 => registered default 0; shipped .cfg src/commands.c:4199 sets 1 but description asserts NO explicit default value -- describes 0 vs 1 behavior only; no admin/player/spectator class claim made)

## Wave 13 -- canary ktx:command:autotrack (slot blind), expected C-FIX, returned C-FIX -- GATE 1 PASS. GATE 2 (orchestrator re-grep): _k_worldspawns C-FIX confirmed (world.c:545 read in SP_worldspawn entity-parse vs :1116 increment in FirstFrame which runs AFTER -> first map reads 0 -> 60-90s branch; '0.5s first spawn' off-by-one) + clean srv_practice_mode (world.c:944 RegisterCvar, :549/551 SP_worldspawn path).

RESULT | ktx:cvar:k_vp_captain | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=4 | Routing (etCaptain), 51/100 floor/cap, and ceil(pct*(players-bots)) all map to enforcing lines in vote.c + captain.c.
### ktx:cvar:k_vp_captain
- "percentage of eligible voters required to pass a captain election" -> src/vote.c:276-278 `else if (el_type == etCaptain) { percent = cvar("k_vp_captain"); break; }` -> MATCH (captain election routes to this cvar)
- "the /captain vote" -> src/captain.c:336 `self->v.elect_type = etCaptain;` -> MATCH (the /captain command sets etCaptain; vote.c:276 routes etCaptain -> k_vp_captain)
- "effective value is floored at 51 and capped at 100, so values below 51 behave as 51" -> src/vote.c:330 `percent = bound(0.51, bound(51, percent, 100) / 100, 1);` (bound def src/g_utils.c:351-354 clamps b into [a,c]) -> MATCH (inner bound(51,percent,100) floors at 51, caps at 100)
- "required vote count is ceil(percent/100 * (players minus bots))" -> src/vote.c:343 `vt_req = ceil(percent * (CountPlayers() - CountBots()));` -> MATCH (percent is already clamped/100 from vote.c:330)
WI-2: n/a

RESULT | ktx:cvar:k_vp_hookstyle | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=4 | CTF hook-style vote routing, smooth/fast/classic, 51/100 floor/cap and ceil math all trace; "/hookstyle command" is still-true loose naming for hook_smooth/fast/classic.
### ktx:cvar:k_vp_hookstyle
- "percentage of eligible voters required to pass a grappling-hook-style vote in CTF" -> src/vote.c:315-318 `case OV_HOOKSMOOTH: case OV_HOOKFAST: case OV_HOOKCLASSIC: percent = cvar("k_vp_hookstyle"); break;` + src/vote.c:1205 `if (!isCTF()) { G_sprint(... "hook style can only be set in CTF mode"); return; }` -> MATCH (CTF-gated hook-style vote uses this cvar)
- "the /hookstyle command, which switches the hook behavior, e.g. smooth / fast / classic" -> src/commands.c:917-919 `{ "hook_smooth", hooksmooth, ...}` `{ "hook_fast", hookfast, ...}` `{ "hook_classic", hookclassic, ...}` + src/vote.c:1231/1275/1318 `cvar_fset("k_ctf_hookstyle", 1/2/3)` -> MATCH (no literal /hookstyle command; the three hook_* commands switch hook behavior smooth/fast/classic via k_ctf_hookstyle -- still-true minor naming vagueness, "hookstyle" appears in the feature's own user strings)
- "floored at 51 and capped at 100, so values below 51 behave as 51" -> src/vote.c:330 `percent = bound(0.51, bound(51, percent, 100) / 100, 1);` (bound src/g_utils.c:351-354) -> MATCH
- "required vote count is ceil(percent/100 * (players minus bots))" -> src/vote.c:343 `vt_req = ceil(percent * (CountPlayers() - CountBots()));` -> MATCH
WI-2: n/a

RESULT | ktx:cvar:_k_worldspawns | C-FIX | flavourC=1 | wi2=0 | clauses=5 | Counter/per-map-increment/first-map-sv_minping sentinel all trace, but "0.5s on the first spawn" is wrong: SP_worldspawn (read, ==1?0.5) runs BEFORE FirstFrame (increment 0->1), so the 0.5s branch fires on the SECOND map, first map gets 60-90s.
### ktx:cvar:_k_worldspawns
- "Internal counter of how many maps the server has spawned since the process started; incremented by 1 on each map spawn" -> src/world.c:1116 `cvar_fset("_k_worldspawns", (int)cvar("_k_worldspawns") + 1);` (in FirstFrame, gated framecount==1 which fires once per map since G_InitGame resets framecount=0 at g_main.c:480; cvar persists across G_InitGame) -> MATCH
- "A value of 1 marks the very first map of the server process and is used as a sentinel" -> src/world.c:1118 `if (cvar("_k_worldspawns") == 1) { // server spawn first map` -> MATCH (value is 1 here only on first map: 0->1 at 1116 just above; adjacent comment confirms)
- "triggers first-map-only initialization (capturing the initial sv_minping)" -> src/world.c:1120 `sv_minping = cvar("sv_minping"); // remember, so we can broadcast changes` (inside the ==1 block) -> MATCH
- "shortens the default-map-checker delay (about 0.5 seconds on the first spawn versus 60-90 seconds on later spawns)" -> src/world.c:545 `Spawn_DefMapChecker(cvar("_k_worldspawns") == 1 ? 0.5 : 60 + g_random() * 30);` (in SP_worldspawn, called from g_spawn.c:851 during entity parse, BEFORE FirstFrame which runs at first StartFrame framecount==1) -> MISMATCH (on the FIRST map _k_worldspawns is still 0 at line 545 -- not incremented to 1 until FirstFrame runs AFTER SP_worldspawn -- so first map takes the 60-90s branch; the 0.5s branch fires on the SECOND map spawn. "0.5s on the first spawn" is off-by-one vs the enforcing line. Only write to _k_worldspawns is world.c:1116; no cfg sets it)
- "Integer; internal, not for manual setting" -> src/world.c:782 `RegisterCvar("_k_worldspawns"); // internal usage, count of maps server spawned` (RegisterCvar->RegisterCvarEx("",..) default empty/0) + int cast at 1116 -> MATCH
WI-2: n/a (no default-value claim asserted as a number; behavior-clause defect handled as C-FIX above)

RESULT | ktx:cvar:srv_practice_mode | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=6 | Both announce strings, off-state map reload, match-only-mechanic disabling (items/doors/plats/buttons/triggers/teledeath all #practice mode# gated), and match-in-progress lock all map to enforcing lines.
### ktx:cvar:srv_practice_mode
- "Server-wide practice-mode toggle" -> src/commands.c:4893-4894 `k_practice = srv_practice_mode; cvar_fset("srv_practice_mode", srv_practice_mode);` + globals.c:59 `int k_practice; // is server in practice mode` -> MATCH (server-wide global)
- "0 = off (normal play): announces 'Server in normal mode'" -> src/commands.c:4900-4902 `else { G_bprint(2, "%s\n", redtext("Server in normal mode")); }` -> MATCH
- "and reloads the map when practice is turned off" -> src/commands.c:4903-4906 `if (map) { changelevel((strnull(map) ? mapname : map)); }` (in normal-mode branch; comment at 4884 "change map if srv_practice_mode == 0 and 'map' is not NULL") -> MATCH
- "1 = on: announces 'Server in practice mode'" -> src/commands.c:4896-4898 `if (k_practice) { G_bprint(2, "%s\n", redtext("Server in practice mode")); }` -> MATCH
- "enters practice state, which disables match-only mechanics across the gameplay code (item/pack respawn, door/plat/button/trigger gating, teledeath, related match logic)" -> src/items.c:2028 `if (!k_practice) // #practice mode#` (item respawn) + src/items.c:2113 `if (k_practice) // #practice mode#` ("all powerups respawn after 30 seconds in practice mode") + src/doors.c:229/259/288/773/942 + src/plats.c:124/160/364 + src/buttons.c:67/90/102/119 + src/triggers.c:115 (`if (!k_practice) // #practice mode#`) + src/combat.c:955 `|| (k_practice && targ->ct != ctPlayer) // #practice mode#` (teledeath/damage) -> MATCH (every named sub-area has a real k_practice gate with confirming #practice mode# comments)
- "Cannot be changed while a match is in progress" -> src/commands.c:4888-4891 `if (match_in_progress) { G_Error("SetPractice: match_in_progress"); }` + src/commands.c:4916-4919 TogglePractice `if (match_in_progress) { return; }` -> MATCH
WI-2: n/a (RegisterCvar("srv_practice_mode") world.c:944 -> default 0; consistent with "0 = off" baseline; no access-class claim)

RESULT | ktx:info_key:*mu:userinfo | TRACED-CLEAN | flavourC=0 | wi2=0 | clauses=4 | Bitmask (1<<client_index), MMODE_MULTI==3 gate, route-to-set-bits send loop, and multi/mmode-multi update path all map to enforcing lines in g_cmd.c.
### ktx:info_key:*mu:userinfo
- "Server-set star userinfo key holding the multi-target player set as a bitmask (bit i = client index i)" -> src/g_cmd.c:985 `SetUserInfo(self, "*mu", va("%d", m), SETUSERINFO_STAR);` + src/g_cmd.c:949/793 `bit = 1 << (int)(p - g_edicts - 1);` -> MATCH (SETUSERINFO_STAR = server-set star key; p-g_edicts-1 is the client index, so bit i == client index i)
- "used when the client's mmode (*mm) is multi mode (3)" -> src/g_cmd.c:368-387 `mmode = iKey(self, "*mm"); switch (mmode) { ... case MMODE_MULTI: s_m_do(str, iKey(self, "*mu")); }` + include/g_consts.h:294 `#define MMODE_MULTI (3)` -> MATCH
- "When a message is sent in multi mmode the server routes it to every player whose bit is set in this mask" -> src/g_cmd.c:781-802 `for (...find_client...) { bit = 1 << (p - g_edicts - 1); if (!(m & bit)) continue; G_sprint(p, PRINT_CHAT, "[%s <m:%d>]: %s\n", ...); }` -> MATCH (sends only to clients whose bit is set; m is the *mu value passed in)
- "Updated by the multi/mmode multi commands when the selected player set changes" -> src/g_cmd.c:957/966 `m &= ~bit; // minus bit` / `m |= bit; // plus bit` then src/g_cmd.c:985 `SetUserInfo(self, "*mu", va("%d", m), SETUSERINFO_STAR); // this must be set in both cases` (multi_do, src/commands.c:939 "multi" + src/commands.c:938 "mmode" -> g_cmd.c:1121 mmode=MMODE_MULTI) -> MATCH
WI-2: n/a (server-set star userinfo key, not a RegisterCvar cvar; no admin/player/spec access-class claim in description)
