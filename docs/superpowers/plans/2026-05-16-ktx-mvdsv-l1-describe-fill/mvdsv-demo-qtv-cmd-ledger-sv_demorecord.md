# MVDSV L1 describe-fill ledger -- `sv_demorecord`

Knob: `sv_demorecord` (command) -- canonical name; bare alias `record`. Handler `SV_MVD_Record_f` (src/sv_demo.c:1695), registered src/sv_demo.c:1934.

```json
{ "project":"mvdsv","knob":"sv_demorecord","type":"command","description":"Starts recording a server-side MVD (multi-view demo) of the current game to a named file. An MVD captures every player's point of view in one file, so it can be replayed from any player's perspective afterward.\n\nsv_demorecord <demoname> = begin recording to <demoname>. A .mvd extension is added if you do not include one; the configured demo prefix, suffix, and demo directory are applied to the final filename.\n\nThe server must be running a map (it refuses with \"Not active yet.\" otherwise). Use sv_demostop to finish and save the recording.\n\nSet by: server console / rcon.\n\nAlso registered as `record`.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"COLD-SYNTH (no prior description on this entry). Handler SV_MVD_Record_f registered at src/sv_demo.c:1934; the in-code comment at src/sv_demo.c:1933 (\"that how thouse commands should be called.\") marks the sv_demo*-prefixed name as the canonical/recommended form, with `record` (src/sv_demo.c:1929, same handler, inside #ifdef SERVERONLY) kept for mod backward-compat -> alias-pair, canonical form. Identical handler => identical behavior; substance mirrors the `record` ledger, usage string adjusted to sv_demorecord, cross-ref appends \"Also registered as `record`.\" F-MV1 KTX-override check: grep of research/repos/ktx/src/commands.c for \"sv_demorecord\"/\"record\" -> only unrelated `demomark` (commands.c:1043); NO KTX registration -> clean. ENFORCE-TRACE per clause: (a) starts server-side MVD recording -- SV_MVD_Record_f calls SV_MVD_Record(SV_InitRecordFile(name), false) at src/sv_demo.c:1732; SV_MVD_Record(mapchange=false) adds dest and SV_MVD_SendInitialGamestate sets sv.mvdrecording=true at src/sv_demo.c:1205 -> MATCH. (b) usage `<demoname>` required (exactly one arg) -- src/sv_demo.c:1702-1706 `if (c != 2){ Con_Printf(\"record <demoname>\\n\"); return; }` (engine prints the bare-form usage but the arity rule is the same for both names) -> MATCH. (c) .mvd auto-appended -- src/sv_demo.c:1725-1727 -> MATCH. (d) prefix/suffix/dir applied -- src/sv_demo.c:1718-1723 (sv_demoPrefix/sv_demoSuffix/sv_demoDir) -> MATCH. (e) refuses when not active \"Not active yet.\" -- src/sv_demo.c:1708-1712 -> MATCH. (f) finish via sv_demostop -- companion SV_MVDStop_f; cross-ref. (g) MVD multi-view nature stated as user-observable. Set-by: Cmd_AddCommand server command table, no per-command access flag -> server console / rcon. No Default line (required arg). D20: all cites in reasoning only.","description_proposed":null }
```

## Per-clause enforce-trace

| Clause | file:line | snippet | Verdict |
|---|---|---|---|
| Starts server-side MVD recording | src/sv_demo.c:1732 / :1205 | `SV_MVD_Record (SV_InitRecordFile(name), false);` -> callee `sv.mvdrecording = true;` | MATCH |
| Usage `<demoname>` / exactly one arg | src/sv_demo.c:1702-1706 | `if (c != 2) { Con_Printf ("record <demoname>\n"); return; }` | MATCH |
| `.mvd` extension auto-appended | src/sv_demo.c:1725-1727 | `if (strcmp(name + c - 4, ".mvd")) strlcat(name, ".mvd", ...)` | MATCH |
| Prefix / suffix / demo-dir applied | src/sv_demo.c:1718-1723 | `va("%s%s%s", sv_demoPrefix.string, SV_CleanName(...), sv_demoSuffix.string)` ; `sv_demoDir.string` | MATCH |
| Refuses unless server active | src/sv_demo.c:1708-1712 | `if (sv.state != ss_active) { Con_Printf ("Not active yet.\n"); return; }` | MATCH |
| Use `sv_demostop` to finish | src/sv_demo.c:1016-1019 (companion) | `SV_MVDStop_f` -> `SV_MVDStop(0, true)` | MATCH (cross-ref) |
| Canonical name; also registered as `record` | src/sv_demo.c:1934 vs :1929 | both `Cmd_AddCommand(..., SV_MVD_Record_f)`; comment :1933 marks sv_demo* canonical | MATCH |
| KTX-override (F-MV1) | research/repos/ktx/src/commands.c | no `sv_demorecord`/`record` row; only unrelated `demomark` (:1043) | clean / not overridden |
