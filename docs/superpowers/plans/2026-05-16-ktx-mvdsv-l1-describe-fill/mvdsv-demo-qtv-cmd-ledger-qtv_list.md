```json
{ "project":"mvdsv","knob":"qtv_list","type":"command","description":"Lists the QTV (QuakeTV) proxy connections currently attached to the server's demo stream. Prints a table with each connection's numeric id and network address. If no QTV proxies are connected it prints \"QTV list: empty\". Takes no arguments.\n\nSet by: server console / rcon.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"Cold synth; no prior description. Registration: src/sv_demo_qtv.c:1519 Cmd_AddCommand(\"qtv_list\", Qtv_List_f) -- plain Cmd_AddCommand, no access-flag wrapper, so baseline reach is server console + rcon (all three qtv_* commands registered identically at sv_demo_qtv.c:1519-1521). Handler: src/sv_demo_qtv.c:1368 Qtv_List_f. CLAUSE 'lists QTV connections attached to the demo stream': sv_demo_qtv.c:1373 iterates `for (cnt = 0, d = demo.dest; d; d = d->nextdest)` over the demo destination linked list and sv_demo_qtv.c:1375-1376 `if (d->desttype != DEST_STREAM) continue; // not qtv` filters to QTV stream destinations only. DEST_STREAM is the QTV destination type, confirmed at src/server.h:473 `typedef enum {DEST_NONE, DEST_FILE, DEST_BUFFEREDFILE, DEST_STREAM} desttype_t;` plus the inline `// not qtv` comment at sv_demo_qtv.c:1376. CLAUSE 'prints a table with each connection's numeric id and network address': banner at sv_demo_qtv.c:1378-1380 `if (!cnt) Con_Printf (\"QTV list:\\n\" \"%4.4s %s\\n\", \"#Id\", \"Addr\");` and per-row at sv_demo_qtv.c:1384 `Con_Printf (\"%4d %s\\n\", d->id, NET_AdrToString(d->na));` -- id is d->id, address is NET_AdrToString(d->na). CLAUSE 'prints \"QTV list: empty\" when none': sv_demo_qtv.c:1387-1388 `if (!cnt) Con_Printf (\"QTV list: empty\\n\");`. CLAUSE 'takes no arguments': handler body never calls Cmd_Argc/Cmd_Argv -- it reads only the global demo.dest list; no arg parsing exists. F-MV1: grep of research/repos/ktx/src/commands.c for a KTX { \"qtv_list\", ... } registration -> none; no KTX override. Confidence high: every material clause maps to an enforcing line; would classify TRACED-CLEAN.","description_proposed":null }
```

## Per-clause enforce-trace

| Clause | Enforcing file:line | Verbatim snippet | Result |
|---|---|---|---|
| Registered, server-side, console/rcon baseline | src/sv_demo_qtv.c:1519 | `Cmd_AddCommand ("qtv_list", Qtv_List_f);` | MATCH |
| Lists QTV connections attached to the demo stream | src/sv_demo_qtv.c:1373-1376 | `for (cnt = 0, d = demo.dest; d; d = d->nextdest) { if (d->desttype != DEST_STREAM) continue; // not qtv` | MATCH |
| DEST_STREAM == QTV destination type | src/server.h:473 | `typedef enum {DEST_NONE, DEST_FILE, DEST_BUFFEREDFILE, DEST_STREAM} desttype_t;` | MATCH |
| Prints id + network address per connection | src/sv_demo_qtv.c:1379-1380, 1384 | `Con_Printf ("QTV list:\n" "%4.4s %s\n", "#Id", "Addr");` ... `Con_Printf ("%4d %s\n", d->id, NET_AdrToString(d->na));` | MATCH |
| Prints "QTV list: empty" when none | src/sv_demo_qtv.c:1387-1388 | `if (!cnt) Con_Printf ("QTV list: empty\n");` | MATCH |
| Takes no arguments | src/sv_demo_qtv.c:1368-1389 | handler body has no Cmd_Argc/Cmd_Argv call; reads only global demo.dest | MATCH |
| F-MV1: no KTX override | research/repos/ktx/src/commands.c | grep for `{ "qtv_list"` -> no result | MATCH (no override) |
