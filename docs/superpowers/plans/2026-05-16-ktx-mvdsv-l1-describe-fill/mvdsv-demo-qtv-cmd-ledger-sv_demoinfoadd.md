# Ledger -- mvdsv:sv_demoinfoadd (command)

```json
{ "project":"mvdsv","knob":"sv_demoinfoadd","type":"command","description":"Appends a line of text to a recorded demo's companion info file (a plain-text file stored alongside the demo, used to hold notes, scores, or other metadata about that demo). The text is added as a new line; it does not overwrite existing info.\n\nsv_demoinfoadd <number> <text...> = append <text...> to the info file of the demo at that list position.\nsv_demoinfoadd * <text...> = append <text...> to the info file of the demo currently being recorded.\nsv_demoinfoadd ** <filename> = append the raw contents of <filename> (taken from the game directory) to the info file of the demo currently being recorded.\n\nSet by: server console / rcon.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"Handler SV_MVDInfoAdd_f at src/sv_demo_misc.c:706; registered src/sv_demo.c:1949 via Cmd_AddCommand (server command table; absent from sv_user.c ucmds[] -> server console / rcon only). CLAUSE 'companion info file = plain-text sidecar of the demo': the target path is built from SV_MVDName2Txt(...) which replaces the demo's extension with '.txt' (sv_demo_misc.c:500-553, ext write at 546-550); for a numeric arg path comes via SV_MVDTxTNum(Q_atoi(Cmd_Argv(1))) (line 728) = SV_MVDName2Txt(SV_MVDNum(num)) (sv_demo_misc.c:556-558). CLAUSE 'appends / does not overwrite': file opened in append mode fopen(path, ... 'a+t') (line 738); the normal branch skips the demonum token in Cmd_Args() (lines 776-779) then fwrite(args, strlen(args), 1, f) and fwrite('\\n',1,1,f) (lines 781-782), i.e. writes the remaining text plus a newline at end of file. CLAUSE 'number = list position / * = currently recording': numeric path via SV_MVDTxTNum->SV_MVDNum (date-sorted list, same numbering as sv_demonumremove); the '*'/'**' branch (lines 717-726) uses SV_MVDDemoName() (sv_demo.c:1961, first non-stream recording dest) and errors 'Not recording demo!' if !sv.mvdrecording or no demoname (lines 720-723). CLAUSE 'requires number + text': Cmd_Argc()<3 -> usage 'demoInfoAdd <demonum> <info string>' (lines 711-715). CLAUSE '** appends raw contents of <filename> from the game dir': for '**' the file is opened binary 'a+b' (line 738), then a second file at fs_gamedir/Cmd_Argv(2) (line 749) is opened 'rb' (line 751), up to 200 KB read (byte buf[1024*200], line 757) and fwritten into the info file (lines 758-768). On invalid number SV_MVDTxTNum/SV_MVDNum yields NULL -> 'invalid demo num' (lines 730-733). D20: file:line + helper names kept in reasoning; user doc states observable effect only. F-MV1: KTX src/commands.c has NO registration of sv_demoinfoadd (the only demo-info-related token there is a 'cmd demoinfo' stuffcmd at commands.c:7991, a client ucmd, unrelated to this command); no KTX override.","description_proposed":null }
```

## Per-clause enforce-trace

| # | Clause | Enforcing file:line | Verbatim snippet | Verdict |
|---|--------|--------------------|------------------|---------|
| 1 | Target is the demo's `.txt` sidecar (companion info file) | src/sv_demo_misc.c:728, 556-558, 546-550 | `name = SV_MVDTxTNum(Q_atoi(Cmd_Argv(1)));` ; `return SV_MVDName2Txt (SV_MVDNum(num));` ; `s[len++]='.'; s[len++]='t'; s[len++]='x'; s[len++]='t';` | MATCH |
| 2 | Appends (does not overwrite) | src/sv_demo_misc.c:738 | `if ((f = fopen(path, !strcmp(Cmd_Argv(1), "**") ? "a+b" : "a+t")) == NULL)` | MATCH |
| 3 | Writes the supplied text + a trailing newline | src/sv_demo_misc.c:776-782 | `args = Cmd_Args();` ... `fwrite(args, strlen(args), 1, f); fwrite("\n", 1, 1, f);` | MATCH |
| 4 | Numeric arg = list position (date-sorted), like other demo-num commands | src/sv_demo_misc.c:728 -> 556-558 -> 474/484/490-494 | `SV_MVDTxTNum(Q_atoi(Cmd_Argv(1)))` -> `SV_MVDName2Txt(SV_MVDNum(num))` -> `Sys_listdir(... SORT_BY_DATE)` ; `--num;` | MATCH |
| 5 | `*` = the demo currently being recorded; errors if not recording | src/sv_demo_misc.c:717-723, 718 + sv_demo.c:1961-1975 | `if (!strcmp(Cmd_Argv(1), "*") || !strcmp(Cmd_Argv(1), "**")) { const char* demoname = SV_MVDDemoName(); if (!sv.mvdrecording || !demoname) { Con_Printf("Not recording demo!\n"); return; }` | MATCH |
| 6 | Requires number + text (Argc<3 -> usage) | src/sv_demo_misc.c:711-713 | `if (Cmd_Argc() < 3) { Con_Printf("usage:demoInfoAdd <demonum> <info string>\n...");` | MATCH |
| 7 | `**` mode appends raw contents of a file from the game dir | src/sv_demo_misc.c:744-771, 749, 751, 757 | `if (!strcmp(Cmd_Argv(1), "**")) { ... snprintf(path, ... "%s/%s", fs_gamedir, Cmd_Argv(2)); if ((src = fopen(path, "rb")) ...` ; `byte buf[1024 * 200] = { 0 };` ; `if (sz != fwrite((void*)buf, 1, sz, f))` | MATCH |
| 8 | Set by: server console / rcon (not a client ucmd) | src/sv_demo.c:1949 ; src/sv_user.c (absent) | `Cmd_AddCommand ("sv_demoinfoadd", SV_MVDInfoAdd_f);` ; (no `SV_MVDInfoAdd_f` in `ucmds[]`) | MATCH |
| 9 | Invalid number -> "invalid demo num" | src/sv_demo_misc.c:730-732 | `if (!name) { Con_Printf("invalid demo num\n"); return; }` | MATCH |

V-pass self-classification: TRACED-CLEAN. The `**` clause is asserted only as "appends raw contents of <filename> from the game directory" -- exactly what lines 744-771 enforce; no claim about size limit or format is made in the user doc (the 200 KB read buffer is reasoning-only, not asserted as a guaranteed cap). F-MV1: no KTX registration.
