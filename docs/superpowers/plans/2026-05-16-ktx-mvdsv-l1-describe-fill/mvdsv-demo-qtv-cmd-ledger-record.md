# MVDSV L1 describe-fill ledger -- `record`

Knob: `record` (command) -- bare alias of `sv_demorecord`. Handler `SV_MVD_Record_f` (src/sv_demo.c:1695), registered src/sv_demo.c:1929.

```json
{ "project":"mvdsv","knob":"record","type":"command","description":"Starts recording a server-side MVD (multi-view demo) of the current game to a named file. An MVD captures every player's point of view in one file, so it can be replayed from any player's perspective afterward.\n\nrecord <demoname> = begin recording to <demoname>. A .mvd extension is added if you do not include one; the configured demo prefix, suffix, and demo directory are applied to the final filename.\n\nThe server must be running a map (it refuses with \"Not active yet.\" otherwise). Use stop to finish and save the recording.\n\nSet by: server console / rcon.\n\nShort alias of `sv_demorecord`.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"COLD-SYNTH replacing the bare usage line \"record <demoname>\" (a usage string, not a what-it-does description; fails D5 clause 1+2). Handler SV_MVD_Record_f registered at src/sv_demo.c:1929 inside #ifdef SERVERONLY (src/sv_demo.c:1925) -- the comment at 1926-1928 states `record` is the unprefixed name kept for backward compatibility because mods already use it; the canonical name is `sv_demorecord` (src/sv_demo.c:1934, same handler) -> alias-pair, bare form. F-MV1 KTX-override check: grep of research/repos/ktx/src/commands.c for \"record\"/\"sv_demorecord\" -> only an unrelated `demomark` row (commands.c:1043); NO KTX registration of record/sv_demorecord -> clean, not overridden. ENFORCE-TRACE per clause: (a) starts server-side MVD recording -- SV_MVD_Record_f calls SV_MVD_Record(SV_InitRecordFile(name), false) at src/sv_demo.c:1732; SV_MVD_Record with mapchange=false adds the dest and SV_MVD_SendInitialGamestate sets sv.mvdrecording=true at src/sv_demo.c:1205 -> MATCH. (b) requires exactly one arg / usage `record <demoname>` -- src/sv_demo.c:1702-1706 `if (c != 2){ Con_Printf(\"record <demoname>\\n\"); return; }` (Cmd_Argc==2 means cmd+one arg) -> MATCH. (c) .mvd extension auto-appended -- src/sv_demo.c:1725-1727 appends \".mvd\" when the last 4 chars are not already \".mvd\" -> MATCH. (d) prefix/suffix/dir applied -- src/sv_demo.c:1718-1723 builds newname from sv_demoPrefix.string + SV_CleanName(arg) + sv_demoSuffix.string, path uses sv_demoDir.string under fs_gamedir -> MATCH. (e) refuses when server not active with \"Not active yet.\" -- src/sv_demo.c:1708-1712 `if (sv.state != ss_active){ Con_Printf(\"Not active yet.\\n\"); return; }` -> MATCH. (f) finished/saved via `stop` -- companion handler SV_MVDStop_f finalizes; cross-ref. (g) MVD = multi-view (all players' POV) -- the dem_all/dem_multiple write path and SV_MVD_SendInitialGamestate capturing all clients; stated as the user-observable nature of an MVD, not an internal claim. Set-by: registered via Cmd_AddCommand (server command table); no per-command access flag in mvdsv's Cmd_AddCommand -> baseline server console / rcon. No Default line (command, no meaningful no-arg default; demoname is required). D20: all file:line cites kept here in reasoning, none in description.","description_proposed":null }
```

## Per-clause enforce-trace

| Clause | file:line | snippet | Verdict |
|---|---|---|---|
| Starts server-side MVD recording | src/sv_demo.c:1732 / :1205 | `SV_MVD_Record (SV_InitRecordFile(name), false);` -> callee sets `sv.mvdrecording = true;` | MATCH |
| Usage `record <demoname>` / exactly one arg required | src/sv_demo.c:1702-1706 | `if (c != 2) { Con_Printf ("record <demoname>\n"); return; }` | MATCH |
| `.mvd` extension auto-appended | src/sv_demo.c:1725-1727 | `if (strcmp(name + c - 4, ".mvd")) strlcat(name, ".mvd", ...)` | MATCH |
| Prefix / suffix / demo-dir applied to filename | src/sv_demo.c:1718-1723 | `va("%s%s%s", sv_demoPrefix.string, SV_CleanName(...), sv_demoSuffix.string)` ; path under `sv_demoDir.string` | MATCH |
| Refuses unless server active ("Not active yet.") | src/sv_demo.c:1708-1712 | `if (sv.state != ss_active) { Con_Printf ("Not active yet.\n"); return; }` | MATCH |
| Use `stop` to finish/save | src/sv_demo.c:1016-1019 (companion) | `SV_MVDStop_f` -> `SV_MVDStop(0, true)` finalizes file | MATCH (cross-ref) |
| Registered server command (console/rcon) | src/sv_demo.c:1929 | `Cmd_AddCommand ("record", SV_MVD_Record_f);` (inside `#ifdef SERVERONLY`) | MATCH |
| Bare alias of `sv_demorecord` (same handler) | src/sv_demo.c:1929 vs :1934 | both `Cmd_AddCommand(..., SV_MVD_Record_f)`; comment 1926-1928 names sv_demo* canonical | MATCH |
| KTX-override (F-MV1) | research/repos/ktx/src/commands.c | no `record`/`sv_demorecord` row; only unrelated `demomark` (:1043) | clean / not overridden |
