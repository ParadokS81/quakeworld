# Ledger -- mvdsv `sv_demolistr`

```json
{ "project":"mvdsv","knob":"sv_demolistr","type":"command","description":"Lists the server-side MVD demo files stored in the demo directory, oldest first, filtering by regular expression. With no arguments it lists every demo; each argument is a case-insensitive regular expression and a demo is shown only if its filename matches all of the given expressions. Each line shows an index number, the filename, and its size; a demo that is currently being recorded is marked with a leading '*'. During a live game, if more than 100 demos match, only the 100 most recent are shown. After the list it prints the total size of the demo directory, and (when a maximum directory size is configured) the remaining free space. This is the regex-matching counterpart to sv_demolist, which filters by plain text instead.\n\nsv_demolistr [regex] [regex] ... = list demos whose filename matches every given expression.\n\nSet by: server console / rcon. Also registered as `sv_demolistregex`.","description_origin":"synthesized","description_anchor_version":"1.11-53-g18d0362","description_provenance":null,"description_verdict":"synthesized","description_confidence":"high","description_reasoning":"SEEDED CORRECTION (B4): an independent V-pass flagged the listing-order clause as inverted and HG2 confirmed it; this re-synthesis fixes 'newest first' -> 'oldest first' and surfaces the previously-omitted live-game >100 truncation caveat. Order re-traced independently and confirmed: src/sv_demo_misc.c:351 passes SORT_BY_DATE (==1, src/sys.h:65) to Sys_listdir, which at src/sv_sys_unix.c:189-194 dispatches to qsort(list, ..., Sys_compare_by_date); Sys_compare_by_date at src/sv_main.c:4190-4193 returns (int)(a->time - b->time) -- ASCENDING by file_t.time (a time_t, src/sys.h:44, set from buf.st_mtime in Sys_FileSizeTime at src/sv_sys_unix.c:65/:178); larger time = more recent, so ascending puts OLDEST first. Print loop iterates the sorted array front-to-back (src/sv_demo_misc.c:398-406), so printed order is oldest first -- prior 'newest first' inverted. Truncation caveat re-traced: src/sv_demo_misc.c:398 `for (j = (GameStarted() && n > 100) ? n - 100 : 0; files[j]; j++)` -- when GameStarted() (src/sv_main.c:218) is true and match count n exceeds 100, the loop starts at n-100, skipping the oldest matches and printing only the last 100 (the 100 most recent matches); otherwise all matches print. Registered Cmd_AddCommand(\"sv_demolistr\", SV_DemoListRegex_f) at src/sv_demo.c:1945. This is the short alias of sv_demolistregex (src/sv_demo.c:1946) -- both bind the identical handler SV_DemoListRegex_f with no name-branching, so behavior is identical. Handler SV_DemoListRegex_f at src/sv_demo_misc.c:430-433 is a thin wrapper calling SV_DemoList(true); the true selects regex filtering. Other clauses spot-confirmed clean (V-pass): 'lists demos from fs_gamedir/sv_demoDir' src/sv_demo_misc.c:351; 'no args lists every demo' inner loop src/sv_demo_misc.c:360 j=1..Cmd_Argc()-1 so Cmd_Argc()==j at src/sv_demo_misc.c:391 immediately true; 'each argument is a case-insensitive regular expression' use_regex=true takes branch src/sv_demo_misc.c:362-385, src/sv_demo_misc.c:364 pcre_compile(Q_normalizetext(Cmd_Argv(j)), PCRE_CASELESS, ...) enforces case-insensitivity; 'shown only if filename matches all expressions' pcre_exec src/sv_demo_misc.c:371-372, case 0 (match) `continue`s (src/sv_demo_misc.c:374-376), PCRE_ERROR_NOMATCH `break`s (src/sv_demo_misc.c:377-378), so files[n++] src/sv_demo_misc.c:393 reached only when j==Cmd_Argc() at src/sv_demo_misc.c:391; 'index, filename, size' src/sv_demo_misc.c:405; 'recording demo marked with leading *' src/sv_demo_misc.c:402-403 DestByName -> '*%4d' form; 'total directory size' src/sv_demo_misc.c:415; 'free space only when max dir size set' src/sv_demo_misc.c:416-421 gated on `if ((int)sv_demoMaxDirSize.value)`; 'regex counterpart to sv_demolist (plain text)' sv_demolist calls SV_DemoList(false) at src/sv_demo_misc.c:427 taking the else strstr branch src/sv_demo_misc.c:386-388. Set-by: Cmd_AddCommand server console command, reachable over rcon. Same handler is ALSO bound in the client ucmd table under separate command names (dlistr/demolistr) -- distinct entity names, out of scope. F-MV1: no KTX registration of sv_demolistr or sv_demolistregex in research/repos/ktx/src/commands.c (only unrelated `dlist` at commands.c:965). No Default line: no meaningful no-arg default value. Confidence high: every clause traced to its enforcing line; the corrected order clause and the added truncation caveat are each verified against their enforcing source lines above. Alias note: sv_demolistregex (src/sv_demo.c:1946) is the long-form canonical name sharing this handler; see its own ledger for the standalone entry.","description_proposed":null }
```

## Per-clause enforce-trace

| Clause | Enforcing file:line | Verbatim snippet | Result |
|---|---|---|---|
| Registered as server console command | src/sv_demo.c:1945 | `Cmd_AddCommand ("sv_demolistr", SV_DemoListRegex_f);` | MATCH |
| Canonical long-form shares same handler | src/sv_demo.c:1946 | `Cmd_AddCommand ("sv_demolistregex", SV_DemoListRegex_f);` | MATCH |
| Handler selects regex filtering | src/sv_demo_misc.c:430-433 | `void SV_DemoListRegex_f (void) { SV_DemoList (true); }` | MATCH |
| Lists demos from fs_gamedir/sv_demoDir, oldest first (CORRECTED from "newest first") | src/sv_demo_misc.c:351 -> src/sv_sys_unix.c:192-193 -> src/sv_main.c:4192 | `dir = Sys_listdir(..., SORT_BY_DATE);` -> `qsort(..., Sys_compare_by_date);` -> `return (int)(((file_t *)a)->time - ((file_t *)b)->time);` (ascending mtime; print loop front-to-back at :398-406) | MATCH |
| Live game with >100 matches shows only 100 most recent (ADDED caveat) | src/sv_demo_misc.c:398 | `for (j = (GameStarted() && n > 100) ? n - 100 : 0; files[j]; j++)` | MATCH |
| No args -> lists every demo | src/sv_demo_misc.c:360,391 | `for (j = 1; j < Cmd_Argc(); j++)` ... `if (Cmd_Argc() == j)` | MATCH |
| Each arg = case-insensitive regex | src/sv_demo_misc.c:362-364 | `if (use_regex) { if (!(preg = pcre_compile(Q_normalizetext(Cmd_Argv(j)), PCRE_CASELESS, &errbuf, &r, NULL)))` | MATCH |
| Shown only if all expressions match | src/sv_demo_misc.c:371-391 | `switch (r = pcre_exec(...)) { case 0: ... continue; case PCRE_ERROR_NOMATCH: break; ...}` ... `if (Cmd_Argc() == j) files[n++]=i;` | MATCH |
| Each line shows index, filename, size | src/sv_demo_misc.c:405 | `Con_Printf("%4d: %s (%dk)\n", i, list[i - 1].name, list[i - 1].size / 1024);` | MATCH |
| Recording demo marked with leading '*' | src/sv_demo_misc.c:402-403 | `if ((d = DestByName(list[i - 1].name))) Con_Printf("*%4d: %s (%dk)\n", ...)` | MATCH |
| Prints total directory size | src/sv_demo_misc.c:415 | `Con_Printf("\ndirectory size: %.1fMB\n", (float)dir.size / (1024 * 1024));` | MATCH |
| Free space printed only when max dir size set | src/sv_demo_misc.c:416-421 | `if ((int)sv_demoMaxDirSize.value) { ... Con_Printf("space available: %.1fMB\n", free_space); }` | MATCH |
| Counterpart to sv_demolist (plain text) | src/sv_demo_misc.c:386-388,427 | else branch `if (strstr(list->name, Cmd_Argv(j)) == NULL) break;` (sv_demolist's false path) | MATCH |
| F-MV1 no KTX override | research/repos/ktx/src/commands.c | only `{ "dlist", dlist, ... }` at :965; no `sv_demolistr`/`sv_demolistregex` | MATCH (no override) |
