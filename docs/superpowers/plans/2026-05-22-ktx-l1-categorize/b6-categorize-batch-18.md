# B6 categorize ledger -- batch 18

Generated by: claude-sonnet-4-6 | b6-categorize-v1 | 2026-05-22

---

B6-RESULT | ktx:cvar:k_allowcountchange | CATEGORIZED | category=Admin & permissions | confidence=HIGH

### ktx:cvar:k_allowcountchange

- canonical_id: ktx:cvar:k_allowcountchange
- name: k_allowcountchange
- type: cvar

- NEW category_inferred: Admin & permissions
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: This is a permission tier controlling who may run slot-count change commands (upplayers/downplayers/upspecs/downspecs). The description maps a 0-5 enum to admin permission levels (no one / real admin / admin / denied / anyone), which is the canonical Admin & permissions shape -- a check_perm()-gated role tier.

---

B6-RESULT | ktx:cvar:k_allowed_free_modes | CATEGORIZED | category=Admin & permissions | confidence=MED

### ktx:cvar:k_allowed_free_modes

- canonical_id: ktx:cvar:k_allowed_free_modes
- name: k_allowed_free_modes
- type: cvar

- NEW category_inferred: Admin & permissions
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: This bitmask governs which game modes players may invoke via the usermode command -- it is a server-side permission gate on user-driven mode switching rather than a mode-scoped knob or a mode selection command. The closest fit is Admin & permissions (controlling what actions players are allowed to perform), though it sits near the Mode selection boundary. Confidence is MED because it could also be read as a server config gate; the permission-control framing wins because UserMode() enforces it as an allow/deny decision.

---

B6-RESULT | ktx:cvar:k_allowklist | CATEGORIZED | category=Admin & permissions | confidence=HIGH

### ktx:cvar:k_allowklist

- canonical_id: ktx:cvar:k_allowklist
- name: k_allowklist
- type: cvar

- NEW category_inferred: Admin & permissions
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Controls whether players may use /klist during a live match -- a 0/1 gate on a player-accessible command. The description explicitly frames it as an access-control toggle (refused vs allowed), matching the Admin & permissions disambiguation (permission tiers, k_allow* family). The name prefix k_allow* directly signals this bucket.

---

B6-RESULT | ktx:cvar:k_allowtracklist | CATEGORIZED | category=Admin & permissions | confidence=HIGH

### ktx:cvar:k_allowtracklist

- canonical_id: ktx:cvar:k_allowtracklist
- name: k_allowtracklist
- type: cvar

- NEW category_inferred: Admin & permissions
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Structural sibling of k_allowklist: controls whether players may use the tracklist command during a live match, with the same 0/1 allow/deny shape and ctPlayer gate. The k_allow* prefix and the permission-tier pattern are identical; Admin & permissions is unambiguous.

---

B6-RESULT | ktx:cvar:k_allowvoteadmin | CATEGORIZED | category=Admin & permissions | confidence=HIGH

### ktx:cvar:k_allowvoteadmin

- canonical_id: ktx:cvar:k_allowvoteadmin
- name: k_allowvoteadmin
- type: cvar

- NEW category_inferred: Admin & permissions
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Toggles whether players may elect a temporary admin via /elect. This is explicitly an admin-system permission toggle (k_allow* prefix, interacts with k_admins, governs admin role acquisition). Admin & permissions is the direct fit; the description reasoning cites world.c:878 and the admin election system.

---

B6-RESULT | ktx:cvar:k_allow_vwep | CATEGORIZED | category=Gameplay rules | confidence=MED

### ktx:cvar:k_allow_vwep

- canonical_id: ktx:cvar:k_allow_vwep
- name: k_allow_vwep
- type: cvar

- NEW category_inferred: Gameplay rules
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Master enable for visible weapons (vwep) -- a gameplay visual feature that applies across all modes (no mode-scope). The k_allow* prefix might suggest Admin & permissions, but vwep is not a player-action permission; it is a server-side gameplay feature toggle (precaching model, enabling per-player visual state). Gameplay rules (applies across all modes, not scoped to a single mode) is the better fit. Confidence MED because the k_allow* prefix creates superficial overlap with Admin & permissions.

---

B6-RESULT | ktx:cvar:k_ann | CATEGORIZED | category=Spectator chat & visibility | confidence=HIGH

### ktx:cvar:k_ann

- canonical_id: ktx:cvar:k_ann
- name: k_ann
- type: cvar

- NEW category_inferred: Spectator chat & visibility
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Controls whether spectator join/leave announcements are shown to players during a live match. The source (src/spectate.c:180/239) shows it governs the audience of spec-enter/leave messages -- spec-visibility communication policy. This is precisely the Spectator chat & visibility bucket (k_spectalk family; spec-chat policy). The description explicitly scopes it to spectator message visibility for players.

---

B6-RESULT | ktx:cvar:k_auto_xonx | CATEGORIZED | category=Match flow | confidence=HIGH

### ktx:cvar:k_auto_xonx

- canonical_id: ktx:cvar:k_auto_xonx
- name: k_auto_xonx
- type: cvar

- NEW category_inferred: Match flow
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Automatically sets the XonX user mode based on player + ready-spectator counts, and blocks manual user-mode commands while active. This governs pre-match state transitions and mode assignment outside a live match -- the definition of Match flow (prewar, ready, match-state transitions). The description confirms it is "only active outside of a live match and outside matchless mode," directly placing it in the match-state transition bucket.

---

B6-RESULT | ktx:cvar:k_bloodfest | CATEGORIZED | category=Mode selection | confidence=HIGH

### ktx:cvar:k_bloodfest

- canonical_id: ktx:cvar:k_bloodfest
- name: k_bloodfest
- type: cvar

- NEW category_inferred: Mode selection
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Enables Bloodfest game mode (wave-based cooperative monster survival). This is a mode-enable toggle analogous to k_instagib -- the cvar IS the mode switch, cached into the global k_bloodfest that gates the wave spawner at src/sp_monsters.c:773. The disambiguation guide lists bloodfest under Mode selection. Although it is technically a cvar (not a command), the pattern matches the Mode selection bucket: it is the primary mode-activation knob.

---

B6-RESULT | ktx:cvar:k_btime | CATEGORIZED | category=Gameplay rules | confidence=MED

### ktx:cvar:k_btime

- canonical_id: ktx:cvar:k_btime
- name: k_btime
- type: cvar

- NEW category_inferred: Gameplay rules
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Sets the countdown threshold (seconds of remaining match time) at which the berzerk event fires -- granting all players Quad + invulnerability for the rest of the match. Berzerk is a cross-mode gameplay mechanic tuned by k_bzk/k_btime; it is not scoped to a single named mode like CA or Instagib. Gameplay rules (damage/item tuning that applies across modes) is the best fit. Confidence MED because it has strong match-flow flavor (triggered at a match-time threshold), but the effect is gameplay/item-granting, not state transition.

---

B6-RESULT | ktx:cvar:k_bzk | CATEGORIZED | category=Gameplay rules | confidence=MED

### ktx:cvar:k_bzk

- canonical_id: ktx:cvar:k_bzk
- name: k_bzk
- type: cvar

- NEW category_inferred: Gameplay rules
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Toggle for berzerk mode -- arms the k_btime countdown and triggers Quad + invulnerability for all players at end-of-match stretch. Like k_btime, berzerk is a cross-mode gameplay feature (not scoped to CA/Instagib/Race). Gameplay rules is the best fit. MED confidence because the "mode-like" framing (berzerk mode toggle) creates some overlap with Mode selection, but berzerk is a modifier to standard play rather than a distinct game mode with its own command entry point.

---

B6-RESULT | ktx:cvar:_k_captcolor2 | CATEGORIZED | category=Internal state | confidence=HIGH

### ktx:cvar:_k_captcolor2

- canonical_id: ktx:cvar:_k_captcolor2
- name: _k_captcolor2
- type: cvar

- NEW category_inferred: Internal state
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: _k_ prefix, described as "Internal mod-state cvar. Not operator-tuned; managed automatically by the team-picking code." This is the canonical Internal state pattern -- set only by KTX itself, never by config. The description explicitly says "Not configurable" and the reasoning confirms it is written via dynamic cvar_set during captain-pick events, never by server config.

---

B6-RESULT | ktx:cvar:_k_captteam1 | CATEGORIZED | category=Internal state | confidence=HIGH

### ktx:cvar:_k_captteam1

- canonical_id: ktx:cvar:_k_captteam1
- name: _k_captteam1
- type: cvar

- NEW category_inferred: Internal state
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: _k_ prefix, described as "Internal mod-state cvar. Stores the first captain's team name during captain-based team picking. Not operator-tuned; set and consumed automatically by the team-picking code." Exactly matches Internal state disambiguation: _k_* engine internals, set only by KTX itself, never by config. "Set by: server runtime only (not intended for server config)" in the description confirms this.

---

B6-RESULT | ktx:cvar:_k_captteam2 | CATEGORIZED | category=Internal state | confidence=HIGH

### ktx:cvar:_k_captteam2

- canonical_id: ktx:cvar:_k_captteam2
- name: _k_captteam2
- type: cvar

- NEW category_inferred: Internal state
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: _k_ prefix, described as "Internal runtime state -- not for manual configuration." Structurally identical to _k_captteam1 -- stores captain 2's team name during team picking, written by captain.c dynamically. "Set by: server automatically during captain team selection" confirms Internal state.

---

B6-RESULT | ktx:cvar:k_cg_kb | CATEGORIZED | category=Mode-scoped knobs | confidence=HIGH

### ktx:cvar:k_cg_kb

- canonical_id: ktx:cvar:k_cg_kb
- name: k_cg_kb
- type: cvar

- NEW category_inferred: Mode-scoped knobs
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Coilgun kickback toggle in Instagib mode -- the description explicitly states "Only active while k_instagib is enabled." This is a cvar whose effect is scoped to one specific mode (Instagib), matching the Mode-scoped knobs disambiguation exactly. The toggle command in commands.c:7891 even gates itself with `cg_kb requires Instagib`, confirming the single-mode scope.

---

B6-RESULT | ktx:cvar:k_clan_arena | CATEGORIZED | category=Mode selection | confidence=HIGH

### ktx:cvar:k_clan_arena

- canonical_id: ktx:cvar:k_clan_arena
- name: k_clan_arena
- type: cvar

- NEW category_inferred: Mode selection
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Selects the round-based arena mode (0 = off, 1 = Clan Arena, 2 = Wipeout). This is the primary mode-activation knob for CA/Wipeout, analogous to k_bloodfest for Bloodfest mode. The disambiguation guide lists clan_arena under Mode selection. The description confirms it switches between "normal team game" and the round-based arena modes.

---

B6-RESULT | ktx:cvar:k_clan_arena_max_respawns | CATEGORIZED | category=Mode-scoped knobs | confidence=HIGH

### ktx:cvar:k_clan_arena_max_respawns

- canonical_id: ktx:cvar:k_clan_arena_max_respawns
- name: k_clan_arena_max_respawns
- type: cvar

- NEW category_inferred: Mode-scoped knobs
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Number of respawns per round in Clan Arena / Wipeout -- a cvar whose effect is scoped to the CA/Wipeout mode (k_ca* pattern). The description says "per Clan Arena / Wipeout round" and the source enforces it only within the clan_arena.c respawn logic. Matches Mode-scoped knobs (k_ca* family, effect scoped to one specific mode).

---

B6-RESULT | ktx:cvar:k_clan_arena_rounds | CATEGORIZED | category=Mode-scoped knobs | confidence=HIGH

### ktx:cvar:k_clan_arena_rounds

- canonical_id: ktx:cvar:k_clan_arena_rounds
- name: k_clan_arena_rounds
- type: cvar

- NEW category_inferred: Mode-scoped knobs
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Number of rounds in a Clan Arena / Wipeout series, consumed by CA_wins_required in clan_arena.c. Like k_clan_arena_max_respawns, this is a CA/Wipeout-scoped tuning knob (k_ca* family). Mode-scoped knobs is unambiguous.

---

B6-RESULT | ktx:cvar:k_classic_shotgun | CATEGORIZED | category=Gameplay rules | confidence=HIGH

### ktx:cvar:k_classic_shotgun

- canonical_id: ktx:cvar:k_classic_shotgun
- name: k_classic_shotgun
- type: cvar

- NEW category_inferred: Gameplay rules
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Controls whether shotgun/super-shotgun show per-pellet or combined impact effects -- a weapon-behavior tunable that applies across all modes (no mode scope, no admin permission component). The description states "damage dealt is identical either way," confirming this is a gameplay-visual/feedback rule for the SG/SSG, matching Gameplay rules (weapon balance, tuning that applies across all modes).

---

B6-RESULT | ktx:cvar:k_cmd_fp_count | CATEGORIZED | category=Server config & network | confidence=HIGH

### ktx:cvar:k_cmd_fp_count

- canonical_id: ktx:cvar:k_cmd_fp_count
- name: k_cmd_fp_count
- type: cvar

- NEW category_inferred: Server config & network
- NEW category_inferred_origin: claude-sonnet-4-6|b6-categorize-v1

- reasoning: Number of console commands allowed per flood-protection window (isCmdFlood enforcement in commands.c). This is a server-side anti-abuse / rate-limiting knob -- the description explicitly scopes it as "server config only" and distinguishes it from say-flood protection. Server config & network (rates, broadcast intervals, sv_* server-side) is the right bucket for flood-protection rate tuning.

---
