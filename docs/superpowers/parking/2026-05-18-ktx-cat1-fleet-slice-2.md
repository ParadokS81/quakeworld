<!-- FLEET WORKER HANDOFF PROMPT -- KTX Phase-3 cat-1, SLICE 2 of 4.
     Paste this ENTIRE file as the first message in a fresh `claude`
     terminal opened in the monorepo root. ONE slice per terminal --
     double-check the slice number matches the terminal you intend
     (slices are disjoint; a mis-paste only wastes compute, the
     idempotent UPSERT cannot corrupt the DB). -->

KTX Phase-3 cat-1 FLEET WORKER -- SLICE 2 of 4 (unattended overnight)

You are an arc-executor fleet worker for Phase 3 of the 2026-05-16-ktx-mvdsv-l1-describe-fill arc. Invoke the `arc-executor` skill FIRST (cold start). Working dir: /home/paradoks/projects/quakeworld/apps/qw-oracle.

YOUR LANE (disjoint, exclusive): the canonical_ids listed in `output/describe-fill/fleet/slice-2-ids.txt` -- 119 ids: 119 command (pure command lane). You evaluate ONLY ids in that file. 3 sibling terminals own the other 3 disjoint lanes in parallel; lanes never overlap so NO coordination is needed. Goal: drain your lane through the proven D6 synthesis loop, persisting F-D6a-verified records, until your lane shows 0 remaining OR you reach ~300k context (wrap early -- unattended judgment past the smell zone is unreliable).

== PRE-FLIGHT (once, before any batch) ==
1. Invoke `arc-executor`. Read the fleet contract `docs/superpowers/parking/2026-05-18-ktx-cat1-fleet-handoff.md` and the main resume handoff it points to (`docs/superpowers/parking/2026-05-17-ktx-mvdsv-l1-describe-fill-phase3-executor-resume.md`).
2. git-immutable cheap drift-check: `git -C /home/paradoks/projects/quakeworld log --oneline 292d3ad5..HEAD -- docs/superpowers/plans/2026-05-16-ktx-mvdsv-l1-describe-fill/` and `git status --porcelain docs/superpowers/plans/2026-05-16-ktx-mvdsv-l1-describe-fill/`. BOTH empty -> trust the distilled rules below; do NOT full-read the 26k phase MD / decisions.md. ANY non-empty -> read ONLY the changed file's diff.
3. live-mutable re-verify (postgres-js, .env DATABASE_URL): M = command 358 / cvar 260 / info_key 7; `bun scripts/describe-fill/synthesize-ktx.ts --status` evaluated+remaining reconcile to 624; `--fingerprint` returns an md5; base commits 546610a2 / 54b27d0f / c8a17cd3 / 2fd1421e exist; F-D4a guard `IS DISTINCT FROM` live in scripts/load-knowledge/derive-entity-description.ts; `--verify-binding ChangeDM` prints FAMILY_OK (machinery health only -- do NOT re-eval ChangeDM).
4. ASSERT the shared ktx clone is pinned: `git -C /home/paradoks/projects/quakeworld/research/repos/ktx rev-parse --short=8 HEAD` == `67253dc9` (== git describe `1.47-2-g67253dc` == anchor). If it is NOT, HALT immediately (record why in your status file). NEVER checkout / pull / fetch that clone -- it is shared read-only; you only rev-parse / log / show / grep it.
A mismatch in step 3 or 4 means INVESTIGATE, not proceed.

== THE LOOP (repeat until lane drained or smell zone) ==
A. Next batch = the FIRST <=10 canonical_ids in `output/describe-fill/fleet/slice-2-ids.txt` that are STILL listed remaining by `bun scripts/describe-fill/synthesize-ktx.ts --status`. Use an EXACT set intersection (comm -12 of two sorted exact-id lists), NEVER substring grep. This is the idempotent skip -- already-persisted lane ids are auto-skipped, so a restart is safe.
B. Dispatch ONE sub-agent: subagent_type general-purpose, model opus, MAX reasoning. The model dial is SPEC-LOCKED by D7 inside the describe-fill-synthesis skill -- NOT lowerable, "fast affirm" is the in-invocation early exit, never a cheaper pass. Brief = the PROVEN PER-KNOB BRIEF below, verbatim, with this batch's <=10 canonical_ids and records filename `output/describe-fill/phase3-records-slice2-batchNN.json` (NN = your sequential batch number for this slice, zero-padded e.g. 01; if a file for that NN already exists from a prior run, continue numbering past the highest existing).
C. F-D6a TWO-STAGE REVIEW (dispatcher's job -- non-negotiable, never delegated, never skipped):
   c1. Shape-check the records file (do NOT ingest description/reasoning/proposed prose): exactly the 11 fields per record; description_provenance a populated JS array of {source_file, source_line:int, shipped_value:string|null, raw_comment:string|null}; no top-level source_ref; knob byte-for-byte == the manifest packet's knob (the persist key = entities.name, source case); anchor/origin/verdict consistent (synthesized->origin synthesized + anchor 1.47-2-g67253dc; affirmed->origin source_inline + null anchor).
   c2. Independently re-grep EVERY cited (source_file, source_line) in the ktx clone at 67253dc9. ASSERT each entry's raw_comment equals the trimmed verbatim text at that exact line OR is null. (THE BATCH-04 LESSON: a prior batch cited correct registration lines but put a CD_* macro's resolved string in raw_comment instead of the line's actual text. If ANY raw_comment != its line's verbatim text and != null, the batch is DEFECTIVE -> SendMessage the SAME sub-agent the surgical raw_comment-precision fix [keep descriptions/verdicts/lines; only repair raw_comment + add a separate entry at the real #define line], re-verify; do NOT persist until clean.) Distinguish benign raw_comment-imprecision (line + behaviour correct) from a real wrong-line/invented-behaviour fabrication -- the latter never persists; re-dispatch, and if it persists HALT that batch and record it.
   c3. 2-sample prose tripwire: print only the `description` of 2 random records; reject GROSS violations only -- a dead-stamped KTX command (F-C3c: KTX commands are never dead-stamped, suspect_pool_member is FALSE arc-wide), a recommended/opinion value (D5/D8 -- tuning advice routes to L3, not L1), or a bare name-restatement.
D. Persist: `bun scripts/describe-fill/synthesize-ktx.ts --persist <file> --dry-run` then `--persist <file>`. PARALLEL-MODE GATING (this differs from single-terminal): do NOT assert the global --fingerprint moved by your delta -- sibling terminals commit concurrently and perturb the global md5; that is expected and benign. Assert instead: dry-run persisted == batch size, skipped-terminal == expected (0 unless a lane id is a pre-existing terminal-owned row), errors == 0; AND each of your N knobs now has a non-null verdict (postgres-js: SELECT count(*) FROM entities WHERE project='ktx' AND name = ANY(<your batch knobs>) AND description_verdict IS NOT NULL -- must == N).
E. Loop to A.

== PROVEN PER-KNOB SUB-AGENT BRIEF (paste verbatim into each dispatch; fill the {IDS} list + {RECORDS_FILE}) ==
---8<--- BEGIN BRIEF ---8<---
You are a Phase-3 KTX describe-fill D6 synthesis worker. Apply the `describe-fill-synthesis` skill to EACH listed knob, one full skill application per knob, at Opus 4.7 MAX reasoning (ultrathink). The dial is SPEC-LOCKED by D7 in the skill -- NOT yours to lower; "fast affirm" is the in-invocation early exit, never a cheaper pass. Working dir: /home/paradoks/projects/quakeworld/apps/qw-oracle.

KNOBS (canonical_ids, process in order): {IDS}

For EACH id: parse `output/describe-fill/phase3-ktx-manifest.json` (top-level keys generated_against / entities / config_drift_nonresolvers) and read that id's self-contained packet. It supplies every non-inferential input: project=ktx; knob (EXACT Layer-1 entity name, source case, VERBATIM -- namespaced commands keep the full string e.g. `anglehint:frogbot:editor`; emit knob byte-for-byte as the packet states it -- it is the persist match key against entities.name); anchor_version=1.47-2-g67253dc (must == generated_against.anchor_version); mechanical_candidate (may be "none" for commands, or a real shipped_doc candidate for cvars -- absence is NOT a skip, every entity is fully evaluated per the D5 amendment; a present candidate is graded keep-vs-synthesize by the skill); suspect_pool_member (FALSE arc-wide for KTX -- F-C3c: a KTX entity is NEVER dead-stamped, describe from source behaviour); source_root=/home/paradoks/projects/quakeworld/research/repos/ktx; source_ref (a starting hint, not the citation -- find the authoritative behaviour-exhibiting use-site yourself).

Invoke the `describe-fill-synthesis` skill (Skill tool) per knob -- it hard-codes the D5-amended rubric, read-site grounding, evidence/citation mechanism, hard confabulation guard, C3 sibling, D8 bot/judgment sibling, and loads its own 4 references. Do NOT restate or re-derive the rubric. Every knob ends with exactly one verdict: affirmed / synthesized / hedged / residue_routed (dead_stamped impossible -- suspect=FALSE, F-C3c). Not source-legible even at Opus-MAX -> hedge or residue_route; NEVER guess, NEVER infer behaviour from the name. Hedged/residue is CORRECT (C1-routed for the operator D7 tail), not a failure.

KTX source ground truth: `/home/paradoks/projects/quakeworld/research/repos/ktx`, git HEAD MUST be 67253dc9 == git describe 1.47-2-g67253dc == anchor. Before citing ANY (source_file, source_line): grep/Read it in that tree and confirm the line exhibits the cited behaviour. Trace registration -> handler (DEF()/function) -> the behaviour the handler performs; cite the authoritative behaviour site. The dispatcher independently re-greps EVERY cited line; zero fabrication is the held bar.

SHARPENED requirements (verbatim, load-bearing):
(a) description_provenance MUST be a populated JS array (NEVER null, NEVER pre-stringified), each entry EXACTLY {source_file, source_line:int, shipped_value:string|null, raw_comment:string|null}.
(b) Verify EVERY source line by grepping the live tree at HEAD 67253dc9 == anchor; do NOT reason line numbers from memory; do NOT assert an off-by-one; do NOT fabricate a C2 source-to-source meaning conflict (a genuine one -> record it in description_reasoning for the D7 operator tail, never auto-resolve).
(c) Do NOT add a top-level source_ref field.
(d) RAW_COMMENT PRECISION (a prior batch failed exactly here -- load-bearing): each provenance entry's `raw_comment` MUST be the verbatim text that physically appears AT that exact source_line in the live tree (trimmed), OR null if that line has no comment. NEVER a macro's resolved value, NEVER a paraphrase, NEVER what a symbol expands to. A `cmd_t cmds[]` / registration line `{ "name", Handler, ..., CD_FOO },` has raw_comment null (or that literal line text). If the CD_FOO STRING VALUE is your evidence, add a SEPARATE provenance entry whose source_line is the ACTUAL `#define CD_FOO "..."` line (grep it: `grep -n '#define CD_FOO' src/commands.c`), raw_comment = that verbatim #define line. Two entries, each raw_comment matching its own line.

RECORD SHAPE (exactly these 11 fields per knob): project, knob, type (= packet entity_type), description, description_origin (source_inline if affirmed / synthesized otherwise), description_anchor_version (1.47-2-g67253dc for synthesized; skill's rule for affirmed), description_provenance (JS array per (a)/(d)), description_verdict, description_confidence, description_reasoning (full grading + grounding rationale, stored; include any genuine C2 conflict note), description_proposed. knob MUST equal the manifest packet knob byte-for-byte.

OUTPUT (Execution-efficiency protocol -- operator-directed override): WRITE the JSON array of all records (pretty-printed) to `{RECORDS_FILE}`. This single records-file write is EXPLICITLY AUTHORIZED, overriding the packet's "do not write files" line for THIS ONE FILE ONLY. Still do NOT touch the DB, NOT run --persist, NOT commit, NOT write any other file. Return INLINE only a COMPACT control summary (NO description/reasoning/proposed prose): records written + path; per knob `canonical_id -> VERDICT conf=<c> origin=<tag> ref=<file:line>`; verdict + confidence distribution; every hedged/residue_routed knob + one-line why; any knob whose registration shares one DEF()/handler with siblings (flag canonical_id + handler -- EXPECTED none, cohorts drained); any ABORT + reason; final line: count, file written, DB/commit untouched. Out of scope: ONLY the listed knobs.
---8<--- END BRIEF ---8<---

== UNATTENDED HARD RULES (do NOT violate) ==
- Do NOT commit. Do NOT git add. Do NOT edit/write ANY doc under docs/ (the shared handoffs included). Records + status files go under output/describe-fill/ (gitignored data).
- Do NOT run Task 3 (D7 tier-1 gate), Task 4, Task 5, or the phase boundary. You drain VOLUME only.
- Stay strictly inside `output/describe-fill/fleet/slice-2-ids.txt`. If --verify-binding flags a twinnable cluster (unlikely -- cohorts drained), hedge-individually is fine; do NOT family-collapse, do NOT reach outside your lane.
- Zero-fabrication is the held bar. Benign raw_comment-imprecision -> surgical re-dispatch (keep descriptions/lines). A real fabrication that survives one re-dispatch -> HALT that batch, record it, NEVER persist it, NEVER relax the bar under budget (wrap instead).
- The shared ktx clone is read-only/pinned. Never checkout/pull/fetch it.

== HALT-AND-REPORT ==
When your lane shows 0 remaining (lane ids intersect --status remaining == empty) OR you reach ~300k context: write `output/describe-fill/fleet/slice-2-STATUS.md` -- lane size, ids persisted this run, ids still remaining (if smell-zone halt), verdict distribution, any DEFECTIVE/HALTED batch + why, last --fingerprint (informational only). Then halt with a one-paragraph structured status (DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED). Do NOT continue past your lane. Do NOT commit the status file -- a later single reconciliation terminal reads all 4 slice-STATUS files, runs Task 3/4/5 + the phase boundary, and commits.
