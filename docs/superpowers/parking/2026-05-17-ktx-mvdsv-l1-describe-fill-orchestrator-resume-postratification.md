# KTX/MVDSV L1 describe-fill -- orchestrator RESUME (Phase 3, amendment RATIFIED, Stage B dispatched)

**For:** a fresh terminal resuming the `arc-orchestrator` role. Created
2026-05-17 after the parameterized-family-lane D6 amendment was drafted,
operator-corrected to a THREE-way taxonomy, and RATIFIED. SUPERSEDES
`2026-05-17-ktx-mvdsv-l1-describe-fill-orchestrator-resume-postphase3gated.md`
(that said "amendment orchestrator-owned, draft + ratify pending" -- now
done; kept only as git trail).

---

## THE HOLISTIC GATE IS CLEAN AND CONSUMED. DO NOT RE-RUN IT. DO NOT RE-READ THE 9,300-LINE PLAN.

Once-per-arc, consumed. Phases 0+1+2 SHIPPED + orchestrator-verified.
The D6 family-lane amendment is DRAFTED + REVISED + RATIFIED + LOCKED.
Do not re-litigate any of it.

## Where things are (verified live this session 2026-05-17 -- not relayed)

- **Phase 3: IN PROGRESS, mid-volume-loop, amendment RATIFIED, Stage B
  dispatched.** `--status` **26 / 598** (command 22/336, cvar 4/255,
  info_key 0/7); `--fingerprint` **`87349f25a85a37b0c25e5529ea5600f5`**
  (re-confirmed live this session -- the loop has NOT advanced since the
  gate; the 26 done are intact + carried). 134 owned rows;
  `k_short_gib` `synthesized` terminal, 1 row.
- **F-D4a HOLDING** (re-proven byte-identical mid-loop at `fe31ab68`,
  owned-rows fingerprint `a37eb98a...`; the desync did zero data harm).
  Do NOT re-run the re-derive proof now -- it is the phase-BOUNDARY
  deliverable, not a mid-loop one.
- **The D6 amendment is LOCKED + RATIFIED** in
  `docs/superpowers/plans/2026-05-16-ktx-mvdsv-l1-describe-fill/decisions.md`
  (the dated block under D6; Ratification line reads "RATIFIED
  2026-05-17 (operator); cat-3 = option (b) cohort-scaffold"). The
  taxonomy is **THREE-way** (operator-surfaced, orchestrator-verified +
  drafted + locked):
  - **Cat 1 heterogeneous individual (512; 508 remaining):** per-knob
    Opus-4.7-MAX, the proven loop, UNCHANGED.
  - **Cat 2 index-twin (74; 53 remaining; 6 families xfav_go 20 /
    favx_add 20 / UserMode 17 / TimeSet 6 / ksound 6 / ChangeDM 5):**
    ONE Opus-4.7-MAX family eval + N cheap per-member index
    substitutions + the HARD binding-divergence catch.
  - **Cat 3 namespace cohort `k_fbskill_*` (38; 37 remaining) = RATIFIED
    option (b) cohort-scaffold:** ONE Opus-4.7-MAX shared-mechanism pass
    + each of 38 INDIVIDUALLY source-grounded (D8 bar per-member, NO
    semantic collapse, NEVER twin-collapsed).
  - The divergence-catch is a **CLASSIFIER, not just a safety net**: a
    cohort fed to the cat-2 lane mass-rejects -> route to the cat-3
    scaffold. This is the load-bearing risk -- not tokens.
- **Stage B = a fresh arc-executor terminal** (NOT this orchestrator).
  Entry point:
  `docs/superpowers/parking/2026-05-17-ktx-mvdsv-l1-describe-fill-phase3-executor-resume.md`
  (GATE banner flipped to "STAGE B ... RATIFIED"; it chains to
  `phase-3-executor-prompt.md` + invoke `arc-executor`). The executor
  builds the cat-2 lane + cat-3 scaffold beside the proven cat-1 loop,
  PROVES them (planted false-twin ejected + a `k_fbskill_*` slice the
  classifier mass-rejects and routes to scaffold) before volume, then
  resumes the three-way-split volume loop.
- **Commits this arc since the gate:** `fe31ab68` (desync-recovery) ->
  `d2a299b5` (parallel terminal: three-way taxonomy surfaced in the
  intent doc; +calibration 3/3 exact) -> `3e667f83` (orchestrator: draft
  amendment) -> `c211a72a` (orchestrator: revise to three-way) ->
  `aeb3bc84` (RATIFIED + Stage B unblocked). `51604f67` is the parallel
  `enforce-L1` arc -- NOT ours.

## The orchestrator's next action: STAND BY (do not poll)

Stage B is a fresh executor terminal's job. The orchestrator does NOT
execute it and does NOT poll it (`feedback_be_decisive`; the handoff
discipline). The next orchestrator action is **phase-boundary
verification when the executor halts** -- many terminals away (cat-1 is
508 knobs; this is a long multi-terminal loop). If resuming cold and the
executor has not yet halted at the Phase-3 boundary: confirm state
(below), then stand by.

## Reads required (MINIMAL)

1. This resume.
2. `decisions.md` -- the dated D6 amendment block (LOCKED; the three-way
   taxonomy + cat-3=(b) + the classifier-catch + the inventory table).
3. The Stage-B entry point
   (`...-phase3-executor-resume.md`) -- the banner + the recipe +
   batch-loop learnings (cat-1 mechanics stay valid).
4. `review-findings.md` -- F-D6a, F-D4a, F-C3c, F-D11c, F-D9b,
   F-D10c + the ownership table.
5. `2026-05-17-phase3-family-lane-amendment.md` ORCHESTRATOR ADDENDUM
   (the operator-locked three-way + cohort-scaffold source-of-intent).
6. Invoke `arc-orchestrator`. Confirm state WITHOUT re-deriving
   (tell-tales below). Sibling-arc (`enforce-L1`) misdirection -> STOP.

## Confirm-state tell-tales (cheap, read-only -- NOT a re-derive)

- `cd apps/qw-oracle && bun scripts/describe-fill/synthesize-ktx.ts
  --status` == 26/598; `--fingerprint` == `87349f25a85a37b0c25e5529ea5600f5`.
- `decisions.md` D6 Ratification line == "RATIFIED ... cat-3 = option
  (b)"; the Stage-B banner present on the executor-resume.
- `git log --oneline` shows `aeb3bc84`/`c211a72a`/`3e667f83`. Owned
  rows 134; `k_short_gib` synthesized 1 row.
- A mismatch (esp. `--fingerprint` drift) means INVESTIGATE, not
  proceed -- but the loop should not have advanced unless a Stage-B
  executor ran.

## When the executor halts at the Phase-3 boundary (run YOURSELF -- a dispatched PASS is a hypothesis)

The non-negotiable F-D4a owned-rows re-derive-safe proof (both types,
byte-identical before/after a real `re-derive --project ktx --type
cvar` AND `--type command`); coverage == the live POST-Phase-0 M per
bucket with the C1-outreach residue enumerated; D7 tier-1 ran on every
synthesized row; F-C3c (ZERO KTX commands dead-stamped -- the whole
competitive surface); F-C2a/D10 meaning-conflicts at the D7 tail;
F-D11c flat `structured_choices`; idempotent re-run byte-identical;
C5 probes GREEN; **the cat-2 lane proof (planted false-twin EJECTED) +
the cat-3 classifier proof (a `k_fbskill_*` slice mass-rejected from
the twin lane and routed to the scaffold) present in the run report**;
the 22 carried laneable members not redone. Then ratify + capture
cross-phase memory + arc-history + generate the Phase 4 executor prompt
(carry: the three-way taxonomy is reusable -- Phase 4 classifies its
OWN MVDSV categories by source-grep, never assumes KTX's; F-D6a
grep-verify per-member all lanes; F-D12a no NN/NN; F-C1a recon
POST-Phase-0 M live; the `mvdsv.6` D9 sibling parser; F-C3d Phase-5
fetch+SHA-pin).

## Critical rules (locked; carried)

- The three-way taxonomy + cat-3=(b) cohort-scaffold + the
  divergence-catch-as-classifier are RATIFIED + LOCKED. A dated
  amendment GOVERNS original C/D text (Amendment precedence). Never
  twin-collapse a cohort (the one hard LOCK on cat-3).
- D6 + D7 = Opus 4.7 MAX, spec-locked, NOT lowerable. The lane is
  effort-routing at family/cohort granularity, NOT a model downgrade
  and NOT a scope cut (D5-faithful; every member still its own owned
  row + verdict + D7 tier-1 + anchor + `source_ref`; C1 M unchanged).
- F-D4a non-negotiable: re-confirm green at the fill-phase boundary
  (the owned-row guard is LIVE; phrased `IS DISTINCT FROM`, not `IN`).
- F-D6a per-member in ALL THREE lanes -- independently grep-verify any
  sub-agent line/handler/conflict claim BEFORE persist; concentrated by
  the lane, not relaxed. Per-member verifier keys off the manifest
  `canonical_id` (F-D10b lowercase: `"XonX"` -> `ktx:command:xonx`).
- Verification discipline highest priority: re-derive load-bearing
  numbers via psql/grep/ls; a prior session's "verified" (incl. this
  doc) and a dispatched terminal's "PASS" are hypotheses.
  (`docker exec qw-oracle-postgres-dev psql -U qworacle -d qw_oracle
  -tAc "<SQL>"`; `cd apps/qw-oracle && bun
  scripts/describe-fill/synthesize-ktx.ts --status|--fingerprint`.)
- Operator: non-coder, conceptually fluent, NOT the technical gate (you
  are); plain-English-first, one decision at a time, recommend don't
  poll; momentum over ceremony. The operator self-surfaced the
  three-way correction -- treat operator conceptual signals as
  load-bearing, verify + draft + lock them. Commit ONLY this arc's
  files (parallel `enforce-L1` + sidecar drift is not ours).

## When in doubt

The amendment is LOCKED + RATIFIED (cat-3=(b)). Phases 0+1+2 verified.
F-D4a holds mid-loop. The three-way split is the contract; the
divergence-catch-as-classifier is the real risk (not tokens). Stage B
is a fresh executor terminal -- stand by, do not poll. Genuine
decisions route to the operator with a decisive plain-English
recommendation, one question at a time.
