# Handover

Thin docket of pending work. The index below points at lifecycle-grouped sub-sections; arc bodies live in `docs/superpowers/parking/<topic>.md` and shipped retrospectives live in each project's `arc-history.md`. Small-followup bodies stay inline.

This file is referenced from `MEMORY.md` so every new session sees the open-items count at context load. Memory is for durable facts; this file is for todo state.

**How to work an item:** pick from the index below, jump to its destination (parking file or inline section), verify the described state still matches reality (code changes quickly, handover notes can rot), then address it. When done, delete BOTH the index line AND the destination content (parking file → `git rm`; inline section → Edit; arc-history entries are append-only and stay).

---

## Open items

### Active arcs (in-flight; pickup-from-cold dashboard)

Per-arc state for arcs currently in flight. Each arc's plan-dir README has a "Where we are right now" block (Stage / Last action / Next action) -- this section lists the headline so you can pick which arc to work on at a glance. Update when stage/next-action changes.

- **KTX L1 catalog visual review walk (in-flight, end of session 3 2026-05-23)** -- operator-driven cold review of the rendered catalog HTML to surface description-quality findings and template patterns. Findings + drafts + cold-resume block live in `apps/qw-oracle/docs/reviews/2026-05-22-ktx-l1-catalog-findings.md` (read the top `## Resume in a fresh session` section). State end of session 3: **48 L1 drafts locked** (32 session-1 + 13 session-2 + 3 session-3: `k_entityfile` / `callalias` / `qizmo`); 4 open findings; 7 follow-up arcs surfaced (incl. `ktx-l1-rewrite` skill build with park-when-ambiguous spec); KTX shape catalog at **14+ shape variants** (sessions 2-3 added Shapes 1c/1d/6/7-7a-7b/8/9-9a-9b/10 + canonical-card + command-per-value fan-out + tooling-mode prereq + earn-their-keep discipline). Four template-system memory files locked ([[feedback-l1-description-template]] v2 + [[feedback-mod-l1-documentation-architecture]] + [[reference-ktx-entity-categories]] + [[reference-ktx-cvar-command-pairing]]). **`ktx-l1-rewrite` skill BUILT + battle-tested + amended 2026-05-23** (Step 1.5 behavioral unpacking + shape-less verdict baked in after battle-test). Apply path stays parked; drafts NOT yet written to L1 `entities.description`.
- **ktx-l1-rewrite Server config & network fan-out -- SHIPPED 2026-05-24** -- all 57 cards in the category drafted (sub-batches 1+2 prior + 29-card continuation 2026-05-24 = 37 drafted clean + 20 drafted_with_flag + 1 parked callalias; ~35% cumulative flag rate caught real factual errors in the format-unify arc's output -- wrong defaults, mislabeled fields, missing prereqs, permission framing errors). Files: `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-{drafts,parked}-2026-05-23.md`. SKILL.md amendments (Step 1.5 + shape-less verdict) battle-validated at scale across 57 cards. Residual items captured in Small followups below.
- **KTX post-review investigation + prod-prep** -- KTX Layer 1 Onboarding SHIPPED 2026-05-07 (retrospective in `apps/qw-oracle/docs/arc-history.md`; review `docs/superpowers/reviews/2026-05-07-ktx-onboarding-arc-review.md`). Post-review Issues #1 (cvar embedding gap) + #2 (match_event descriptions) SHIPPED -- commits `02155560` / `7eeaa410` / `7cd951c4` + migration 012 (description_origin). **Still pending -- prod deploy gated on these:** #3 (gameplay_taxonomies parallel-stat fix, ~30 LOC, Pattern 13 retrofit), #4 (orientation blob update -- operator decision), #5 (idempotency-ktx.sh excludes timestamp/run-metadata cols). Fresh-terminal handoff: `docs/superpowers/parking/2026-05-07-ktx-postreview-investigation-and-prod-prep.md`.
- **qwiki-v1-beta** -- **Phases 1, 2, 3 SHIPPED 2026-05-14** at https://wiki.slipgate.me. Phase 1 (MW core substrate; nginx 1.30-alpine + mediawiki:1.43-fpm + mariadb 11.4 LTS + Citizen v3.16.0). Phase 2 (Page Forms 5.8.1 + Semantic MediaWiki 6.0.1 + F6 LOAD list: ParserFunctions / Cite / CategoryTree / TemplateData). Phase 3 (PluggableAuth v7.5.0 + OpenIDConnect v8.3.0 + Discord OAuth bot-mode role sync via wiki.Quake.World bot + wiki-contributor / wiki-curator MW groups + D5 namespace gates). Findings F1-F9 all RESOLVED + closed (D2 Amendments #1+#2; D3 Amendment 2026-05-14 path-info URL form; D4 Amendment 2026-05-14 ratifying bot-mode role sync). Phase 4 (quality-tag categories + Layer 3 harvest verification) approved + executor prompt drafted; **awaiting once-over by fresh orchestrator terminal before dispatch.** Orchestrator session retired at ~400k+ context 2026-05-14; resume handoff at `docs/superpowers/parking/2026-05-14-qwiki-v1-beta-orchestrator-resume-phase4.md`. Phase 4 executor prompt at `docs/superpowers/parking/2026-05-14-qwiki-v1-beta-phase-4-executor.md`. Phases 5-8 (Modes vertical slice) deferred until substrate ships. Plan: `docs/superpowers/plans/2026-05-12-qwiki-v1-beta/README.md` ("Where we are right now" block has current Stage / Last action / Next action). Two Phase 3 cosmetic followups deferred to a separate polish micro-commit (see Small followups).
- **QWiki community-reference -- Phase 4 LLM-extraction (paused, ready to resume)** -- the modernize-in-place arc is SUPERSEDED by qwiki-v1-beta; what stays LIVE is the community-data Phase 4: Phases 0-3 SHIPPED (players + clans + snapshot + curated/ rename + migration 008), Phase 4 PAUSED at T2 (D4 pivot to LLM-with-checklist extraction for tournaments; deterministic stays for players/clans). Resume: fresh terminal with `superpowers:brainstorming`, deliverable = design doc + redrafted Phase 4 MD, then orchestrator session #3. Sidequest design doc: `docs/superpowers/parking/2026-05-05-qwiki-phase-4-llm-extraction-sidequest.md`. Plan: `docs/superpowers/plans/2026-05-04-qwiki-community-reference/README.md`. F28/F29 tracked in that plan's review-findings.md.

When an arc finishes its planning stage and moves to implementation, the entry stays here with stage updated to "implementing." When an arc ships, delete the entry; the retrospective lives in the project's `arc-history.md`.

### Small followups
- **KTX info_key wider-sweep triage (follow-up to 2026-05-27 userinfo arc)** -- the all-sites emission amendment surfaced 51 KTX info_key entities (up from 7). The 6 named user-facing keys (`kf`, `k_nick`, `k`, `k_sdir`, `postmsg`, `premsg`) shipped with rich v2-shape descriptions; 7 producer-side star keys (`*at`, `*is`, `*ml`, `*mm`, `*mp`, `*mt`, `*mu`) already had synthesized descriptions; the remaining ~38 entities surfaced with `source_inline` auto-stub descriptions and need triage: (a) cross-engine reads (`bottomcolor`, `topcolor`, `rate`, `team`, `gender`, `ip`, `login`, `nocolors`, `Qizmo`, `ping`, `skin`, `*cheats`, `*client`, `*state`, `*userid`, `*version`, `tp_need_weapon`, `tptook`) -- belong semantically to ezQuake CVAR_USERINFO / QW protocol; should get one-line "read by KTX; defined by <producer>" pointer descriptions or stay null; (b) KTX-semantic candidates worth investigating (`matchtag`, `runes`, `railcolor`, `pbspeed`, `wps`, `wpsx`, `w_rank`, `ktpl`, plus the 2-letter `iKey` codes `di`, `ev`, `fpd`, `fs`, `ti`, `ln`, `lra`, `ls`, `lw`, `lw_x`, `mi`, `nrb`) -- need per-key source investigation, likely a single batch under `ktx-l1-rewrite` skill once triaged. The 2026-05-27 design-decision parking doc at `docs/superpowers/parking/2026-05-27-ktx-userinfo-consumer-handler-design-decision.md` captures the architectural shift. Sized ~2-4 hours for triage + cold-synth + apply across the ~38 entities. [small followup]


- **ktx-l1-rewrite Frogbot category (follow-up) -- apply pass for 15 `k_fb_*` cvars missed by chunked-mode dispatch** -- post-arc 16-entity gap closure batch for the Frogbot category. The original `2026-05-26-frogbot` batch (78 cards) used semantic-intuition entity-list assembly (catalog HTML walk grouped by what `botcmd` dispatches and what's the primary command in a Shape 1c pair) and missed the 15 underlying `k_fb_*` cvars themselves -- they were captured in DB `category_inferred = 'Frogbot'` but not in the entity-list for that batch. The pre-flight #5 category-enumeration audit gate (amendment 2026-05-27) surfaced the deliberate 78-entity diff at dispatch time; explicit accept proceeded. This follow-up batch ships the 15 cvar cards (13 drafted_clean + 2 drafted_with_flag + 0 parked) at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-frogbot-followup.md`. Apply-pass-author: 2 FLAG bullets -- (a) `k_fb_debug` registered-but-inert at anchor; awaiting community/operator confirmation on whether the cvar has any out-of-tree consumer or whether the commented-out set-site is expected to be re-enabled; (b) `k_fb_easy_skill_mode` existing description names a wrong downstream-cvar family (`k_fb_skill_*` / `k_fb_aim_*` / `k_fb_movement_*` -- none exist; the real family is `k_fbskill_*`). Cross-card finding F1 surfaces 5 ToT-mode-scoped cvars (`k_fb_break_on_death`, `k_fb_health`, `k_fb_quad_multiplier`, `k_fb_item_pickup_bonus`, `k_fb_weapon`) currently mislabeled `category_inferred = 'Frogbot'` in DB -- tracked separately as categorization-accuracy follow-up #15 (this batch does NOT touch DB categorization, only L1 description prose). F2 highlights `k_fb_quad_multiplier`'s universal-attacker semantics (no `isBot` gate -- affects humans too in dmm4+ToT); preserved in headliner + Effect. [small followup]
- **ktx-l1-rewrite Race category (follow-up) -- apply pass for 1 missing pair-half** -- post-arc 16-entity gap closure batch for the Race category. The original `2026-05-27-race` batch (45 cards) classified `k_race_simultaneous` + `race_simultaneous` as Shape 1 + Shape 1c but folded the cvar half into the command card's body instead of emitting a separate `## k_race_simultaneous` header. This follow-up batch ships the missing cvar card (1 drafted_with_flag, 0 parked) at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-race-followup.md`. Bidirectional See-also is already in place (the original command card at line 564 of `ktx-l1-rewrite-drafts-2026-05-27-race.md` already cross-links back to `k_race_simultaneous`), so no amendment to the original race batch is required. Apply-pass-author: 2 FLAG bullets in Notes -- (a) Default 0 at registration vs race_settings[] bundle overriding to 1 at race-mode activation (race.c:308); (b) Set-by adds the paired toggle command (omitted in current L1). [small followup]
- **ktx-l1-rewrite Server config & network -- apply pass + 2 deferred followups** -- (a) **Apply pass** -- operator audits the drafts file then applies clean drafts to `entities.description`; 20 `drafted_with_flag` entries need operator review of the surfaced factual change before applying. Drafts at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-23.md`. (c) **Cross-card consistency on FPS-kick threshold** -- `k_noframechecks` says "after 3 accumulated FPS warnings" (imprecise); `maxfps` says "the fourth warning in a session triggers a forced disconnect" (source-true per `fIllegalFPSWarnings > 3` at `client.c:3868-3870`). Apply-pass-author should align both cards' wording to the maxfps phrasing. (d) **`teamoverlay` vote command pending** -- the Shape 7b pair partner of `k_teamoverlay` (drafted 2026-05-24 as Shape 7b state-cvar) is NOT in the Server config & network category catalog -- should be drafted in a later batch (likely Spectator chat & visibility or a future Voting category). (Items (b) shape catalog correction + (e) spec amendment for Step 1.5 + shape-less SHIPPED 2026-05-25.) [small followup]
- **ktx-l1-rewrite Spectator chat & visibility -- apply pass + cross-card findings** -- 8-card batch SHIPPED 2026-05-25 (re-drafted 2026-05-26 after Shape 11 crystallization): all 8 cards now drafted, **0 parked**. **5 drafted clean** (k_ann Shape 3 / nospecs Shape 7b / silence Shape 1 / infolock Shape 11a / infospec Shape 11a) + **3 drafted_with_flag** (k_sayteam_to_spec Shape 3 / k_spectalk Shape 1 / k_spec_info Shape 11a). All in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-25.md`; no parked file. (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 3 `drafted_with_flag` entries need review before applying. Same operator phase as the Server-config-batch apply pass above; tackle in sequence. (b) **Cross-card findings (4 actionable + 1 obsoleted + 2 confirmed clean)** -- F1: k_ann + k_sayteam_to_spec See-also misdescribe `silence` as per-spectator mute (it's a server-wide `k_spectalk` toggle at `commands.c:745`); F2: k_spectalk Permission line says "admin command" but `silence` is `CF_PLAYER | CF_SPC_ADMIN` -- align to silence card's two-phase Permission; F3 **OBSOLETED 2026-05-26** (k_spec_info now drafted, See-also references safe in both directions); F4: k_sayteam_to_spec value descriptions inherit `match_in_progress` 0/1/2 semantics imprecision (`world.c:1456`); F5+F6 confirmed clean (Shape 1 cvar_fset classification + bit ownership). Full details in `## Cross-card consistency notes` section at end of drafts file (includes a 2026-05-26 addendum). (c) **A/B probe finding** -- cards 1+2 at Sonnet xhigh produced comparable depth to the prior Server-config batch at Sonnet high; high is sufficient. No SKILL.md model-dial amendment recommended. Cards 3-8 ran at high. (d) **Shape 11 crystallized 2026-05-26** -- "per-bit XOR toggle on shared bitmask state container" with sub-facets 11a (cvar-backed; k_spec_info family) and 11b (serverinfo-backed; fpd qizmo family). Earn-their-keep gate met with 2 instance families (k_spec_info MI_ON/MI_ADM_ONLY + fpd qlag/qenemy/qpoint bits at `commands.c:3686-3736`). Skill `references/shape-catalog.md` and spec doc both amended (identification-guide decision tree + section between Shape 10 and Tooling-mode prerequisite). (e) **Open follow-ups** -- (i) Shape 11b candidates for a future Spectator-area batch: qizmo's `ToggleQLag` / `ToggleQEnemy` / `ToggleQPoint` exist as L1 entities under Shape 10 (qizmo's curated family) but have not been recast under Shape 11b; also surface a Shape-11b-composition note for `silence`'s mid-match fpd-bit-64 side effect. (ii) `moreinfo` (`commands.c:7151-7175`, registered at `commands.c:932`) -- valid L1 entity not in this batch's 8-card scope; needs drafting in a future Spectator-area batch. [small followup]
- **ktx-l1-rewrite Voting -- apply pass + cross-card findings** -- 34-card batch SHIPPED 2026-05-26: all 34 cards drafted, **0 parked**. **10 drafted clean** + **24 drafted_with_flag** (high flag rate dominated by the systematic k_vp_* "stored 0, clamped to 51 at tally" default-handling FLAG, which surfaces real-but-non-foundational drift between user-facing defaults and source-stored values). All in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-26.md`; no parked file. (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 24 `drafted_with_flag` entries need review before applying. Same operator phase as the Server-config + Spectator-chat batch apply passes above; tackle in sequence (third batch now queued). (b) **Cross-card findings (4 actionable)** -- F1: **swapall admin-veto Effect is factually wrong** -- draft says "the vote then clears without executing the swap" but source `vote_check_swapall()` shows the team-swap for-loop is INSIDE the same `if (veto || !get_votes_req(...))` block; swap IS executed on admin veto (broadcast distinguishes path "Admin veto for Swapall" vs "Majority vote for Swapall" but effect identical). Apply-pass-author MUST fix this before applying swapall's draft. F2: **k_vp_hookstyle Effect/Notes internal inconsistency** -- Effect prose says "A single admin vote passes any of the three hook-style channels" but `is_admins_vote(OV_HOOKCRHOOK)` at `vote.c:1358` confirms hook_crhook also has admin veto; Notes correctly say "all 4 hook handlers." Align Effect to match Notes ("all 4"). F3: **hook_crhook header includes "Shape 1c" inconsistently** -- hook_classic / hook_fast / hook_smooth headers all omit Shape 1c; hook_crhook alone has `"Shape 7b + Shape 1c, reference card"`. Align to `"Shape 7b + fan-out modifier [REFERENCE CARD]"` (Shape 1c is for binary toggle commands per catalog, NOT vote commands; CTF prereq belongs in Prerequisites slot). swapall's Shape 1c tag has no sibling so stands. F4: **k_vp_map skipped the default-stored-0 FLAG annotation** all other non-k_vp_suggestcolor k_vp_* cards carry; content factually accurate but lacks the FLAG prefix; apply-pass-author should treat default consistently as "51 (effective; stored as 0)" per peer cards. Full details + clean assessments for checks 1/3-7/9-12 in `## Cross-card consistency notes` section at end of drafts file. (c) **No new shapes surfaced** -- Shape 11 already crystallized in prior batch. Sub-agent verified NO Shape 11b composition for teamoverlay (handoff speculation was wrong: `vote_check_teamoverlay` + `teamoverlay` handler touch only `self->v.teamoverlay` and `k_teamoverlay`, no fpd writes). (d) **Cross-card facts established (operator reference for future Voting-area batches)** -- (i) `k_vp_suggestcolor` is the ONE k_vp_* registered via `RegisterCvarEx("k_vp_suggestcolor", "51")` with an explicit default; all other k_vp_* use bare `RegisterCvar` (stored 0, clamped to 51 at tally time via `bound(51, percent, 100)` at `vote.c:330`). (ii) Min-vote floors vary by OV_* case: `max(2, vt_req)` for OV_ELECT (all election types: admin/captain/coach/suggestcolor/latejoin) + OV_PRIVATE + OV_TEAMOVERLAY + OV_NOSPECS-ON-direction; `max(1, vt_req)` for OV_HOOKSMOOTH/FAST/CLASSIC + OV_COOP + OV_NOSPECS-OFF-direction; `max(3, vt_req)` for OV_RPICKUP + OV_SWAPALL (highest); no min floor for OV_PICKUP + OV_MAP; OV_HOOKCRHOOK ABSENT from `get_votes_req()` switch (always uses hardcoded 51%, `k_vp_hookstyle` does NOT apply to crhook); OV_BREAK has `max(1)` (live match) / `max(2)` (matchless countdown). (iii) Admin-veto presence per OV_* case: PRESENT for OV_ANTILAG + OV_COOP + OV_NOSPECS + OV_TEAMOVERLAY + OV_PICKUP + OV_RPICKUP + OV_SWAPALL + OV_PRIVATE + all OV_HOOK* (incl. OV_HOOKCRHOOK); ABSENT for OV_ELECT (no admin-veto for elections of any type) + OV_MAP (admin gets tie-breaking priority among equally-nominated maps instead) + OV_BREAK (separate `forcebreak` command for admin override). (iv) Threshold-cvar sharing: `k_vp_map` governs both `cm` AND `next_map` (multi-consumer per vote.c:244-246); `k_vp_rpickup` governs both `rpickup` AND `swapall` (deliberately per source comment at `vote.c:252-253` "don't need a dedicated 'swapall' percentage"); `next_map` uses the OV_BREAK channel (shares PlayerBreak handler with `break`; next_map is the CF_MATCHLESS_ONLY alias). (v) `hook_crhook` tally is INVISIBLE in `whovote` -- `ModStatusVote` at `commands.c:2290-2348` enumerates OV_HOOKSMOOTH/FAST/CLASSIC tallies but silently skips OV_HOOKCRHOOK. Surfaced in both whovote and hook_crhook cards. (e) **Open follow-ups for future batches** -- (i) `break` / `forcebreak` / `rpickup` commands referenced as cross-links but NOT in this batch's category; likely surface in Match flow or related future Voting-area batch. (ii) State cvar `k_ctf_hookstyle` referenced by all 4 hook cards but in a non-Voting category (likely Mode-scoped knobs or Gameplay rules); cross-linked by name only. Engine cvars `coop` / `sv_login` / `sv_antilag` referenced by name only (mvdsv-level, not KTX L1 surface). (iii) `captain` (commands.c:803, CF_PLAYER) and `coach` (commands.c:804, CF_SPECTATOR) election-starter commands referenced in k_vp_captain + k_vp_coach drafts -- verified registered as KTX L1 entities; likely surface in a future Election or Team-management batch. (iv) Cumulative ktx-l1-rewrite drafts: 99 of 618 KTX L1 entities = ~16%. [small followup]
- **ktx-l1-rewrite Scoring & stats -- apply pass + cross-card findings (chunked-mode calibration)** -- 19-card batch SHIPPED 2026-05-26: all 19 cards drafted, **0 parked**, **0 novelty halts**. **14 drafted clean** + **5 drafted_with_flag**. First-ever chunked-mode dispatch (4 chunks of 6+5+4+4 entities). All in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-26-scoring-stats.md`; no parked file. (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 5 `drafted_with_flag` entries need review before applying. Same operator phase as the Server-config + Spectator-chat + Voting batch apply passes above; tackle in sequence (fourth batch now queued). (b) **Cross-card findings (6 actionable + 2 confirmed clean)** -- F1: **`stats` post-match vs mid-match timing INVERSION** -- existing L1 description says "Only works after a match has ended" but source `PlayerStats` (commands.c:3557) fires only when `match_in_progress == 2`; handler comment explicitly says "Nothing to do with the endgame stats." The post-match equivalent is `laststats`. Foundational framing error caught at flag level; apply-pass-author MUST verify timing before applying. F2: **`scores` permission mismatch** -- existing says "any player" but CF_BOTH includes spectators; corrected to "any player or spectator". F3: **`k_on_*_f_*` "KTX broadcasts" framing inaccurate** -- 2 canonical cards (k_on_end_f_modified, k_on_start_f_modified) flagged. Source shows KTX stuffcmds ONE player to say the trigger word (end group: first player in entity iteration; start group: self = last-ready player); rest of f_ chain is client-side alias behavior. 4 reference cards inherit corrected framing. F4: **k_on_start_f_* hoonymode exception missing** -- isHoonyModeAny() branches to HM_all_ready() bypassing cvar checks; existing descriptions silent on this. F5: **k_dmgfrags LGC interaction missing** -- enabling LGC mode forcibly clears k_dmgfrags (commands.c:7869-7871); both cannot be active simultaneously. F6: **`stats`/`effi` alias content drift** -- byte-identical PlayerStats handler but existing descriptions diverged (stats wrong, effi right); v2 standardizes both to mid-match. F7 + F8: **CONFIRMED CLEAN** -- cross-batch state-printer family See-also threading symmetric with prior Server-config batch (`fpslist`, `rules`); canonical-card pattern applied consistently 5 times across 12 of 19 cards (k_on_end_f_* trio + k_on_start_f_* trio + lastscores/lastscoresktx + +scores/-scores + +wp_stats/-wp_stats). Full details in `## Cross-card consistency notes` section at end of drafts file. (c) **Chunked-mode calibration observations** -- (i) 0 novelty halts (no trigger 1/4) -- the 14+ shape catalog covered the category cleanly; matches expectation that mature catalog reduces park rate. (ii) Token cost: chunk 1 (6 entities) 12.9k/entity, chunk 2 (5) 17.8k, chunk 3 (4) 21.0k, chunk 4 (4) 20.4k. Front-matter overhead amortizes better with larger chunks; chunk_size=6 is the sweet spot in this batch. (iii) Total 332k tokens for 19 entities vs ~950k baseline (one-sub-agent-per-entity) = **65% token reduction**. Matches theoretical ~56-83% range; landed at the better end because category mix allowed deep cross-source family-walking. (iv) Engine `+`/`-` press/release bind pairs (+scores/-scores, +wp_stats/-wp_stats) correctly classified as shape-less -- engine convention, not KTX Layer B relationship; no trigger 1 park, earn-their-keep test fails (universal QW engine pattern, not KTX-relational). (v) The 5 flag rate (26%) is dominated by the 6 k_on_* canonical+factual issues (broadcast framing + hoonymode + LGC interaction) -- real factual-fix queue, not noise. (d) **Open follow-ups for future batches** -- (i) `fpslist` and `rules` referenced as state-printer family siblings (Server-config batch, already drafted); bidirectional See-also confirmed clean at F7. (ii) Hoonymode reference at F4 suggests a future hoonymode-area batch should consider the start-of-match k_on_start_f_* exception when drafting hoonymode-scoped cvars. (iii) LGC mode interaction at F5 cross-references LGC family (likely in Mode-scoped knobs or Gameplay rules); when those batches run, document `k_lgc` -> `k_dmgfrags` mutual exclusion symmetrically. (iv) Cumulative ktx-l1-rewrite drafts: 118 of 618 KTX L1 entities = ~19%. [small followup]
- **ktx-l1-rewrite Mode selection -- apply pass + cross-card findings (chunked-mode batch 5; chunk_size=7)** -- 28-card batch SHIPPED 2026-05-26: all 28 cards drafted, **0 parked**, **0 novelty halts**. **21 drafted clean** + **7 drafted_with_flag**. 4 parallel chunks of 7 entities each (chunk_size up from 6 in the prior batch's calibration; held within context budget cleanly). All in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-26-mode-selection.md`; no parked file. (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 7 `drafted_with_flag` entries need review before applying (`dmm5`, `berzerk`, `midair`, `totmode`, `k_mode`, `lgcmode`, `coop_nm_pu`). Same operator phase as the Server-config + Spectator-chat + Voting + Scoring-stats batches; tackle in sequence (fifth batch now queued). (b) **Cross-card findings (5 actionable + 3 confirmed clean)** -- F1: **"Admin command" mislabeling pattern across 5+ commands** -- multiple existing L1 descriptions use "Admin command" / "Set by: admin command" wording for commands whose source flags are `CF_PLAYER | CF_SPC_ADMIN` (any player invokes; spectators need admin). Affects `dmm5`, `midair`, `totmode`, `race`, `fresh`, `dmm2` (corrected silently). Systemic L1 corpus issue likely beyond this batch; apply v2 Permission lines as drafted and treat as same family fix in future batches. F2: **CONFIRMED CLEAN** -- two mode-preset mechanisms both correctly shape-less. `dmm1-5` use `ChangeDM(N)` dispatch (direct `deathmatch` cvar set, no `*_um_init[]` bundle); all other presets use `DEF(UserMode)` with named bundles + configs/usermodes/<mode>/*.cfg overrides. Both classifications correct per shape-less discipline; future L3 concept note should distinguish the two mechanisms. F3: **Shape 1d triad partial** -- `tot` + `totmode` in this batch, but the state cvar `k_tot_mode` is in a different category and not yet drafted. Apply-pass-author should verify symmetric See-also threading when `k_tot_mode` lands (or defer applying `tot` + `totmode` until the triad is complete). F4: **CONFIRMED CLEAN** -- `gamemodes` correctly NOT Shape 10. Filtered discovery printer (output filtered by which 25 known mode commands are server-registered; prints names only, no inline descriptions); the catalog's "neighbors-not-Shape-10" test correctly ruled it out. Second batch to surface a Shape 10 false-positive (prior: `rules` in Server-config batch). F5: **k_bloodfest paired toggle commented out at this anchor** -- `bloodfest()` handler exists at commands.c:3134 with full Shape 1 signature, but registration row at commands.c:740 is commented out. Shape 3 holds at v1.36-1633-g67253dc; if a future KTX release uncomments the row, k_bloodfest shape shifts to Shape 1 and the card needs updating. F6: **CONFIRMED CLEAN** -- cross-batch lgcmode/k_dmgfrags mutual exclusion threaded symmetrically. This batch surfaces the `lgcmode` side (Effect bullet: clears `k_midair`, `k_instagib`, `k_dmgfrags` on toggle); the prior Scoring & stats batch (F5) had already surfaced the `k_dmgfrags` side. Bidirectional cross-link complete. F7: **CONFIRMED CLEAN** -- `dmm4` lever/leaf shape-less pattern applied consistently. `dmm4` referenced by `midair`, `totmode`, `lgcmode` as Prerequisite + by `dmm1/2/3/5` as the "switching away clears modifiers" anchor; dmm4 itself correctly stays shape-less (the relationship lives on the modifier cards). F8: **`berzerk` Shape 1 discipline** -- existing description doesn't name paired cvar `k_bzk`; v2 recast adds it per Shape 1 command-side discipline (Headliner should name the cvar it toggles). Additionally flagged: existing claims a named-player broadcast format but actual is `cvar_toggle_msg` "X on/off" pattern. Full details in `## Cross-card consistency notes` section at end of drafts file. (c) **chunk_size=7 calibration result** -- 4 chunks of 7 entities each fired cleanly; no context-exhaustion warnings from sub-agents. Validates the calibration handoff prediction. Next batch can try chunk_size=8 if category fits (Frogbot at 78 entities / Match flow at ~30 are good candidates). (d) **Open follow-ups for future batches** -- (i) `k_tot_mode` (state cvar, Shape 1d triad partner of `tot` + `totmode`) -- needs drafting in a future batch (likely Mode-scoped knobs or Gameplay rules); apply-pass should verify See-also symmetry when it lands. (ii) `k_midair`, `k_instagib`, `k_lgcmode`, `k_hoonymode`, `k_hoonyrounds`, `k_bzk`, `k_freshteams`, `k_race`, `k_nightmare_pu` -- referenced as paired cvars throughout the batch but in different categories; surface in subsequent batches. (iii) `k_clan_arena_rounds`, `k_clan_arena_max_respawns` -- referenced by `carena` + `wipeout` bundles; in adjacent categories. (iv) `bloodfest` command status revisit -- if a future KTX release uncomments commands.c:740, `k_bloodfest` shape shifts Shape 3 -> Shape 1; flag for next drift-detection pass. (v) Cumulative ktx-l1-rewrite drafts: 146 of 618 KTX L1 entities = ~24%. [small followup]
- **ktx-l1-rewrite Mode-scoped knobs -- apply pass + cross-card findings (chunked-mode batch 6; chunk_size=8 with halt-on-novelty)** -- 66-card batch SHIPPED 2026-05-26: **64 drafted** (35 clean + 29 flagged) + **2 parked** (`roundsdown` + `roundsup`, trigger 1 no-shape-match). 9 parallel chunks of sizes 6-8 (chunk_size=8 calibration target validated; no context-exhaustion warnings on any chunk). All drafts in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-26-mode-scoped-knobs.md`; park file at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-parked-2026-05-26-mode-scoped-knobs.md`. (a) **Apply pass** -- operator audits + applies clean drafts; 29 `drafted_with_flag` entries need review before applying. Flag rate (~44%) is dominated by the F1-audit residue (~24 Permission-line corrections; same systemic mislabel pattern from Mode selection batch F1, now caught batch-wide by the corrected catalog). Same operator phase as the Server-config + Spectator-chat + Voting + Scoring-stats + Mode-selection batch apply passes above; tackle in sequence (sixth batch now queued). (b) **Cross-card findings (11 actionable + 6 confirmed clean + 1 accepted park = 18 total)** -- F1: **batch-wide "Admin command" mislabel pattern** (~24 cards) -- continuation of Mode selection F1; corrected catalog (universal-shape-v2.md Permission table) caught all systematically; the single legitimate "admin only" card is `noga` (CF_BOTH_ADMIN). F2: **k_freshteams_limit_packs / k_freshteams_limit_sweep_ammo default-value factual error** -- existing says Default: 0, source RegisterCvarEx sets '1'. F3: **CONFIRMED CLEAN** Shape 1d triad completion via k_tot_mode (tot + totmode in Mode selection batch); apply-pass verifies symmetric See-also on tot/totmode. F4: **CONFIRMED CLEAN** k_ctf_hookstyle Shape 7b state cvar completion (Voting batch hook fan-out); See-also references all 4 hook commands + k_vp_hookstyle + k_ctf_hook. F5: **DMM4 mutual exclusion network is ASYMMETRIC** -- k_dmm4_gren_mode excluded only by midair+instagib; LGC and TOT do NOT clear it. Each modifier's clear-set is selective; do NOT claim symmetric mutual exclusion across all modifiers. Possible L3 concept note candidate (directed-graph mapping). F6: **Naming-mismatch flags** -- k_cg_kb existing names "cg_kb" (actual: instagib_coilgun_kickback); k_teleport_cap existing names internal C function "setTeleportCap" (actual: teleportcap). v2 corrects. F7: **CONFIRMED CLEAN** k_disallow_weapons + noweapon + no_gl/no_lg correctly NOT Shape 11a (proxy aliases via stuffcmd_flags, not direct per-bit XOR). Third batch with a Shape false-positive correctly ruled out (prior: rules / gamemodes ruled-out-of-Shape-10). F8: **k_midair / k_instagib silent-reset on non-dmm4 map start** not in existing descriptions; v2 adds to Effect. F9: **spawn666time clamp asymmetry** -- command clamps SET to 2s (DMM4_INVINCIBLE_DEFAULT) but direct cvar set accepts up to 30s; tot preset sets -1. Three write paths with three different bounds. F10: **k_ctf_based_spawn 3-value enum** (0/1/2) but toggle only covers 0↔1; value 2 reachable only via direct cvar set. F11: **k_ctf_custom_models scope expanded to CTF || RACE** -- existing said CTF-only; world.c:1162 + race.c:4934 use it for race pacemaker model. F12: **CONFIRMED CLEAN** canonical-card pattern -- 3 applications in batch: k_freshteams_pack_* (4 entities, cells canonical) + k_freshteams_sweep_*_ammo (6 entities, gl_ammo canonical) + k_ctf_rune_power_* (4 entities, DOWNGRADED -- meaningfully different effect formulas; per shape-catalog "near-identical only" caveat). F13: **k_nosweep re-classified Shape 3 → Shape 1c** -- paired toggle `nosweep` exists at source but is NOT in this batch's catalog scope; needs drafting in a future batch with symmetric See-also verification. F14: **CONFIRMED CLEAN** k_hoonymode + Scoring & stats F4 cross-batch coherence (k_on_start_f_* hoonymode bypass lives on consumer side). F15: **CONFIRMED CLEAN** cross-chunk pair bidirectional See-also -- k_ctf_ga ↔ noga + k_cg_kb ↔ instagib_coilgun_kickback both verified symmetric (sub-agents in different chunks independently surfaced the pair). F16: **ACCEPTED PARK** roundsdown + roundsup -- two-command bounded increment/decrement pair on k_hoonyrounds; no cataloged shape captures the pattern; 1-of-1 in KTX (doesn't earn new shape entry per earn-their-keep). Apply-pass hand-drafts as shape-less command-side levers (sub-agent provided suggested Headliners in park entries). F17: **k_instagib auto-enables k_cg_kb on enable** -- surprise-bearing behavior; v2 Effect adds. F18: **k_clan_arena_rounds odd-rounding surprise** -- even values silently rounded up to next odd at match time (CA_wins_required at clan_arena.c:286-290); stored ≠ effective. Full details in `## Cross-card consistency notes` section at end of drafts file. (c) **chunk_size=8 calibration result + halt-on-novelty exercised** -- 9 parallel chunks of sizes 6-8 fired cleanly; no context-exhaustion warnings from any sub-agent. Validates handoff prediction. **First-ever halt-on-novelty exercise in production batches:** trigger-1 park on roundsdown/roundsup fired correctly; dispatcher halted per skill discipline; operator chose ship-and-park override given 1-of-1 evidence (doesn't earn new shape). Halt mechanism + operator-override path both validated. Next batch candidate for chunk_size=10: Frogbot at 78 entities (Shape 8 subcommand dispatcher pattern; expected fewer cross-card threads than this batch). (d) **Open follow-ups for future batches** -- (i) `nosweep` toggle command needed (k_nosweep paired with it but only cvar in this batch; F13). (ii) Hand-draft `roundsdown` + `roundsup` as shape-less levers in apply pass (F16 accepted park; suggested Headliners in park file). (iii) If a future codebase walk surfaces another bounded up/down pair, revisit shape catalog for the F16 candidate-shape question. (iv) DMM4 mutual exclusion network L3 concept note candidate -- F5 surfaced the asymmetry but the directed-graph view doesn't fit in L1 cards. (v) `k_disallow_weapons` + `noweapon` parent-dispatcher follow-up -- `noweapon` classified shape-less here despite Shape 8-like surface; if a future Frogbot or `nwp` walk crystallizes Shape 8 for `cmd_t cmds[]` multi-arg dispatchers, revisit. (vi) Cumulative ktx-l1-rewrite drafts: 210 of 618 KTX L1 entities = ~34% (146 prior + 64 this batch). 2 parked entities tracked for apply-pass hand-drafting. [small followup]
- **ktx-l1-rewrite Frogbot -- apply pass + cross-card findings (chunked-mode batch 7; chunk_size=10)** -- 78-card batch SHIPPED 2026-05-26: all 78 cards drafted, **0 parked**, **0 novelty halts**. **57 drafted clean** + **21 drafted_with_flag**. 8 parallel chunks (7 of 10 + 1 of 8); chunk_size=10 calibration target validated; no context-exhaustion warnings on any chunk. All drafts in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-26-frogbot.md`; no parked file (zero parks). (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 21 `drafted_with_flag` entries need review before applying. Flag rate (~27%) is dominated by the F1-audit residue (~10 Permission-line corrections; the Frogbot variant is `CF_BOTH + runtime-gate via k_fb_admin_only`, distinct from prior batches' `CF_PLAYER | CF_SPC_ADMIN` mislabels). Same operator phase as the Server-config + Spectator-chat + Voting + Scoring-stats + Mode-selection + Mode-scoped-knobs batch apply passes above; tackle in sequence (seventh batch now queued). (b) **Cross-card findings (13 actionable + 4 confirmed clean + 1 follow-up = 18 total)** -- F1: **CF_BOTH + runtime-gate Permission-line mislabel pattern (~10 cards)** -- third F1 audit instantiation; v2 standardizes to `any player or spectator (runtime gate via k_fb_admin_only -- default 0 allows anyone, 1 requires admin, 2 requires real server admin)`. F2: **Parent-name + cvar-name framing inconsistencies** -- existing descriptions say `frogbot easyskillmode` (should be `botcmd easyskillmode`), `fb itempickupbonus` (should be `botcmd itempickupbonus`), typo `k_fb_adminonly` (should be `k_fb_admin_only`); v2 corrects to canonical names. F3: **ToT name expansion inconsistency** -- `health` says "Tunnel of Terror", `quadmultiplier` says "Take-of-the-Throne"; source consistently uses "Tribe of Tjernobyl" (commands.c:586/4553/7937); cross-batch coherence check needed against Mode-scoped knobs `tot`/`totmode` cards. F4: **CONFIRMED CLEAN** tooling-mode prereq applied consistently across 25 editor-scope cards (Shape 8 tooling-mode-prereq modifier holds at scale -- "literally not in the menu" framing consistent). F5: **`weapon:frogbot:std` ToT-mode scope restriction missing** -- weapon lock has no effect outside ToT mode (FrogbotWeapon consumed at bot_botweap.c:958 only inside `if (tot_mode_enabled())`); random/0 in ToT restricts to `i_rnd(2,8)` not free choice. F6: **`mapinfo:frogbot:editor` is aerowalk-only** -- existing claims general map diagnostic; source FrogbotMapInfo at bot_commands.c:1126-1174 is hardcoded aerowalk-only (quad entity + specific marker indices); other maps print "No map-specific info available". F7: **`disable:frogbot:std` framing error** -- "player/spectator mode" should be "game mode" (UserMode at commands.c:809-817 restores 1on1/ffa/ctf etc.; `_k_last_xonx` is XonX command history). F8: **`debug:frogbot:std` undocumented `marker [n]` sub-subcommand + nested-dispatch shape question** -- 1-of-1 nested Shape 8 in this batch; flagged as future shape-catalog work if other engines surface siblings. F9: **`k_fbskill_use_rocketjumps` asymmetric skill-preset write** -- standard skill mode (setSkillAttributes at bot_botimp.c:156-205) does NOT write the cvar (only easy-mode at :250 sets it); bots silently lack rocket-jump capability unless server.cfg overrides. F10: **All 38 `k_fbskill_*` cvars share `botcmd skill <N>` overwrite risk** -- category-wide Prerequisites bullet across chunks 5/6/7/8; manual server.cfg overrides silently rewritten by setSkillAttributes when skill command runs in-game (bot_commands.c:489). F11: **CONFIRMED CLEAN** canonical-card pattern -- 1 application this batch (`vol_bot_midair_incr` canonical + `vol_opp_midair_incr` reference, near-identical pair with identical formula and clamp) + 1 within-batch rejection (aim_pitch_*/aim_yaw_* families with source-verified clamp/formula differences) + continuity with Mode-scoped knobs F12 rejection (rune_powers). Canonical-card discipline holding at scale. F12: **CONFIRMED CLEAN** `dmm4wiggle` / `dmm4wiggletoggle` cvar-side mode-precondition (gated at bot_movement.c:141 on `(deathmatch >= 4) && isDuel()`); not Shape 1c (no paired toggle); cross-link to dmm4 entities in adjacent batches. F13: **`k_fbskill_aim_attack_respawns` Rocket Arena not Clan Arena** -- existing says "clan arena" exclusion; source `isRA()` = `isDuel() && cvar("k_rocketarena")` (Rocket Arena = 1on1 duel modifier), NOT `isCA()` (Clan Arena). Different game modes. F14: **`k_fbskill_aim_yaw_min` formula-vs-clamp truncation** -- formula RangeOverSkill(aimskill, 1.5, 1) produces up to 1.5 but SetAttribs clamp `bound(0, cvar, 1)` truncates to 1.0; possible upstream code-quality follow-up. F15: **`anglehint` / `rjfields` require `savemarker` first (two-step workflow surprise)** -- both call FindPathIndex(saved_marker, nearest) which returns -1 if saved_marker is NULL; v2 surfaces Prerequisites + cross-link to savemarker. F16: **Several std subcommand handlers lack `bots_enabled()` guard** (removebot, removeall, disable, togglequad) -- minor UX inconsistency; possible upstream PR follow-up. F17: **`removepath` removes each direction independently** -- not bidirectional-only; works on unidirectional links (bot_commands.c:1392-1399). F18: **Editor "no marker nearby" / "nearest marker" framing corrections** across `clearmarkerflag`, `clearpathflag`, `move` -- existing framed refusals as silent no-ops; source prints refusal messages (lines 1542/1660); `move` actually restricts to manually-placed markers (skips bot-auto-placed silently). Full details in `## Cross-card consistency notes` section at end of drafts file. (c) **chunk_size=10 calibration result** -- 8 parallel chunks (7 of 10 + 1 of 8) fired cleanly; no context-exhaustion warnings from any sub-agent. Validates handoff prediction. Largest single-chunk size to date; sustainable for Shape 8 categories where per-card depth is moderate (parent + 39 subcommands + 38 set-once cvars). Halt-on-novelty did NOT fire this batch (prior Mode-scoped batch was first production halt; this batch had no trigger 1/4 verdicts -- the shape catalog covered all entities cleanly including the Shape 8 + tooling-mode-prereq + canonical-card composition). Next batch candidate for chunk_size=10+: Match flow (71) or Demo & spectator (69) at chunk_size=10; or stay at chunk_size=10 if those categories surface higher per-card depth. (d) **Open follow-ups for future batches** -- (i) `k_fb_admin_only` cvar (DRAINED 2026-05-26: now lives in **Frogbot** category, not Admin & permissions; surfaced + fixed via the depth-N extractor lift -- see "ktx-l1-rewrite Frogbot remediation" small-followup below for the 15 k_fb_* v2 cards that now need drafting). (ii) `tot` / `totmode` / `k_tot_mode` cross-batch ToT name verification (F3) -- apply-pass-author should confirm "Tribe of Tjernobyl" is canonical across the Mode-scoped knobs preset card. (iii) Nested-dispatch Shape 8a candidate (F8) -- watch for sibling patterns in MVDSV / unezQuake walks; if siblings surface, crystallize Shape 8a in the catalog. (iv) `bots_enabled()` guard upstream PR (F16) for 4 std handlers; low priority UX consistency. (v) `k_fbskill_use_rocketjumps` standard-mode default upstream code-quality (F9); low priority. (vi) `k_fbskill_aim_yaw_min` formula/clamp alignment (F14); low priority. (vii) ToT-related cross-link verification: `weapon:frogbot:std` (F5) -> tot/k_tot_mode cards. (viii) dmm4 cross-link verification: `k_fbskill_dmm4wiggle` / `_dmm4wiggletoggle` (F12) -> dmm4 entities in adjacent batches. (ix) `mapinfo:frogbot:editor` upstream improvement (F6) -- single-map hardcode; consider extension to other maps or removal. (x) Cumulative ktx-l1-rewrite drafts: 288 of 633 KTX L1 entities = ~46% (210 prior + 78 this batch; denominator bumped from 618 to 633 on 2026-05-26 when depth-N extractor fix added 15 `k_fb_*` cvars under Frogbot -- the 15 are NOT drafted yet, hence the percentage dip). 0 new parked entities (prior tally still 3: callalias, roundsdown, roundsup). [small followup]
- **KTX d4-extractor wipe-regression -- audit complete; 5 entities affected (2026-05-27)** -- The 2026-05-26 `d4-extractor-fix` selectively wiped `description` + `description_origin` + `description_anchor_version` on 5 KTX cvars while preserving `description_verdict='affirmed'` + `description_confidence='high'` + full `description_reasoning` + `description_provenance`. DB sweep `WHERE description_verdict IS NOT NULL AND description IS NULL` returned exactly 5 entities: **k_defmap / k_mode / k_spm_custom_model / k_sready / k_timetop** (all `source_state='source_backed'`, all `affirmed-by-evaluation` from the 2026-05-22 describe-fill pass; the affirmed metadata is the d4-extractor's signal "re-affirm at new anchor", not a regression). (a) **k_sready RESTORED + v2 RECAST 2026-05-27**: two-step fix. (i) 2026-05-22 affirmed description preserved in `apps/qw-oracle/docs/reviews/2026-05-22-ktx-l1-catalog.html:8729`; verified against `src/client.c:4266` + `src/world.c:881` at HEAD; persisted to `entities.description`. (ii) Single-entity `ktx-l1-rewrite` dispatch at Sonnet 4.6 high upgraded the description to v2 universal shape (Headliner / Effect / Prerequisites / Permission / Default / Example / See-also); Shape 3 confirmed; persisted via second UPDATE. Drafts file at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-k_sready-gapfill.md`. Recast caught a source-precision improvement: source sets `EF_BLUE` (blue glow), prior text said just "glow"; v2 specifies blue. Also added matchless+race prerequisite blocks (source `!k_matchLess && !isRACE()` previously unmentioned). Parent terminal corrected sub-agent draft from `k_matchLess` (C-side cached global) to `k_matchless` (the cvar entity) in See-also + Prerequisites. Final state: `description_verdict='affirmed'`, `description_origin='source_inline'`, `description_anchor_version='1.47-2-g67253dc'`, 504-char description. Match-flow recast batch had correctly aborted k_sready to needs-synthesis (F12) but the 2026-05-27 cold-synth follow-up's hard-bounded scope ("15 k_fb_*") missed it -- now fully closed. (b) **k_defmap / k_mode / k_spm_custom_model / k_timetop: deferred to apply pass** -- all 4 have v2 recast drafts in their respective batches (k_defmap + k_spm_custom_model in 2026-05-23 Server config; k_mode in 2026-05-26 Mode selection; k_timetop in 2026-05-26 Mode-scoped knobs). The recasts were authored before the 2026-05-26 wipe and capture richer v2 universal-shape text than the prior affirmed descriptions; apply pass writes them directly to `entities.description`. No additional cold-synth dispatch needed. (c) **MVDSV cold-synth scope lesson**: dispatch prompts should specify SWEEP criterion (`WHERE description IS NULL OR length(description) = 0`), not a hand-picked entity list. The 2026-05-27 follow-up's "15 k_fb_*" framing missed k_sready because it wasn't an obvious k_fb_* member; a sweep query would have surfaced it. The k_fb_* categorization-correction (origin `manual|d4-extractor-fix-2026-05-26` bypassed formal b6-categorize fan-out; partly ToT-scoped per cold-synth findings) stays tracked in task #15. Anchor 1.47-2-g67253dc. [small followup]
- **ktx-l1-rewrite See-also asymmetry: `break` missing back-ref to `ready` (2026-05-27)** -- spot-check across 10 sampled entities + 6 bidirectional pair checks found 1 asymmetry: `ready` See-also references `break` ("clears ready state"), but `break`'s See-also focuses on the voting-system flow (`forcebreak`/`k_vp_break`/`k_vp_map`/`next_map`) and omits the player-command peer set (`ready`/`slowready`/`toggleready`). `break` has 4 See-also entries; the per-card skill caps at 4-5, so adding `ready` as a 5th fits. **Apply-pass action**: when applying the Match flow batch (`ktx-l1-rewrite-drafts-2026-05-27-match-flow.md`), hand-edit `break`'s See-also to add `ready (paired peer -- sets ready state)`. Other 5 paired relationships sampled (`freeze`↔`k_freeze`, `qlag`/`qpoint`/`qenemy` triangle, `race_simultaneous`↔`k_race_simultaneous`, `powerups`↔`k_pow`, `discharge`↔`k_dis`) all verified symmetric. [small followup]
- **ktx-l1-rewrite parks pile DRAINED -- 5 hand-drafts at v2 universal shape (2026-05-27)** -- `callalias` / `y` / `n` / `roundsdown` / `roundsup` (the 5 entities the recast skill couldn't classify under any Layer B shape) now have v2 Layer A drafts at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-parks-handdraft.md`. Hand-authored directly (not via `ktx-l1-rewrite` -- it would re-park on the same shape-catalog miss). Source citations verified at HEAD anchor `1.47-2-g67253dc`. **Verdict mix**: 3 drafted_clean (callalias, y, n) + 2 drafted_with_flag (roundsdown + roundsup -- existing descriptions wrongly framed both as "admin command" but source registration is `CF_PLAYER` for both; v2 Permission lines corrected to "any player"). **Apply-pass actions surfaced**: (i) `kick`'s drafted See-also (Admin & permissions batch) currently lists only `mkick + force_spec`; add `y` + `n` per F2 cross-card finding to close the bidirectional triangle (kick ↔ y ↔ n). The 5 parks are now ready to flow through the apply pass alongside the 14+2 batch drafts. **Arc state**: all 633 KTX cvar+command entities have v2 recast drafts (627 in 16 batch drafts files + 1 k_sready gapfill + 5 parks hand-drafts). [small followup]
- **ktx-l1-rewrite F1 audit + Permission-line discipline corrections (2026-05-26 follow-up to Mode selection batch F1)** -- F1 from the Mode selection batch surfaced a systemic "Admin command" mislabeling pattern across 5+ entities; this followup ran a rear-view audit of the 4 prior batches and forward-fixed the catalog prescription that propagated the error. **Source check (`include/g_local.h:647-658`):** `CF_PLAYER` (bit 0) = "command valid for players" (any player); `CF_SPC_ADMIN` (bit 3) = "client is spectator, requires admin rights." `CF_PLAYER | CF_SPC_ADMIN` literally means "any player slot, OR admin spectator" -- NOT admin-only. `CF_PLR_ADMIN` (bit 2, "client is player, requires admin rights") is a DIFFERENT flag and is rare; `CF_BOTH_ADMIN = CF_PLR_ADMIN | CF_SPC_ADMIN` is the truly admin-only combination. **Rear-view audit result:** of 12 `CF_PLAYER | CF_SPC_ADMIN` entities across prior batches, 11 were correctly framed (antilag / infospec / nospecs / qenemy / silence (pre-match/mid-match split) / swapall / teamoverlay / downplayers / upplayers / downspecs / upspecs); 1 silent miss + 1 mixed-discipline case identified. **Apply-pass corrections needed:** (a) **`dmgfrags`** in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-26-scoring-stats.md` -- Permission line says `admin only` and Notes claim "CF_PLAYER | CF_SPC_ADMIN maps to admin only" -- both wrong per source. Correct Permission line to `any player or admin spectator`; reframe Headliner to drop "Admin command" prefix. (b) **`silence`** in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-25.md` -- Headliner says "Admin command that toggles spectator talk (k_spectalk)..." but Permission line correctly splits "pre-match: any player or admin spectator. Mid-match: admin only (non-admins silently ignored)." Reframe Headliner to drop "Admin command" prefix; Permission line is correct (canonical example of two-phase runtime gating). **Forward-fix (catalog level, user-global skill -- not in repo):** `~/.claude/skills/ktx-l1-rewrite/references/shape-catalog.md` Shape 1 command-side prose prescription updated 2026-05-26 to qualify "Admin command that toggles..." by CF flag (only for CF_BOTH_ADMIN; default Headliner is "Toggles the X rule (cvar)"). `~/.claude/skills/ktx-l1-rewrite/references/universal-shape-v2.md` Permission section expanded with explicit CF-flag-to-wording table + critical-mistake note. Future Shape 1 batches inherit the corrected prescription. [small followup]
- **ktx-l1-rewrite Admin & permissions -- apply pass + cross-card findings (chunked-mode batch 8; chunk_size=10)** -- 37-card batch SHIPPED 2026-05-26: **35 drafted** (21 clean + 14 flagged) + **2 parked** (`y` + `n`, trigger 1 no-shape-match; **operator-accepted park** -- kick-walkthrough session-response pattern is 1-of-1 in KTX, doesn't earn new shape per earn-their-keep). 4 parallel chunks (3 of 10 + 1 of 7); chunk_size=10 sustained for cross-category mix of Shape 3 admin cvars + Shape 4/4b gated commands + shape-less admin actions + cross-batch Shape 1/2/7a/11b composition partners; no context-exhaustion warnings. All drafts in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-26-admin-permissions.md`; park file at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-parked-2026-05-26-admin-permissions.md`. (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 14 `drafted_with_flag` entries need review before applying. Flag rate (~38%) is dominated by the F1-audit residue (5 Permission-line corrections; this batch surfaces a NEW F1 variant: `CF_PLAYER` alone -- spectators excluded -- on `speed` + `socd`, complementing prior batches' `CF_PLAYER | CF_SPC_ADMIN` and Frogbot's `CF_BOTH + runtime k_fb_admin_only` variants). Same operator phase as the 7 prior batch apply passes above; tackle in sequence (eighth batch now queued). (b) **Cross-card findings (7 actionable + 2 confirmed clean = 9 total)** -- F1: **CF_PLAYER-alone Permission mislabel variant** (5 cards: `lockmode`, `speed`, `socd`, `qlag`, `qpoint`) -- third F1 audit variant; `speed` and `socd` are `CF_PLAYER` alone (any player, spectators excluded), the new wording row that prior batches didn't surface. F2: **10 cross-batch threading dependencies** -- `k_allowklist`/`k_allowtracklist` paired with `toggleklist`/`toggletracklist` (CF_BOTH | CF_MATCHLESS, verified at commands.c:834/843; not yet in scope -- likely a future Server-config sub-batch); `lockmap` <-> `k_lockmap` (Match flow); `lockmode` <-> `k_lockmode` (Match flow / Server config); `socd` <-> `k_socd`; `fp`/`fp_spec` <-> `k_fp`/`k_fp_spec` (Server config); `hdptoggle` <-> `k_lock_hdp` (Gameplay rules); `qlag`/`qpoint` <-> `qenemy` (Spectator chat -- long-standing open followup from Spectator chat batch). F3: **V1-shape artifacts on 4 command cards** -- `hdptoggle` carries "Default: unlocked" (commands have no default in v2); `lockmap`/`lockmode`/`socd` carry value enums (Shape 1/2 discipline places enum on cvar card). Apply-pass-author moves these to paired cvar cards when those batches ship. F4: **Privategame family default-value error class** -- `k_privategame_allow_specs` + `k_privategame_force_reconnect` both claim Default=0; source RegisterCvarEx specifies "1" at world.c:1090-1091. Family sweep recommended when other `k_privategame_*` cvars surface (likely Match flow / Mode selection). F5: **`k_admins` scope overstatement** -- existing says "all admin-related commands check this"; source shows only `/admin` (admin.c:347) and `VoteAdmin`/`elect` (admin.c:489) check `cvar("k_admins")` directly. Other admin-gated commands check `is_adm(self)` on the caller. `k_admins` is the gate at admin DESIGNATION time, not at every admin-command invocation. F6: **`lock` auto-expire broadcast claim contradicts source** -- existing says "Auto-expires with a broadcast on timeout"; source `admin.c:912-933` + `client.c:1319-1328` shows silent expiry. Only initial lock + manual early-unlock broadcast. F7: **y/n park accepted + `kick` See-also bidirectional fix** -- `y`/`n` are admin-only kick-walkthrough commands (handlers `YesKick`/`DontKick`, CF_BOTH_ADMIN; commands.c:796-797) gated on the parent `kick` command's per-admin session state (`k_kicking` field). Pattern not in shape catalog (1-of-1 KTX; doesn't earn new shape). Apply-pass-author may hand-author shape-less Layer-A cards manually; AND should add `y`/`n` to `kick`'s See-also (chunk B sub-agent chose to omit; cross-card pass disagrees -- they're peer entities in the walkthrough family). F8: **CONFIRMED_CLEAN** dispatcher-brief misidentification caught by sub-agent. The handoff doc classified `y`/`n` as Shape 7a vote responses; chunk D sub-agent source-verified and corrected -- the actual vote-response commands are `yes`/`no` at commands.c:801-802 (handlers `VoteYes`/`VoteNo`, `CF_PLAYER | CF_MATCHLESS`). Process-level verification discipline working as intended. F9: **CONFIRMED_CLEAN** `commands` correctly shape-less per shape-catalog disambiguation -- chunk B sub-agent followed the explicit "introspective command lister NOT Shape 10" guidance; documents the catalog's per-shape disambiguation sections are doing their job. Full details in `## Cross-card consistency notes` section at end of drafts file. (c) **Halt-on-novelty exercised + operator-accepted-park path** -- second production halt-on-novelty (first was Mode-scoped knobs batch's roundsdown/roundsup). Trigger 1 fired on `y`/`n` per skill discipline; halt report surfaced candidate "Multi-step wizard/walkthrough session" shape signature; operator chose ship-and-park option (35 entities ship cleanly; `y`/`n` in park file with full source-pattern docs). Halt mechanism + operator-override-via-AskUserQuestion path validated; the halt is the catalog's earn-their-keep discipline working at batch scale. (d) **chunk_size=10 calibration** -- 4 parallel chunks (3 of 10 + 1 of 7) fired cleanly; no context-exhaustion warnings. Validates chunk_size=10 holds for smaller mixed-category batches (37 entities; cf. Frogbot at 78 entities also chunk_size=10). Next batch candidate: Match flow (71) or Demo & spectator (69) at chunk_size=10. (e) **Open follow-ups for future batches** -- (i) `toggleklist` / `toggletracklist` paired toggle commands (CF_BOTH | CF_MATCHLESS at commands.c:834/843) -- verify L1-entity status against live catalog; if entities, draft in a future batch with symmetric See-also threading to `k_allowklist`/`k_allowtracklist`. (ii) Cross-batch paired cvars (per F2 list) -- apply-pass-author verifies symmetric See-also when each paired batch ships. (iii) Privategame family default-value sweep (F4) when other `k_privategame_*` cvars surface. (iv) Hand-author `y` + `n` v2 cards at apply-pass time using Layer A only; ADD `y`/`n` to `kick`'s See-also bidirectional (F7). (v) If a future codebase walk (MVDSV / unezQuake / KTX additions) surfaces a sibling wizard-walkthrough trio (parent-command-initiated per-admin session-state + per-key response commands), revisit shape catalog for the F7 candidate-shape question ("Shape N: multi-step wizard session"). (vi) `qenemy` Shape 11b reclassification pending -- the entity was drafted 2026-05-23 in the Server config & network batch as "v2 universal, no Layer B tag" (before Shape 11b was crystallized 2026-05-26). NOT a future-batch concern; the apply-pass-author should hand-edit qenemy's draft entry in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-23.md` to add Shape 11b classification + cross-link to qlag (bit 8) and qpoint (bit 128) when applying the q* family to L1. Source: src/commands.c with handler `ToggleQEnemy` XOR-flipping bit 32 of fpd serverinfo. (vii) Cumulative ktx-l1-rewrite drafts: 323 of 633 KTX L1 entities = ~51% (288 prior + 35 this batch). Total parks across all 8 batches: 5 (`callalias` + `roundsdown` + `roundsup` + `y` + `n`); all queued for apply-pass hand-drafting. [small followup]
- **ktx-l1-rewrite Demo & spectator -- apply pass + cross-card findings (chunked-mode batch 9; chunk_size=10)** -- 69-card batch SHIPPED 2026-05-27: all 69 cards drafted, **0 parked**, **0 novelty halts**. **60 drafted clean** + **9 drafted_with_flag**. 7 parallel chunks of 10+10+10+10+10+10+9. **Largest single canonical-card application to date**: 40 of 69 entities classified under canonical-card pattern (fav1_add canonical + 19 favN_add refs; 1fav_go canonical + 19 Nfav_go refs). All in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-demo-spectator.md`; no parked file. (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 9 `drafted_with_flag` entries need review before applying (`_k_nospecs`, `k_demo_mintime`, `k_demotxt_format`, `1fav_go`, `autotrack`, `autotrackktx`, `auto_pow`, `next_pow`, `demomark`). Same operator phase as the prior 8 batches' apply passes; tackle in sequence (ninth batch now queued). (b) **Cross-card findings (8 actionable + 5 confirmed clean)** -- F1: `demomark` permission mislabel ("Set by: any player" but CF_BOTH at registration); 5th batch surfacing the systematic Permission mislabel pattern (this batch's variant: existing prose UNDER-states scope, inverse direction from prior batches' "Admin command" over-state). F2: `autotrack` "not available during a live match" claim is backwards -- CF_SPECTATOR|CF_MATCHLESS means available during matches AND matchless; existing wording inverts the semantics. F3: **`next_pow` draft regression** -- Headliner + Effect bullet enumerate 3 powerups (pentagram/quad/ring) despite Notes section verifying source includes 4 (radsuit too); existing L1 description correctly listed all 4; apply-pass-author restores suit. NEW class of cross-card finding (sub-agent's proposed draft contradicting its own Notes). F4 CONFIRMED CLEAN: `auto_pow` vs `next_pow` suit semantics diverge by design (auto_pow excludes per CalculateBestPowPlayers source comment about 2024 maps; next_pow includes via own iteration loop). F5 CONFIRMED CLEAN: canonical-card pattern applied cleanly at 40 entities -- Frogbot-batch F11-style rejection (clamp/formula divergence) did NOT fire (slot-keyed family behaviorally uniform; same handler + same CF flag across all 40 entities). F6: matchless-mode behavioral split surfaced within fav family -- generic family (fav_add/del/all_del/next/show) carries `CF_SPECTATOR|CF_MATCHLESS` (matchless-available); slot-keyed family (favN_add, Nfav_go) carries `CF_SPECTATOR` only (rejected in matchless via `DoCommand()` at `commands.c:1078`). No existing description mentioned this; both canonical cards now flag it. F7 CONFIRMED CLEAN: two-array storage model (`self->fav[]` for generic + `self->favx[]` for slot-keyed); `fav_show` is the bridge card that reads both. F8: **`_k_nospecs` reclassified -- NOT Shape 9b engine state-mirror as handoff hypothesized**; source check shows it's the vote-result state container for the `nospecs` Shape 7b vote command (written by `vote_check_nospecs()` on threshold pass). Cross-batch threading: requires `nospecs` + `k_vp_nospecs` See-also wiring. F9 CONFIRMED CLEAN: `autotrack` vs `autotrackktx` behaviorally distinct -- autotrack passes `atKTPRO` (event-driven on RL/death/powerup), autotrackktx passes `atBest` (continuous score re-eval); per Frogbot F11 precedent, drafted as separate full cards (no canonical-card collapse). F10 CONFIRMED CLEAN: **trex family (`trx_play`/`trx_rec`/`trx_stop`) NOT sui-generis** -- handoff hypothesized trigger-4 park candidate (compile-time-bound external tool integration); source check shows fully internal per-player movement-capture system (1155-frame buffer at `include/progs.h:284`; handlers `mv_cmd_record`/`mv_cmd_playback`/`mv_cmd_stop` in `src/commands.c:8229+8321+8341`); three shape-less command actions with mutual See-also cross-references. F11: **Cross-batch See-also threading** -- `_k_nospecs` -> `nospecs` + `k_vp_nospecs` (likely Voting or Server-config batch); `tracklist` -> `k_allowtracklist` + `toggletracklist` + `klist` (Admin & permissions batch); `moreinfo` -> `k_spec_info` + `infospec` + `infolock` (Spectator chat & visibility batch); apply-pass-author validates existence + bidirectional wiring. F12: `k_demo_mintime` matchtag exception missing from existing description -- `match_can_cancel_demo()` at `src/match.c:2499-2503` returns false unconditionally when `matchtag` infokey set, preserving demo regardless of duration vs mintime. F13: `k_demotxt_format` framing misleading -- JSON always written internally for demo embedding (per `src/stats.c:574-601` comment "Always write json, so it can be embedded in demo"); cvar controls only the ADDITIONAL non-JSON file (setting to "json" suppresses the additional file rather than producing it -- semantically inverted). Full details in `## Cross-card consistency notes` section at end of drafts file. (c) **Canonical-card scale validation + handoff-hypothesis-vs-source-truth wins** -- (i) 40-of-69 canonical-card application is largest to date (prior batches at 4-6); behavioral uniformity at slot level source-verified across all 38 reference cards. (ii) Handoff doc made 3 source-uncertain hypotheses; sub-agents source-verified and OVERRODE 2: `_k_nospecs` (not Shape 9b -- vote-result state container) and trex family (not sui-generis -- internal movement-capture). Handoff Rule 7 discipline ("HYPOTHESES not contracts; trust source over handoff") worked as intended; documented in F8 + F10. (iii) Permission mislabel pattern continues -- 5 batches affected; `demomark` is a NEW under-state direction. (d) **Open follow-ups for future batches** -- (i) Cross-batch See-also validation (F11) at apply-pass time: confirm `nospecs`, `k_vp_nospecs`, `klist`, `infospec`, `infolock` are present in L1 from prior batches' applied or pending drafts; surface any missing entities as drafting follow-ups. (ii) `_k_nospecs` reclassification (F8) requires bidirectional See-also wiring with the `nospecs` Shape 7b card (in Voting or Server-config batch); surface as See-also amendment when that prior batch's drafts are next applied. (iii) Permission mislabel pattern cumulative tracker: 5 batches affected; consider spec-amendment if pattern continues in next 1-2 batches. (iv) `next_pow` draft regression (F3) is a NEW class of cross-card finding (sub-agent's draft contradicting its own Notes); watch for this class in future batches. (v) Cumulative ktx-l1-rewrite drafts: **392 of 633 KTX L1 entities = ~62%** (323 prior + 69 this batch). Total parks across all 9 batches: 5 (`callalias` + `roundsdown` + `roundsup` + `y` + `n`); all queued for apply-pass hand-drafting. [small followup]
- **ktx-l1-rewrite Match flow -- apply pass + cross-card findings (chunked-mode batch 10; chunk_size=9)** -- 71-card batch SHIPPED 2026-05-27: **70 drafted** (41 clean + 29 flagged) + **0 parked** + **1 aborted to needs-synthesis** (`k_sready`, empty existing description) + **0 novelty halts**. 8 parallel chunks of 9+9+9+9+9+9+9+8. All drafts in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-match-flow.md`; no parked file. (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 29 `drafted_with_flag` entries need review before applying. Flag rate ~41% dominated by F1 Permission-line CF flag mislabel pattern (now 6 batches running -- 15 affected cards this batch alone) + F2 default-value error class (4 cvars with bare `RegisterCvar` = 0 vs existing-description non-zero values) + F4/F5/F6 foundational framing fixes from the mechanism map (votemap / agree / rpickup). Same operator phase as the prior 9 batches' apply passes; tackle in sequence (tenth batch now queued). `k_sready` aborted to describe-fill-synthesis queue (separate workstream; empty existing description per the per-card skill's hard pre-flight gate point 2). (b) **Cross-card findings (8 actionable + 1 confirmed clean + 3 informational = 12 total)** -- F1: **Permission-line CF flag mislabel pattern -- 6th batch continuing** (15 cards: fragsdown/fragsup/overtime/overtimeup/time10/timedown/timedown1/timeup/timeup1/toggleklist/pause/ready/toggleready/sh_speed/practice). NEW direction this batch: both over-state (12 cards) AND under-state (ready/toggleready/sh_speed) directions surface together; Demo & spectator's `demomark` under-state direction (5th batch) is no longer one-off. F2: **Default-value error class** (4 cvars: k_exttime/k_freeze/k_lockmax/k_lockmin; all bare `RegisterCvar` = 0 vs existing 5/1/2/2). Same root-cause class as Admin & permissions batch's F4 (k_privategame_* family). F3: **Shape paired-command omissions** in cvar Set-by lines (k_exttime/k_freeze/k_lockmode/k_overtime). F4: **votemap foundational framing wrong** -- mechanism map (`ktx-map-voting-mechanism-map.md`) applied at draft time; existing description's "immediate switch" framing replaced with Shape 7b vote-cast semantics. F5: **agree mechanism framing wrong** -- calls DoSelectMap (= Shape 7b OV_MAP vote path), not "without a further vote" as existing description claims. F6: **rpickup admin-veto framing reversed** -- admin vote = fast-pass / immediate execution (per `is_admins_vote(OV_RPICKUP)` at vote.c:792), NOT a block as existing description claims. F7 INFORMATIONAL: **Canonical-card decisions on 3 families** -- time fan-out APPLIED (time10 canonical + 5 reference cards: time5/time15/time20/time25/time30; 6 of 70 = ~9%); Mode presets NOT (5 separate full cards: 1on1/2on2/3on3/3on3on3/10on10; bundles structurally differ); pos_* NOT (5 separate full cards: pos_show/pos_save/pos_move/pos_origin/pos_angles; behavioral differences). F8: **options Shape 10 roster source-verified (22 siblings, not ~16 as handoff guessed)** + bidirectional cross-link inconsistencies -- 3 missing (timedown1/timeup1/dm need See-also -> options added) + 1 incorrect (pickspawn incorrectly cross-links options; remove). Apply-pass action. F9 CONFIRMED CLEAN: **dm classified as pure state-printer** -- handoff "dual-purpose state/setter" hypothesis overridden by source verification of ShowDMM (3-line G_sprint state-printer, no arg dispatch; dmm1-dmm5 are the setters). Third consecutive batch where handoff-Rule-7/9 (hypotheses-not-contracts; trust source over handoff) catches a misattribution. F10: **4 localized factual contradictions** (srv_practice_mode reload-direction / prewar jump-vs-fire gating / ra_break three silent-return conditions / time10 clamp semantics "no-op vs clamps"; all drafted_with_flag). F11 INFORMATIONAL: **Election commands deep details** -- captain has TWO subsequent-invocation branches (abort + step-down) surfaced as labeled Effect bullets; coach `elect_block_till` 30s cooldown missing from existing description (drafted_with_flag); latejoin Shape 7a election with 30s electguard timeout (faster than typical 60s for elect/captain/coach). F12 INFORMATIONAL: **Cross-batch See-also threading inventory** -- 14+ paired entities across Voting (k_vp_break, k_vp_map, k_vp_captain, k_vp_coach, k_vp_rpickup, voteprivate/k_vp_privategame, cm) + Admin & permissions (k_allowtracklist, lockmode, lockmap) + internal pairs (k_pause_without_matchtag <-> pause, k_overtime/k_exttime <-> overtime/overtimeup, k_prewar <-> prewar, k_privategame <-> k_privategame_default) + cross-batch follow-up names (freeze command for k_freeze; k_allowklist for toggleklist; both not yet in any batch). Full details in `## Cross-card consistency notes` section at end of drafts file. (c) **Canonical-card patterns + handoff-hypothesis-vs-source wins** -- (i) Time fan-out canonical-card mandate validated source-verified (6 cards); mode preset + pos_* candidate families correctly rejected via source verification (Frogbot F11 / Demo F5 precedent doing its job). Cumulative canonical-card scale across this session-pair: Demo & spectator's 40-of-69 + Match flow's 6-of-70 = 46 of 139 entities (~33%). (ii) Handoff doc made 4 source-uncertain hypotheses this batch; sub-agents source-verified and OVERRODE 2: `dm` (pure state-printer, not dual-purpose) and `pickspawn` (NOT in options menu, despite handoff claim). Handoff Rule 7 discipline ("HYPOTHESES not contracts; trust source over handoff") continues to work as intended -- third consecutive batch where it triggers. (iii) Permission mislabel pattern continues -- 6 batches affected. Consider permanent `~/.claude/skills/ktx-l1-rewrite/SKILL.md` amendment if pattern continues in next 1-2 batches. (iv) The 3 new sub-agent disciplines (verdict-marker consistency / FLAG: prefix discipline / draft-vs-verified content sanity check) baked into chunk prompts from Demo & spectator's findings prevented the marker-inconsistency bug class -- 0 marker repairs needed mid-stream this batch (Discipline 1 final-pass scan found 0 inconsistencies across all 70 sections). The 3 disciplines have battle-validated on this batch; consider promoting Discipline 3 (draft-vs-verified) to a permanent Step 5 amendment per the Demo & spectator deferral. (d) **Open follow-ups for future batches** -- (i) `freeze` command (k_freeze's Shape 1 paired toggle, NOT yet in any batch -- surface in Gameplay rules or future). (ii) `k_allowklist` cvar (toggleklist's Shape 1 paired cvar, NOT yet in any batch -- surface in Admin & permissions follow-up or future). (iii) `k_sready` empty-description route to describe-fill-synthesis (separate dispatch); track as part of the synthesis-pile workstream. (iv) Permission mislabel pattern cumulative tracker: 6 batches affected; SKILL.md amendment candidate if 7th batch surfaces same. (v) Default-value error class: consider a wider sweep of bare `RegisterCvar` vs `RegisterCvarEx` across all KTX cvars to surface other prior-batch cards with wrong-default flag opportunities (apply-pass-author may run this as a one-time audit). (vi) `pickspawn` over-cross-link to `options`: apply-pass-author removes the incorrect See-also reference at apply time (chunk F sub-agent followed an incorrect hint in the dispatcher prompt; source-verified pickspawn is NOT in the options roster). (vii) Cumulative ktx-l1-rewrite drafts: **462 of 633 KTX L1 entities = ~73%** (392 prior + 70 this batch). Total parks across all 10 batches: 5 (`callalias` + `roundsdown` + `roundsup` + `y` + `n`); all queued for apply-pass hand-drafting. Total aborted-to-synthesis: 1 (`k_sready`); routes to describe-fill-synthesis. [small followup]
- **ktx-l1-rewrite Gameplay rules -- apply pass + cross-card findings (chunked-mode batch 11; chunk_size=10)** -- 69-card batch SHIPPED 2026-05-27: **69 drafted** (37 clean + 32 flagged) + **0 parked** + **0 aborted** + **0 novelty halts**. 7 parallel chunks of 10+10+10+10+10+10+9. All drafts in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-gameplay-rules.md`; no parked file. Sub-grouped by mechanism family: Powerup family (10) / Drop-quad-discharge (10) / Spawn-item rules (10) / Movement-jumps (10) / CTF-nightmare-rules (10) / Weapons-gates-handicap (10) / Misc utility (9). (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 32 `drafted_with_flag` entries need review before applying. Flag rate ~46% dominated by F1 Permission-line CF flag mislabel pattern (now **7th consecutive batch** — ~20 cards affected this batch alone, the largest single-batch F1 hit yet) + F2 default-value error class (4 more bare-`RegisterCvar` zero-init cvars: `k_pow`/`k_highspeed`/`k_btime`/`k_short_gib`) + F4 foundational framing fixes on 8 entities. Same operator phase as the prior 10 batches' apply passes; tackle in sequence (eleventh batch now queued). (b) **Cross-card findings (12 actionable + 1 confirmed clean + 2 informational = 15 total)** -- F1: **Permission-line CF flag mislabel pattern -- 7th batch continuing** (~20 cards: `powerups`/`powerups_pickup`/`dropquad`/`discharge`/`k_tp_tele_death`/`k_noitems`/`noitems`/`spawn`/`spawn_show`/`spawnicide`/`teleteam`/`fallbunny`/`airstep`/`k_spw`/`k_frp`/`k_socd`/`fairpacks`/`wreg`/`nosweep`/`tp`/`removeitem`). NEW variants this batch: (i) `CF_PLAYER` alone mislabel (socd, fairpacks); (ii) `CF_BOTH` under-label (wreg, removeitem). **SKILL.md amendment THRESHOLD HIT** -- 7th consecutive batch is the trigger per the Match flow batch's deferral memo; operator should decide whether to bake the CF-flag table into Step 5 / amend chunk-prompt template / author a one-time audit script. F2: **Default-value error class** (4 more cvars: k_pow/k_highspeed/k_btime/k_short_gib; all bare `RegisterCvar` = 0 vs existing-description non-zero claims). Same root-cause class as Admin & permissions F4 + Match flow F2. F3: **Cross-chunk Shape classification discrepancy** — manual `cvar_fset`/`trap_cvar_set_float` binary-flip patterns: sub-agents disagreed (Chunks C/D/F → Shape 1 functionally; Chunk G `teleteam` → shape-less because no `cvar_toggle_msg`). **Strong recommendation**: treat manual-flip as Shape 1 (functional); revise `teleteam` classification at apply time + add SKILL.md note extending Shape 1's source signature to include manual flips. F4: **Foundational framing errors on 8 cards** — `discharge` (radius damage centered on shooter, NOT chain-reaction); `powerups_pickup` (same-type block, NOT multiple-at-same-time); `k_pow` (toggle is `powerups`, not "pow"); `k_nightmare_pu` (toggle is `coop_nm_pu`, not `nightmare_pu`); `k_bzk` (2-second burst, NOT match-persistent invulnerability); `k_lock_hdp` (toggler is `hdptoggle`, not `handicap`); `k_disallow_krjump` ("server config only" — but `tkrjump` toggles it); `k_killquad` (carrier-death + NeedDropQuad gate, not "appears in play"). All FLAG'd; apply-pass-author reviews each before applying. F5: **Canonical-card REJECTED for `k_pow_p/q/r/s`** via source-verified drop-path asymmetry (k_pow_q/k_pow_r gate `dq`/`dr` drops; k_pow_p/k_pow_s don't). Third consecutive batch where source-verification overrides a handoff canonical-card hypothesis (Frogbot F11 / Match flow F7 / this batch F5 — Rule 7 discipline working). F6: **vwep three-entity composition** (`k_allow_vwep` master gate + `k_vwep` state + `vwep` toggle) — L3 concept-note candidate (visible weapons; ezquake.com/docs/player-skins anchor). F7: **`dq`/`dr` prerequisite gap** — existing descriptions omitted `k_pow_q`/`k_pow_r` + `k_pow` enable prereqs that gate the actual drop paths (items.c:1974, 1989). Added to drafts; bidirectional See-also chains required at apply time. F8: **`k_dis` edge case — value 2 not toggleable** — `discharge` cvar_toggle_msg is binary (0↔1); operators setting `k_dis 2` in server.cfg will lose the value when in-game toggle fires. Surprise-bearing; surfaced in both `k_dis` and `discharge` drafts. F9: **k_end_tele_spawn / k_remove_end_hurt cross-link** — both 'end' map-specific. F10: **spawnicide ↔ spawn_show coupling** — toggling spawnicide refreshes spawn-point display (commands.c:2753-2756). F11: **freeze cross-batch See-also** — `freeze` (this batch) Shape 1 pair with `k_freeze` (Match flow 2026-05-27, prior batch). Match flow's `k_freeze` draft does NOT include `freeze` in See-also (couldn't); apply-pass-author MUST add `freeze` to `k_freeze`'s See-also when applying Match flow drafts. F12: **OctaPower/dmm4 claim in `k_bzk`+`k_btime` dropped — unverifiable** at `match.c:689-714`; apply-pass-author may investigate separately if a dmm4 branch lives elsewhere. F13 CONFIRMED_CLEAN: **dispatcher recovered from sub-agent /tmp file collision** — Chunks B and C's `/tmp/chunk_X_output.md` files contained stale content from prior batches (sub-agents appended rather than wrote-clobber); dispatcher extracted only this batch's sections via line-range slicing. SKILL.md amendment candidate — either batch-date-suffix /tmp filenames OR explicit "Write not Edit" instruction in sub-agent prompts. F14: **`wreg` permission classification** — `CF_BOTH` includes non-admin spectators; existing "spectator-admin" wording wrong. F15: **`removeitem` permission classification** — `CF_BOTH` includes spectators; existing "any player" too narrow + handler also has Shape 4b `*cheats` gate. Full details in `## Cross-card consistency notes` section at end of drafts file. (c) **Canonical-card patterns + handoff-hypothesis-vs-source wins** -- (i) k_pow_p/q/r/s canonical-card REJECTED per F5 (drop-path asymmetry; 0 of 69 entities consolidated). Cumulative canonical-card scale across the chunked-mode era: Demo & spectator 40 + Match flow 6 + Gameplay rules 0 = 46 of 208 batched-entities (~22%). (ii) The 3 sub-agent disciplines (verdict-marker consistency / FLAG: prefix discipline / draft-vs-verified content sanity check) baked into chunk prompts performed cleanly across all 7 chunks (0 marker-inconsistency repairs needed mid-stream). Discipline 3 (draft-vs-verified) battle-validated for the second consecutive batch — SKILL.md Step 5 amendment is overdue. (iii) Permission mislabel pattern at 7 batches — F1 SKILL.md amendment is the action item this batch hits the threshold for. (d) **Open follow-ups for future batches** -- (i) **F1 SKILL.md amendment** (7th batch hit the threshold per Match flow's deferral memo) — operator decides between Step 5 mandatory check / chunk-prompt template line / one-time audit script. (ii) **F3 Shape 1 source-signature extension** — recommend SKILL.md note that manual `cvar_fset`/`trap_cvar_set_float` binary-flip patterns qualify for Shape 1 (functional); resolve the cross-chunk `teleteam` discrepancy at apply time. (iii) **F13 sub-agent /tmp file collision fix** — batch-date-suffix /tmp filenames OR sub-agent prompt amendment ("Write tool, not Edit"); applies to `ktx-l1-batch-dispatcher` SKILL.md and per-chunk prompts. (iv) **Discipline 3 promotion** — promote draft-vs-verified content sanity check to permanent Step 5 amendment in `~/.claude/skills/ktx-l1-rewrite/SKILL.md` (battle-validated on Match flow + Gameplay rules). (v) **Default-value sweep** (F2 cumulative across batches now totals 12+ cvars across 5 batches) — apply-pass-author one-time audit script across all KTX cvars to surface remaining bare-`RegisterCvar` vs `RegisterCvarEx` divergences. (vi) **`freeze` ↔ `k_freeze` cross-batch See-also fix** at apply time (F11 — `k_freeze` Match flow draft pre-dates this batch). (vii) **vwep family L3 concept-note** queued (F6) — `k_allow_vwep` master-gate + `k_vwep` state + `vwep` toggle three-entity composition; ezquake.com/docs/player-skins anchor. (viii) **OctaPower/dmm4 follow-up** (F12) — apply-pass-author may investigate before applying `k_bzk`/`k_btime` drafts. (ix) Cumulative ktx-l1-rewrite drafts: **531 of 633 KTX L1 entities = ~84%** (462 prior + 69 this batch). Total parks across all 11 batches: 5 (`callalias` + `roundsdown` + `roundsup` + `y` + `n`); all queued for apply-pass hand-drafting. Total aborted-to-synthesis: 1 (`k_sready`); routes to describe-fill-synthesis. **Categories remaining: 3** (Internal state 19 / Player communication 18 / Race 45 = 82 entities). [small followup]
- **ktx-l1-rewrite Internal state -- apply pass + cross-card findings (chunked-mode batch 12; chunk_size=7)** -- 19-card batch SHIPPED 2026-05-27: **19 drafted** (18 clean + 1 flagged) + **0 parked** + **0 aborted** + **0 novelty halts**. 3 parallel chunks of 7+6+6. All drafts in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-internal-state.md`; no parked file. Sub-grouped by mechanism family: Captain-election state-mirrors (4) / Coach-election vestigial-state (2) / Team-name state-mirrors (3) / HoonyMode persistence (2; canonical Shape 9b worked examples) / Match-bookkeeping (3; `_k_host` + `_k_worldspawns` + `__k_ls`) / Map-transition state-mirrors (4) / Internal command (1; `cmdslist_dl`). (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 1 `drafted_with_flag` entry needs review before applying (`_k_coachteam2` -- existing "branch unreachable" framing is factually wrong; branch is reachable but no-op because coach-side cvars are vestigial). Same operator phase as the prior 11 batches' apply passes; tackle in sequence (twelfth batch now queued). Lowest flag rate of any chunked-mode batch (1/19 = ~5%) -- Internal state's homogeneous Shape 9b mechanism + 17/19 `_k_*` engine-internal cvars made source verification straightforward. (b) **Cross-card findings (1 actionable + 1 confirmed clean catalog-amendment-candidate + 4 informational = 6 total)** -- F1: **`_k_*` underscore-prefix as Shape 9b identification heuristic** (CONFIRMED_CLEAN, catalog-amendment candidate -- 17/19 cards classify cleanly; naming-hint not new shape; operator decides whether to amend `shape-catalog.md` Shape 9 identification guide). F2: **`__k_*` double-underscore family as Shape 9b sub-family** (informational; `__k_ls` is the only `__k_*` in this batch; the full ring-buffer family at `world.c:1036-1185` would all classify Shape 9b with the same naming-outlier note; if future batches surface the 210+ siblings, apply canonical-card pattern). F3: **`_k_pow_last` <-> `k_pow` cross-batch See-also back-link MISSING** (ACTIONABLE -- apply pass) -- `_k_pow_last` (this batch) See-also correctly references `k_pow`; `k_pow` (Gameplay rules batch drafted 2026-05-27, line 75) See-also does NOT include `_k_pow_last`. Grep-verified. Apply-pass-author adds `_k_pow_last (engine carry-over state-mirror -- seeds first-frame powerup check after map transition)` to `k_pow`'s See-also when applying Gameplay rules drafts. F4: **SKILL amendment validation** -- F1 PARTIAL (only 1 command in batch, `cmdslist_dl` correctly derived Permission from CF flags `CF_BOTH | CF_MATCHLESS | CF_PARAMS | CF_NOALIAS | CF_CONNECTION_FLOOD`; broader test surface in upcoming Race + Player communication batches); F3 NOT TRIGGERED (no Shape 1 toggle commands in this Shape 9b-only batch -- amendment dormant; first true validation in command-heavy batches); F13 CONFIRMED WORKING (all 3 chunk scratch files used `/tmp/chunk_<A|B|C>_2026-05-27.md` batch-date-suffix correctly; dispatcher validation passed 7+6+6=19 sections; no stale-content collisions). F5: **Cross-batch See-also threading** (informational) -- 6 cards in this batch reference entities drafted in the Match flow batch (`captain`, `coach`, `1on1`, `ffa`, `ctf`); apply-pass-author preserves wording since drafts apply in batch order. F6: **Vestigial Shape 9b pattern -- first occurrence in any batch** (informational, novel pattern) -- `_k_coachteam1`/`_k_coachteam2` registered + read but never written; source comment at `g_userinfo.c:355` confirms "coach stuff copied from captain" with write side never implemented. Classified Shape 9b with vestigial-state observation in Notes; do NOT promote to new shape. Full details in `## Cross-card consistency notes` section at end of drafts file. (c) **SKILL-amendment validator batch** -- this batch's primary purpose was to validate the 3 amendments that landed 2026-05-27 after the Gameplay rules ship (F1 mandatory CF-flag extraction / F3 manual-flip Shape 1 variant / F13 batch-date-suffix `/tmp` filenames). Outcome: F13 fully validated and shippable; F1 partial-validated, broader test needed in Race + Player communication; F3 still dormant (zero Shape 1 toggles in this Shape-9b-dominated batch). The amendment trio is shipping-ready for the remaining 2 batches. (d) **Open follow-ups for future batches** -- (i) **`_k_*` prefix as Shape 9b identification hint** in `shape-catalog.md` (F1 catalog-amendment candidate; operator decides). (ii) **`_k_pow_last` ↔ `k_pow` See-also back-link** at apply time (F3 -- one-line addition to `k_pow`'s See-also when applying Gameplay rules drafts). (iii) **F1 SKILL amendment ongoing validation** -- Race (45) batch is the next stress test; Player communication (18) is the last. (iv) **F3 manual-flip Shape 1 amendment** -- if Race + Player communication also produce zero Shape 1 toggle triggers, shelve until MVDSV / QWFWD / QTV forks. (v) Cumulative ktx-l1-rewrite drafts: **550 of 633 KTX L1 entities = ~87%** (531 prior + 19 this batch). Total parks across all 12 batches: 5 (`callalias` + `roundsdown` + `roundsup` + `y` + `n`); all queued for apply-pass hand-drafting. Total aborted-to-synthesis: 1 (`k_sready`); routes to describe-fill-synthesis. **Categories remaining: 2** (Race 45 / Player communication 18 = 63 entities). [small followup]
- **ktx-l1-rewrite Race -- apply pass + cross-card findings (chunked-mode batch 13; chunk_size=9)** -- 45-card batch SHIPPED 2026-05-27: **45 drafted** (31 clean + 14 flagged) + **0 parked** + **0 aborted** + **0 novelty halts**. 5 parallel chunks of 9+9+9+9+9 (family-grouped: core mode + countdown + status / match + scoring / pacemaker family / route definition / display + chasecam). All drafts in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-race.md`; no parked file. (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 14 `drafted_with_flag` entries need review before applying. Flag rate ~31% (14/45) dominated by F1 Permission-line CF flag mislabel pattern (now **8th consecutive batch** and the F1 amendment's first true stress test: 9 cards flagged for Permission corrections + 1 silent correction = 10 of 29 commands affected, ~34% catch rate) + F2 default-value/factual flags on 5 cvars (k_race_countdown mid-run-adjustable claim wrong; k_race_simultaneous race_settings[] override surprise; k_race_custom_models unverified; k_race_match_rounds missing default; k_race_pace_resolution wrong default). Same operator phase as the prior 12 batches' apply passes; tackle in sequence (thirteenth batch now queued). (b) **Cross-card findings (3 actionable + 5 confirmed clean + 4 informational = 12 total)** -- F1: **F1 SKILL amendment validation -- STRONG SHIPPING SIGNAL** (10 of 29 commands had Permission mislabels caught; per-chunk: A=2 / B=3 / C=0 / D=1 / E=4; without F1 all 10 would have shipped as L1 lies, most labelling `CF_PLAYER|CF_SPC_ADMIN` or `CF_PLAYER` commands as "admin command"; F1 amendment is shipping-ready). F2: **F3 SKILL amendment status -- DORMANT for top-level Shape 1; sub-action manual-flip pattern observed** (all 3 batch Shape 1 paired toggles use canonical `cvar_toggle_msg` at race.c:269 / 5130 / 5243; ONE sub-action manual-flip observed inside race_pacemaker handler at race.c:4331-4336 for the "jumps" sub-action -- NOT a true F3 trigger because sub-action of dispatcher not top-level paired toggle; if MVDSV/QWFWD/QTV forks surface similar dispatchers, operator may want to extend F3 to sub-action contexts). F3: **k_race_pace_resolution default mismatch** ACTIONABLE (existing description says 0; world.c:919 says 2). F4: **k_race_match_rounds default missing** ACTIONABLE (existing description omits value; world.c:924 confirms 9). F5: **race_settings[] mode-activation override of RegisterCvar defaults** (CONFIRMED_CLEAN cross-cutting pattern; race.c:293-322 bundles applied via apply_race_settings(); k_race_simultaneous RegisterCvar=0 but race_settings[] sets 1, so server running k_race 1 gets simultaneous=1 at runtime; surfaced explicitly on k_race_simultaneous draft). F6: **race_match sv_silentrecord cross-engine side-effect** (CONFIRMED_CLEAN; race.c:5244 flips non-KTX mvdsv cvar in lockstep with k_race_match; surfaced on both cvar + command cards). F7: **race_chasecam family asymmetry** (informational; race_chasecam + race_chasecam_view check `self->racer` and refuse for active racers; race_chasecam_freelook does NOT skip racers; asymmetry preserved). F8: **k_race_countdown + race_countdown_up/down paired-numeric-adjuster pattern** (CONFIRMED_CLEAN catalog-amendment-candidate; neither Shape 1 nor Shape 2 fits cleanly; classified Shape 3 cvar + shape-less commands; if future batches surface 2+ more instances operator may crystallize new Shape N). F9: **Pacemaker family L3 concept-note candidate** (apply-pass follow-up; 6 entities form coherent feature family; race_pacemaker See-also at 5-cap). F10: **Cross-batch See-also -- k_race ↔ `race` (Mode selection batch)** (CONFIRMED_CLEAN; k_race's See-also references the `race` command drafted 2026-05-26 in Mode selection drafts file; cross-link resolves naturally at apply). F11: **Dispatcher-hypothesis corrections via Rule 11** (informational; 3 caught: (i) race_toggle is NOT the k_race toggle -- it's `r_changestatus(3)` per-player state setter, not a cvar toggle; the k_race toggle is the `race` command in Mode selection batch. (ii) r_clear_route does NOT write k_race_route_* cvars -- those writes happen in race_route_now_custom called by r_Xset and r_route's failure path. (iii) k_race_countdown NOT mid-run adjustable -- race_is_started() gate at race.c:278 blocks). F12: **See-also bidirectional spot-check** (CONFIRMED_CLEAN; 22 of 25 sibling pairs symmetric; 3 defensible asymmetries: k_race_route_mapname/number to race_route_clear are non-relationships per F11 #2; race_break_all->race_ready one-way follows order-by-relationship-strength). Full details in `## Cross-card consistency notes` section at end of drafts file. (c) **SKILL-amendment stress-test outcome** -- this batch was the F1 + F3 amendment stress-test per the handoff (29 commands × CF-flag extractions = F1's largest production test). Outcome: F1 SHIPPING-READY (validated at scale; ~34% catch rate); F3 dormant for top-level Shape 1 in Race (canonical `cvar_toggle_msg` used throughout; sub-action manual-flip pattern observed inside race_pacemaker as F3-extension candidate signal but not a true F3 trigger); F13 confirmed working across 5 chunks (all scratch files used `/tmp/chunk_<A|B|C|D|E>_2026-05-27.md` naming; dispatcher validation passed 9×5=45 sections, no stale collisions). The 3-amendment trio is shipping-ready for the final batch (Player communication). Rule 11 (source over handoff) operated correctly 3 times in this batch -- worth noting as continued discipline validation. (d) **Open follow-ups for future batches** -- (i) **F1 SKILL amendment final validation** -- Player communication (18) is the last KTX L1 batch; estimated 10-15 commands; expect 3-5 F1 catches at the current rate. (ii) **F8 paired-numeric-adjuster shape candidate** -- track if pattern recurs in future codebases (MVDSV/QWFWD/QTV). (iii) **F9 pacemaker family L3 concept-note** queued; tracked as operator-side authoring not skill-side. (iv) **F10 cross-batch See-also** -- apply Mode selection drafts before/with Race drafts so k_race ↔ `race` cross-link is resolved on apply. (v) **F3 cross-batch See-also threading** (precedent: Internal state's `_k_pow_last` ↔ `k_pow` back-link) -- Race did not surface comparable cross-batch See-also gaps; the cross-batch references this batch makes are all to entities that exist (race in Mode selection batch). (vi) Cumulative ktx-l1-rewrite drafts: **595 of 633 KTX L1 entities = ~94%** (550 prior + 45 this batch). Total parks across all 13 batches: 5 (`callalias` + `roundsdown` + `roundsup` + `y` + `n`); all queued for apply-pass hand-drafting. Total aborted-to-synthesis: 1 (`k_sready`); routes to describe-fill-synthesis. **Categories remaining: 1** (Player communication 18 = 18 entities; this batch finishes KTX L1 100%). [small followup]
- **ktx-l1-rewrite Player communication -- FINAL KTX L1 BATCH + apply pass + cross-card findings (chunked-mode batch 14; chunk_size=9)** -- 18-card batch SHIPPED 2026-05-27: **18 drafted** (15 clean + 3 flagged) + **0 parked** + **0 aborted** + **0 novelty halts**. 2 parallel chunks of 9+9 (Chunk A: Shape 6 family + tpmsg + report; Chunk B: Shape 7 fan-out canonical-card + shape-less message commands). All drafts in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-player-communication.md`; no parked file. Sub-grouped by mechanism family: Shape 6 family (7; `mmode` + `multi` + `s-p`/`s-r`/`s-m`/`s-l`/`s-t`) / Shape 7 fan-out + canonical-card pattern (6; `ksound1` canonical + `ksound2`-`ksound6` reference) / shape-less prose-wrap message commands (3; `killer` + `victim` + `newcomer`) / shape-less other commands (2; `tpmsg` + `report`). (a) **Apply pass** -- operator audits + applies clean drafts to `entities.description`; 3 `drafted_with_flag` entries need review before applying (`mmode` -- existing "open multi-message editor" framing wrong, source shows `mmode multi` calls `multi_do(2, true)` which prints/confirms the multi set, not an editor; `multi` -- direct `multi` does NOT set `*mm`, only `mmode multi` does, the `from_mmode=false` guard at g_cmd.c:982 confirmed; `newcomer` -- CF_BOTH source vs existing "any player" prose, spectators are permitted). Same operator phase as the prior 13 batches' apply passes; tackle in sequence (fourteenth and FINAL chunked-mode batch now queued). Lowest flag rate of any commands-only chunked-mode batch (3/18 = ~17%) -- mature SKILL discipline + pre-verified CF flag table from Race-session recon caused 4 silent CF_BOTH corrections in the s-* family to absorb directly into Permission lines without separate FLAGs. (b) **Cross-card findings (4 CONFIRMED_CLEAN + 4 ACTIONABLE + 1 concept-note candidate + 1 informational = 10 total)** -- F1: **F1 SKILL amendment final validation -- SHIPPING-READY** (CONFIRMED_CLEAN; pre-verified CF flag distribution from Race-session recon matched source for all 18 entities -- 100% pre-verified accuracy; 5 silent corrections caught (4 in s-r/s-m/s-l/s-t absorbed into Permission lines; 1 explicit FLAG on newcomer because the CF_BOTH-vs-CF_PLAYER asymmetry vs killer/victim is itself worth surfacing); 28% catch rate (5/18) consistent with Race's 34% (10/29) and Internal state's partial validation; amendment is shipping-ready and ready for permanent SKILL.md fixture lock). F2: **F3 manual-flip Shape 1 amendment DORMANT, SHELVE** (CONFIRMED_CLEAN; zero cvars in batch = zero Shape 1 cvar+toggle pairs; F3 dormant across both Internal state + Player communication post-Gameplay-rules-landing; per Internal state deferral, SHELVE until MVDSV / QWFWD / QTV forks -- KTX L1 catalog is closed so no further KTX validation possible). F3: **VERDICT-marker emission fully validated** (CONFIRMED_CLEAN; both sub-agents emitted all 9 markers immediately before each `## <entity>` header -- Race's 3-of-5-sub-agent omission gap DID NOT recur; promoted-Discipline-1 wording ("MUST emit VERDICT comment immediately before each `## <entity>` header" promoted from sub-bullet to PRIMARY discipline) is the validated fix; lock as standard template for MVDSV/QWFWD/QTV L1-batch-dispatcher forks). F4: **Canonical-card pattern for ksound1-6 applied correctly** (CONFIRMED_CLEAN; ksound1 = full v2 card with Effect/Prerequisites/Permission/Match-state/Example/See-also; ksound2-6 = short reference cards (~30 lines each: Headliner + Permission + See-also pointing at ksound1 + sibling ksounds + kf); ~83% content consolidation vs hypothetical 6-full-card draft; cumulative canonical-card application across chunked-mode era: Demo & spectator 40 + Match flow 6 + Player communication 6 = 52 of 226 batched-cards under canonical-card discipline = ~23%). F5: **Cross-batch See-also -- userinfo-pile UNWALKED -- RULE-11 CATCH ON HANDOFF** (ACTIONABLE for 14-entity gap audit) -- handoff claimed `kf` drafted 2026-05-23 in Server config & network batch with KF_KTSOUNDS bitmask documented; dispatcher pre-flight grep across ALL 13 prior drafts files returned ZERO matches for `kf`, `premsg`, `postmsg`, `k_sdir`, `k_nick`, `k`; handoff hypothesis RULE-11-WRONG; these userinfo keys likely belong to the 14-entity unwalked pile (633 - 613 = 20 remaining = 5 parks + 1 aborted + 14 unaccounted; userinfo-key pile is the leading hypothesis); See-also references resolve in `entities` table regardless of recast status, so Player communication drafts apply as-is; but operator should walk the 14-entity gap as a follow-up batch (likely new file `ktx-l1-rewrite-drafts-YYYY-MM-DD-userinfo-keys.md`). F6: **Bidirectional See-also spot-check** (CONFIRMED_CLEAN with one defensible asymmetry) -- 12+ sibling pairs checked across in-batch See-also adjacency graph, all symmetric except `multi` See-also includes `s-p` ("one-shot send to a specific player") but `s-p` See-also does NOT include `multi`; asymmetry defensible because s-p's See-also is at the 5-cap (mmode/s-l/s-r/s-t/s-m) and adding multi would exceed the v2 4-5 cap rule; s-p's focus is messaging not set-management. F7: **Match-state surfacing inconsistency between chunks** (ACTIONABLE for apply-pass normalization) -- chunk A omitted Match-state for any-time commands per v2 standard (mmode/multi/tpmsg/report -- all CF_MATCHLESS or any-time); chunk B verbose "any time (CF_MATCHLESS -- usable outside a match)" for killer/victim/newcomer; apply-pass-author should normalize chunk B to v2 standard (omit Match-state for any-time; per universal-shape-v2.md "Match-state: omit when 'any time'"). F8: **MMODE_RCON dispatch commented out in source** (ACTIONABLE operator decision) -- mmode handler at g_cmd.c:1092 accepts `rcon` mode and writes `*mm = MMODE_RCON` if caller has rcon password or VIP_RCON rights; ClientSay rcon dispatch at g_cmd.c:394 is COMMENTED OUT with "WARNING: FULL ACCESS TO SERVER CONSOLE" comment; typing `mmode rcon <password>` succeeds in setting state, but subsequent `say <command>` does NOT execute as rcon (dispatch is no-op-with-warning); user's mental model differs from observed behavior; apply-pass-author decides: (a) add Prerequisites/Notes line to mmode L1 documenting dispatch-disabled state, OR (b) defer to F10 concept note. F9: **TeamSay match_in_progress absence** (CONFIRMED_CLEAN; incorporated into draft) -- existing ksound1 description says "Only active in team or CTF games" (mode prerequisite); source check at commands.c:3377 confirms NO `match_in_progress` early-return, so ksound also works in warmup if mode is team/CTF; canonical ksound1 draft incorporates "Match-state: any time (warmup and mid-match both work; mode prerequisite is the only gate)"; ksound2-6 inherit via reference card pattern. F10: **Concept-note candidate: "KTX private messaging system"** (ACTIONABLE for concept-note queue, not apply-pass) -- mmode + multi + s-p/s-r/s-m/s-l/s-t = 7-entity dense subgraph; L1 captures per-entity behavior but the user navigating the family misses the overall architecture (mmode = stateful router; multi = multi-set manager; s-* = one-shot sends bypassing router; reply-chain mechanics via s_last_to/s_last_from in s_common); concept note should cover ClientSay dispatch chain + multi-set lifecycle (multi command manages set; mmode multi activates routing; s-m fires one-shot) + reply-chain mechanics + cross-batch userinfo references (kf, premsg, postmsg, k_nick, k); author after F5 userinfo-pile drafted so concept note cites finalized L1 entities; estimated ~30 min author time. Full details in `## Cross-card consistency notes` section at end of drafts file. (c) **FINAL-BATCH milestone -- KTX L1 chunked-mode dispatch arc CLOSES** -- this batch closes the KTX L1 catalog-walk arc started 2026-05-22 (session 1 manual catalog walk -> session 2 first non-chunked batches -> chunked-mode dispatcher introduced 2026-05-26 -> 14 batches shipped under chunked-mode through 2026-05-27). Cumulative: **613 of 633 KTX L1 entities drafted under v2 = ~97%**. Remaining 20 entities = 5 parks (`callalias` / `roundsdown` / `roundsup` / `y` / `n` -- hand-drafting pile) + 1 aborted-to-synthesis (`k_sready` -- describe-fill-synthesis pile) + 14 unaccounted (likely userinfo-key pile per F5; gap audit follow-up). 3 SKILL amendments shipped during the arc and validated to ship: **F1 mandatory CF-flag extraction** (28-34% catch rate across 3 post-amendment batches: Internal state partial / Race 10-of-29 / Player communication 5-of-18; SHIPPING-READY; permanent SKILL.md fixture); **F13 batch-date-suffix /tmp filenames** (validated in Internal state + Race + Player communication; zero stale collisions across 10 chunks; permanent fixture); **F3 manual-flip Shape 1 variant** (DORMANT in KTX post-Gameplay-rules; SHELVED until MVDSV/QWFWD/QTV forks per Internal state deferral). **Promoted-Discipline-1 VERDICT-marker emission** validated final in this batch (Race's 3-of-5 omission gap closed; both Player communication sub-agents emitted all 9 markers cleanly). The chunked-mode dispatch arc is now COMPLETE; operator's remaining queue: apply-pass (14 batches' drafts) + parks-hand-drafting (5 entities) + describe-fill-synthesis (1 entity) + 14-entity gap audit (likely userinfo-key pile) + open-follow-ups from prior batches. (d) **Open follow-ups for future batches** -- (i) **F5 14-entity gap audit + userinfo-key pile batch** -- operator walks catalog HTML index for the 14 unaccounted entities; if confirmed as userinfo-key pile (kf/premsg/postmsg/k_sdir/k_nick/k + others), dispatch a follow-up batch (estimated ~14 entities; new file `ktx-l1-rewrite-drafts-YYYY-MM-DD-userinfo-keys.md`); when shipped, apply-pass-author adds reverse See-also from those entities back to Player communication entities (ksound1 family ↔ `kf`; killer/victim/newcomer ↔ `premsg`/`postmsg`; report/tpmsg ↔ `k_nick`/`k`). (ii) **F8 MMODE_RCON L1-vs-L3 decision** -- apply-pass-author picks: (a) add Prerequisites/Notes line to mmode L1 surfacing dispatch-disabled state, OR (b) defer to F10 concept note. (iii) **F10 "KTX private messaging system" L3 concept note** queued (after F5 userinfo-key pile drafted; ~30 min author time). (iv) **Apply pass for 14 batches' drafts** -- operator drains pending apply queue (Server config & network through Player communication, in commit order). (v) **5 parks hand-drafting** -- callalias / roundsdown / roundsup / y / n; queued for operator (separate hand-drafting workstream). (vi) **1 describe-fill-synthesis** -- `k_sready` routes to describe-fill-synthesis (Opus 4.7 MAX dial; separate skill). (vii) **Open follow-ups carried forward from prior batches** -- F1 SKILL.md amendment shipping decision (operator may now lock); F3 shelved until forks; F2 default-value sweep (~17 cvars across 6 batches; one-time audit candidate); vwep L3 concept note (Gameplay rules F6); pacemaker L3 concept note (Race F9); freeze ↔ k_freeze See-also fix (Gameplay rules F11); _k_pow_last ↔ k_pow See-also fix (Internal state F3); F7 Match-state normalization at apply pass for chunk-B entities. (viii) Cumulative ktx-l1-rewrite drafts: **613 of 633 KTX L1 entities = ~97%** (595 prior + 18 this batch). Total parks across all 14 batches: 5 (`callalias` + `roundsdown` + `roundsup` + `y` + `n`); all queued for apply-pass hand-drafting. Total aborted-to-synthesis: 1 (`k_sready`); routes to describe-fill-synthesis. **Categories remaining: 0** (all KTX L1 chunked-mode categories drafted; the 14-entity gap audit is a follow-up workstream, not a "category" in the chunked-mode catalog-walk sense). **KTX L1 chunked-mode dispatch arc CLOSED.** [small followup]
- **L3 concept note: QW team-chat visibility across the stack** -- surfaced 2026-05-21 Session #9 during the KTX describe-fill screening pass when operator + cross-codebase verification produced a non-trivial synthesis (KTX `k_spectalk` + `k_sayteam_to_spec` sets engine cvars; MVDSV `sv_user.c:1890-1900` spec-filter gates broadcast based on the `$\` byte marker / chr(13); ezQuake `TP_ShortNick` and `cl_fakename` interceptor inject the marker; fteqtv relays MVD `dem_multiple` packets transitively without extra filter; net practical = teambinds gated by cvar / plain say_team always private / QTV inherits the gate per POV). L1 descriptions for the affected cvars (k_spectalk + k_sayteam_to_spec) point here via `See also:`. Path: `apps/qw-oracle/curated/concept-notes/qw-team-chat-visibility.md`. Sized ~30-45 min in a fresh terminal with the audit-trail in this conversation's git history (commits `2ac11298` + `a8ce9345`). [small followup]
- **arc-reviewer post-arc walkthrough for the 96-row KTX describe-fill cohort** -- per the arc-reviewer skill, must run in a FRESH terminal that did NOT execute any phase. Reads: spec `docs/superpowers/specs/2026-05-15-ktx-mvdsv-l1-describe-fill-design.md` + plan `docs/superpowers/plans/2026-05-16-ktx-mvdsv-l1-describe-fill/` (incl. decisions.md D20 amendment + all 10 ledger files + the screening-pass ledger). Produces DELIVERED / DELIVERED-DIFFERENT / DEFERRED / MISSING walkthrough + YELLOW open issues + Arc N+1 prep recommendations (notably: format-unify arc + L3 concept note + skill folds queued as N+1 work). [small followup]
- **describe-fill-synthesis skill folds (3 items)** -- (1) Add `ENGINE-BOUNDARY-HEDGED-OK` as a valid TRACED-CLEAN-compatible verdict subclass in `~/.claude/skills/describe-fill-synthesis/references/enforce-trace-discipline.md` (per Session #8 B5 surface). (2) Add "commented-out runtime check masquerading as live condition" as a 4th sub-pattern in the ELABORATION DISCIPLINE list (alongside flag-NAME inversions / callee-branch dead code / command-name pattern inversions). (3) Add "no engine/code jargon in L1 description" as a QA rule (file:line refs / cf_flags / think-handler / stuffcmd / fpd bit references belong in `description_reasoning`, never in `description`). Calibration case: the Session #9 k_dmm4_gren_mode draft leaked "explosion-on-think handler" which the operator caught on review. Sized ~20 min skill edits in user-global skills dir. [small followup]
- **quad auto-record suppression-stuck after manual `/record stop`** -- surfaced 2026-05-20 during the 4on4 verification round after the Mumble silent-recording fix landed. `auto-record.ts:stopForTeam` adds the channel to `suppressedChannels` and only starts the grace timer if the channel is already empty; if the operator who triggered the stop stays in the channel, suppression sticks indefinitely (channel never empties -> `onUserRemove` never starts the grace timer -> suppression never clears). Symptom: subsequent user joins log `AutoRecord: suppressed for #<channel> -- not starting` even hours later. Operator workaround today was `/record start` (which explicitly clears suppression via the manual path). Fix: bound suppression by time -- add a separate timeout when `suppressedChannels.add()` is called that fires after `GRACE_PERIOD_MS` regardless of channel-empty state. ~5 lines in `apps/quad/src/modules/mumble/auto-record.ts`. Latent bug predating the Mumble-silence fix (only surfaced now because the silence fix made manual-stop testing actually meaningful). [small followup]
- **ezQuake `help_variables.json` mis-lists commands as variables (upstream PR)** -- verified 2026-05-16 during the entity-name source-case-fidelity arc: `userdir`, `floodprotmsg`, `scr_weaponstats_{x,y,order,scale,frame_color}` are registered in ezquake-source ONLY via `Cmd_AddCommand` (host.c / cl_cmd.c) yet also appear in `help_variables.json` (in addition to the correct `help_commands.json`). Our L1 correctly keeps them as `command` source_backed; the phantom `doc_only` cvar twins are removed by `pruneCrossTypeOrphans` (this is exactly what dropped ezquake cvar 2997->2992 on the first clean reload -- expected, floor re-baselined at `quality-grid.ts:1583`). Cross-type variant of the help-only-ghost family the `docs/superpowers/plans/2026-05-15-help-json-doc-consistency-gate.md` plan covers -- feed into that gate + the `docs/upstream-prs/` digest pipeline. Route nano/slime; operator signs DCO. ~upstream PR. [small followup]
- **MCP smoke scripts pre-broken (env + stale counts)** -- surfaced 2026-05-16. `serve/mcp/scripts/verify-rewrite.ts` spawns the server via `StdioClientTransport` without propagating `DATABASE_URL` to the child -> server throws at `shared/db.ts:10` -> `MCP error -32000 Connection closed` (fix: pass `env` through, or have the script set it). `verify-gameplay.ts` asserts stale hardcoded counts (entity 37 vs live 50; mechanic 41 vs live 487) on gameplay tables untouched by recent arcs. Neither is caused by the case-fidelity arc -- verified via an in-process `lookup_entity` test (9/9 green incl. case-insensitive resolution + `$B`/`$b` carve-out + ILIKE wildcard-bug fix). ~15 min each. [small followup]
- **Prod MCP image rebuild (entity-name case-fidelity residual)** -- per the 2026-05-16 arc-history retrospective (commit `8093e42f`; executor's claim, not independently re-verified here): prod DB is in lockstep and needs NO data refresh (old image still correct because `ILIKE` was always case-insensitive), but the prod MCP image still runs pre-`8093e42f` code -- it does not yet carry the `name_fold`-based `lookup_entity` match or the latent `ILIKE` `_`/`%` wildcard-bug fix. Low urgency (behaviour is correct today via case-insensitive `ILIKE`; the rebuild is a code-freshness + wildcard-correctness catch-up). Trigger: next qw-oracle prod deploy, or sooner if a `_`/`%`-bearing entity name surfaces a wrong `lookup_entity` result. ~standard image rebuild per `DEPLOYMENT.md`. [small followup]
- **MCP Tailscale direct-route for batch jobs (avoid CF rate limit)** -- surfaced 2026-05-23 during ktx-l1-rewrite sub-batch 2 pre-fetch: 15 parallel `lookup_entity` calls hit Cloudflare 1015 rate-limit at ~10 requests (`oracle.slipgate.me` is fronted by CF Tunnel; CF's per-config rate limit kicks in on parallel bursts). Worked around by reading the 5 missing source_refs from the catalog HTML metadata-strip. The MCP server is reachable directly over Tailscale (Unraid `100.114.81.91`) which bypasses CF entirely. Action: teach the MCP client config (or the dispatcher pattern used by ktx-l1-rewrite + describe-fill-synthesis fan-out arcs) to prefer the Tailscale direct route when on-network. Per-arc fan-outs of 15+ entities are routine; the CF hop is unnecessary friction for them. Need to expose / document the MCP port on Unraid for direct access. ~30 min to add the Tailscale endpoint to `.mcp.json` or equivalent config + smoke test. [small followup]
- [Synthesis-report numerical-claim provenance gap](#synthesis-report-numerical-claim-provenance-gap-2026-04-29) — discipline note for future validation arcs; no retroactive fix.
- **Extend F-D4a owned-row guard to remaining derivers (synthesis-track support)** — original framing (2026-05-07) was wrong: per the derive-entity-description.ts header verified 2026-05-23, all 13 derivers DO write `description_origin` on every UPDATE. What was actually open was the **F-D4a guard** (`description_origin IS DISTINCT FROM 'synthesized'` / `'shipped_doc'`) which protects operator-authored synthesis from re-derive clobbering. Status 2026-05-23: 4 derivers guarded (deriveCvar / deriveCommand / deriveCmdlineParam from enforce-L1 arc 2026-05-17; deriveMacro added today during macros help-JSON synthesis arc, commit `51ec7220`). Remaining 9 derivers (deriveHudElement / deriveAssetCategory / deriveTokenPrimitive / deriveFlagBit / deriveCvarAlias / deriveProtocolMessage / deriveInfoKey / deriveLogTemplate / deriveQcBuiltin / deriveMatchEvent) are unguarded by design — they own no arc user-doc track yet. Add the guard on a deriver only when a synthesis arc claims that entity type. ~3 lines per deriver when needed. [small followup]
- **F-D4a reconciliation for ezquake macros when PR #1127 merges** -- when QW-Group/ezquake-source#1127 lands upstream and the next ezquake walk pulls the populated help-JSON, run the one-line SQL to release the F-D4a guard on the 36 affected rows so deriveMacro can overwrite the synthesized prose with help_json-sourced values: `UPDATE entities SET description_origin = NULL WHERE project='ezquake' AND type='macro' AND description_origin='synthesized' AND id IN (SELECT entity_id FROM macro_versions WHERE version='head' AND NULLIF(TRIM(help_desc),'') IS NOT NULL);`. Snippet also documented inline in `apps/qw-oracle/scripts/load-knowledge/derive-entity-description.ts:deriveMacro`. Trigger: slime merges PR #1127. ~30 seconds. [small followup]
- **F-D4a reconciliation for ezquake commands when PR #1128 merges** -- same shape as the macros reconciliation above, but scoped to commands. When QW-Group/ezquake-source#1128 lands and the next ezquake walk pulls the populated help-JSON, run: `UPDATE entities SET description_origin = NULL WHERE project='ezquake' AND type='command' AND description_origin='synthesized' AND id IN (SELECT entity_id FROM command_versions WHERE version='head' AND NULLIF(TRIM(help_desc),'') IS NOT NULL);`. CAVEAT: the 10 dev_* commands intentionally diverge -- L1 has the rich prose, upstream ships short markers; the reconciliation snippet should NOT touch those 10 (their `help_desc` from upstream would be the short marker, not the rich prose -- releasing the guard would downgrade L1). Add an exclusion for `name LIKE 'dev_%'` in that subquery before running. Snippet also documented inline in the inserter script header. Trigger: a maintainer merges PR #1128. ~30 seconds. [small followup]
- **F-D4a reconciliation for ezquake variables when PR #1130 merges** -- **TRIGGER MET 2026-05-26** (PR #1130 merged today at commit `e4a2c20a`; observed incidentally during the depth-N extractor regression-check ezQuake re-extraction -- AST diff surfaced ~50 new `desc` fields populated on cvars like `cl_mvinset_offset_x/y`; those re-extraction changes were reverted to keep the depth-N commit focused, so a clean `extract-tag --project ezquake --version head --force` is the actionable step). Same shape as the macros + commands reconciliations above, scoped to cvars. After the clean walk, run: `UPDATE entities SET description_origin = NULL WHERE project='ezquake' AND type='cvar' AND description_origin='synthesized' AND id IN (SELECT entity_id FROM cvar_versions WHERE version='head' AND NULLIF(TRIM(help_desc),'') IS NOT NULL);`. No divergence exclusion needed -- all 58 entries in L1 match the PR-payload prose exactly. Snippet also documented inline in `apps/qw-oracle/scripts/insert-helpjson-synthesis-variables.py` header. ~5 min (extract + SQL). [small followup]
- **F-D4a reconciliation for ezquake cmdline_params when PR #1131 merges** -- same shape, scoped to cmdline_params. When QW-Group/ezquake-source#1131 lands and the next ezquake walk pulls the populated help-JSON, run: `UPDATE entities SET description_origin = NULL WHERE project='ezquake' AND type='cmdline_param' AND description_origin='synthesized' AND id IN (SELECT entity_id FROM cmdline_param_versions WHERE version='head' AND NULLIF(TRIM(help_desc),'') IS NOT NULL);`. CAVEAT: 2 entries (`-ruleset`, `-nostdout`) ship CONSERVATIVE descriptions in PR #1131 pending the upstream source-bug fixes -- but the L1 synthesis carries the SAME conservative descriptions (no rich-prose divergence like commands' dev_*), so the reconciliation can release all 49 guards safely; the source-bug-fix PRs come after with their own description updates (tracked separately below). Snippet also documented inline in `apps/qw-oracle/scripts/insert-helpjson-synthesis-cmdline_params.py` header. Trigger: a maintainer merges PR #1131. ~30 seconds. [small followup]
- **rulesets.c cmdline-dispatch bug-fix PR #1132 awaiting review + `-ruleset` description follow-up** -- surfaced during PR #1131 cmdline-pass verifier walk; PR #1132 (https://github.com/QW-Group/ezquake-source/pull/1132) opened 2026-05-26 against QW-Group/ezquake-source. Patch (5 ins / 2 del): added `!` to `src/rulesets.c:629/632` (qcon + default branches) + 4-line `smackdrive` dispatch branch inserted in OnChange-matching order (qcon -> smackdrive -> default). Investigation confirmed unambiguous bug (no Chesterton's fence): "default" was the original sin (dimman 2015-10-19 `156b70d3` -- same commit that wrote the 3 correct `!strcasecmp` branches forgot the `!` on the 4th); "qcon" inherited the broken pattern in 2016 (`be6109f3`); `rs_smackdrive` was already integrated at 8+ runtime sites including the cvar OnChange dispatch at `rulesets.c:863`, only the cmdline dispatch was missing. **Trigger: PR #1132 merges.** Then file small follow-up to `help_cmdline_params.json` updating `-ruleset` description to list all 6 working values (`smackdown`, `thunderdome`, `mtfl`, `qcon`, `smackdrive`, `default`); PR #1131 ships conservative 3-value description pending this fix. Investigation log: `docs/superpowers/parking/2026-05-26-handoff-ezquake-bugfix-prs-rulesets-sys_posix-investigate.md`. [small followup]
- **sys_posix.c Sys_Printf bug-fix PR #1133 awaiting review + `-nostdout` description follow-up** -- surfaced during PR #1131 cmdline-pass verifier walk; PR #1133 (https://github.com/QW-Group/ezquake-source/pull/1133) opened 2026-05-26 against QW-Group/ezquake-source. **Scope narrower than originally anticipated**: investigation confirmed the DEBUG-branch early return at `src/sys_posix.c:77` IS unintentional (contradicts the 2013-12-29 commit's own message "Sys_Printf is DEBUG only (stdout)", `9972c26c`) but the non-DEBUG `#else return;` at line 92 IS intentional (matches that same commit's intent + mirrors `sys_win.c:503-507`'s `#ifndef _DEBUG return;` pattern). Patch (0 ins / 2 del): only the DEBUG-branch `return;` + its trailing blank line. Net effect: POSIX DEBUG client builds now honor `sys_nostdout`/`-nostdout`; non-DEBUG / release client builds remain intentionally silent; dedicated server (`sv_sys_unix.c`) unaffected -- it honors `sys_nostdout` independently and already worked. **Trigger: PR #1133 merges.** Then file small follow-up to `help_cmdline_params.json` sharpening `-nostdout` description to reflect the per-build behavior (DEBUG client + dedicated server honor it; release client is silent regardless because Sys_Printf is silenced by design on the client). PR #1131 ships description-of-intent only pending this. Investigation log: `docs/superpowers/parking/2026-05-26-handoff-ezquake-bugfix-prs-rulesets-sys_posix-investigate.md`. [small followup]
- **mp3 dead-enum cleanup PR (`macro_ids.h`)** -- surfaced 2026-05-23 during macros help-JSON audit. `macro_mp3info` + `macro_mp3_volume` enum entries survived the 2019 deletion of `mp3_player.c` (`ae8b552f`); no handler is registered, no help-JSON entry exists. Same shape as the PR #1126 runtime-dead-entities cleanup. ~2-line deletion in `src/macro_ids.h` against ezquake-source. Could fold into next code-cleanup PR rather than ship standalone. [small followup]
- **Phase 8 eval q5/q8 L3 authoring leads** — Phase 8 eval surfaced two vague-NL Linux/Windows symptom queries that have no L3 concept-note coverage: q5 "screen brightness flickering on Windows after closing ezquake" (HDR + Windows display tech) and q8 "Linux stutters with sys_highpriority and CPU affinity set" (Linux + NVIDIA performance baseline). Both are recipe-shape candidates per the L3 multi-domain framework parking doc. Authored alongside Arc 3 unshelving; bucket-tagged per `docs/superpowers/parking/2026-05-03-layer3-multidomain-bucket-framework.md` (system + engine-config for q5, system + hardware + engine-config for q8). Linked from the broader 6-candidate authoring queue in that parking doc.
- **Per-project conftest.py for extractor pytest** -- extractor-discipline-catchup arc Phase 3 surfaced full-suite pytest collection errors due to sys.path pollution across multiple `tests/` package namespaces in `apps/qw-oracle/scripts/extractors/`. Three pre-existing collection errors (`fte/tests/test_fte_asset_paths.py` + `qw/tests/test_{bsp_parser,pak_extract}.py`); 15 affected tests pass in isolation. F1 in `docs/superpowers/plans/2026-05-08-extractor-discipline-catchup/review-findings.md` carries detailed evidence + proposed fix (per-project `conftest.py` with explicit sys.path discipline OR rename per-project test packages to project-prefixed). Sized ~1-2 hours. Deferral safe through arc close (P5/P6/P7 don't add tests); fix needed before next test-authoring effort touches the tree.
- **Cron-based upstream-drift detector for Layer 1 codebases** — surfaced during KTX Onboarding Pass 2 (`docs/superpowers/specs/2026-05-04-oracle-prod-update-lifecycle.md`). Today's trigger model is operator-poll for all codebases (ezquake / KTX / MVDSV / FTE) — low cadence (~1-5 stable releases per codebase per year) makes operator-poll sufficient. Revisit if (a) cadence increases substantially, (b) non-operator contributors join the loader workflow, or (c) the showcase-site admin panel arc lands and an automated drift signal becomes useful UI. Mechanism would be a script checking `git ls-remote` against last-loaded SHA per codebase, notifying via the admin panel or Discord. No urgency.
- **Dev DB backup hygiene** — surfaced during KTX Onboarding Pass 2 rollback discussion. The dev Postgres container (`qw-oracle-postgres-dev`) has no automated backup; data lives in a Docker volume on WSL on Windows. If WSL gets wiped, all loaded data (every codebase, every tag) must be re-extracted via libclang — slow but reproducible (Voyage cost negligible per arc-history; operator time the real cost). Mitigation candidates: manual weekly `pg_dump` to the Windows-side filesystem, or a wsl-cron that automates it. Low pressure — the prod Synology backup + GitHub-rebuild path (Tier 3 DR in the prod-update-lifecycle spec) already cover the catastrophic case at the cost of operator time.
- **D4 depth-N Pattern 6 lift revisit** — surfaced during KTX onboarding Phase 5 drafting (2026-05-05). Phase 1's lift walks depth-1 `#include` only; KTX `H_ROTTEN`/`H_MEGA` macros live at depth-2 (`commands.c` -> `g_local.h` -> `include/g_consts.h:241-242`). Phase 5 ships a handler-private `_DROPITEM_MACRO_FALLBACK` dict as the tactical workaround per F11 amendment. Principled fix: amend D4 to walk transitive `#include` closure (or named depth-N), update Phase 1's `_handler_modes.py` lift, re-test parse-time impact (F16 gate). KTX is the first surfaced multi-hop case per D4's own "Revisit if a multi-hop case surfaces" caveat. Trigger: when a 2nd engine surfaces a depth-2+ macro dependency, OR when the Phase 5 fallback dict needs to grow beyond the current 2 entries (signal that ad-hoc maintenance > principled lift). Sized as a small arc (~1-2 hour scope: one decision amendment + one phase MD edit + re-test).
- **Cross-project audit cadence** -- trigger-based rule: run `validate-extractor` in cross-project mode after arcs that add a new project, new entity type, schema migration, or modify `extractor_lib/` or `load-version.ts`. Rule at EXTRACTOR-PLAYBOOK.md "Cross-project audit cadence" section; last audit: `docs/superpowers/reviews/2026-05-06-ktx-onboarding-cross-project-audit.md`. Next trigger: whichever of the four trigger-set conditions fires first in a future arc.
- **arc-planner / arc-orchestrator pre-flight: per-phase executor-prompt symmetry** -- post-arc reviewer surfaced 2026-05-08 that the extractor-discipline-catchup arc has `phase-N-executor-prompt.md` for every phase EXCEPT P2 (universal reproducibility probe shipped at `2e7808eb`; same phase also broke the `extractor-discipline-catchup phase N:` commit-subject convention). Not actionable retroactively; the file-as-prompt is a planner-side artifact and re-creating it post-ship has no value. Forward-action: add a one-line check to the `arc-planner` skill's scaffold-close step (or an `arc-orchestrator` phase-boundary check) -- "every phase that has `phase-N-drafter-prompt.md` should also have `phase-N-executor-prompt.md` before declaring the scaffold complete." Cost of missing it: that phase's executor terminal runs without the strong-arc-identification block + V-probe substitutions + halt-shape spec, losing the wrong-arc-walk defense the file-as-prompt convention was built for. Evidence: `docs/superpowers/reviews/2026-05-08-extractor-discipline-catchup-post-arc-analysis.md` YELLOW item 4. Sized: ~10 min skill edit. Trigger: next arc-planner pass touches the skill, or operator decides to harden the scaffold checklist between arcs.
- **MW_ADMIN_PASSWORD rotation (qwiki-v1-beta)** -- value exposed in prior transcript when an `.env` tail-output during a recovery step showed it. Rotate via `Special:ChangePassword` while logged in as Admin at `https://wiki.slipgate.me`. ~30 seconds. Old OAuth Client Secret was rotated during the deploy (no longer leaked); MW Admin password remains the only outstanding rotation hygiene item.
- **qwiki-v1-beta Phase 3 cosmetic followups** -- two items deferred from Phase 3 boundary (NOT Phase 4 scope; separate auth-shape polish micro-commit when convenient):
  - (a) Discord username extraction renders as literal `User` in Citizen user menu. Needs deploy-time recon of actual Discord OIDC payload claims before shipping a `$wgOpenIDConnect_UsernameClaim` fix; `preferred_username` is not guaranteed for Discord. Recon path: temporarily extend `qwikiBetaSyncDiscordRole` helper to `wfDebugLog('qwiki-beta', json_encode(...))` the session contents during one login, inspect `/var/www/html/log/qwiki-beta.log` to see actual claim names, then pick the right one (could become a small F10 if surfaced as cross-phase). The debug-log group `qwiki-beta` is already wired per F9 resolution.
  - (b) `MediaWiki:Group-wiki-contributor-member` + `MediaWiki:Group-wiki-curator-member` interface-message pages don't exist; user menu renders the message-key literals (`<group-wiki-contributor-member>`). ~30 seconds each in operator's Admin browser session: visit `MediaWiki:Group-wiki-contributor-member&action=edit`, type `wiki contributor`, save. Same for curator. If EnableLocalLogin=false blocks re-login as Admin, fallback is `docker exec qwiki-mediawiki php maintenance/edit.php` from `ssh unraid-deploy`.
- **ktx-l1-rewrite k_fb_* cold-synth COMPLETED 2026-05-27** -- 15 Frogbot cvars cold-synthesized via `describe-fill-synthesis` (Opus 4.7 MAX, spec-locked); persisted to L1 via `bun apps/qw-oracle/scripts/describe-fill/synthesize-ktx.ts --persist`. Records local at `apps/qw-oracle/output/describe-fill/2026-05-27-ktx-fb-followup.json` (gitignored per project convention). KTX status now 639/639 evaluated (0 remaining). Verdict breakdown: **14 synthesized + 1 hedged**. **Cross-card findings (3 actionable)** -- F1: **the handoff's "bloodfest" framing was wrong** -- 5 of 15 cvars are actually `k_tot_mode` (Tribe of Tjernobyl) scoped (`k_fb_health`, `k_fb_weapon`, `k_fb_break_on_death`, `k_fb_quad_multiplier`, `k_fb_item_pickup_bonus`), not bloodfest; the `k_fb_*` naming family is mixed-scope (general Frogbot mechanics + ToT-mode mechanics). Downstream cross-references should treat the family heterogeneously. F2: **`k_fb_debug` is structurally vestigial at anchor `g67253dc`** -- no `cvar(FB_CVAR_DEBUG)` read use-site exists anywhere in the tree; the actual path-debug toggle lives on a per-bot struct field (`self->fb.debug`), and the only set-to-1 site (`bot_commands.c:788`) is commented out. Verdict `hedged`, confidence `low`, routed to C1; needs ezQuake/KTX maintainer confirmation whether the cvar is genuinely vestigial (delete from L1?) or whether there's a re-enable path missing from the source. F3: **`k_fb_quad_multiplier` despite the `k_fb_` prefix affects ALL attackers, not bots-only** -- `T_Damage` is the universal damage routine with no `isBot` gate (`combat.c:545`). A name-only synthesizer would have asserted bots-only -- caught by enforce-trace discipline. The mis-namespacing is an upstream KTX naming issue worth flagging if MVDSV-fork work touches this cvar. (b) **Anchor used = `1.47-2-g67253dc`** (not `v1.36-1633-g67253dc` from the handoff doc) -- `git describe --tags` for the same commit `g67253dc` yielded the `1.47-` form because lightweight tag `1.47` was added to KTX between Phase 3 main and this terminal; stamped consistent with the rest of the KTX corpus (the existing 624 evaluated rows all use `1.47-2-g67253dc`). (c) **Next: recast pass** -- the 15 now-synthesized `k_fb_*` cvars need v2 universal shape recast via `ktx-l1-rewrite` / `ktx-l1-batch-dispatcher` (Sonnet 4.6 high) in a separate fresh terminal; would land as a small followup batch alongside `k_race_simultaneous` (which already has a synthesized description; only needs recast). Idempotency fingerprint after live persist: `197c909f11ebf415b41d8b4e3e3fb016`. [small followup]

### Sidequests
- **Lift nginx resolver + variable-upstream pattern to qwiki / qw-stats / phoenix nginx fronts** -- the qw-oracle nginx broke 2026-05-11 because default nginx caches upstream hostname -> IP at startup; container IP drift after the weekly Synology backup-restart cycle leaves the cache stale and produces silent 502s. Fix shipped at commit `8f1dfd82` (resolver + `set $mcp_upstream` + variable `proxy_pass`). Same pattern applies to every other nginx-fronted docker stack on Unraid. **Action:** audit `qwiki-nginx`, `qw-stats` (if it has an nginx), and `phoenix-*` for the same shape and lift the resolver pattern. Each is ~5 min config edit + scp + nginx -s reload. Could fold into the parked unraid-scoping convergence arc (different mechanism but same parent class) or ship as standalone polish.
- **Plugin v-table asset detection (loader-sites handler)** — FTE plugins reach loaders through `cvarfuncs->...` v-table calls, not direct CALL_EXPR. Only `plugin:ezhud` affected; ezhud images ship bundled with FTE. Low pressure. (Engine-agnostic structural finding; no body to migrate.)
- **Cvar-binding handler indirection gap (snprintf chains + CVARFC callbacks)** — engine-agnostic limitation. Bundle reconciliation correctly treats these as `seedRetained`; only the AST-corroboration signal is partial. Low pressure.
- **Phase 2e follow-up arc residuals** — partially superseded by cross-extractor arc and by 2026-05-01 help-JSON classification arc. Remaining: 1 pre-existing ezquake F2 informational anomaly (`gl_lightmode`). Triage alongside next ezQuake deep-time refresh.
- **Map knowledge layer follow-ups** — slipgate map-browser UI; advanced `search_maps` filters; author seed-YAML curation; automated quarterly stats refresh; future `maps.quake.world` richer-metadata refactor. (Bundled from the Map knowledge SHIPPED retrospective's "Remaining" list; arc body now in `apps/qw-oracle/docs/arc-history.md`.)
- **matchscheduler doc system reconciliation** — 17-file `apps/matchscheduler/context/` predates monorepo doctrine. Earned its own brainstorm when matchscheduler work next surfaces friction with the existing system. Per docs-redesign spec Plan 2 narrowed scope (2026-04-29).
- **`pm_rampjump` description for mvdsv help-JSON (if/when mvdsv grows one)** — surfaced 2026-05-07 during PR #1120 prep. Source-level investigation produced a clear description (raises ground-speed cap proportionally to slope angle, fixes ramp-jump stutter on angled surfaces, caps jump-off speed to flat-ground baseline). Excluded from #1120 per slime's "sv_* belongs to mvdsv" convention rule. Description text parked in PR #1120's "Side findings" section. No urgency; reactivate if mvdsv adds help-JSON or convention shifts.
- **`cl_voip_demorecord` is dead code** — surfaced 2026-05-07 during PR #1120 audit. Cvar is registered at `src/snd_voip.c:53` with default `"1"` and trailing comment "Record VOIP in demo." but has **zero read sites** anywhere in `src/` — no `.integer`/`.value`/`.string` access. Either dead code or partially-removed feature. Recommend either implementing the gating logic or removing the registration. Flagged in PR #1120 body as info; slime's call.
- **`cl_pext_serversideweapon` OnChange handler missing** — surfaced 2026-05-07 during PR #1120 audit. `onchange_pext_serversideweapon` is forward-declared at `src/cl_main.c:86` and registered as the OnChange callback, but the function definition appears absent from the source tree. Connect-path behavior intact (so the help-JSON description in #1120 stands), but the OnChange path is broken or stale-ref. Flagged in PR #1120 body as info; slime's call.
- **qw-oracle Layer 1 database design audit** (operator-flagged 2026-05-05 during KTX-onboarding Pass 5.4) — schema has grown organically across v1-v18 + KTX-onboarding ahead. Audit scope: index coverage vs actual MCP query patterns (GIN indexes on JSONB columns, common-WHERE column coverage); storage shape (text where ENUM/numeric would be cheaper; JSONB-everywhere vs relational normalisation pressure points); CHECK constraint sprawl as kind/type values grow across 5+ engines; pgvector index params (lists/m/ef_construction tuning); dead columns / unused fields from SQLite-era leftovers; implications for loader scripts and natural-key upserts. Outcome: `docs/superpowers/specs/<date>-qw-oracle-db-audit.md` findings spec + actionable-items arc. Run AFTER KTX onboarding ships -- not folded into KTX execution. Operator's framing: "an audit to see if there are any rookie mistakes or obvious low hanging fruit to optimize, and what it means for the scripts that produce the databases."

### Ongoing arcs
- [Asset-type curate skill arc](docs/superpowers/parking/2026-05-13-asset-type-curate-skill-arc.md) — Phase 1 SHIPPED 2026-05-13 (7 commits, head `3fcfcf41`). Phase 2 + Round 3 calibration SHIPPED 2026-05-14 across 5 commits (`3d2a1867` skybox post-audit re-run + `03449c65` charset/hud_element/map slices + `45617006` seed corrections + `ff76967b` side-quest handovers + `430be56e` oversight handoff). L1 vocab alignment audit closed + refinement arc reorg at `b0b736be`. L1 refinement Phase A (Cat 1 misroutes + Cat 2 console reroutes) SHIPPED 2026-05-14 at `beb57c46` -- 11 ENCLOSING_FN_CATEGORY_OVERRIDES + 5 new FTE L1 asset_category labels + 2 seed bridges. L1 refinement Phase B (Cat 3 write-path leakage + 2 inline NF1/NF3 scope adds) SHIPPED 2026-05-14 at `d4c6f1ba` -- note: commit subject reads as qwiki-v1-beta orchestrator work due to a parallel-session staging collision; full Phase B file list is in the commit body. 5 asset-notes shipped across 4 status flags (player_skin CONFIDENT / skybox DIVERGENT / charset CONFIDENT / hud_element DIVERGENT / map DOC-GAP). 9 user-global skill patches landed at `~/.claude/skills/asset-type-curate/` from Round 3 findings (adjacency-cutoff rule / L1-CAT-AMBIGUOUS named pattern / bulk-L1 selection rule / `l1_hint_bare_categories` Step 1 fallback / procedural-family scope rule / seed-flat-list-to-per-engine-keys translation / cross-engine wording / QWCL-MVDSV JSON existence / companion typology trigger-vs-co-installed). Phase 3 fan-out (16 remaining slugs) + 3 sibling arcs (L1 extractor refinement / L1 vocabulary alignment audit / concept-note partner authoring) queued via 4 new parking docs at `docs/superpowers/parking/2026-05-14-*.md`. Oversight handoff for fresh-terminal continuation at `docs/superpowers/parking/2026-05-14-asset-type-oversight-handoff.md`. Phase 3 sidecar: re-walk obligation across 15 ezQuake tags + 1 FTE version (still pending; runs alongside Phase 3 fan-out). Spec: `docs/superpowers/specs/2026-05-13-asset-type-curate-skill-design.md`. Plan: `docs/superpowers/plans/2026-05-13-asset-type-curate-skill-arc.md`.
- [Slipgate Managed Mode pivot](docs/superpowers/parking/2026-04-28-slipgate-managed-mode.md) — **HIGH PRESSURE.** All 6 brainstorm passes COMPLETE; cold-eyes review shipped 2026-05-11. **Three design passes 2026-05-12** settled the full Critical + Important tier + over-engineering review + cross-platform commitment (findings doc grew 698 → 1111 lines): (a) first session settled C-1 through C-7; (b) second session settled all 8 remaining Important findings (I-3 / I-5 / I-6 / I-8 / I-9 / I-10 / I-12 / I-13) + surfaced new C-8 (engine homedir, ezQuake-specific cross-platform) + W-11 (import-classification handler) + substrate principle (metadata enrichment is delta-sync-driven); (c) evening Opus Max simplicity-lens pass yielded S-1 through S-5 simplifications (drop `profile-roles-history.json`, drop `.slipgate-light/` state, collapse C-8 motions, drop `source_root` annotation, reframe C-5 pre-push as exception assertion) + Path B cross-platform commitment (spec portable now, Windows-only V1 build, Linux/macOS in later arcs) + per-platform mechanism table. Findings doc + memory files current. **Next move:** Browse + Manager UI brainstorm (NEW gating step — substrate well-specced but UI surface undefined; operator flagged current Browse is in a regressed state with missing domain list). Pulling the QW gfx corpus (1.4GB from nicotinelounge.com via ciscon) as input to the brainstorm. After UI brainstorm lands → spec amendment cluster (C-1 through C-8 + I-3 / I-5 / I-6 / I-8 / I-9 / I-10 / I-12 / I-13 + S-1 through S-5 + cross-platform) + cheap spikes (I-1 FTE `cfg_save` + I-4 ezQuake `Cmd_Exec_f`) → arc-planner for Arc A + Arc Z (Browse/Manager UI). V1+ roadmap items consolidated: free-baseline install (C-4), pak-builder (C-5), P2P send-anywhere transfer (I-3 Reality-2), smart import classifier (W-11), hub-as-delivery (I-3 Reality-2). V1 = Arcs A+B+D+E+C-minimal + Z (Browse/Manager UI) + Backup sub-arc.
- [qw-oracle showcase site + contributor pipeline](docs/superpowers/parking/2026-04-30-qw-oracle-showcase-site-contributor-pipeline.md) — design landed 2026-05-01 (`docs/superpowers/specs/2026-05-01-qw-oracle-showcase-site-design.md`); next step is claude.ai/design mockups, then implementation plan.
- [Cross-extractor pattern audit follow-up arc](docs/superpowers/parking/2026-04-28-cross-extractor-pattern-audit.md) — five phases shipped + 13 audit-deferred residuals + 2 new follow-ups.
- [Per-project Mode B validation synthesis follow-ups](docs/superpowers/parking/2026-04-28-mode-b-validation-followups.md) — three Mode B validations shipped; some closed, some queued.
- [KTX/MVDSV Layer-1 describe-fill](docs/superpowers/parking/2026-05-15-ktx-mvdsv-l1-describe-fill.md) -- KTX side closed (`arc-ktx-format-unify-shipped`; 607 rows on D20 template; retrospective in arc-history 2026-05-21; 618 KTX rows still need re-embed for `description_embedding_stale=true`). **MVDSV side remains open** -- same template + apply mechanism mirrors directly. Spec `docs/superpowers/specs/2026-05-15-ktx-mvdsv-l1-describe-fill-design.md`; plan `docs/superpowers/plans/2026-05-16-ktx-mvdsv-l1-describe-fill/` (D20 + D21 in decisions.md). Three Session #9 follow-ups queued in Small followups (L3 team-chat note, arc-reviewer 96-row walkthrough, describe-fill-synthesis skill folds) -- none gate the MVDSV side. Session #9 wrap: `docs/superpowers/parking/2026-05-21-ktx-mvdsv-l1-describe-fill-orchestrator-resume.md`.
### Future arcs (waiting on trigger)
- **[ezQuake contributor map](docs/superpowers/parking/2026-05-23-ezquake-contributor-map.md)** -- per-subsystem authorship/maintainer lookup from git log/blame on `research/repos/ezquake-source` for routing upstream PRs. Surfaced 2026-05-23 mid-commands-PR drafting when "ask slime" was being assumed but he's a later contributor, not the original author. Trigger: next non-trivial upstream-PR routing question we can't answer, or operator-initiated.
- **MCP tool upgrades (`version` param + `get_entity_history` tool + version/date filters on `search_entities`)** -- Phase 2g residual from the 2026-04-18 qw-knowledge-rollout (Tier 4, deprioritized at the time because the dir-browser reads SQLite directly; MCP upgrades served only Oracle-bot / Claude-session users). Status verified 2026-05-21 against MCP source: `version` NOT in `lookup_entity` args (`serve/mcp/src/tools/lookup-entity.ts:8-12`); no `get_entity_history` tool in `serve/mcp/`; no version/date filters in `search_entities` args (`serve/mcp/src/tools/search-entities.ts:25-30`). Pressure: low -- current MCP answers about head; deep-time queries are rare in the eval corpus. Trigger: operator-initiated when Oracle-bot / MCP-consumer demand surfaces version-aware queries, or when the showcase site / slipgate snapshot consumer needs version-specific tool answers. Original parking doc (covering Phases 2d-2h, mostly shipped) `git rm`'d 2026-05-21 -- shipped tier records live in `apps/qw-oracle/docs/arc-history.md`. [future arc]
- **[L1 contextual build-availability arc](docs/superpowers/parking/2026-05-20-l1-contextual-build-availability-arc.md)** -- relevance-at-answer-time framing captured from enforce-L1 Phase 5 slime review (2026-05-20). L1 is build-agnostic, but the user's question is build-specific: an oracle/LLM answering about `gl_program_sky` / `cache_print` / `addloc` today has no way to say "renderer-scoped / SERVERONLY / legacy alias -- may not be in your build." Two paths: (1) cheap interim -- oracle answer-shape hedge using the existing Track-A level-2 signal + a small heuristic table (seeded by the `scope_label` rule prototyped in `enforce-l1-pop1-triage.html`), sized for `superpowers:writing-plans` directly; (2) proper per-build-profile arc -- explicit CMake-options modeling + renderer-dispatch-table modeling + legacy-alias chain capture + per-fork client profile passed at query time, multi-week, needs `arc-brainstormer`. D5/F9-bound: per_variant alone is too coarse (2786/2788 = client-reachable). Also captures the zero-unknown population-2 finding from this 166-pool (after the ciscon split, residue = exactly internal0-internal9 known-wired). Trigger: operator-initiated; ready for arc-classifier when picked up.
- **[quad Discord-surface enhancements](docs/superpowers/parking/2026-05-20-quad-discord-surfaces.md)** -- three progressive tiers unified by "quad as Discord presentation layer for QW data": (1) auto-channel-topic with the share link on Mumble activation, (2) auto-mirror matchscheduler scheduled matches into native Discord Scheduled Events per team, (3) the actual arc -- subscribable community feeds (LAN events from quake.world, divisional ladder matches, KTX cup schedules) opt-in via quad commands, surfaced as Discord Events or DM digests. Surfaced 2026-05-20 mid-Mumble-cert work after operator set `#sr` topic manually with the share link and noticed native Discord Events. Trigger: operator-initiated after auto-record + share-link UX soaks in real squad use for at least one matchday cycle.
- **[Unraid backup redesign (borgmatic 3-2-1)](docs/superpowers/plans/2026-05-19-unraid-backup-redesign.md)** -- replace the single-copy / unverified / stop-the-world weekly appdata tar with borgmatic -> encrypted borg repo on Synology over SSH -> Prague offsite pull; dedup + versioned history, Healthchecks + Discord visibility, restore-drill acceptance gate. Spec `docs/superpowers/specs/2026-05-19-unraid-backup-redesign-design.md`; 7-phase operator-paced plan (incl. ~3-4wk parallel soak before retiring the old tar). Surfaced 2026-05-19 from the qwiki-v1-beta V6 false-alarm investigation (the V6 command + `unRAID/docs/server/backup.md` were already corrected, commit `80218de0`). Trigger: operator-initiated; Phase 0 `[SSH]` discovery is the cheap kickoff.
- **[Cross-project Unraid scoping convergence](docs/superpowers/parking/2026-05-14-unraid-scoping-convergence.md)** -- lift the qwiki-v1-beta unraid-deploy scoped non-root convention to quad / qw-stats / qw-oracle. Trigger: operator-initiated.
- **[QWCL cross-engine description borrow](docs/superpowers/parking/2026-05-07-qwcl-cross-engine-description-borrow.md)** -- 156/187 QWCL cvars (83%) have name-matches with documented ezquake cvars; clean implementation is one column on `cvar_versions` (`description_inherited_canonical_id`) + deriver fallback + backfill script. Surfaced 2026-05-07 during KTX post-review cvar embedding gap investigation. Gated on the trailing-comment fix (parent arc) shipping first per "narrow arc before broad". Open questions in parking doc: extend to MVDSV/FTE/KTX where applicable. **Update 2026-05-07:** trailing-comment fix shipped (commit `02155560`); borrow arc no longer gated. Description_origin provenance column shipped (migration 012); borrow arc emits 'inherited' value when it ships.
- **[KTX game-mode Layer 3 concept notes](docs/superpowers/parking/2026-05-09-ktx-game-mode-l3-concept-notes.md)** -- ~25 concept notes covering KTX's playable modes; wiki-sourced where `wiki_ref` exists, hand-authored otherwise; frontmatter cross-references L1 `gameplay_mechanics` rows. Closes the bloodfest-shape vague-NL retrieval gap surfaced 2026-05-09 in the prod smoke after KTX onboarding shipped. Replaces the retired "Gameplay-table description schema arc" entry (mis-scoped 2026-05-07; the architectural read 2026-05-09 confirmed Layer 1 should stay structured / no description column on gameplay tables; prose belongs in Layer 3). Trigger: operator-initiated; brainstorm handoff prompt ready at `docs/superpowers/parking/2026-05-09-ktx-game-mode-l3-concept-notes-brainstorm-handoff.md`.
- **[dusty-* antilag fork L1 onboarding](docs/superpowers/parking/2026-05-16-dusty-antilag-fork-l1.md)** — `research/repos/dusty-ktx` + `dusty-mvdsv` (cloned, never extracted; doc-landscape probes + M denominators are mainline-only). Surfaced 2026-05-16 during describe-fill Pass 3.2. A **behavior/description fork, NOT an entity-set fork**: antilag-named entity surface is identical across forks (`sv_antilag` / `k_vp_antilag` / `antilag` cmd), but `dusty-ktx` ships a 783-line `antilag.c` and `sv_antilag` semantics diverge (mainline 0↔2 on==2 passthrough vs dusty ==1 multi-mode); both deployed in production on different port ranges. Onboard-extractor fork path; cheap probe first to size divergent surface beyond antilag-named (only that surface verified this session). describe-fill D10 ships the `sv_antilag` dual-description L1 stopgap; this arc is the full fork extraction. Trigger: operator-initiated (known-arc; backlog).
- **Author trust weighting in retrieval ranking** -- surfaced during Phase 3 close (2026-05-02). Raw author-frequency signal lives in `qw_oracle.messages` already; trust-weight computation belongs in Phase 6 retrieval ranking (or Arc 3 if LLM topic-classification is added). Two approaches: auto-derived from frequency (failure mode: frequency != expertise; e.g. helpdesk askers vs. answerers) or hand-curated authority list (~20-50 names with topic tags). Defer to Phase 6+ retrieval refinement.
- [Layer 2 thread reconstruction](docs/superpowers/parking/2026-05-03-layer2-thread-reconstruction.md) -- LLM-driven re-segmentation of Discord chat into topic threads (replacing timestamp-bucket sessions as the primary retrieval unit). Architecture + research summary parked 2026-05-03; Stage 4 bucket-tagging extension added 2026-05-03 per the multi-domain framework. Cost equivalent to the original per-session summarize plan that was already deferred to Arc 3. Trigger: post-Phase-8 deploy when `query_log` evidence shows whether FTS-only Layer 2 retrieval is the bottleneck.
- [Layer 3 multi-domain expansion + bucket framework](docs/superpowers/parking/2026-05-03-layer3-multidomain-bucket-framework.md) -- L3 expanded from engine-internal to player-as-system ontology (9-bucket taxonomy: engine-config / engine-content / visual-customization / system / hardware / peripherals / network / server-side / community); recipe-shape (not encyclopedia) note convention; lockstep flagging architecture (cross-ref showcase-site doc) for sustainable staleness management. Surfaced 2026-05-03 during Phase 8 eval-set walkthrough when q5/q8 (vague-NL Linux/Windows symptom queries) failed retrieval revealing the structural gap. Three connected parking docs (this + L2 thread reconstruction + showcase-site contributor pipeline) form the Arc 3 architecture.
- [Cross-extractor Phase 6 residuals](docs/superpowers/parking/2026-04-28-cross-extractor-phase6-residuals.md) — D.1.8 lifecycle hooks + broader positive-contracts coverage + deep-time-walk obligation.
- [Cross-extractor Phase 6 ezquake exemptions: r_bloom_* shape](docs/superpowers/parking/2026-04-28-phase6-ezquake-r-bloom-shape.md) — convergent with QWCL 1996-vintage shape; joint positive-contract arc candidate.
- [Cross-engine alias scaffolding + slipgate version-awareness follow-ups](docs/superpowers/parking/2026-04-26-cross-engine-alias-followups.md) — sub-thread #4 (FTE asset bundle, superseded by Slipgate Managed Mode TAIL-1) + sub-thread #5 (slipgate version-awareness, tracked-by Quake Dir Control plan).
- [Retired cvars in snapshot + stale-config warning UX](docs/superpowers/parking/2026-04-26-retired-cvars-stale-config-warning.md) — coupled producer+consumer work blocked on UX design.
- [Slipgate SCHEMA.md for snapshot consumer interface](docs/superpowers/parking/2026-04-26-slipgate-schemamd.md) — gated on next slipgate UI arc surfacing version-arc badges.
- [Feed tab future content](docs/superpowers/parking/2026-04-27-feed-tab-content.md) — tournaments / dev landscape / GitHub monitoring / community announcements.
- [Screenshot POC → Profile picture generator](docs/superpowers/parking/2026-04-27-screenshot-profile-picture.md) — graduate POC into Profile.
- [Tray menu launch](docs/superpowers/parking/2026-04-27-tray-menu-launch.md) — optional arc; documented home if launch needs to come back.
- [Interactive HTML dashboard](docs/superpowers/parking/2026-04-XX-interactive-html-dashboard.md) — shelved with documented unshelve triggers.
- [qw_event_log as cross-validation oracle for Layer 1](docs/superpowers/parking/2026-04-XX-qw-event-log-cross-validation.md) — gated on KTX cvars + KTX gameplay overrides.
- [SCHEMA.md doc-style inconsistency](docs/superpowers/parking/2026-04-29-schemamd-style-refresh.md) — per-table body refresh + topical-vs-per-version style decision.
- [Layer 3 concept note: death rules](docs/superpowers/parking/2026-04-29-layer3-death-rules-concept-note.md) — gated on KTX gameplay overrides; three-anchor synthesis.
- [L1-alpha: Ecosystem-tools registry](docs/superpowers/parking/2026-04-29-l1-alpha-ecosystem-tools.md) — Pass 3 carry-forward.
- [L1-beta: Cross-format binary fingerprinting](docs/superpowers/parking/2026-04-29-l1-beta-cross-format-fingerprinting.md) — Pass 3 carry-forward.
- [L1-gamma: Engine helpdoc / data-file recognition](docs/superpowers/parking/2026-04-29-l1-gamma-helpdocs.md) — Pass 3 carry-forward.
- [L1-delta: Stock asset catalog](docs/superpowers/parking/2026-04-29-l1-delta-stock-pak.md) — Pass 3 carry-forward.
- [Memory system consolidation](docs/superpowers/parking/2026-04-29-memory-system-consolidation.md) — **PRESSURE INCREASING.** File count: **148** (was ~77 at 2026-04-29 post-trim baseline; was 125 at 2026-05-17; was 133 at 2026-05-20). MEMORY.md: **21.6KB / 202 lines** — exceeds BOTH 20KB and 150-line thresholds per docs-check Phase 1 Step 5; the system-reminder banner notes "Only part of it was loaded" -- index is being truncated at session-load. Schedule consolidation pass when convenient; recent inflow vectors include the 2026-05-26 help-JSON arc closure + 2026-05-27 L1 extractor refinements arc (+1 feedback memory `feedback_handover_claims_decay`).
- [Promote methodology learnings to served admin/player concept notes](docs/superpowers/parking/2026-05-29-served-admin-knowledge-from-methodology.md) — game-mode classification relations + hosting model (`k_allowed_free_modes` bitmask, dmm flags, dynamic one-server model) live only in internal `_methodology/` docs (never ingested). Mold into served notes (`game-modes` overview for players+admins; `server-setup`). Trigger: admin-facing query stream ("what modes should I run?") or the old-wiki admin-guide restructuring arc. **Partial (2026-05-30):** the dmm-flags piece is now served by `deathmatch-modes.md` (game-mode arc); the player+admin game-modes overview + `server-setup` notes remain unwritten.

### Recently opened (this session)
- **KTX game-mode concept-note arc -- SHIPPED 2026-05-30** (30 notes: 27 modes + deathmatch-modes + dmm4 + rocket-arena; full record in arc-history 2026-05-30, tag `arc-ktx-game-modes-shipped`). Safe to prune.
- **Game-mode hosting-section reframe + frogbots gap (follow-on, NOT started)** -- operator review of the hosting model produced a per-audience reframe: `Starting a game` = user activation, `Hosting & settings` = admin (availability + tunables), deferring the bitmask/`k_defmode`/force-path mechanics to the new `server-setup.md`. This session: directive rewritten, `rocket-arena` reworked as the worked example, `server-setup.md` seeded with the source-verified gating model. **Remaining:** reframe 8 toggle-mode notes (their "set `k_x 1` to enable" is wrong -- `common_um_init` resets the cvar; lgc excepted) + ~17 UserMode notes (the "set `k_allowed_free_modes 4095`" enable-framing), settle the enforced-vs-tunable-knobs structure question, and scope a `frogbots.md` note (bots run under the hood in tot/LGC/practice but are undocumented). Handoff: `docs/superpowers/parking/2026-05-30-game-mode-hosting-reframe-and-bots-gap-handoff.md`.
- **L1 extractor entity-classification followups (from help-JSON audit probes)** -- spawned 2026-05-15 during command-pass review of the help-JSON empty-entries audit. Two of four audit passes complete (cvar shipped 2026-05-14; command shipped 2026-05-15); macros + cmdline pending. **2026-05-15 update (CORRECTED 2026-05-15 PM):** the cvar source-comment promotion (`2edce42b`) was later found to be a PROVENANCE BUG under slime's two-audience doc model (help-JSON = user docs / source `// comment` = coder rationale; primary-sourced, 97% differ) and was **REVERTED** -- commits `dc50b3ef` (handler) + `1f0227f5` (regression guard `F1.ezquake.anchor.no_comment_promotion` + retreat-fix floor recompute) + `99c21532` (docs); all 15 ezquake tags re-walked clean (bug signature 502 rows/47 entities -> 39/3 genuine upstream mirrors). The macro `teamplay-restricted` "inverted 9/9" claim is a **VERIFIED NON-FINDING** (DB matches `src/teamplay.c` HEAD; `_derive_teamplay_restricted` derives source-truth-correctly; never inverted) -- a dispatched-terminal claim that failed primary-source verification; do NOT propagate. Landscape + verification in the convergence doc's "Cvar provenance pass (2026-05-15)" SHIPPED block; the help-JSON audit RESTARTS clean (authoritative handoff `docs/superpowers/parking/2026-05-15-handoff-helpjson-cvar-pass-and-reachability-blindspot.md`). The earlier `Cmd_AddLegacyCommand not distinguished` finding is STALE -- the handler already emits `ast.legacy_alias_of`; the real gap is loader-persistence + a schema column (arc work). **2026-05-15 closure:** ghost classification = verified no-op (intended output of `3be4d576`; functionally inert), F2 flicker scoping = fixed (`9a5a0c2d`), `291268bd` quarantine = retracted (`fb633ed0`) -- all folded into the convergence doc. The `kick_to_ciscon`->nano/slime relabel remains open, routed to the help-JSON empty-entries audit workstream (semantic re-bucketing, vocabulary source unlocated; tracked in the convergence doc, not coupled to extractor/loader changes). Cold-start handover `docs/superpowers/parking/2026-05-15-handoff-ghost-classification-f2-scoping.md` (commit `b417b61f`) is now closed; cross-references the entity-state-retreat bug below. Parking doc `docs/superpowers/parking/2026-05-15-l1-extractor-entity-classification-followups.md` accumulates findings per audit pass; becomes input for arc-planning the L1 extractor improvements once all 4 audits land. Audit deliverables (kick_to_ciscon questions for ciscon) are valid output independently. Sister docs at `docs/superpowers/parking/2026-05-13-l1-extractor-asset-loader-enhancements.md` and `docs/superpowers/parking/2026-05-14-l1-extractor-refinement-arc.md` -- may consolidate at arc-planning time. **2026-05-16:** the HUD dynamic-name family + command-direction case-fold gap were pulled OUT of this feeder INTO the libclang two-track arc (Track B + shared foundation); only `Cmd_AddLegacyCommand` persistence + trailing-comment harvester precision remain as this doc's siblings. **2026-05-20:** F20 from enforce-L1 (the Class 3 cmdline-consumer-presence feeder is `.c`-only-scoped; 5 of 11 shipped Track-A entries were LIVE via `.h` macro wrappers fanning into `.c` call sites; visible artifact corrected via `_runtime_dead_entities.py:_CLASS3_BLOCK` regen to 6 entities, L1 mislabeling of the 5 cmdline_params persists) joins as a third sibling -- the cmdline-liveness feeder mechanization. See `docs/superpowers/plans/2026-05-17-enforce-l1-runtime-truth/review-findings.md` F20 for the full investigation; the proper fix mechanizes `.c`+`.h`-aware consumer-presence detection. **2026-05-27 closure update:** the L1 extractor improvements arc shipped 2026-05-27 (arc-history entry of same date). Of the open siblings: (a) `Cmd_AddLegacyCommand` persistence SHIPPED (migration 017 + loader, commit `c91c5ece`); (b) `COM_CheckParm` bare-call detection VERIFIED NO-OP (already in handler since an earlier arc); (c) F20 `.h`-aware liveness SHIPPED (4 of 5 cases already correctly detected via libclang macro expansion -- `-cheats`, `-progtype`, `-r-debug`, `-democache`; the `-enablelocalcommand` residual shipped 2026-05-27 commit `547038e3` via STRING_LITERAL cursor-walk in `_handler_cmdline.py` + 15-tag re-walk -- root cause was libclang reporting macro-expanded literal extents as the invocation source span, not the literal itself, NOT the variant-dispatch hypothesis); (d) HUD dynamic-name family VERIFIED NO-OP (`_handler_cvars.py:HUD_Register` synthesis already extracts 1,463 hud_* cvars across 87 elements). Only `trailing-comment harvester precision` remains genuinely open from this feeder doc -- low-pressure, metadata-fidelity sibling.
- **L1 extractor refinement arc** -- Phase A (`beb57c46`) + Phase B (`d4c6f1ba`) SHIPPED. **Phase C (Cat 4 FTE charset watchlist gap, ~30-60 min) is next and closes the arc.** Parking doc: `docs/superpowers/parking/2026-05-14-l1-extractor-refinement-arc.md` (full scope + Phase A/B status at top). Phase-C executor handoff: `docs/superpowers/parking/2026-05-14-handoff-l1-refinement-phase-c-worker.md`.
- **Asset-type-curate Phase 3 fan-out** -- spawned 2026-05-14. 16 remaining slugs after Round 3 calibration; skill is production-ready. Three waves recommended (4 low-risk Brief + 1 L1-GAP test + 11 mixed). Sized 1-2 sessions (parallel dispatch). Parking doc: `docs/superpowers/parking/2026-05-14-asset-type-phase-3-fanout.md`.
- **Concept-note partner authoring (hud-configuration + map-selection-workflow)** -- spawned 2026-05-14. Two new concept-notes warranted by Round 3 calibration (hud_element earned a partner for HUD configuration system; map earned a partner for map-selection workflow). Sized 2 sessions. Parking doc: `docs/superpowers/parking/2026-05-14-concept-note-partners-authoring.md`.
- **QWiki sandbox modernization arc spawned 2026-05-09** — Phase B brainstorm uncovered structural messiness gating downstream oracle work; operator + bps + ciscon aligned on Discord; ciscon shipped 87M SQL + 6.4G image tarball (with bonus 178 QW demo files); project scaffold landed at `apps/qwiki-sandbox/`. **Status:** awaiting arc-classifier → arc-brainstormer → arc-planner in fresh session. Handoff at `docs/superpowers/parking/2026-05-09-qwiki-sandbox-arc-planning-handover.md`. Phase plan sketched (6 phases): scaffolding (done) / MW 1.35 import / MW 1.39 LTS upgrade / Citizen + VisualEditor / Page Forms / EQL cleanup pilot / showcase to maintainers. Sister-arc dependency: Phase B drain (Phase 4 of qwiki community-reference arc) couples to this arc's phase 4-5 via the cleanup pilot.
- **Phase B drain brainstorm paused mid-session 2026-05-09** — captured amendments + workflow shape decisions parked at `docs/superpowers/parking/2026-05-09-qwiki-phase-b-brainstorm-pause.md`. Resume after qwiki-sandbox phase 4 lands Page Forms (path A, sandbox-first) or before (path B, snapshot-only). Captured: `country` + `division_label` schema amendments, modal-review workflow shape, brand-row 27-field checklist (4 tiers), tournament-row checklist sketch, EQL as pilot brand, 5 silent-drop members in pre-fill state to patch. `gap-check.ts` script committed at `apps/qw-oracle/scripts/curate-brands/gap-check.ts` for re-runnable drift detection.
- **Re-walk obligation 2026-05-13** (small followup) — handler edits applied to HEAD checkouts only; 15 ezQuake tags (v3.0 → v3.6.9 + head) and 1 FTE version (build-6698) in the L1 DB still carry pre-fix `asset_loader_sites` rows. **Scheduled** as Task 3.6 sidecar in the asset-type-curate arc plan (`docs/superpowers/plans/2026-05-13-asset-type-curate-skill-arc.md`). Will run alongside Phase 3 fan-out. ~10 min wall-clock; idempotent. Delete this line once Phase 3 ships.
- **Slipgate Browse + Manager UI brainstorm needed 2026-05-12** — surfaced during arc-planner readiness check. Substrate (Manager engine) thoroughly designed; UI surface (Browse view in Managed Mode + per-domain views + multi-engine filter + private/promote/fork gestures + Updater UI + Settings) referenced in spec but never designed. Current Browse is in a regressed state per operator screenshot (mis-labeled extension filter as "FILTER BY DOMAIN"; domain list missing from left sidebar; "0 loaded / available / shipped / 15366 other" implies half-wired manifest-aware classification). One focused brainstorm session (not multi-pass) should cover it — most decisions are downstream of substrate already settled. Folded into Slipgate Managed Mode arc as "Arc Z" placeholder per current Slipgate handover line above. Input: gfx corpus inventory (next item).
- **gfx corpus investigation 2026-05-12 (sidequest, in-flight)** — ciscon shared `https://nicotinelounge.com/qw3-abab/gfx.tar.gz` (1.41GB) as a community-curated QW asset catalog (qw.nu/gfx site dump). Pulled to `/home/paradoks/sandboxes/qw3-abab-gfx/`. Two artifacts: (a) **structural inventory** at `docs/superpowers/parking/2026-05-12-gfx-corpus-inventory.md` (10-category taxonomy + 30 subcategories + 11-field schema), (b) **investigation findings** at `docs/superpowers/parking/2026-05-12-asset-corpus-investigation-findings.md` (Pass 1 path-pattern → Pass 2 DB-category reframe, 95.9% install-coverage manifest, 7 W-11 design refinements, 11 threads worth pulling). Working manifest exists as `pass2-manifest.ndjson` (6.2MB, 11,173 per-(bundle,file) records). **Operator taking findings to a new terminal for deeper investigation** before moving to Browse UI brainstorm. Highest-value threads queued: mine `gfx_comment` (1,449 rows) for ad-hoc install instructions; cross-verify inferred install paths against Layer 1 asset-loader-sites data; spot-check the 32 EMPTY bundles + 799 multi-target_path hash cases. All artifacts self-pointed-to from the investigation findings doc.
- **KTX gameplay_taxonomies handler `_stats.election_type.source_total` is parallel-aggregator-naive** — surfaced during KTX Phase 7 cross-project audit (2026-05-06; D.3.1 + D.6.1 of `docs/superpowers/reviews/2026-05-06-ktx-onboarding-cross-project-audit.md`). `_handler_gameplay_taxonomies.py:385` computes `source_total = len(raw_election_rows)` over pre-dedup data; under `--workers 4`, value reports 20 (4x correct) because `progs.h` is included by every TU and each worker's election rows accumulate. Actual `count` field correctly dedups to 5; ONLY the `source_total` stat differs. Stat-only; functional correctness and emitted rows are unaffected. Breaks Section-1.1 byte-reproducibility under default `--workers 12`. Workaround: run `--workers 1` for serial reproducibility checks. Principled fix mirrors Phase 5.5 Pattern 13 retrofit (typed pseudo-row aggregation). Sized <30 lines; Phase 8 PLAYBOOK candidate joins F23 / F27 / F29 + F25 references.
- **KTX handler class-name shape inconsistent** — surfaced 2026-05-06 (D.2.1 of cross-project audit). 4 handlers use `<Type>KtxHandler` (CvarsKtxHandler / InfoKeysKtxHandler / CommandsKtxHandler / LogTemplatesKtxHandler) and 4 use `Ktx<Type>Handler` (KtxModesHandler / KtxGameplayTaxonomiesHandler / KtxGameplayTablesHandler / KtxMatchEventsHandler). Cosmetic only; no behavioral consequence. Defer to a future cosmetic-cleanup pass (matches 2026-04-28 cross-extractor audit D.5.1 / D.5.2 FTE class-name nits).
- **F29 -- Phase 3 missed 24+ Infobox 4on4team articles (Black_Book / Milton_s_Mutants / FAgomatic / ELAK / etc.) because their wiki articles have empty `categories` arrays; `isClanArticle` filter is category-driven and falls through.** Surfaced 2026-05-05 during Phase 4 T2 review pass. Resolution path is a one-line broadening of `isClanArticle` in `apps/qw-oracle/scripts/load-community/clans/index.ts` to also accept articles whose wikitext contains `{{Infobox 4on4team` regardless of category presence. Re-run loader (idempotent UPSERT). Roughly +24 rows appear. Matters for Phase 5 cross-link backfill: players whose `current_clan: Black Book` need a Black_Book row to JOIN against. Recommended: ship as Phase 3.5 small followup BEFORE Phase 5 executes; sized ~30 minutes inline. Captured as F29 in `docs/superpowers/plans/2026-05-04-qwiki-community-reference/review-findings.md`.
- **Layer 2 hygiene research** -- sidequest dissolved into Phase 3. Design doc at `docs/superpowers/specs/2026-05-02-layer2-hygiene-design.md` (permanent reference). Four port-time hygiene fixes + reply-reference graph absorbed into Phase 3 via `decisions.md` D18. Micro-session over-segmentation (#2) and reply-chain merging (#6) remain open for a future post-Arc-1 pass; no parking prompt exists for them yet.
- **New-doc_only-on-next-ezquake-bump alert** — when ezquake-source HEAD advances to a new tag and we re-walk, run `python3 apps/qw-oracle/scripts/classify-help-json.py --project ezquake --propose` from monorepo root. The seed already covers the 193 existing doc_only entries; the CLI will only print proposals for any newly-arrived doc_only names. Operator manually triages or seeds the new entries. No automatic alerting between sessions — Claude only runs when invoked.
- **Help-JSON classifier: retired-bucket commit-attribution heuristic looseness** — spot-check via subagents found that the blame heuristic in `extractor_lib/_help_json_blame.py` cites the last-touch commit for retired entries, which can be a default-value tweak (`s_alsa_latency`, `in_raw_allbuttons`) or a comment-out (`sv_highchars`) rather than the actual removal commit. Core "no live registration at HEAD" claim is sound; commit citations are 30-40% loose. Improvement: detect `-` registration-line removals specifically rather than any source-side touch. Low pressure — caveats noted in upstream PR digest cover the gap for now.
---

## Synthesis-report numerical-claim provenance gap (2026-04-29)

**Added:** 2026-04-29. **Status:** Open. Process-improvement note for future validation arcs.

Validation reports authored 2026-04-28 (synthesis report at `docs/superpowers/reviews/2026-04-28-per-project-validation-synthesis.md` plus per-project deep-validation reports) contain numerical claims (e.g., "230 trailing_comments", "2989 cvars at HEAD") with no preserved SQL. The numbers did not match live DB at the moment of writing during the 2026-04-29 zero-debt arc, costing ~90 minutes reconciling irreproducible figures.

### Process improvement

Future validation reports must inline the SQL (or other reproducible derivation) for every numerical claim, as a comment block in the report itself. Three formats are acceptable:

1. Inline ` ```sql ... ``` ` block immediately after the claim.
2. Footnote-style citation pointing at a script file checked into the repo with the report.
3. A "queries" appendix section at the bottom of the report listing every claim and its derivation.

Without this, downstream readers can't distinguish "stale snapshot at write-time" from "extractor regression since write-time" from "writer arithmetic error."

### Pressure

Low; aspirational rule for future arcs. No retroactive fix planned for the 2026-04-28 reports — rather, calibrate the next validation pass against this rule.
