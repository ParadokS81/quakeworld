# Handover

Thin docket of pending work. The index below points at lifecycle-grouped sub-sections; arc bodies live in `docs/superpowers/parking/<topic>.md` and shipped retrospectives live in each project's `arc-history.md`. Small-followup bodies stay inline.

This file is referenced from `MEMORY.md` so every new session sees the open-items count at context load. Memory is for durable facts; this file is for todo state.

**How to work an item:** pick from the index below, jump to its destination (parking file or inline section), verify the described state still matches reality (code changes quickly, handover notes can rot), then address it. When done, delete BOTH the index line AND the destination content (parking file → `git rm`; inline section → Edit; arc-history entries are append-only and stay).

---

## Open items

### Active arcs (in-flight; pickup-from-cold dashboard)

Per-arc state for arcs currently in flight. Each arc's plan-dir README has a "Where we are right now" block (Stage / Last action / Next action) -- this section lists the headline so you can pick which arc to work on at a glance. Update when stage/next-action changes.

- **KTX L1 catalog visual review walk (in-flight, end of session 3 2026-05-23)** -- operator-driven cold review of the rendered catalog HTML to surface description-quality findings and template patterns. Findings + drafts + cold-resume block live in `apps/qw-oracle/docs/reviews/2026-05-22-ktx-l1-catalog-findings.md` (read the top `## Resume in a fresh session` section). State end of session 3: **48 L1 drafts locked** (32 session-1 + 13 session-2 + 3 session-3: `k_entityfile` / `callalias` / `qizmo`); 4 open findings; 7 follow-up arcs surfaced (incl. `ktx-l1-rewrite` skill build with park-when-ambiguous spec); KTX shape catalog at **14+ shape variants** (sessions 2-3 added Shapes 1c/1d/6/7-7a-7b/8/9-9a-9b/10 + canonical-card + command-per-value fan-out + tooling-mode prereq + earn-their-keep discipline). Four template-system memory files locked ([[feedback-l1-description-template]] v2 + [[feedback-mod-l1-documentation-architecture]] + [[reference-ktx-entity-categories]] + [[reference-ktx-cvar-command-pairing]]). **`ktx-l1-rewrite` skill BUILT + battle-tested + amended 2026-05-23** (Step 1.5 behavioral unpacking + shape-less verdict baked in after battle-test). **Apply DONE (stale-correction 2026-06-04):** all 633 KTX cvar+command v2 recasts ARE written to L1 `entities.description` (via `apply-ktx-l1-recasts.ts`; origin `synthesized`, folded 2026-06-04 from the interim `recast_v2` tag -- see decisions.md D21 amendment). The per-category "apply pass" small-followups below predate this and are stale on the apply question -- re-verify against the live DB before acting on them.
- **KTX post-review investigation + prod-prep** -- KTX Layer 1 Onboarding SHIPPED 2026-05-07 (retrospective in `apps/qw-oracle/docs/arc-history.md`; review `docs/superpowers/reviews/2026-05-07-ktx-onboarding-arc-review.md`). Post-review Issues #1 (cvar embedding gap) + #2 (match_event descriptions) SHIPPED -- commits `02155560` / `7eeaa410` / `7cd951c4` + migration 012 (description_origin). **Still pending -- prod deploy gated on these:** #3 (gameplay_taxonomies parallel-stat fix, ~30 LOC, Pattern 13 retrofit), #4 (orientation blob update -- operator decision), #5 (idempotency-ktx.sh excludes timestamp/run-metadata cols). Fresh-terminal handoff: `docs/superpowers/parking/2026-05-07-ktx-postreview-investigation-and-prod-prep.md`.
- **qwiki-v1-beta** -- **Phases 1, 2, 3 SHIPPED 2026-05-14** at https://wiki.slipgate.me. Phase 1 (MW core substrate; nginx 1.30-alpine + mediawiki:1.43-fpm + mariadb 11.4 LTS + Citizen v3.16.0). Phase 2 (Page Forms 5.8.1 + Semantic MediaWiki 6.0.1 + F6 LOAD list: ParserFunctions / Cite / CategoryTree / TemplateData). Phase 3 (PluggableAuth v7.5.0 + OpenIDConnect v8.3.0 + Discord OAuth bot-mode role sync via wiki.Quake.World bot + wiki-contributor / wiki-curator MW groups + D5 namespace gates). Findings F1-F9 all RESOLVED + closed (D2 Amendments #1+#2; D3 Amendment 2026-05-14 path-info URL form; D4 Amendment 2026-05-14 ratifying bot-mode role sync). Phase 4 (quality-tag categories + Layer 3 harvest verification) approved + executor prompt drafted; **awaiting once-over by fresh orchestrator terminal before dispatch.** Orchestrator session retired at ~400k+ context 2026-05-14; resume handoff at `docs/superpowers/parking/2026-05-14-qwiki-v1-beta-orchestrator-resume-phase4.md`. Phase 4 executor prompt at `docs/superpowers/parking/2026-05-14-qwiki-v1-beta-phase-4-executor.md`. Phases 5-8 (Modes vertical slice) deferred until substrate ships. Plan: `docs/superpowers/plans/2026-05-12-qwiki-v1-beta/README.md` ("Where we are right now" block has current Stage / Last action / Next action). Two Phase 3 cosmetic followups deferred to a separate polish micro-commit (see Small followups).
- **QWiki community-reference -- Phase 4 LLM-extraction (paused, ready to resume)** -- the modernize-in-place arc is SUPERSEDED by qwiki-v1-beta; what stays LIVE is the community-data Phase 4: Phases 0-3 SHIPPED (players + clans + snapshot + curated/ rename + migration 008), Phase 4 PAUSED at T2 (D4 pivot to LLM-with-checklist extraction for tournaments; deterministic stays for players/clans). Resume: fresh terminal with `superpowers:brainstorming`, deliverable = design doc + redrafted Phase 4 MD, then orchestrator session #3. Sidequest design doc: `docs/superpowers/parking/2026-05-05-qwiki-phase-4-llm-extraction-sidequest.md`. Plan: `docs/superpowers/plans/2026-05-04-qwiki-community-reference/README.md`. F28/F29 tracked in that plan's review-findings.md.
- **MCP realignment to KTX-era data (seeded 2026-05-30, fresh terminal not yet started)** -- the qw-oracle MCP was built before the KTX arc and never updated to serve the KTX gameplay L1 layer (`gameplay_mechanics`: `game_mode` + ~309-317 mode-gated `mode_default` overlays + 5 more kinds). Concrete gaps: `search_mechanics` can't filter the new kinds or by mode (and doesn't return the gate), no resolver reads the `mode_default_init_array` pointer, and the orientation blob over-promises kinds the tools don't expose. Data is correctly shaped -- **fix = tools + docs, not data**. Run the KTX-era data through `apps/qw-oracle/API_CONTRACTS.md`'s new-dataset checklist, decide the verb fork (widen `search_mechanics` vs add a composite `describe_mode` verb), re-truth `VISION.md` + `API_CONTRACTS.md`. Same discipline must gate MVDSV's incoming L1 layer (caveat injected in the MVDSV terminal). Source-verified handoff + first actions: `docs/superpowers/parking/2026-05-30-mcp-realignment-ktx-data-handoff.md`.
- **MVDSV describe-fill (Layer 1) -- cvar/command/cmdline COMPLETE + verdict-traced (rigor catch-up SHIPPED 2026-06-03)** -- all 299 cvar/command/cmdline knobs synthesized + cold-V-pass-traced (commits `9a719327` + `a9f05d55`; the `floodprot`-was-`gamedir` mis-doc corrected). **Remaining MVDSV L1:** (a) info_keys -- **COMPLETE 2026-06-04** (KTX `_handler_info_keys.py` entity-aware-scope + `infokey()` fix shipped: the 4 `world`-reads `fpd`/`matchtag`/`*cheats`/`*version` re-scoped to serverinfo, 5 keys surfaced -- `mapname`/`modelname` serverinfo + `name`/`nomaps`/`*spectator` userinfo, `*VIP` correctly excluded since its only KTX read is commented out; KTX info_key floor re-baselined 7->56; load idempotent + grid-clean); (b) `sv_antilag*` trio -- `sv_antilag` + `sv_antilag_projectiles` synthesized from mainline source in the docs-l1-enrichment arc (2026-06-09; both were empty); `sv_antilag_no_pred` still carries its raw-comment desc; fork-specific antilag refinement -> dusty-* antilag fork arc (entry below); (c) **MCP realignment** -- the new MVDSV L1 is NOT yet queryable through the oracle (see the MCP-realignment entry). **Findings:** 74 logged in the plan's `mvdsv-describe-fill-findings.md` -- #15 + #22 MERGED upstream (mvdsv PR #205/#206) + #63/#65/#62 FILED 2026-06-03 (PR #207 setmaster OOB / #208 SV_LoadAccounts fscanf / #209 rm/rmdir path-escape, all clean off master, built green); ~56 open (secondary candidates #64/#70/#71 next if these land cleanly). Arc record: `workflow-chunk-campaign-brief.md`.
- **docs.quake.world -- L1 reference SHIPPED + preview-deployed 2026-06-11; Phase 5 production DEFERRED** -- the per-codebase L1 reference site (`apps/docs-web`, VitePress+Tailwind v4/daisyUI). Built + cold-verified through Phase 4 + the F14 pre-deploy pass (cvar->cvar auto-links, global MiniSearch entity search, daisyUI trim, density polish; findings F1-F23 in the plan's `review-findings.md`, all resolved). **Preview-deployed** to `https://quakeworld-docs.pages.dev/` on the operator's PERSONAL Cloudflare (a throwaway link to show vikpe for greenlight; procedure in `apps/docs-web/DEPLOYMENT.md`). Design spec at `docs/superpowers/specs/2026-06-09-docs-quake-world-design.md` (federation split docs=reference / wiki=narrative; 6 codebases, FTE deferred; per-type filterable lists + category-as-filter; inline entity cards with player-facing type words toggle/number/choice/text; extend build-snapshot as the 3rd consumer; VitePress + daisyUI/CF Pages; 11 locked decisions + 5-phase suggestion + verified-data appendix). **Phase 5 production** (`docs.quake.world` + vikpe DNS, on vikpe's CF) DEFERRED until vikpe greenlights the domain -- resume handoff `docs/superpowers/parking/2026-06-11-docs-quake-world-orchestrator-resume-phase5.md` (pre-written `phase-5-drafter-prompt.md` exists; `phase-5-deploy.md` not drafted). **Next:** (1) show vikpe the preview link; (2) a fresh-session FRONT-PAGE / DESIGN brainstorm -- the current landing is the bare CodebaseGrid+hero, needs a real front page + a design pass (`frontend-design` skill + claude.ai). Front-page content candidate (2026-06-11, game-content brainstorm carry-forward): an oracle **coverage map** -- "what does the oracle know": every dataset bucket (knobs / maps / gameplay / L2 corpus / L3 notes) x per-kind counts x which have L3 layers. Receipt for why it matters: the current landing shows exactly the slice the operator remembers (knobs) and omits what he forgot existed (the `gameplay_*` tables) -- the 2026-06-11 catalog-arc brainstorm initially scoped an extraction arc for data that shipped in April. **Doc-hygiene followup:** docs-web has no `apps/docs-web/CLAUDE.md` and is absent from the root CLAUDE.md `## Subsystem scopes` table + OVERVIEW -- add those (rides with the design pass). Spine: `docs/superpowers/parking/2026-06-07-quake-world-docs-federation-roadmap.md`. L1-enrichment precursor SHIPPED 2026-06-09 (6 codebases docs-ready: MVDSV/QTV/QWFWD categorized 14/7/5; QWCL 100% described+categorized via ezQuake borrow; apply scripts in `docs/superpowers/plans/2026-06-09-docs-l1-enrichment/`). (Optional: re-embed 382 new descriptions via `bun run embed:entities` for MCP semantic-search parity -- FTS already works; not on the docs-site path.)
- **game-content-catalog (EXECUTING as of 2026-06-12 -- all 5 phase MDs approved; M4 wait-for-notes gate LIFTED by operator same day (D16 amendment); drive via arc-orchestrator + `docs/superpowers/parking/2026-06-12-game-content-catalog-orchestrator-handoff.md`)** -- COMPLETION arc over the existing `gameplay_*` L1 layer: the genesis premise ("never extracted") was stale -- the id1 baseline (37 entity defs + 41 mechanics, per-value cited) shipped 2026-04-27 and that arc queued "KTX overrides" as arc 2; this arc IS arc 2 + monsters + audit + join keys. Spec: `docs/superpowers/specs/2026-06-11-game-content-catalog-completion.md` (D1-D7 + M1-M5). Plan: `docs/superpowers/plans/2026-06-11-game-content-catalog/` (22 decisions incl. the D22 `{"cvar":...}` gate form; findings F1-F10 -- incl. the hardcoded 37/41 load STOP-gate that would have bricked every load (F2), the ktx dual-writer keyspace hazard (F3), and the describe_mode override-surfacing gap (F9, carried forward to the MCP-realignment entry above); 5 phases: 0 prereqs+loader / 1 audit / 2 monsters / 3 KTX overlay / 4 join keys+docs+surfacing). Track-A notes remain unblocked NOW against existing rows; inline cited backfills to `id1-gameplay.yaml` encouraged; Phase 1's audit absorbs them (execution-time recounts are designed in).

When an arc finishes its planning stage and moves to implementation, the entry stays here with stage updated to "implementing." When an arc ships, delete the entry; the retrospective lives in the project's `arc-history.md`.

### Small followups
- **mvdsv L1 extraction gap -- server `ucmds[]` table not walked (`dl` / `dlist` / `demolist`)** -- the mvdsv extractor walks Cvar/Cmd registrations but NOT the server-side user-command dispatch table `ucmds[]` (`mvdsv/src/sv_user.c:3325-3340`), so the demo-download commands `dl` (`Cmd_DemoDownload_f`) and `dlist` / `demolist` (`SV_DemoList_f`) are source-present but absent from Layer 1 (verified empty in `entities` 2026-06-11). Surfaced authoring demand-driven-l3 note #3 (`match-recording-playback.md`): the note names them in prose (source-verified, reached from a client as `cmd dl` / `cmd dlist`) and anchors its edges on the resolvable `ezquake:command:cmd` + `ktx:command:dlist` instead. Fix = extend the mvdsv extractor to walk the `ucmds[]` table (a third registration mechanism beyond `Cvar_Register` / `Cmd_AddCommand`). Low priority -- the note handled it honestly; the gap matters when a future note or the docs site needs `cmd dl` as a resolvable entity. [small followup]
- **ktx-l1-rewrite Frogbot category (follow-up) -- apply pass for 15 `k_fb_*` cvars missed by chunked-mode dispatch** -- post-arc 16-entity gap closure batch for the Frogbot category. The original `2026-05-26-frogbot` batch (78 cards) used semantic-intuition entity-list assembly (catalog HTML walk grouped by what `botcmd` dispatches and what's the primary command in a Shape 1c pair) and missed the 15 underlying `k_fb_*` cvars themselves -- they were captured in DB `category_inferred = 'Frogbot'` but not in the entity-list for that batch. The pre-flight #5 category-enumeration audit gate (amendment 2026-05-27) surfaced the deliberate 78-entity diff at dispatch time; explicit accept proceeded. This follow-up batch ships the 15 cvar cards (13 drafted_clean + 2 drafted_with_flag + 0 parked) at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-frogbot-followup.md`. Apply-pass-author: 2 FLAG bullets -- (a) `k_fb_debug` registered-but-inert at anchor; awaiting community/operator confirmation on whether the cvar has any out-of-tree consumer or whether the commented-out set-site is expected to be re-enabled; (b) `k_fb_easy_skill_mode` existing description names a wrong downstream-cvar family (`k_fb_skill_*` / `k_fb_aim_*` / `k_fb_movement_*` -- none exist; the real family is `k_fbskill_*`). Cross-card finding F1 surfaces 5 ToT-mode-scoped cvars (`k_fb_break_on_death`, `k_fb_health`, `k_fb_quad_multiplier`, `k_fb_item_pickup_bonus`, `k_fb_weapon`) currently mislabeled `category_inferred = 'Frogbot'` in DB -- tracked separately as categorization-accuracy follow-up #15 (this batch does NOT touch DB categorization, only L1 description prose). F2 highlights `k_fb_quad_multiplier`'s universal-attacker semantics (no `isBot` gate -- affects humans too in dmm4+ToT); preserved in headliner + Effect. [small followup]
- **ktx-l1-rewrite Race category (follow-up) -- apply pass for 1 missing pair-half** -- post-arc 16-entity gap closure batch for the Race category. The original `2026-05-27-race` batch (45 cards) classified `k_race_simultaneous` + `race_simultaneous` as Shape 1 + Shape 1c but folded the cvar half into the command card's body instead of emitting a separate `## k_race_simultaneous` header. This follow-up batch ships the missing cvar card (1 drafted_with_flag, 0 parked) at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-race-followup.md`. Bidirectional See-also is already in place (the original command card at line 564 of `ktx-l1-rewrite-drafts-2026-05-27-race.md` already cross-links back to `k_race_simultaneous`), so no amendment to the original race batch is required. Apply-pass-author: 2 FLAG bullets in Notes -- (a) Default 0 at registration vs race_settings[] bundle overriding to 1 at race-mode activation (race.c:308); (b) Set-by adds the paired toggle command (omitted in current L1). [small followup]
- **ktx-l1-rewrite Server config & network -- apply pass + 2 deferred followups** -- (a) **Apply pass** -- operator audits the drafts file then applies clean drafts to `entities.description`; 20 `drafted_with_flag` entries need operator review of the surfaced factual change before applying. Drafts at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-23.md`. (c) **Cross-card consistency on FPS-kick threshold** -- `k_noframechecks` says "after 3 accumulated FPS warnings" (imprecise); `maxfps` says "the fourth warning in a session triggers a forced disconnect" (source-true per `fIllegalFPSWarnings > 3` at `client.c:3868-3870`). Apply-pass-author should align both cards' wording to the maxfps phrasing. (d) **`teamoverlay` vote command pending** -- the Shape 7b pair partner of `k_teamoverlay` (drafted 2026-05-24 as Shape 7b state-cvar) is NOT in the Server config & network category catalog -- should be drafted in a later batch (likely Spectator chat & visibility or a future Voting category). (Items (b) shape catalog correction + (e) spec amendment for Step 1.5 + shape-less SHIPPED 2026-05-25.) [small followup]
- **ktx-l1-rewrite -- all 13 per-category L1 batches SHIPPED (2026-05-23 -> 05-27), collapsed from this docket 2026-06-03** -- pure ship logs (Spectator chat / Voting / Scoring & stats / Mode selection / Mode-scoped knobs / Frogbot / Admin & permissions / Demo & spectator / Match flow / Gameplay rules / Internal state / Race / Player communication). Per-batch detail: `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-*.md` + git; arc recorded in arc-history (`KTX L1 format-unify` 2026-05-21, tag `arc-ktx-format-unify-shipped`). Open residuals stay tracked individually: d4-extractor audit / See-also fix / parks drafts / F1 corrections below, Server config apply pass above.
- **KTX d4-extractor wipe-regression -- audit complete; 5 entities affected (2026-05-27)** -- The 2026-05-26 `d4-extractor-fix` selectively wiped `description` + `description_origin` + `description_anchor_version` on 5 KTX cvars while preserving `description_verdict='affirmed'` + `description_confidence='high'` + full `description_reasoning` + `description_provenance`. DB sweep `WHERE description_verdict IS NOT NULL AND description IS NULL` returned exactly 5 entities: **k_defmap / k_mode / k_spm_custom_model / k_sready / k_timetop** (all `source_state='source_backed'`, all `affirmed-by-evaluation` from the 2026-05-22 describe-fill pass; the affirmed metadata is the d4-extractor's signal "re-affirm at new anchor", not a regression). (a) **k_sready RESTORED + v2 RECAST 2026-05-27**: two-step fix. (i) 2026-05-22 affirmed description preserved in `apps/qw-oracle/docs/reviews/2026-05-22-ktx-l1-catalog.html:8729`; verified against `src/client.c:4266` + `src/world.c:881` at HEAD; persisted to `entities.description`. (ii) Single-entity `ktx-l1-rewrite` dispatch at Sonnet 4.6 high upgraded the description to v2 universal shape (Headliner / Effect / Prerequisites / Permission / Default / Example / See-also); Shape 3 confirmed; persisted via second UPDATE. Drafts file at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-k_sready-gapfill.md`. Recast caught a source-precision improvement: source sets `EF_BLUE` (blue glow), prior text said just "glow"; v2 specifies blue. Also added matchless+race prerequisite blocks (source `!k_matchLess && !isRACE()` previously unmentioned). Parent terminal corrected sub-agent draft from `k_matchLess` (C-side cached global) to `k_matchless` (the cvar entity) in See-also + Prerequisites. Final state: `description_verdict='affirmed'`, `description_origin='source_inline'`, `description_anchor_version='1.47-2-g67253dc'`, 504-char description. Match-flow recast batch had correctly aborted k_sready to needs-synthesis (F12) but the 2026-05-27 cold-synth follow-up's hard-bounded scope ("15 k_fb_*") missed it -- now fully closed. (b) **k_defmap / k_mode / k_spm_custom_model / k_timetop: deferred to apply pass** -- all 4 have v2 recast drafts in their respective batches (k_defmap + k_spm_custom_model in 2026-05-23 Server config; k_mode in 2026-05-26 Mode selection; k_timetop in 2026-05-26 Mode-scoped knobs). The recasts were authored before the 2026-05-26 wipe and capture richer v2 universal-shape text than the prior affirmed descriptions; apply pass writes them directly to `entities.description`. No additional cold-synth dispatch needed. (c) **MVDSV cold-synth scope lesson**: dispatch prompts should specify SWEEP criterion (`WHERE description IS NULL OR length(description) = 0`), not a hand-picked entity list. The 2026-05-27 follow-up's "15 k_fb_*" framing missed k_sready because it wasn't an obvious k_fb_* member; a sweep query would have surfaced it. The k_fb_* categorization-correction (origin `manual|d4-extractor-fix-2026-05-26` bypassed formal b6-categorize fan-out; partly ToT-scoped per cold-synth findings) stays tracked in task #15. Anchor 1.47-2-g67253dc. [small followup]
- **ktx-l1-rewrite See-also asymmetry: `break` missing back-ref to `ready` (2026-05-27)** -- spot-check across 10 sampled entities + 6 bidirectional pair checks found 1 asymmetry: `ready` See-also references `break` ("clears ready state"), but `break`'s See-also focuses on the voting-system flow (`forcebreak`/`k_vp_break`/`k_vp_map`/`next_map`) and omits the player-command peer set (`ready`/`slowready`/`toggleready`). `break` has 4 See-also entries; the per-card skill caps at 4-5, so adding `ready` as a 5th fits. **Apply-pass action**: when applying the Match flow batch (`ktx-l1-rewrite-drafts-2026-05-27-match-flow.md`), hand-edit `break`'s See-also to add `ready (paired peer -- sets ready state)`. Other 5 paired relationships sampled (`freeze`↔`k_freeze`, `qlag`/`qpoint`/`qenemy` triangle, `race_simultaneous`↔`k_race_simultaneous`, `powerups`↔`k_pow`, `discharge`↔`k_dis`) all verified symmetric. [small followup]
- **ktx-l1-rewrite parks pile DRAINED -- 5 hand-drafts at v2 universal shape (2026-05-27)** -- `callalias` / `y` / `n` / `roundsdown` / `roundsup` (the 5 entities the recast skill couldn't classify under any Layer B shape) now have v2 Layer A drafts at `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-27-parks-handdraft.md`. Hand-authored directly (not via `ktx-l1-rewrite` -- it would re-park on the same shape-catalog miss). Source citations verified at HEAD anchor `1.47-2-g67253dc`. **Verdict mix**: 3 drafted_clean (callalias, y, n) + 2 drafted_with_flag (roundsdown + roundsup -- existing descriptions wrongly framed both as "admin command" but source registration is `CF_PLAYER` for both; v2 Permission lines corrected to "any player"). **Apply-pass actions surfaced**: (i) `kick`'s drafted See-also (Admin & permissions batch) currently lists only `mkick + force_spec`; add `y` + `n` per F2 cross-card finding to close the bidirectional triangle (kick ↔ y ↔ n). The 5 parks are now ready to flow through the apply pass alongside the 14+2 batch drafts. **Arc state**: all 633 KTX cvar+command entities have v2 recast drafts (627 in 16 batch drafts files + 1 k_sready gapfill + 5 parks hand-drafts). [small followup]
- **ktx-l1-rewrite F1 audit + Permission-line discipline corrections (2026-05-26 follow-up to Mode selection batch F1)** -- F1 from the Mode selection batch surfaced a systemic "Admin command" mislabeling pattern across 5+ entities; this followup ran a rear-view audit of the 4 prior batches and forward-fixed the catalog prescription that propagated the error. **Source check (`include/g_local.h:647-658`):** `CF_PLAYER` (bit 0) = "command valid for players" (any player); `CF_SPC_ADMIN` (bit 3) = "client is spectator, requires admin rights." `CF_PLAYER | CF_SPC_ADMIN` literally means "any player slot, OR admin spectator" -- NOT admin-only. `CF_PLR_ADMIN` (bit 2, "client is player, requires admin rights") is a DIFFERENT flag and is rare; `CF_BOTH_ADMIN = CF_PLR_ADMIN | CF_SPC_ADMIN` is the truly admin-only combination. **Rear-view audit result:** of 12 `CF_PLAYER | CF_SPC_ADMIN` entities across prior batches, 11 were correctly framed (antilag / infospec / nospecs / qenemy / silence (pre-match/mid-match split) / swapall / teamoverlay / downplayers / upplayers / downspecs / upspecs); 1 silent miss + 1 mixed-discipline case identified. **Apply-pass corrections needed:** (a) **`dmgfrags`** in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-26-scoring-stats.md` -- Permission line says `admin only` and Notes claim "CF_PLAYER | CF_SPC_ADMIN maps to admin only" -- both wrong per source. Correct Permission line to `any player or admin spectator`; reframe Headliner to drop "Admin command" prefix. (b) **`silence`** in `apps/qw-oracle/docs/reviews/ktx-l1-rewrite-drafts-2026-05-25.md` -- Headliner says "Admin command that toggles spectator talk (k_spectalk)..." but Permission line correctly splits "pre-match: any player or admin spectator. Mid-match: admin only (non-admins silently ignored)." Reframe Headliner to drop "Admin command" prefix; Permission line is correct (canonical example of two-phase runtime gating). **Forward-fix (catalog level, user-global skill -- not in repo):** `~/.claude/skills/ktx-l1-rewrite/references/shape-catalog.md` Shape 1 command-side prose prescription updated 2026-05-26 to qualify "Admin command that toggles..." by CF flag (only for CF_BOTH_ADMIN; default Headliner is "Toggles the X rule (cvar)"). `~/.claude/skills/ktx-l1-rewrite/references/universal-shape-v2.md` Permission section expanded with explicit CF-flag-to-wording table + critical-mistake note. Future Shape 1 batches inherit the corrected prescription. [small followup]
- **L3 concept note: QW team-chat visibility across the stack** -- surfaced 2026-05-21 Session #9 during the KTX describe-fill screening pass when operator + cross-codebase verification produced a non-trivial synthesis (KTX `k_spectalk` + `k_sayteam_to_spec` sets engine cvars; MVDSV `sv_user.c:1890-1900` spec-filter gates broadcast based on the `$\` byte marker / chr(13); ezQuake `TP_ShortNick` and `cl_fakename` interceptor inject the marker; fteqtv relays MVD `dem_multiple` packets transitively without extra filter; net practical = teambinds gated by cvar / plain say_team always private / QTV inherits the gate per POV). L1 descriptions for the affected cvars (k_spectalk + k_sayteam_to_spec) point here via `See also:`. Path: `apps/qw-oracle/curated/concept-notes/qw-team-chat-visibility.md`. Sized ~30-45 min in a fresh terminal with the audit-trail in this conversation's git history (commits `2ac11298` + `a8ce9345`). [small followup]
- **arc-reviewer post-arc walkthrough for the 96-row KTX describe-fill cohort** -- per the arc-reviewer skill, must run in a FRESH terminal that did NOT execute any phase. Reads: spec `docs/superpowers/specs/2026-05-15-ktx-mvdsv-l1-describe-fill-design.md` + plan `docs/superpowers/plans/2026-05-16-ktx-mvdsv-l1-describe-fill/` (incl. decisions.md D20 amendment + all 10 ledger files + the screening-pass ledger). Produces DELIVERED / DELIVERED-DIFFERENT / DEFERRED / MISSING walkthrough + YELLOW open issues + Arc N+1 prep recommendations (notably: format-unify arc + L3 concept note + skill folds queued as N+1 work). [small followup]
- **describe-fill-synthesis skill folds (3 items)** -- (1) Add `ENGINE-BOUNDARY-HEDGED-OK` as a valid TRACED-CLEAN-compatible verdict subclass in `~/.claude/skills/describe-fill-synthesis/references/enforce-trace-discipline.md` (per Session #8 B5 surface). (2) Add "commented-out runtime check masquerading as live condition" as a 4th sub-pattern in the ELABORATION DISCIPLINE list (alongside flag-NAME inversions / callee-branch dead code / command-name pattern inversions). (3) Add "no engine/code jargon in L1 description" as a QA rule (file:line refs / cf_flags / think-handler / stuffcmd / fpd bit references belong in `description_reasoning`, never in `description`). Calibration case: the Session #9 k_dmm4_gren_mode draft leaked "explosion-on-think handler" which the operator caught on review. Sized ~20 min skill edits in user-global skills dir. [small followup]
- **quad auto-record suppression-stuck after manual `/record stop`** -- surfaced 2026-05-20 during the 4on4 verification round after the Mumble silent-recording fix landed. `auto-record.ts:stopForTeam` adds the channel to `suppressedChannels` and only starts the grace timer if the channel is already empty; if the operator who triggered the stop stays in the channel, suppression sticks indefinitely (channel never empties -> `onUserRemove` never starts the grace timer -> suppression never clears). Symptom: subsequent user joins log `AutoRecord: suppressed for #<channel> -- not starting` even hours later. Operator workaround today was `/record start` (which explicitly clears suppression via the manual path). Fix: bound suppression by time -- add a separate timeout when `suppressedChannels.add()` is called that fires after `GRACE_PERIOD_MS` regardless of channel-empty state. ~5 lines in `apps/quad/src/modules/mumble/auto-record.ts`. Latent bug predating the Mumble-silence fix (only surfaced now because the silence fix made manual-stop testing actually meaningful). [small followup]
- **ezQuake `help_variables.json` mis-lists commands as variables (upstream PR)** -- verified 2026-05-16 during the entity-name source-case-fidelity arc: `userdir`, `floodprotmsg`, `scr_weaponstats_{x,y,order,scale,frame_color}` are registered in ezquake-source ONLY via `Cmd_AddCommand` (host.c / cl_cmd.c) yet also appear in `help_variables.json` (in addition to the correct `help_commands.json`). Our L1 correctly keeps them as `command` source_backed; the phantom `doc_only` cvar twins are removed by `pruneCrossTypeOrphans` (this is exactly what dropped ezquake cvar 2997->2992 on the first clean reload -- expected, floor re-baselined at `quality-grid.ts:1583`). Cross-type variant of the help-only-ghost family the `docs/superpowers/plans/2026-05-15-help-json-doc-consistency-gate.md` plan covers -- feed into that gate + the `docs/upstream-prs/` digest pipeline. Route nano/slime; operator signs DCO. ~upstream PR. [small followup]
- **MCP smoke scripts pre-broken (env + stale counts)** -- surfaced 2026-05-16. `serve/mcp/scripts/verify-rewrite.ts` spawns the server via `StdioClientTransport` without propagating `DATABASE_URL` to the child -> server throws at `shared/db.ts:10` -> `MCP error -32000 Connection closed` (fix: pass `env` through, or have the script set it). `verify-gameplay.ts` asserts stale hardcoded counts (entity 37 vs live 50; mechanic 41 vs live 487) on gameplay tables untouched by recent arcs. Neither is caused by the case-fidelity arc -- verified via an in-process `lookup_entity` test (9/9 green incl. case-insensitive resolution + `$B`/`$b` carve-out + ILIKE wildcard-bug fix). ~15 min each. [small followup]
- **Prod MCP image rebuild (entity-name case-fidelity residual)** -- per the 2026-05-16 arc-history retrospective (commit `8093e42f`; executor's claim, not independently re-verified here): prod DB is in lockstep and needs NO data refresh (old image still correct because `ILIKE` was always case-insensitive), but the prod MCP image still runs pre-`8093e42f` code -- it does not yet carry the `name_fold`-based `lookup_entity` match or the latent `ILIKE` `_`/`%` wildcard-bug fix. Low urgency (behaviour is correct today via case-insensitive `ILIKE`; the rebuild is a code-freshness + wildcard-correctness catch-up). Trigger: next qw-oracle prod deploy, or sooner if a `_`/`%`-bearing entity name surfaces a wrong `lookup_entity` result. ~standard image rebuild per `DEPLOYMENT.md`. [small followup]
- **MCP Tailscale direct-route for batch jobs (avoid CF rate limit)** -- surfaced 2026-05-23 during ktx-l1-rewrite sub-batch 2 pre-fetch: 15 parallel `lookup_entity` calls hit Cloudflare 1015 rate-limit at ~10 requests (`oracle.slipgate.me` is fronted by CF Tunnel; CF's per-config rate limit kicks in on parallel bursts). Worked around by reading the 5 missing source_refs from the catalog HTML metadata-strip. The MCP server is reachable directly over Tailscale (Unraid `100.114.81.91`) which bypasses CF entirely. Action: teach the MCP client config (or the dispatcher pattern used by ktx-l1-rewrite + describe-fill-synthesis fan-out arcs) to prefer the Tailscale direct route when on-network. Per-arc fan-outs of 15+ entities are routine; the CF hop is unnecessary friction for them. Need to expose / document the MCP port on Unraid for direct access. ~30 min to add the Tailscale endpoint to `.mcp.json` or equivalent config + smoke test. [small followup]
- [Synthesis-report numerical-claim provenance gap](#synthesis-report-numerical-claim-provenance-gap-2026-04-29) — discipline note for future validation arcs; no retroactive fix.
- **Extend F-D4a owned-row guard to remaining derivers (synthesis-track support)** — original framing (2026-05-07) was wrong: per the derive-entity-description.ts header verified 2026-05-23, all 13 derivers DO write `description_origin` on every UPDATE. What was actually open was the **F-D4a guard** (`description_origin IS DISTINCT FROM 'synthesized'` / `'shipped_doc'`) which protects operator-authored synthesis from re-derive clobbering. Status 2026-05-23: 4 derivers guarded (deriveCvar / deriveCommand / deriveCmdlineParam from enforce-L1 arc 2026-05-17; deriveMacro added today during macros help-JSON synthesis arc, commit `51ec7220`). Remaining 9 derivers (deriveHudElement / deriveAssetCategory / deriveTokenPrimitive / deriveFlagBit / deriveCvarAlias / deriveProtocolMessage / deriveInfoKey / deriveLogTemplate / deriveQcBuiltin / deriveMatchEvent) are unguarded by design — they own no arc user-doc track yet. Add the guard on a deriver only when a synthesis arc claims that entity type. ~3 lines per deriver when needed. [small followup]
- **F-D4a reconciliation for ezquake macros when PR #1127 merges** -- when QW-Group/ezquake-source#1127 lands upstream and the next ezquake walk pulls the populated help-JSON, run the one-line SQL to release the F-D4a guard on the 36 affected rows so deriveMacro can overwrite the synthesized prose with help_json-sourced values: `UPDATE entities SET description_origin = NULL WHERE project='ezquake' AND type='macro' AND description_origin='synthesized' AND id IN (SELECT entity_id FROM macro_versions WHERE version='head' AND NULLIF(TRIM(help_desc),'') IS NOT NULL);`. Snippet also documented inline in `apps/qw-oracle/scripts/load-knowledge/derive-entity-description.ts:deriveMacro`. Trigger: slime merges PR #1127. ~30 seconds. [small followup]
- **F-D4a reconciliation for ezquake commands when PR #1128 merges** -- same shape as the macros reconciliation above, but scoped to commands. When QW-Group/ezquake-source#1128 lands and the next ezquake walk pulls the populated help-JSON, run: `UPDATE entities SET description_origin = NULL WHERE project='ezquake' AND type='command' AND description_origin='synthesized' AND id IN (SELECT entity_id FROM command_versions WHERE version='head' AND NULLIF(TRIM(help_desc),'') IS NOT NULL);`. CAVEAT: the 10 dev_* commands intentionally diverge -- L1 has the rich prose, upstream ships short markers; the reconciliation snippet should NOT touch those 10 (their `help_desc` from upstream would be the short marker, not the rich prose -- releasing the guard would downgrade L1). Add an exclusion for `name LIKE 'dev_%'` in that subquery before running. Snippet also documented inline in the inserter script header. Trigger: a maintainer merges PR #1128. ~30 seconds. [small followup]
- **F-D4a reconciliation for ezquake variables when PR #1130 merges** -- **TRIGGER MET 2026-05-26** (PR #1130 merged today at commit `e4a2c20a`; observed incidentally during the depth-N extractor regression-check ezQuake re-extraction -- AST diff surfaced ~50 new `desc` fields populated on cvars like `cl_mvinset_offset_x/y`; those re-extraction changes were reverted to keep the depth-N commit focused, so a clean `extract-tag --project ezquake --version head --force` is the actionable step). Same shape as the macros + commands reconciliations above, scoped to cvars. After the clean walk, run: `UPDATE entities SET description_origin = NULL WHERE project='ezquake' AND type='cvar' AND description_origin='synthesized' AND id IN (SELECT entity_id FROM cvar_versions WHERE version='head' AND NULLIF(TRIM(help_desc),'') IS NOT NULL);`. No divergence exclusion needed -- all 58 entries in L1 match the PR-payload prose exactly. Snippet also documented inline in `apps/qw-oracle/scripts/insert-helpjson-synthesis-variables.py` header. ~5 min (extract + SQL). [small followup]
- **F-D4a reconciliation for ezquake cmdline_params when PR #1131 merges** -- same shape, scoped to cmdline_params. When QW-Group/ezquake-source#1131 lands and the next ezquake walk pulls the populated help-JSON, run: `UPDATE entities SET description_origin = NULL WHERE project='ezquake' AND type='cmdline_param' AND description_origin='synthesized' AND id IN (SELECT entity_id FROM cmdline_param_versions WHERE version='head' AND NULLIF(TRIM(help_desc),'') IS NOT NULL);`. CAVEAT: 2 entries (`-ruleset`, `-nostdout`) ship CONSERVATIVE descriptions in PR #1131 pending the upstream source-bug fixes -- but the L1 synthesis carries the SAME conservative descriptions (no rich-prose divergence like commands' dev_*), so the reconciliation can release all 49 guards safely; the source-bug-fix PRs come after with their own description updates (tracked separately below). Snippet also documented inline in `apps/qw-oracle/scripts/insert-helpjson-synthesis-cmdline_params.py` header. Trigger: a maintainer merges PR #1131. ~30 seconds. [small followup]
- **rulesets.c cmdline-dispatch bug-fix PR #1132 awaiting review + `-ruleset` description follow-up** -- surfaced during PR #1131 cmdline-pass verifier walk; PR #1132 (https://github.com/QW-Group/ezquake-source/pull/1132) opened 2026-05-26 against QW-Group/ezquake-source. Patch (5 ins / 2 del): added `!` to `src/rulesets.c:629/632` (qcon + default branches) + 4-line `smackdrive` dispatch branch inserted in OnChange-matching order (qcon -> smackdrive -> default). Investigation confirmed unambiguous bug (no Chesterton's fence): "default" was the original sin (dimman 2015-10-19 `156b70d3` -- same commit that wrote the 3 correct `!strcasecmp` branches forgot the `!` on the 4th); "qcon" inherited the broken pattern in 2016 (`be6109f3`); `rs_smackdrive` was already integrated at 8+ runtime sites including the cvar OnChange dispatch at `rulesets.c:863`, only the cmdline dispatch was missing. **Trigger: PR #1132 merges.** Then file small follow-up to `help_cmdline_params.json` updating `-ruleset` description to list all 6 working values (`smackdown`, `thunderdome`, `mtfl`, `qcon`, `smackdrive`, `default`); PR #1131 ships conservative 3-value description pending this fix. Investigation log: `docs/superpowers/parking/2026-05-26-handoff-ezquake-bugfix-prs-rulesets-sys_posix-investigate.md`. [small followup]
- **sys_posix.c Sys_Printf bug-fix PR #1133 awaiting review + `-nostdout` description follow-up** -- surfaced during PR #1131 cmdline-pass verifier walk; PR #1133 (https://github.com/QW-Group/ezquake-source/pull/1133) opened 2026-05-26 against QW-Group/ezquake-source. **Scope narrower than originally anticipated**: investigation confirmed the DEBUG-branch early return at `src/sys_posix.c:77` IS unintentional (contradicts the 2013-12-29 commit's own message "Sys_Printf is DEBUG only (stdout)", `9972c26c`) but the non-DEBUG `#else return;` at line 92 IS intentional (matches that same commit's intent + mirrors `sys_win.c:503-507`'s `#ifndef _DEBUG return;` pattern). Patch (0 ins / 2 del): only the DEBUG-branch `return;` + its trailing blank line. Net effect: POSIX DEBUG client builds now honor `sys_nostdout`/`-nostdout`; non-DEBUG / release client builds remain intentionally silent; dedicated server (`sv_sys_unix.c`) unaffected -- it honors `sys_nostdout` independently and already worked. **Trigger: PR #1133 merges.** Then file small follow-up to `help_cmdline_params.json` sharpening `-nostdout` description to reflect the per-build behavior (DEBUG client + dedicated server honor it; release client is silent regardless because Sys_Printf is silenced by design on the client). PR #1131 ships description-of-intent only pending this. Investigation log: `docs/superpowers/parking/2026-05-26-handoff-ezquake-bugfix-prs-rulesets-sys_posix-investigate.md`. [small followup]
- **mp3 dead-enum cleanup PR (`macro_ids.h`)** -- surfaced 2026-05-23 during macros help-JSON audit. `macro_mp3info` + `macro_mp3_volume` enum entries survived the 2019 deletion of `mp3_player.c` (`ae8b552f`); no handler is registered, no help-JSON entry exists. Same shape as the PR #1126 runtime-dead-entities cleanup. ~2-line deletion in `src/macro_ids.h` against ezquake-source. Could fold into next code-cleanup PR rather than ship standalone. [small followup]
- **Phase 8 eval q5/q8 L3 authoring leads** — Phase 8 eval surfaced two vague-NL Linux/Windows symptom queries that have no L3 concept-note coverage: q5 "screen brightness flickering on Windows after closing ezquake" (HDR + Windows display tech) and q8 "Linux stutters with sys_highpriority and CPU affinity set" (Linux + NVIDIA performance baseline). Both are recipe-shape candidates per the L3 multi-domain framework parking doc. Authored alongside Arc 3 unshelving; bucket-tagged per `docs/superpowers/parking/2026-05-03-layer3-multidomain-bucket-framework.md` (system + engine-config for q5, system + hardware + engine-config for q8). Linked from the broader 6-candidate authoring queue in that parking doc.
- **Per-project conftest.py for extractor pytest** -- extractor-discipline-catchup arc Phase 3 surfaced full-suite pytest collection errors due to sys.path pollution across multiple `tests/` package namespaces in `apps/qw-oracle/scripts/extractors/`. Three pre-existing collection errors (`fte/tests/test_fte_asset_paths.py` + `qw/tests/test_{bsp_parser,pak_extract}.py`); 15 affected tests pass in isolation. F1 in `docs/superpowers/plans/2026-05-08-extractor-discipline-catchup/review-findings.md` carries detailed evidence + proposed fix (per-project `conftest.py` with explicit sys.path discipline OR rename per-project test packages to project-prefixed). Sized ~1-2 hours. Deferral safe through arc close (P5/P6/P7 don't add tests); fix needed before next test-authoring effort touches the tree.
- **Cron-based upstream-drift detector for Layer 1 codebases** — surfaced during KTX Onboarding Pass 2 (`docs/superpowers/specs/2026-05-04-oracle-prod-update-lifecycle.md`). Today's trigger model is operator-poll for all codebases (ezquake / KTX / MVDSV / FTE) — low cadence (~1-5 stable releases per codebase per year) makes operator-poll sufficient. Revisit if (a) cadence increases substantially, (b) non-operator contributors join the loader workflow, or (c) the showcase-site admin panel arc lands and an automated drift signal becomes useful UI. Mechanism would be a script checking `git ls-remote` against last-loaded SHA per codebase, notifying via the admin panel or Discord. No urgency.
- **Dev DB backup hygiene** — surfaced during KTX Onboarding Pass 2 rollback discussion. The dev Postgres container (`qw-oracle-postgres-dev`) has no automated backup; data lives in a Docker volume on WSL on Windows. If WSL gets wiped, all loaded data (every codebase, every tag) must be re-extracted via libclang — slow but reproducible (Voyage cost negligible per arc-history; operator time the real cost). Mitigation candidates: manual weekly `pg_dump` to the Windows-side filesystem, or a wsl-cron that automates it. Low pressure — the prod Synology backup + GitHub-rebuild path (Tier 3 DR in the prod-update-lifecycle spec) already cover the catastrophic case at the cost of operator time.
- **D4 depth-N Pattern 6 lift revisit** — surfaced during KTX onboarding Phase 5 drafting (2026-05-05). Phase 1's lift walks depth-1 `#include` only; KTX `H_ROTTEN`/`H_MEGA` macros live at depth-2 (`commands.c` -> `g_local.h` -> `include/g_consts.h:241-242`). Phase 5 ships a handler-private `_DROPITEM_MACRO_FALLBACK` dict as the tactical workaround per F11 amendment. Principled fix: amend D4 to walk transitive `#include` closure (or named depth-N), update Phase 1's `_handler_modes.py` lift, re-test parse-time impact (F16 gate). KTX is the first surfaced multi-hop case per D4's own "Revisit if a multi-hop case surfaces" caveat. Trigger: when a 2nd engine surfaces a depth-2+ macro dependency, OR when the Phase 5 fallback dict needs to grow beyond the current 2 entries (signal that ad-hoc maintenance > principled lift). Sized as a small arc (~1-2 hour scope: one decision amendment + one phase MD edit + re-test).
- **Cross-project audit cadence** -- trigger-based rule: run `validate-extractor` in cross-project mode after arcs that add a new project, new entity type, schema migration, or modify `extractor_lib/` or `load-version.ts`. Rule at EXTRACTOR-PLAYBOOK.md "Cross-project audit cadence" section; last audit: `docs/superpowers/reviews/2026-05-06-ktx-onboarding-cross-project-audit.md`. Next trigger: whichever of the four trigger-set conditions fires first in a future arc.
- **arc-planner / arc-orchestrator pre-flight: per-phase executor-prompt symmetry** -- post-arc reviewer surfaced 2026-05-08 that the extractor-discipline-catchup arc has `phase-N-executor-prompt.md` for every phase EXCEPT P2 (universal reproducibility probe shipped at `2e7808eb`; same phase also broke the `extractor-discipline-catchup phase N:` commit-subject convention). Not actionable retroactively; the file-as-prompt is a planner-side artifact and re-creating it post-ship has no value. Forward-action: add a one-line check to the `arc-planner` skill's scaffold-close step (or an `arc-orchestrator` phase-boundary check) -- "every phase that has `phase-N-drafter-prompt.md` should also have `phase-N-executor-prompt.md` before declaring the scaffold complete." Cost of missing it: that phase's executor terminal runs without the strong-arc-identification block + V-probe substitutions + halt-shape spec, losing the wrong-arc-walk defense the file-as-prompt convention was built for. Evidence: `docs/superpowers/reviews/2026-05-08-extractor-discipline-catchup-post-arc-analysis.md` YELLOW item 4. Sized: ~10 min skill edit. Trigger: next arc-planner pass touches the skill, or operator decides to harden the scaffold checklist between arcs.
- **MW_ADMIN_PASSWORD rotation (qwiki-v1-beta)** -- value exposed in prior transcript when an `.env` tail-output during a recovery step showed it. Rotate via `Special:ChangePassword` while logged in as Admin at `https://wiki.slipgate.me`. ~30 seconds. Old OAuth Client Secret was rotated during the deploy (no longer leaked); MW Admin password remains the only outstanding rotation hygiene item.
- **qwiki-v1-beta Phase 3 cosmetic followups** -- two items deferred from Phase 3 boundary (NOT Phase 4 scope; separate auth-shape polish micro-commit when convenient):
  - (a) Discord username extraction renders as literal `User` in Citizen user menu. Needs deploy-time recon of actual Discord OIDC payload claims before shipping a `$wgOpenIDConnect_UsernameClaim` fix; `preferred_username` is not guaranteed for Discord. Recon path: temporarily extend `qwikiBetaSyncDiscordRole` helper to `wfDebugLog('qwiki-beta', json_encode(...))` the session contents during one login, inspect `/var/www/html/log/qwiki-beta.log` to see actual claim names, then pick the right one (could become a small F10 if surfaced as cross-phase). The debug-log group `qwiki-beta` is already wired per F9 resolution.
  - (b) `MediaWiki:Group-wiki-contributor-member` + `MediaWiki:Group-wiki-curator-member` interface-message pages don't exist; user menu renders the message-key literals (`<group-wiki-contributor-member>`). ~30 seconds each in operator's Admin browser session: visit `MediaWiki:Group-wiki-contributor-member&action=edit`, type `wiki contributor`, save. Same for curator. If EnableLocalLogin=false blocks re-login as Admin, fallback is `docker exec qwiki-mediawiki php maintenance/edit.php` from `ssh unraid-deploy`.

- **mvdsv PR #210 (0x000A `mvdhidden_paused_duration` block framing) -- FILED 2026-06-06, awaiting QW-Group review** -- `SV_MVDWritePausedTime` omitted the `[u32 length]` hidden-block-header field (dem_multiple payload size 3 not 7), so the per-frame pause-duration block was present-but-unparseable. Latent on QTV streams (body never parsed); exposed on demo FILES by #192 (which dropped the DEST_STREAM filter) -- nexus saw "zero 0x000A". PR adds the length field -> canonical `[u32 length][u16 type_id][body]`. Root-caused from source + 15 pre-fix sample demos (all-malformed), verified end-to-end on a local KTX bot-match recording (174 canonical blocks, 0 malformed) via the new `apps/qw-oracle/quake/` mvdsv test rig (`run-server.sh`). Branch `ParadokS81:fix/paused-duration-block-framing` -> `QW-Group/mvdsv:master`. **Trigger: PR merges** -> ping nexus to re-test (his demo's pause durations become readable). Detail: memory [[mvdsv-demo-start-timestamp-uleb128]]. [small followup]

### Sidequests
- **Berlin server `dl` fails on the 2 newest demos (deferred 2026-06-06; not reproducible locally)** -- nexus: `cmd dl <n>` works for all but the last 2 demos on Berlin (1.20-dev). mvdsv `dl` / `download demonum` is a raw chunked byte transfer (never parses demo content), so it is UNRELATED to the 0x000A framing bug; the only in-code un-downloadable paths are size-truncation (`sv_demoMaxSize`) or buffered-cache overflow (`sv_demoUseCache`/`sv_demoCacheSize`), both setting `d->error` -> mid-record close. Not reproducible on the local rig (all 3 test demos downloaded clean). Likely Berlin-side config/flush on the largest/newest files. Fastest resolution: `rcon sv_demoMaxSize` / `sv_demoUseCache` / `sv_demoCacheSize` on Berlin. No urgency. [sidequest]
- **Lift nginx resolver + variable-upstream pattern to qwiki / qw-stats / phoenix nginx fronts** -- the qw-oracle nginx broke 2026-05-11 because default nginx caches upstream hostname -> IP at startup; container IP drift after the weekly Synology backup-restart cycle leaves the cache stale and produces silent 502s. Fix shipped at commit `8f1dfd82` (resolver + `set $mcp_upstream` + variable `proxy_pass`). Same pattern applies to every other nginx-fronted docker stack on Unraid. **Action:** audit `qwiki-nginx`, `qw-stats` (if it has an nginx), and `phoenix-*` for the same shape and lift the resolver pattern. Each is ~5 min config edit + scp + nginx -s reload. Could fold into the parked unraid-scoping convergence arc (different mechanism but same parent class) or ship as standalone polish.
- **Plugin v-table asset detection (loader-sites handler)** — FTE plugins reach loaders through `cvarfuncs->...` v-table calls, not direct CALL_EXPR. Only `plugin:ezhud` affected; ezhud images ship bundled with FTE. Low pressure. (Engine-agnostic structural finding; no body to migrate.)
- **Cvar-binding handler indirection gap (snprintf chains + CVARFC callbacks)** — engine-agnostic limitation. Bundle reconciliation correctly treats these as `seedRetained`; only the AST-corroboration signal is partial. Low pressure.
- **Phase 2e follow-up arc residuals** — partially superseded by cross-extractor arc and by 2026-05-01 help-JSON classification arc. Remaining: 1 pre-existing ezquake F2 informational anomaly (`gl_lightmode`). Triage alongside next ezQuake deep-time refresh.
- **Map knowledge layer follow-ups** — slipgate map-browser UI; advanced `search_maps` filters; author seed-YAML curation; automated quarterly stats refresh; future `maps.quake.world` richer-metadata refactor. (Bundled from the Map knowledge SHIPPED retrospective's "Remaining" list; arc body now in `apps/qw-oracle/docs/arc-history.md`.)
- **matchscheduler doc system reconciliation** — 17-file `apps/matchscheduler/context/` predates monorepo doctrine. Earned its own brainstorm when matchscheduler work next surfaces friction with the existing system. Per docs-redesign spec Plan 2 narrowed scope (2026-04-29).
- **`pm_rampjump` description for mvdsv help-JSON (if/when mvdsv grows one)** — surfaced 2026-05-07 during PR #1120 prep. Source-level investigation produced a clear description (raises ground-speed cap proportionally to slope angle, fixes ramp-jump stutter on angled surfaces, caps jump-off speed to flat-ground baseline). Excluded from #1120 per slime's "sv_* belongs to mvdsv" convention rule. Description text parked in PR #1120's "Side findings" section. No urgency; reactivate if mvdsv adds help-JSON or convention shifts.
- **`cl_voip_demorecord` is dead code** — surfaced 2026-05-07 during PR #1120 audit. Cvar is registered at `src/snd_voip.c:53` with default `"1"` and trailing comment "Record VOIP in demo." but has **zero read sites** anywhere in `src/` — no `.integer`/`.value`/`.string` access. Either dead code or partially-removed feature. Recommend either implementing the gating logic or removing the registration. Flagged in PR #1120 body as info; slime's call.
- **`cl_pext_serversideweapon` OnChange handler missing** — surfaced 2026-05-07 during PR #1120 audit. `onchange_pext_serversideweapon` is forward-declared at `src/cl_main.c:86` and registered as the OnChange callback, but the function definition appears absent from the source tree. Connect-path behavior intact (so the help-JSON description in #1120 stands), but the OnChange path is broken or stale-ref. Flagged in PR #1120 body as info; slime's call.
- **qw-oracle Layer 1 database design audit** (operator-flagged 2026-05-05 during KTX-onboarding Pass 5.4) — schema has grown organically across v1-v18 + KTX-onboarding ahead. Audit scope: index coverage vs actual MCP query patterns (GIN indexes on JSONB columns, common-WHERE column coverage); storage shape (text where ENUM/numeric would be cheaper; JSONB-everywhere vs relational normalisation pressure points); CHECK constraint sprawl as kind/type values grow across 5+ engines; pgvector index params (lists/m/ef_construction tuning); dead columns / unused fields from SQLite-era leftovers; implications for loader scripts and natural-key upserts. Outcome: `docs/superpowers/specs/<date>-qw-oracle-db-audit.md` findings spec + actionable-items arc. Run AFTER KTX onboarding ships -- not folded into KTX execution. Operator's framing: "an audit to see if there are any rookie mistakes or obvious low hanging fruit to optimize, and what it means for the scripts that produce the databases."

### Ongoing arcs
- [quake.world docs/oracle federation -- architecture + stack LOCKED 2026-06-07](docs/superpowers/parking/2026-06-07-quake-world-docs-federation-roadmap.md) -- the human-facing surfaces for the L1/L3 corpus. Brainstorm 2026-06-07 (+ live vikpe chat) locked: **note-first** synthesis (concept note canonical -> wiki derived; staleness flags flow downhill L1->note->wiki; L1 self-heals on re-extract); **stack** = Cloudflare + Vite (docs.quake.world = VitePress; oracle.quake.world = SolidJS/Vite; wiki = MediaWiki; cohesion by adopting vikpe's Tailwind v4 + daisyUI theme, framework-agnostic); **roadmap** = Arc 1 docs.quake.world (foundation surface, recommended first; per-codebase auto-projection of L1) -> Arc 2 oracle.quake.world (flagship: showcase + query playground + admin/staleness console; folds in the new-version workflow) -> parallel wiki game-modes section (first note->wiki pilot). **Supersedes** the 2026-05-27 docs-quake-world vision (wiki-first + Firebase, both overturned); **subsumes** the 2026-05-01 showcase-site spec as Arc 2 (retarget Firebase->Cloudflare, framework=Solid). Triggers fired (7 codebases in L1). Repo location DECIDED 2026-06-07: build in workshop + self-host on CF Pages; vikpe points the quake.world subdomain (scheduler.quake.world pattern); graduate to his monorepo later if ever. Arc order DECIDED 2026-06-09: **docs.quake.world first.** Next: focused design brainstorm via fresh-terminal handoff `docs/superpowers/parking/2026-06-09-docs-quake-world-brainstorm-handoff.md` (superpowers:brainstorming -> docs design spec -> arc-planner; the ~2 open questions are IA/taxonomy + per-entity rendering, rest defaulted). L2/Discord-corpus reconstruction running in parallel terminals.
- [Asset-type curate skill arc](docs/superpowers/parking/2026-05-13-asset-type-curate-skill-arc.md) — Phase 1 SHIPPED 2026-05-13 (7 commits, head `3fcfcf41`). Phase 2 + Round 3 calibration SHIPPED 2026-05-14 across 5 commits (`3d2a1867` skybox post-audit re-run + `03449c65` charset/hud_element/map slices + `45617006` seed corrections + `ff76967b` side-quest handovers + `430be56e` oversight handoff). L1 vocab alignment audit closed + refinement arc reorg at `b0b736be`. L1 refinement Phase A (Cat 1 misroutes + Cat 2 console reroutes) SHIPPED 2026-05-14 at `beb57c46` -- 11 ENCLOSING_FN_CATEGORY_OVERRIDES + 5 new FTE L1 asset_category labels + 2 seed bridges. L1 refinement Phase B (Cat 3 write-path leakage + 2 inline NF1/NF3 scope adds) SHIPPED 2026-05-14 at `d4c6f1ba` -- note: commit subject reads as qwiki-v1-beta orchestrator work due to a parallel-session staging collision; full Phase B file list is in the commit body. 5 asset-notes shipped across 4 status flags (player_skin CONFIDENT / skybox DIVERGENT / charset CONFIDENT / hud_element DIVERGENT / map DOC-GAP). 9 user-global skill patches landed at `~/.claude/skills/asset-type-curate/` from Round 3 findings (adjacency-cutoff rule / L1-CAT-AMBIGUOUS named pattern / bulk-L1 selection rule / `l1_hint_bare_categories` Step 1 fallback / procedural-family scope rule / seed-flat-list-to-per-engine-keys translation / cross-engine wording / QWCL-MVDSV JSON existence / companion typology trigger-vs-co-installed). Phase 3 fan-out (16 remaining slugs) + 3 sibling arcs (L1 extractor refinement / L1 vocabulary alignment audit / concept-note partner authoring) queued via 4 new parking docs at `docs/superpowers/parking/2026-05-14-*.md`. Oversight handoff for fresh-terminal continuation at `docs/superpowers/parking/2026-05-14-asset-type-oversight-handoff.md`. Phase 3 sidecar: re-walk obligation across 15 ezQuake tags + 1 FTE version (still pending; runs alongside Phase 3 fan-out). Spec: `docs/superpowers/specs/2026-05-13-asset-type-curate-skill-design.md`. Plan: `docs/superpowers/plans/2026-05-13-asset-type-curate-skill-arc.md`.
- [Slipgate Managed Mode pivot](docs/superpowers/parking/2026-04-28-slipgate-managed-mode.md) — **HIGH PRESSURE.** All 6 brainstorm passes COMPLETE; cold-eyes review shipped 2026-05-11. **Three design passes 2026-05-12** settled the full Critical + Important tier + over-engineering review + cross-platform commitment (findings doc grew 698 → 1111 lines): (a) first session settled C-1 through C-7; (b) second session settled all 8 remaining Important findings (I-3 / I-5 / I-6 / I-8 / I-9 / I-10 / I-12 / I-13) + surfaced new C-8 (engine homedir, ezQuake-specific cross-platform) + W-11 (import-classification handler) + substrate principle (metadata enrichment is delta-sync-driven); (c) evening Opus Max simplicity-lens pass yielded S-1 through S-5 simplifications (drop `profile-roles-history.json`, drop `.slipgate-light/` state, collapse C-8 motions, drop `source_root` annotation, reframe C-5 pre-push as exception assertion) + Path B cross-platform commitment (spec portable now, Windows-only V1 build, Linux/macOS in later arcs) + per-platform mechanism table. Findings doc + memory files current. **Next move:** Browse + Manager UI brainstorm (NEW gating step — substrate well-specced but UI surface undefined; operator flagged current Browse is in a regressed state with missing domain list). Pulling the QW gfx corpus (1.4GB from nicotinelounge.com via ciscon) as input to the brainstorm. After UI brainstorm lands → spec amendment cluster (C-1 through C-8 + I-3 / I-5 / I-6 / I-8 / I-9 / I-10 / I-12 / I-13 + S-1 through S-5 + cross-platform) + cheap spikes (I-1 FTE `cfg_save` + I-4 ezQuake `Cmd_Exec_f`) → arc-planner for Arc A + Arc Z (Browse/Manager UI). V1+ roadmap items consolidated: free-baseline install (C-4), pak-builder (C-5), P2P send-anywhere transfer (I-3 Reality-2), smart import classifier (W-11), hub-as-delivery (I-3 Reality-2). V1 = Arcs A+B+D+E+C-minimal + Z (Browse/Manager UI) + Backup sub-arc.
- [qw-oracle showcase site + contributor pipeline](docs/superpowers/parking/2026-04-30-qw-oracle-showcase-site-contributor-pipeline.md) — design landed 2026-05-01 (`docs/superpowers/specs/2026-05-01-qw-oracle-showcase-site-design.md`); next step is claude.ai/design mockups, then implementation plan. **-> 2026-06-07: this IS now Arc 2 (oracle.quake.world) of the [docs/oracle federation roadmap](docs/superpowers/parking/2026-06-07-quake-world-docs-federation-roadmap.md). Stack corrected there: Firebase -> Cloudflare, framework = SolidJS+Vite. Content/section design here still stands.**
- [Cross-extractor pattern audit follow-up arc](docs/superpowers/parking/2026-04-28-cross-extractor-pattern-audit.md) — five phases shipped + 13 audit-deferred residuals + 2 new follow-ups.
- [Per-project Mode B validation synthesis follow-ups](docs/superpowers/parking/2026-04-28-mode-b-validation-followups.md) — three Mode B validations shipped; some closed, some queued.
- [KTX/MVDSV Layer-1 describe-fill](docs/superpowers/parking/2026-05-15-ktx-mvdsv-l1-describe-fill.md) -- KTX side closed (`arc-ktx-format-unify-shipped`; 607 rows on D20 template; retrospective in arc-history 2026-05-21; 618 KTX rows still need re-embed for `description_embedding_stale=true`). **MVDSV side remains open** -- same template + apply mechanism mirrors directly. Spec `docs/superpowers/specs/2026-05-15-ktx-mvdsv-l1-describe-fill-design.md`; plan `docs/superpowers/plans/2026-05-16-ktx-mvdsv-l1-describe-fill/` (D20 + D21 in decisions.md). Three Session #9 follow-ups queued in Small followups (L3 team-chat note, arc-reviewer 96-row walkthrough, describe-fill-synthesis skill folds) -- none gate the MVDSV side. Session #9 wrap: `docs/superpowers/parking/2026-05-21-ktx-mvdsv-l1-describe-fill-orchestrator-resume.md`.
- [Layer 2 corpus reconstruction (thread/topic re-segmentation)](docs/superpowers/specs/2026-05-04-layer2-corpus-reconstruction-design.md) -- multi-pass brainstorm **COMPLETE 2026-06-06 (all 5 passes); ready for arc-planner.** Calibration probe verdict: LLM fencing (arm D) wins (D-vs-C 72%, D-vs-B 69%, coherence 4.4 vs 3.5, 0% index-hallucination at cap 750, both channels; embeddings load-bearing; Sonnet not Opus). v1 = prune / fence / embed-raw-messages / hybrid-retrieve, decomposed into spine **A** (migration + increment-1 gate, on the already-fenced Feb-Mar 2021 probe slice) / **B** (chunk-size sweep 750->1500/3000) / **C** (idempotent batched backfill) + planned **buckets-E** (FAQ-substrate enrichment); `resolution_status` rides C (batch-1 kill-switch); merge dropped; D-threshold-recalib + author-trust-note + clustering-contingency are gated stubs. **Next move: arc-planner** -- handoff `docs/superpowers/parking/2026-06-06-layer2-corpus-reconstruction-planner-handoff.md`. SoT spec `docs/superpowers/specs/2026-05-04-layer2-corpus-reconstruction-design.md` ("Pass 5 outputs"); calibration results `docs/superpowers/parking/2026-06-05-layer2-calibration-test-results.md`; spine `docs/superpowers/parking/2026-05-03-layer2-thread-reconstruction.md`; probe code = production recipe `apps/qw-oracle/scripts/calibration/`.
### Future arcs (waiting on trigger)
- **QTV / QWFWD L1 extraction -- SHIPPED 2026-06-06** (`arc-qtv-qwfwd-l1-shipped`; arc-history 2026-06-06; post-arc review + dispositions `docs/superpowers/reviews/2026-06-06-qtv-qwfwd-l1-extraction-post-arc-analysis.md`). 102 L1 rows (qtv 52 via Go `go/ast` -- the pipeline's first non-C front-end; qwfwd 50 via libclang), all source_backed/synthesized/embedded. Seed retired: `docs/superpowers/parking/2026-05-31-qtv-qwfwd-documentation.md`. **Post-ship follow-ups:** (1) **prod deploy [deferred -- bundle with Phase-C L2 deploy]** -- the qtv/qwfwd rows + embeddings + migration 020 ride a full dev->prod `pg_dump`/restore; the orientation Discovery change rides an MCP image rebuild; verify `lookup_entity(project='qtv')` resolves in prod. Deferred deliberately -- no real MCP consumers yet, so it bundles with the imminent L2 backfill deploy rather than an isolated push. (2) **concept-note authoring arc [pending operator greenlight]** -- D9 decided: AUTHOR (a) master-server registration/heartbeat + (b) MVD-streaming/parse_delay ghosting; DEFER (c) qtv_password (See-also the MVDSV `qtv_*` rows). Its own brainstorm+author arc.
- **ezQuake F1 floor re-baseline [small followup]** -- the qtv-qwfwd Phase-4 full-grid run surfaced 8 PRE-EXISTING ezquake floor failures (cvar 2992->2996, command 693->699, doc_only/source_retired/first_seen shifts), NOT arc-caused (the Phase-4 edit was purely additive; no ezquake load ran). Source-walk-gated: confirm the drift is legitimate dev-head growth (not a regression / idempotency inflation -- `feedback_idempotency_before_staleness`) before bumping `EZQUAKE_FLOOR_PROBES`. Surfaced 2026-06-07 (qtv-qwfwd post-arc review F18).
- **[ezQuake contributor map](docs/superpowers/parking/2026-05-23-ezquake-contributor-map.md)** -- per-subsystem authorship/maintainer lookup from git log/blame on `research/repos/ezquake-source` for routing upstream PRs. Surfaced 2026-05-23 mid-commands-PR drafting when "ask slime" was being assumed but he's a later contributor, not the original author. Trigger: next non-trivial upstream-PR routing question we can't answer, or operator-initiated.
- **MCP tool upgrades (`version` param + `get_entity_history` tool + version/date filters on `search_entities`)** -- Phase 2g residual from the 2026-04-18 qw-knowledge-rollout (Tier 4, deprioritized at the time because the dir-browser reads SQLite directly; MCP upgrades served only Oracle-bot / Claude-session users). Status verified 2026-05-21 against MCP source: `version` NOT in `lookup_entity` args (`serve/mcp/src/tools/lookup-entity.ts:8-12`); no `get_entity_history` tool in `serve/mcp/`; no version/date filters in `search_entities` args (`serve/mcp/src/tools/search-entities.ts:25-30`). Pressure: low -- current MCP answers about head; deep-time queries are rare in the eval corpus. Trigger: operator-initiated when Oracle-bot / MCP-consumer demand surfaces version-aware queries, or when the showcase site / slipgate snapshot consumer needs version-specific tool answers. Original parking doc (covering Phases 2d-2h, mostly shipped) `git rm`'d 2026-05-21 -- shipped tier records live in `apps/qw-oracle/docs/arc-history.md`. [future arc]
- **[L1 contextual build-availability arc](docs/superpowers/parking/2026-05-20-l1-contextual-build-availability-arc.md)** -- relevance-at-answer-time framing captured from enforce-L1 Phase 5 slime review (2026-05-20). L1 is build-agnostic, but the user's question is build-specific: an oracle/LLM answering about `gl_program_sky` / `cache_print` / `addloc` today has no way to say "renderer-scoped / SERVERONLY / legacy alias -- may not be in your build." Two paths: (1) cheap interim -- oracle answer-shape hedge using the existing Track-A level-2 signal + a small heuristic table (seeded by the `scope_label` rule prototyped in `enforce-l1-pop1-triage.html`), sized for `superpowers:writing-plans` directly; (2) proper per-build-profile arc -- explicit CMake-options modeling + renderer-dispatch-table modeling + legacy-alias chain capture + per-fork client profile passed at query time, multi-week, needs `arc-brainstormer`. D5/F9-bound: per_variant alone is too coarse (2786/2788 = client-reachable). Also captures the zero-unknown population-2 finding from this 166-pool (after the ciscon split, residue = exactly internal0-internal9 known-wired). Trigger: operator-initiated; ready for arc-classifier when picked up.
- **[quad Discord-surface enhancements](docs/superpowers/parking/2026-05-20-quad-discord-surfaces.md)** -- three progressive tiers unified by "quad as Discord presentation layer for QW data": (1) auto-channel-topic with the share link on Mumble activation, (2) auto-mirror matchscheduler scheduled matches into native Discord Scheduled Events per team, (3) the actual arc -- subscribable community feeds (LAN events from quake.world, divisional ladder matches, KTX cup schedules) opt-in via quad commands, surfaced as Discord Events or DM digests. Surfaced 2026-05-20 mid-Mumble-cert work after operator set `#sr` topic manually with the share link and noticed native Discord Events. Trigger: operator-initiated after auto-record + share-link UX soaks in real squad use for at least one matchday cycle.
- **[Unraid backup redesign (borgmatic 3-2-1)](docs/superpowers/plans/2026-05-19-unraid-backup-redesign.md)** -- replace the single-copy / unverified / stop-the-world weekly appdata tar with borgmatic -> encrypted borg repo on Synology over SSH -> Prague offsite pull; dedup + versioned history, Healthchecks + Discord visibility, restore-drill acceptance gate. Spec `docs/superpowers/specs/2026-05-19-unraid-backup-redesign-design.md`; 7-phase operator-paced plan (incl. ~3-4wk parallel soak before retiring the old tar). Surfaced 2026-05-19 from the qwiki-v1-beta V6 false-alarm investigation (the V6 command + `unRAID/docs/server/backup.md` were already corrected, commit `80218de0`). Trigger: operator-initiated; Phase 0 `[SSH]` discovery is the cheap kickoff.
- **[Cross-project Unraid scoping convergence](docs/superpowers/parking/2026-05-14-unraid-scoping-convergence.md)** -- lift the qwiki-v1-beta unraid-deploy scoped non-root convention to quad / qw-stats / qw-oracle. Trigger: operator-initiated.
- **[dusty-* antilag fork L1 onboarding](docs/superpowers/parking/2026-05-16-dusty-antilag-fork-l1.md)** — `research/repos/dusty-ktx` + `dusty-mvdsv` (cloned, never extracted; doc-landscape probes + M denominators are mainline-only). Surfaced 2026-05-16 during describe-fill Pass 3.2. A **behavior/description fork, NOT an entity-set fork**: antilag-named entity surface is identical across forks (`sv_antilag` / `k_vp_antilag` / `antilag` cmd), but `dusty-ktx` ships a 783-line `antilag.c` and `sv_antilag` semantics diverge (mainline 0↔2 on==2 passthrough vs dusty ==1 multi-mode); both deployed in production on different port ranges. Onboard-extractor fork path; cheap probe first to size divergent surface beyond antilag-named (only that surface verified this session). describe-fill D10 ships the `sv_antilag` dual-description L1 stopgap; this arc is the full fork extraction. Trigger: operator-initiated (known-arc; backlog).- [Layer 3 multi-domain expansion + bucket framework](docs/superpowers/parking/2026-05-03-layer3-multidomain-bucket-framework.md) -- L3 expanded from engine-internal to player-as-system ontology (9-bucket taxonomy: engine-config / engine-content / visual-customization / system / hardware / peripherals / network / server-side / community); recipe-shape (not encyclopedia) note convention; lockstep flagging architecture (cross-ref showcase-site doc) for sustainable staleness management. Surfaced 2026-05-03 during Phase 8 eval-set walkthrough when q5/q8 (vague-NL Linux/Windows symptom queries) failed retrieval revealing the structural gap. Three connected parking docs (this + L2 thread reconstruction + showcase-site contributor pipeline) form the Arc 3 architecture.
- [Cross-extractor Phase 6 residuals](docs/superpowers/parking/2026-04-28-cross-extractor-phase6-residuals.md) — D.1.8 lifecycle hooks + broader positive-contracts coverage + deep-time-walk obligation.
- [Cross-extractor Phase 6 ezquake exemptions: r_bloom_* shape](docs/superpowers/parking/2026-04-28-phase6-ezquake-r-bloom-shape.md) — convergent with QWCL 1996-vintage shape; joint positive-contract arc candidate.
- [Cross-engine alias scaffolding + slipgate version-awareness follow-ups](docs/superpowers/parking/2026-04-26-cross-engine-alias-followups.md) — sub-thread #4 (FTE asset bundle, superseded by Slipgate Managed Mode TAIL-1) + sub-thread #5 (slipgate version-awareness, tracked-by Quake Dir Control plan).
- [Retired cvars in snapshot + stale-config warning UX](docs/superpowers/parking/2026-04-26-retired-cvars-stale-config-warning.md) — coupled producer+consumer work blocked on UX design.
- [Slipgate SCHEMA.md for snapshot consumer interface](docs/superpowers/parking/2026-04-26-slipgate-schemamd.md) — gated on next slipgate UI arc surfacing version-arc badges.
- [Feed tab future content](docs/superpowers/parking/2026-04-27-feed-tab-content.md) — tournaments / dev landscape / GitHub monitoring / community announcements.
- [Screenshot POC → Profile picture generator](docs/superpowers/parking/2026-04-27-screenshot-profile-picture.md) — graduate POC into Profile.
- [Tray menu launch](docs/superpowers/parking/2026-04-27-tray-menu-launch.md) — optional arc; documented home if launch needs to come back.
- [Interactive HTML dashboard](docs/superpowers/parking/2026-04-XX-interactive-html-dashboard.md) — shelved with documented unshelve triggers.
- [qw_event_log as cross-validation oracle for Layer 1](docs/superpowers/parking/2026-04-XX-qw-event-log-cross-validation.md) — gated on KTX cvars + KTX gameplay overrides.
- [SCHEMA.md doc-style inconsistency](docs/superpowers/parking/2026-04-29-schemamd-style-refresh.md) — per-table body refresh + topical-vs-per-version style decision.
- [Layer 3 concept note: death rules](docs/superpowers/parking/2026-04-29-layer3-death-rules-concept-note.md) — gated on KTX gameplay overrides; three-anchor synthesis.
- [L1-alpha: Ecosystem-tools registry](docs/superpowers/parking/2026-04-29-l1-alpha-ecosystem-tools.md) — Pass 3 carry-forward.
- [L1-beta: Cross-format binary fingerprinting](docs/superpowers/parking/2026-04-29-l1-beta-cross-format-fingerprinting.md) — Pass 3 carry-forward.
- [L1-gamma: Engine helpdoc / data-file recognition](docs/superpowers/parking/2026-04-29-l1-gamma-helpdocs.md) — Pass 3 carry-forward.
- [L1-delta: Stock asset catalog](docs/superpowers/parking/2026-04-29-l1-delta-stock-pak.md) — Pass 3 carry-forward.
- [Memory system consolidation](docs/superpowers/parking/2026-04-29-memory-system-consolidation.md) — **PRESSURE INCREASING.** File count: **148** (was ~77 at 2026-04-29 post-trim baseline; was 125 at 2026-05-17; was 133 at 2026-05-20). MEMORY.md: **21.6KB / 202 lines** — exceeds BOTH 20KB and 150-line thresholds per docs-check Phase 1 Step 5; the system-reminder banner notes "Only part of it was loaded" -- index is being truncated at session-load. Schedule consolidation pass when convenient; recent inflow vectors include the 2026-05-26 help-JSON arc closure + 2026-05-27 L1 extractor refinements arc (+1 feedback memory `feedback_handover_claims_decay`).
- [Promote methodology learnings to served admin/player concept notes](docs/superpowers/parking/2026-05-29-served-admin-knowledge-from-methodology.md) — game-mode classification relations + hosting model (`k_allowed_free_modes` bitmask, dmm flags, dynamic one-server model) live only in internal `_methodology/` docs (never ingested). Mold into served notes (`game-modes` overview for players+admins; `server-setup`). Trigger: admin-facing query stream ("what modes should I run?") or the old-wiki admin-guide restructuring arc. **Partial (2026-05-30):** the dmm-flags piece is now served by `deathmatch-modes.md` (game-mode arc); the player+admin game-modes overview + `server-setup` notes remain unwritten.

### Older open items (carried over)
- **Game-mode hosting-section reframe + frogbots gap (follow-on, NOT started)** -- operator review of the hosting model produced a per-audience reframe: `Starting a game` = user activation, `Hosting & settings` = admin (availability + tunables), deferring the bitmask/`k_defmode`/force-path mechanics to the new `server-setup.md`. This session: directive rewritten, `rocket-arena` reworked as the worked example, `server-setup.md` seeded with the source-verified gating model. **Mostly shipped with the v2 arc** (all 28 notes carry the show-and-tell `Hosting`; the enforced-vs-tunable structure question is settled via `Basic ruleset` / `Settings to tune` / `Hosting`). **Remaining:** scope a `frogbots.md` note -- bots run under the hood in tot/LGC/practice but are undocumented. Handoff: `docs/superpowers/parking/2026-05-30-game-mode-hosting-reframe-and-bots-gap-handoff.md`.
- **L1 extractor entity-classification followups (from help-JSON audit probes)** -- spawned 2026-05-15 during command-pass review of the help-JSON empty-entries audit. Two of four audit passes complete (cvar shipped 2026-05-14; command shipped 2026-05-15); macros + cmdline pending. **2026-05-15 update (CORRECTED 2026-05-15 PM):** the cvar source-comment promotion (`2edce42b`) was later found to be a PROVENANCE BUG under slime's two-audience doc model (help-JSON = user docs / source `// comment` = coder rationale; primary-sourced, 97% differ) and was **REVERTED** -- commits `dc50b3ef` (handler) + `1f0227f5` (regression guard `F1.ezquake.anchor.no_comment_promotion` + retreat-fix floor recompute) + `99c21532` (docs); all 15 ezquake tags re-walked clean (bug signature 502 rows/47 entities -> 39/3 genuine upstream mirrors). The macro `teamplay-restricted` "inverted 9/9" claim is a **VERIFIED NON-FINDING** (DB matches `src/teamplay.c` HEAD; `_derive_teamplay_restricted` derives source-truth-correctly; never inverted) -- a dispatched-terminal claim that failed primary-source verification; do NOT propagate. Landscape + verification in the convergence doc's "Cvar provenance pass (2026-05-15)" SHIPPED block; the help-JSON audit RESTARTS clean (authoritative handoff `docs/superpowers/parking/2026-05-15-handoff-helpjson-cvar-pass-and-reachability-blindspot.md`). The earlier `Cmd_AddLegacyCommand not distinguished` finding is STALE -- the handler already emits `ast.legacy_alias_of`; the real gap is loader-persistence + a schema column (arc work). **2026-05-15 closure:** ghost classification = verified no-op (intended output of `3be4d576`; functionally inert), F2 flicker scoping = fixed (`9a5a0c2d`), `291268bd` quarantine = retracted (`fb633ed0`) -- all folded into the convergence doc. The `kick_to_ciscon`->nano/slime relabel remains open, routed to the help-JSON empty-entries audit workstream (semantic re-bucketing, vocabulary source unlocated; tracked in the convergence doc, not coupled to extractor/loader changes). Cold-start handover `docs/superpowers/parking/2026-05-15-handoff-ghost-classification-f2-scoping.md` (commit `b417b61f`) is now closed; cross-references the entity-state-retreat bug below. Parking doc `docs/superpowers/parking/2026-05-15-l1-extractor-entity-classification-followups.md` accumulates findings per audit pass; becomes input for arc-planning the L1 extractor improvements once all 4 audits land. Audit deliverables (kick_to_ciscon questions for ciscon) are valid output independently. Sister docs at `docs/superpowers/parking/2026-05-13-l1-extractor-asset-loader-enhancements.md` and `docs/superpowers/parking/2026-05-14-l1-extractor-refinement-arc.md` -- may consolidate at arc-planning time. **2026-05-16:** the HUD dynamic-name family + command-direction case-fold gap were pulled OUT of this feeder INTO the libclang two-track arc (Track B + shared foundation); only `Cmd_AddLegacyCommand` persistence + trailing-comment harvester precision remain as this doc's siblings. **2026-05-20:** F20 from enforce-L1 (the Class 3 cmdline-consumer-presence feeder is `.c`-only-scoped; 5 of 11 shipped Track-A entries were LIVE via `.h` macro wrappers fanning into `.c` call sites; visible artifact corrected via `_runtime_dead_entities.py:_CLASS3_BLOCK` regen to 6 entities, L1 mislabeling of the 5 cmdline_params persists) joins as a third sibling -- the cmdline-liveness feeder mechanization. See `docs/superpowers/plans/2026-05-17-enforce-l1-runtime-truth/review-findings.md` F20 for the full investigation; the proper fix mechanizes `.c`+`.h`-aware consumer-presence detection. **2026-05-27 closure update:** the L1 extractor improvements arc shipped 2026-05-27 (arc-history entry of same date). Of the open siblings: (a) `Cmd_AddLegacyCommand` persistence SHIPPED (migration 017 + loader, commit `c91c5ece`); (b) `COM_CheckParm` bare-call detection VERIFIED NO-OP (already in handler since an earlier arc); (c) F20 `.h`-aware liveness SHIPPED (4 of 5 cases already correctly detected via libclang macro expansion -- `-cheats`, `-progtype`, `-r-debug`, `-democache`; the `-enablelocalcommand` residual shipped 2026-05-27 commit `547038e3` via STRING_LITERAL cursor-walk in `_handler_cmdline.py` + 15-tag re-walk -- root cause was libclang reporting macro-expanded literal extents as the invocation source span, not the literal itself, NOT the variant-dispatch hypothesis); (d) HUD dynamic-name family VERIFIED NO-OP (`_handler_cvars.py:HUD_Register` synthesis already extracts 1,463 hud_* cvars across 87 elements). Only `trailing-comment harvester precision` remains genuinely open from this feeder doc -- low-pressure, metadata-fidelity sibling.
- **L1 extractor refinement arc** -- Phase A (`beb57c46`) + Phase B (`d4c6f1ba`) SHIPPED. **Phase C (Cat 4 FTE charset watchlist gap, ~30-60 min) is next and closes the arc.** Parking doc: `docs/superpowers/parking/2026-05-14-l1-extractor-refinement-arc.md` (full scope + Phase A/B status at top). Phase-C executor handoff: `docs/superpowers/parking/2026-05-14-handoff-l1-refinement-phase-c-worker.md`.
- **Asset-type-curate Phase 3 fan-out** -- spawned 2026-05-14. 16 remaining slugs after Round 3 calibration; skill is production-ready. Three waves recommended (4 low-risk Brief + 1 L1-GAP test + 11 mixed). Sized 1-2 sessions (parallel dispatch). Parking doc: `docs/superpowers/parking/2026-05-14-asset-type-phase-3-fanout.md`.
- **Concept-note partner authoring (hud-configuration + map-selection-workflow)** -- spawned 2026-05-14. Two new concept-notes warranted by Round 3 calibration (hud_element earned a partner for HUD configuration system; map earned a partner for map-selection workflow). Sized 2 sessions. Parking doc: `docs/superpowers/parking/2026-05-14-concept-note-partners-authoring.md`.
- **QWiki sandbox modernization arc spawned 2026-05-09** — Phase B brainstorm uncovered structural messiness gating downstream oracle work; operator + bps + ciscon aligned on Discord; ciscon shipped 87M SQL + 6.4G image tarball (with bonus 178 QW demo files); project scaffold landed at `apps/qwiki-sandbox/`. **Status:** awaiting arc-classifier → arc-brainstormer → arc-planner in fresh session. Handoff at `docs/superpowers/parking/2026-05-09-qwiki-sandbox-arc-planning-handover.md`. Phase plan sketched (6 phases): scaffolding (done) / MW 1.35 import / MW 1.39 LTS upgrade / Citizen + VisualEditor / Page Forms / EQL cleanup pilot / showcase to maintainers. Sister-arc dependency: Phase B drain (Phase 4 of qwiki community-reference arc) couples to this arc's phase 4-5 via the cleanup pilot.
- **Phase B drain brainstorm paused mid-session 2026-05-09** — captured amendments + workflow shape decisions parked at `docs/superpowers/parking/2026-05-09-qwiki-phase-b-brainstorm-pause.md`. Resume after qwiki-sandbox phase 4 lands Page Forms (path A, sandbox-first) or before (path B, snapshot-only). Captured: `country` + `division_label` schema amendments, modal-review workflow shape, brand-row 27-field checklist (4 tiers), tournament-row checklist sketch, EQL as pilot brand, 5 silent-drop members in pre-fill state to patch. `gap-check.ts` script committed at `apps/qw-oracle/scripts/curate-brands/gap-check.ts` for re-runnable drift detection.
- **Re-walk obligation 2026-05-13** (small followup) — handler edits applied to HEAD checkouts only; 15 ezQuake tags (v3.0 → v3.6.9 + head) and 1 FTE version (build-6698) in the L1 DB still carry pre-fix `asset_loader_sites` rows. **Scheduled** as Task 3.6 sidecar in the asset-type-curate arc plan (`docs/superpowers/plans/2026-05-13-asset-type-curate-skill-arc.md`). Will run alongside Phase 3 fan-out. ~10 min wall-clock; idempotent. Delete this line once Phase 3 ships.
- **Slipgate Browse + Manager UI brainstorm needed 2026-05-12** — surfaced during arc-planner readiness check. Substrate (Manager engine) thoroughly designed; UI surface (Browse view in Managed Mode + per-domain views + multi-engine filter + private/promote/fork gestures + Updater UI + Settings) referenced in spec but never designed. Current Browse is in a regressed state per operator screenshot (mis-labeled extension filter as "FILTER BY DOMAIN"; domain list missing from left sidebar; "0 loaded / available / shipped / 15366 other" implies half-wired manifest-aware classification). One focused brainstorm session (not multi-pass) should cover it — most decisions are downstream of substrate already settled. Folded into Slipgate Managed Mode arc as "Arc Z" placeholder per current Slipgate handover line above. Input: gfx corpus inventory (next item).
- **gfx corpus investigation 2026-05-12 (sidequest, in-flight)** — ciscon shared `https://nicotinelounge.com/qw3-abab/gfx.tar.gz` (1.41GB) as a community-curated QW asset catalog (qw.nu/gfx site dump). Pulled to `/home/paradoks/sandboxes/qw3-abab-gfx/`. Two artifacts: (a) **structural inventory** at `docs/superpowers/parking/2026-05-12-gfx-corpus-inventory.md` (10-category taxonomy + 30 subcategories + 11-field schema), (b) **investigation findings** at `docs/superpowers/parking/2026-05-12-asset-corpus-investigation-findings.md` (Pass 1 path-pattern → Pass 2 DB-category reframe, 95.9% install-coverage manifest, 7 W-11 design refinements, 11 threads worth pulling). Working manifest exists as `pass2-manifest.ndjson` (6.2MB, 11,173 per-(bundle,file) records). **Operator taking findings to a new terminal for deeper investigation** before moving to Browse UI brainstorm. Highest-value threads queued: mine `gfx_comment` (1,449 rows) for ad-hoc install instructions; cross-verify inferred install paths against Layer 1 asset-loader-sites data; spot-check the 32 EMPTY bundles + 799 multi-target_path hash cases. All artifacts self-pointed-to from the investigation findings doc.
- **KTX gameplay_taxonomies handler `_stats.election_type.source_total` is parallel-aggregator-naive** — surfaced during KTX Phase 7 cross-project audit (2026-05-06; D.3.1 + D.6.1 of `docs/superpowers/reviews/2026-05-06-ktx-onboarding-cross-project-audit.md`). `_handler_gameplay_taxonomies.py:385` computes `source_total = len(raw_election_rows)` over pre-dedup data; under `--workers 4`, value reports 20 (4x correct) because `progs.h` is included by every TU and each worker's election rows accumulate. Actual `count` field correctly dedups to 5; ONLY the `source_total` stat differs. Stat-only; functional correctness and emitted rows are unaffected. Breaks Section-1.1 byte-reproducibility under default `--workers 12`. Workaround: run `--workers 1` for serial reproducibility checks. Principled fix mirrors Phase 5.5 Pattern 13 retrofit (typed pseudo-row aggregation). Sized <30 lines; Phase 8 PLAYBOOK candidate joins F23 / F27 / F29 + F25 references.
- **KTX handler class-name shape inconsistent** — surfaced 2026-05-06 (D.2.1 of cross-project audit). 4 handlers use `<Type>KtxHandler` (CvarsKtxHandler / InfoKeysKtxHandler / CommandsKtxHandler / LogTemplatesKtxHandler) and 4 use `Ktx<Type>Handler` (KtxModesHandler / KtxGameplayTaxonomiesHandler / KtxGameplayTablesHandler / KtxMatchEventsHandler). Cosmetic only; no behavioral consequence. Defer to a future cosmetic-cleanup pass (matches 2026-04-28 cross-extractor audit D.5.1 / D.5.2 FTE class-name nits).
- **F29 -- Phase 3 missed 24+ Infobox 4on4team articles (Black_Book / Milton_s_Mutants / FAgomatic / ELAK / etc.) because their wiki articles have empty `categories` arrays; `isClanArticle` filter is category-driven and falls through.** Surfaced 2026-05-05 during Phase 4 T2 review pass. Resolution path is a one-line broadening of `isClanArticle` in `apps/qw-oracle/scripts/load-community/clans/index.ts` to also accept articles whose wikitext contains `{{Infobox 4on4team` regardless of category presence. Re-run loader (idempotent UPSERT). Roughly +24 rows appear. Matters for Phase 5 cross-link backfill: players whose `current_clan: Black Book` need a Black_Book row to JOIN against. Recommended: ship as Phase 3.5 small followup BEFORE Phase 5 executes; sized ~30 minutes inline. Captured as F29 in `docs/superpowers/plans/2026-05-04-qwiki-community-reference/review-findings.md`.
- **New-doc_only-on-next-ezquake-bump alert** — when ezquake-source HEAD advances to a new tag and we re-walk, run `python3 apps/qw-oracle/scripts/classify-help-json.py --project ezquake --propose` from monorepo root. The seed already covers the 193 existing doc_only entries; the CLI will only print proposals for any newly-arrived doc_only names. Operator manually triages or seeds the new entries. No automatic alerting between sessions — Claude only runs when invoked.
- **Help-JSON classifier: retired-bucket commit-attribution heuristic looseness** — spot-check via subagents found that the blame heuristic in `extractor_lib/_help_json_blame.py` cites the last-touch commit for retired entries, which can be a default-value tweak (`s_alsa_latency`, `in_raw_allbuttons`) or a comment-out (`sv_highchars`) rather than the actual removal commit. Core "no live registration at HEAD" claim is sound; commit citations are 30-40% loose. Improvement: detect `-` registration-line removals specifically rather than any source-side touch. Low pressure — caveats noted in upstream PR digest cover the gap for now.
---

## Synthesis-report numerical-claim provenance gap (2026-04-29)

**Added:** 2026-04-29. **Status:** Open. Process-improvement note for future validation arcs.

Validation reports authored 2026-04-28 (synthesis report at `docs/superpowers/reviews/2026-04-28-per-project-validation-synthesis.md` plus per-project deep-validation reports) contain numerical claims (e.g., "230 trailing_comments", "2989 cvars at HEAD") with no preserved SQL. The numbers did not match live DB at the moment of writing during the 2026-04-29 zero-debt arc, costing ~90 minutes reconciling irreproducible figures.

### Process improvement

Future validation reports must inline the SQL (or other reproducible derivation) for every numerical claim, as a comment block in the report itself. Three formats are acceptable:

1. Inline ` ```sql ... ``` ` block immediately after the claim.
2. Footnote-style citation pointing at a script file checked into the repo with the report.
3. A "queries" appendix section at the bottom of the report listing every claim and its derivation.

Without this, downstream readers can't distinguish "stale snapshot at write-time" from "extractor regression since write-time" from "writer arithmetic error."

### Pressure

Low; aspirational rule for future arcs. No retroactive fix planned for the 2026-04-28 reports — rather, calibrate the next validation pass against this rule.
