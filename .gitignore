# Dependencies
node_modules/
.venv/

# Build output
dist/
.tsbuildinfo

# Tauri build
apps/slipgate-app/src-tauri/target/

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Environment & secrets
.env
.env.*
!.env.example
!.env.prod.example
service-account.json

# Data files (large, local-only)
apps/qw-stats/data/4on4_full/
apps/qw-stats/data/*.zip
apps/qw-stats/data/*.db
apps/qw-oracle/data/*.db
apps/qw-oracle/data/*.db-journal
apps/qw-oracle/data/*.db-wal
apps/qw-oracle/data/*.db-shm
apps/qw-oracle/*.db
apps/quad/recordings/
apps/quad/exports/

# Layer 2 calibration probe -- throwaway scratch (slice.sqlite, embed cache, per-chunk/session/pair JSON)
apps/qw-oracle/scripts/calibration/scratch/

# Research repos (cloned, not tracked)
research/repos/*/

# Demo archives (local-only, too large for git)
research/assets/demos/*.zip

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
*.swp
*.swo

# Claude Code / tooling artifacts (per-machine, never committed)
.claude/projects/
.claude/*.lock
.superpowers/
# Gemini "antigravity" CLI project symlink (points into ~/.gemini)
.antigravitycli/

# Ad-hoc diagnostic output (local, never committed)
test-output/

# Logs
*.log

# Python bytecode cache
__pycache__/
*.pyc

# Patches cache (global default)
patches/
# But track quad's patches (needed for Docker builds)
!apps/quad/patches/
