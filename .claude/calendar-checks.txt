# Calendar reminders surfaced by .claude/scripts/session-start-git-state.sh
# at session start once today's date >= the listed due date.
#
# Format: YYYY-MM-DD | description
# Lines starting with '#' or empty are ignored.
# Edit this file to clear (delete the line), snooze (push the date), or add new entries.

2026-08-19 | Unraid backup health (monthly, borg-era; replaces the retired CA-tar check 2026-07-20) -- ssh unraid 'docker exec borgmatic /command/with-contenv borgmatic list' | tail -3 -- newest ParadokS-<timestamp> archive must be <= 2 days old (nightly 03:00 CEST host cron; continuous monitors = Healthchecks.io "unraid-borgmatic" + Discord webhook). NOTE: bare `docker exec borgmatic` sees no env and fails on APPRISE_DISCORD -- the /command/with-contenv wrapper is mandatory (infra/unraid-backup/README.md correction #6). Optionally add --consider-checkpoints to see .checkpoint litter (internal-cron wart; maintenance-window item). Drill history + residuals: infra/unraid-backup/README.md closeout section + unRAID repo docs/server/backup.md (host-plane ground truth). On PASS push date +1mo.
2026-06-22 | memory verification (parking 2026-04-29-memory-system-consolidation.md) + run /insights and compare against the 2026-04-29 run for workflow-improvement signal -- 2026-05-22 check-in: mechanical hook-compression brought MEMORY.md 28.1KB -> 24.0KB; file count + line count still over trigger thresholds (138/193 vs 30-project / 150-line caps); structural brainstorm parked rather than executed
2026-06-22 | HANDOVER retrospective sweep (monthly) -- cross-section check for stale entries (items shipped in other sessions, HANDOVER line still pending). Sweep all 6 sections; primary-source verify via arc-history.md + git log + PR state before deleting. Baseline 2026-05-22 took ~1-2h and yielded 4 line deletions + 2 condensations + 2 stale parking files. On completion push date +1mo.
2026-08-05 | Maintenance-bundle CLOSEOUT (bundle EXECUTED by ops 2026-08-04; items 1-4 verified, item 5 schedule-half was already live since >=07-14): await ops final-PASS letter (forced Synology check result + first unattended 03:00 borgmatic run), then (a) update infra/unraid-backup/README.md residuals (Synology weekly-check residual is stale), (b) reply letter to ops: phoenix-postgres datadir exclude sign-off (pending David nod; dump hook since 07-14 makes the torn datadir copy dead weight) + request to bake `pip install clang==18.*` into cockpit image provisioning (F10 oracle-reentry plan: mise python reinstalled at recreate wipes site-packages, second recurrence), (c) delete this line. Operator-only, untimed, carried over: 6.3 full escrow-only file-restore drill from Prague.
2026-10-22 | Mumble LE cert renewal check -- echo | openssl s_client -connect mumble.slipgate.me:64738 -servername mumble.slipgate.me 2>/dev/null | openssl x509 -noout -issuer -enddate 2>/dev/null. Expect issuer "Let's Encrypt" and notAfter LATER than 2026-11-17 (proves the ~Oct 19 acme-sh auto-renew via Cloudflare DNS-01 + scripts/reload-mumble.sh fired; prior PASS 2026-07-28, notAfter 2026-10-17). If notAfter still ~Oct 17 or host unreachable: docker logs acme-sh --tail 80 (acme-sh is a quad-stack member, allowed via dev-deploy-proxy; post-fence there is no host SSH) then run the re-issue block in apps/quad/DEPLOYMENT.md "Mumble TLS certificate". On PASS push this date +3mo.
2026-10-28 | Claude environment review (quarterly, dev plane) -- for each skill/plugin/hook/command/MCP entry: when last used? does the model now do it natively? does it still earn its context cost? Retire losers into _retired-YYYY-MM (your existing pattern). Shared-layer (statusline/hooks/bashrc) changes go through ~/dotfiles, never hand-edits. On completion push +3mo.
