# Calendar reminders surfaced by .claude/scripts/session-start-git-state.sh
# at session start once today's date >= the listed due date.
#
# Format: YYYY-MM-DD | description
# Lines starting with '#' or empty are ignored.
# Edit this file to clear (delete the line), snooze (push the date), or add new entries.

2026-08-19 | Unraid backup health (monthly, borg-era; method de-SSH'd 2026-08-04 -- host SSH dead since the 2026-07-28 fence, borgmatic is ops-plane so no docker exec either) -- read the overnight run via the read-only proxy: curl -s "http://docker-logs-proxy:2375/containers/borgmatic/logs?stdout=1&stderr=1&tail=120" -- expect a successful nightly archive (~03:00 CEST host cron) with no error lines; continuous monitors = Healthchecks.io "unraid-borgmatic" + Discord webhook. If the run is missing or ambiguous in logs: letter to ops (they can run the with-contenv list). Checkpoint-litter wart FIXED 2026-08-04 (bundle item 3 silenced the internal 01:00 cron). Ground truth: infra/unraid-backup/README.md closeout + unRAID repo docs/server/backup.md, readable via git -c safe.directory='*' --git-dir=/home/dev/repos/unraid.git show HEAD:docs/server/backup.md (both literals verified 2026-08-04). On PASS push date +1mo.
2026-09-04 | memory quick-verify (monthly-ish; full ritual EXECUTED 2026-08-04: 168 files verified via 2-agent fan-out, 15 deleted, 26 fixed with dated correction notes, index dead-link-free, store 169 -> 154 files). NOTE: /insights does not exist in the cockpit environment -- that half of the old check is dropped; run it from a context that has it if the comparison still matters. Structural consolidation (file-count growth continues) remains parked: docs/superpowers/parking/2026-04-29-memory-system-consolidation.md + the HANDOVER Future-arcs entry (re-truthed 2026-08-04). On completion push +1mo.
2026-09-04 | HANDOVER retrospective sweep (monthly) -- cross-section check for stale entries; primary-source verify via arc-history.md + git log + GitHub-API PR state before deleting. Baseline 2026-08-04 (2-agent fan-out, ~1h wall): ~105 entries verified, 15 deletions, 30 rewrites, 2 consolidations, 1 stranded-branch MERGE recovered (workstation-sweep-2026-07-28, bb00d86e), 12 upstream PRs confirmed merged with reconciliation states derived. On completion push date +1mo.
2026-10-22 | Mumble LE cert renewal check -- echo | openssl s_client -connect mumble.slipgate.me:64738 -servername mumble.slipgate.me 2>/dev/null | openssl x509 -noout -issuer -enddate 2>/dev/null. Expect issuer "Let's Encrypt" and notAfter LATER than 2026-11-17 (proves the ~Oct 19 acme-sh auto-renew via Cloudflare DNS-01 + scripts/reload-mumble.sh fired; prior PASS 2026-07-28, notAfter 2026-10-17). If notAfter still ~Oct 17 or host unreachable: docker logs acme-sh --tail 80 (acme-sh is a quad-stack member, allowed via dev-deploy-proxy; post-fence there is no host SSH) then run the re-issue block in apps/quad/DEPLOYMENT.md "Mumble TLS certificate". On PASS push this date +3mo.
2026-10-28 | Claude environment review (quarterly, dev plane) -- for each skill/plugin/hook/command/MCP entry: when last used? does the model now do it natively? does it still earn its context cost? Retire losers into _retired-YYYY-MM (your existing pattern). Shared-layer (statusline/hooks/bashrc) changes go through ~/dotfiles, never hand-edits. On completion push +3mo.
2026-09-06 | L2 corpus harvest (monthly) -- keep the Discord corpus current. RUNBOOK: apps/qw-oracle/scripts/load-chat/HARVEST-RUNBOOK.md (3 ingestion commands -> build-sessions.ts --force + 2 rebuilders -> re-fence the CURRENT YEAR's 4 batches via run-backfill-batch.sh). Measured 2026-08-06: <1h wall, ~$2 DeepSeek, no Max quota. Baseline that run: corpus 705,540 msgs / 3,928 chunks / 40,214 threads, current through 2026-08-05. WATCH: (a) an import is NOT availability -- without build-sessions.ts the new messages have no message_labels and count-all shows nothing moved; (b) never run build-sessions.ts while a batch is mid-flight (it TRUNCATEs message_labels); (c) only current-year rows should move in count-all -- a historical row moving means something else changed. On completion push +1mo.
